# MindFlow — 프로젝트 컨텍스트 (팀 공용)

이 문서는 사람과 Claude 에이전트가 **공유하는 단일 컨텍스트**입니다. 새 세션·새 에이전트는
작업 전 이 파일을 먼저 읽습니다.

## 제품 개요
MindFlow는 중심 주제에서 가지를 뻗어 생각을 정리하는 **마인드맵 웹 앱**입니다.
목표: **웹 서비스 → 모바일 웹(PWA) → Android/iOS 앱** 순으로 확장.

## 현재 코드베이스 (디자인 원본)
`claude/mindflow-design-impl-iyxiol` 브랜치는 Claude Design에서 가져온 **디자인 프로토타입**입니다.
프로덕션 기반이 아니라 **픽셀·인터랙션 레퍼런스**로 취급합니다.

| 파일 | 역할 |
| --- | --- |
| `MindFlow.dc.html` | 마인드맵 편집기 (약 3,200줄) — 대상 |
| `Home.dc.html` / `Login.dc.html` | 대시보드 · 로그인(데모) |
| `support.js` | dc-runtime. **수정 금지** (Anthropic 생성물, 프로토타입 엔진) |
| `vendor/` | React 18.3.1 UMD |
| `index.html` | 진입점 |

### dc 포맷 구조
각 `*.dc.html` = `<x-dc>` 템플릿(HTML + `{{ }}`·`<sc-if>`·`<sc-for>`) + `<script type="text/x-dc">`
안의 `class Component extends DCLogic` 컨트롤러. 런타임(`support.js`)이 템플릿을 React로 렌더링.
빌드 단계 없음. 상태는 `localStorage`(`mindflow_doc_<id>`), 페이지 간 `window.location` 이동.

### 마인드맵 엔진의 위치 (핵심 자산)
`MindFlow.dc.html`의 컨트롤러 안에 렌더링과 뒤섞여 있음:
- 데이터 모델(`nodes`, `floats`, `lines`, `zones`)
- 레이아웃 알고리즘 `_layout(nodes, layoutMode)`
- 직렬화 `serializeDoc()` / `loadDoc()` / `cloneNodes()`
- undo/redo, export(PNG·Markdown·JSON)

## 목표 아키텍처
```
packages/
  mindmap-core/   # 순수 TS. DOM/React 없음. 모델·레이아웃·직렬화·undo·export
  web/            # React + Vite + TS. core 사용, SVG/Canvas 렌더 (디자인 재현). PWA
  mobile/         # (2단계) Capacitor 또는 React Native, core 재사용
server/           # 인증(OAuth/이메일) + DB(Postgres) + 문서 동기화 API (+ 협업시 Yjs/CRDT)
```

## 로드맵 / 단계
1. **웹 프로덕션화**: dc → React+TS+Vite 이식, `mindmap-core` 분리, 실제 인증·DB·동기화. PWA화.
2. **앱 스토어**: 빠르게=Capacitor로 PWA 래핑 / 네이티브감=React Native(UI 재작성 + core 재사용).

## 진행 현황 (ADR-0001 기준)
- ✅ **ADR-0001** 아키텍처 결정 (`docs/architecture/0001-architecture.md`)
- ✅ **M0** 모노레포 스캐폴딩 (pnpm+Turbo, strict TS, CI, 코어 순수성 lint)
- ✅ **골든 안전망** (`packages/mindmap-core/test/fixtures/`) — dc 원본을 헤드리스로 캡처. serialize/outline/layout 좌표/node-sizes
- ✅ **M1a** 코어: 모델·`serializeDoc`/`parseDoc`/`cloneNodes`·`toMarkdown`·`HistoryStack` (골든 parity, QA 감사 반영)
- ✅ **M1b** 코어: `layout(doc, mode, sizeOf, opts?)` — `_layout` radial/right/down 이식, 좌표 parity. `_rootAnchor`→opts 승격
- ✅ **M1c** 코어: `resolveLineGeometry`/`cubicAt`/`portPoint` (라인 큐빅 기하, 원본 `lineCPs` parity)
- ✅ **M3-Login/Home** React 이식 (`apps/web`, react-router). localStorage 키는 원본과 호환
- ✅ **M3-Editor-a** 렌더 기반: 코어(`layout`/`serialize`/`geometry`) 소비, 캔버스·노드·커넥터·테마 렌더. 원본과 렌더 parity 확인
- ✅ **M3-Editor-b** 인터랙션: 선택·편집·추가/삭제·드래그·속성패널·저장(자동)·undo/redo(코어 `HistoryStack`)·내보내기(코어 `toMarkdown`)
- ✅ **M3-Editor-c** 완성도: marquee 다중선택(일괄 스타일)·미니맵·아웃라인 편집·드래그 재부모화
- ✅ **M4** 백엔드: `AuthProvider`/`DocStore`/`SpaceStore` 포트 + Local/Supabase 어댑터 + 팩토리(env-게이트). Supabase 마이그레이션·RLS(`supabase/migrations/` + 루트 `supabase/config.toml`, GitHub 연동 자동배포), 문서 `server/supabase/docs/backend.md`, 낙관적 잠금. 스페이스/폴더는 사용자별 `workspaces` 테이블(다기기 동기화). **env 없으면 로컬/데모 폴백**(앱 안 깨짐)
- ✅ **M6** PWA + 모바일 반응형: `vite-plugin-pwa`(Workbox, 오프라인 앱셸), manifest·아이콘·Pretendard self-host. 768px 브레이크포인트 — Home 드로어, Editor 속성패널 바텀시트, 44px 터치타겟
- ✅ **M5** 실시간 협업(Yjs/CRDT): 코어 `crdt/` Doc↔Y.Doc 바인딩(순수, 충돌 없는 수렴) + 웹 전송 포트(BroadcastChannel 로컬 다중탭 / Supabase Realtime / Noop) + `useYjsDocSync` 에디터 통합. env-게이트, 단일 사용자 무회귀
- ✅ **M7** Capacitor 앱셸(`apps/mobile`): android/·ios/ 스캐폴딩, `capacitor.config`(webDir=web dist), `build:mobile`(web build→cap sync), 네이티브 브리지(Share/Filesystem·StatusBar·Keyboard, 웹 폴백). **실기기 빌드·서명·스토어 제출은 로컬 Android Studio/Xcode 필요**(`apps/mobile/README.md`)
- ✅ **M5-awareness** 협업 커서/선택 공유(`y-protocols` Awareness, 웹 전용): `CollabProvider.getAwareness()` + BroadcastChannel/Supabase Realtime 릴레이(에코 방지 origin 필터, 기존 M5 채널 재사용·신규 스키마 불필요) + `usePresence` 훅(정체성=인증 이메일 또는 clientID, 색·이름 시드 결정적, 커서 50ms 스로틀). 에디터 UI: 팬/줌 변환 안의 원격 커서·이름표(`PresenceLayer`), 노드/플로트/라인/존 원격 선택 하이라이트(피어 색), 우상단 접속자 아바타(`PresenceBar`). 단일 사용자 무회귀(peers=[] → null). 실브라우저 2-탭 검증 완료
- ✅ **M3-Editor-d(컨텍스트 메뉴)** 우클릭 메뉴(`ContextMenu.tsx`, 원본 `ctxMenu`/`ctxSub` 이식): 히트테스트 후 대상 선택→종류별 메뉴(node=하위/형제·텍스트 정렬▸ 플라이아웃·삭제 / zone=이름편집·삭제 / float·line=삭제 / multi=삭제(N개) / bg=도형·메모·선·영역 추가, 클릭 좌표에 생성). 뷰포트 clamp·플라이아웃 flip·바깥클릭/Esc/실행후 닫힘. `setTextAlign`(대량 노드 필드 세터) 신설, `add*At`에 `at?:{x,y}` 옵션 추가. **버그 수정**: 메뉴가 `.mf-ed-vp`(배경 드래그 소유) 자식이라 버튼이 `mousedown`만 stop→실제 클릭의 `pointerdown`이 배경 마퀴 드래그를 띄우고 무이동 `pointerup`이 선택 해제 →2단계 정렬 플라이아웃이 대상 상실. 메뉴 루트에서 `pointerdown` stop으로 해결(정렬 테스트를 full 클릭 시퀀스로 강화해 회귀 가드). 실브라우저 검증 완료
- ✅ **M3-Editor-e(라인 앵커 마그넷)** 라인 끝점→노드/플로트 포트 스냅·앵커(원본 `findSnap`/`resolveEnd`/`resolveLine`/`lineTargetBox`/`borderPoint` 이식). 코어: `LineAnchor{kind,id,side?}` + `Line.a1/a2`(순수 추가, 직렬화 라운드트립·골든 무회귀), `findLineSnap`(SNAP=34)·`resolveLineEndpoints`·`borderPoint`(박스 조회 주입식, 순수성 유지). 웹: `line-end` 드래그가 매 이동 `findLineSnap`으로 스냅→`a{which}` 커밋, 앵커 라인은 렌더/히트/마퀴/PNG에서 `resolveLineEndpoints`로 해석해 노드 이동 시 자동 추종, 빈 곳 드롭 시 detach. 렌더: 앵커 끝점 마그넷 dot + 드래그 중 대상 박스 4포트 인디케이터(스냅 side 강조). **버그 수정**: geomRef는 post-commit useEffect에서만 갱신 → 앵커 마그넷이 한 프레임 지연 → 렌더타임용 `*Live`(현재 render의 geom/doc 클로저) 변형 분리. 실브라우저 검증(스냅·추종·detach) 완료
- ✅ **M3-Editor-f(부분 리치텍스트)** 노드 텍스트 일부만 굵게/색상(원본 `applyPartial`/`domToRuns`/`runsToHtml`/`linearize`/`commitRichEdit` 이식). 코어: `richtext.ts`—`applyPartialStyle(src,s0,s1,kind,val)`·`stripRichStyle`·`runsToChars`/`charsToRuns`(순수 char-model, DOM 없음, 골든 무회귀). 웹: `NodeEditBox`를 contentEditable로 교체(`runsToHtml` 시드·IME 가드·Enter/Shift+Enter/Escape), `richtextDom.ts`(escHtml/rgbToHex/runsToHtml/domToRuns/linearize/setLinearSelection), 선택 시 뜨는 `TextToolbar`(B·색 스와치·지우기)→`linearize`로 offset→코어 변환→innerHTML 갱신·선택 복원, `commitNodeRichText`(플레인이면 rich=null), 전체 bold 토글 시 `stripRichStyle`로 부분 bold 정리. **인터랙션 함정 3종 방지**: 툴바 루트 `pointerdown` stop(배경 마퀴 누수 차단), 버튼 `mousedown` preventDefault(편집 blur·선택 소실 방지=원본 `_tctxHold` 역할), 테스트 full 클릭 시퀀스. 실브라우저 검증(부분 bold·색·지우기·plain→rich=null) 완료
- ✅ **M7-assets(네이티브 아이콘/스플래시)** MindFlow 마크(코럴 둥근사각+흰 M)를 `apps/mobile/scripts/generate-native-assets.mjs`(M6 `markSvg` 재사용, `sharp`, 결정적·외부애셋/네트워크 없음)로 전 해상도 생성·설치: Android adaptive(배경색 코럴 + 흰 M 투명 전경 5종)·legacy `ic_launcher`/`_round` 5종·스플래시(기존 치수 유지 11종), iOS `AppIcon-512@2x`(1024 full-bleed)·`Splash.imageset` 3종. `@capacitor/splash-screen` 추가(config `launchAutoHide:false`) + `nativeShell`에서 `SplashScreen.hide()`(웹 no-op 폴백). 기본 Capacitor placeholder 교체 완료, 생성 자산 직접 확인. **실기기 빌드·서명·스토어 제출은 여전히 로컬 Android Studio/Xcode 필요**(`apps/mobile/README.md`)
- ✅ **회원 탈퇴** 프로필 팝오버에 `설정` 진입점 추가 → `AccountSettingsModal`(계정 정보 + `위험 구역`의 회원 탈퇴) → `DeleteAccountModal`(비가역 경고·삭제 항목 명시·`탈퇴` 문구 타이핑 게이트로 파괴 버튼 arming). 포트 `AuthProvider.deleteAccount()` 신설: Supabase는 `delete_account()` SECURITY DEFINER RPC(`supabase/migrations/0005_delete_account.sql`, 본인 `auth.users` 삭제→`on delete cascade`로 documents/workspaces/profiles 동반 삭제) 후 signOut / Local·데모는 `mf_`·`mindflow_` 네임스페이스 저장소 전체 wipe. 컨트롤러는 성공 시 로컬 캐시도 정리하고 `/login`으로 replace, 실패 시 다이얼로그에 에러 노출·재시도 유지. 유닛(문구 게이트·삭제 후 저장소 clear+`/login`) + 실브라우저 전체 흐름 검증 완료
- ✅ **GIS 직접 연동 + 법적 문서** 구글 동의 화면의 `<ref>.supabase.co` 표시 제거: 로그인 페이지가 GIS 공식 버튼을 우리 origin에서 렌더 → ID 토큰을 신설 포트 `AuthProvider.signInWithIdToken`으로 교환(`GoogleSignInButton.tsx`/`googleIdentity.ts`, nonce=원본↔SHA-256 쌍). `VITE_GOOGLE_CLIENT_ID` 미설정·데모 모드·스크립트 차단 시 기존 리다이렉트 버튼 폴백(실브라우저로 차단 환경 degradation 검증). 공개 라우트 `/privacy`·`/terms`(RequireAuth 밖, 로그인 푸터 링크) + 브랜드 인증용 120×120 로고(`/brand/geurio-logo-120.png`). 콘솔 절차는 `backend.md` §1d
- ✅ **브랜드 마크 리디자인** 문자 "G" → 흰 모노라인 **소용돌이**(중심 점으로 감기는 세 호, 사용자 선정안). 지오메트리 3곳 동기화: `src/components/BrandMark.tsx`(앱 내: 로그인 BrandPanel·에디터 툴바·법적 문서 헤더) + `generate-icons.mjs`(원본) + 모바일 `generate-native-assets.mjs`. PWA·파비콘·브랜드 120·Android adaptive/legacy/스플래시·iOS 아이콘/스플래시 전부 재생성
- ✅ **이미지 첨부 ①(캔버스 이미지 플로트)** `Float.img`(데이터 URL, post-dc 순수 추가 — 직렬화/CRDT 제네릭 통과, 골든 무회귀). 저장 전략=문서 인라인: `imageAttach.ts`가 첨부 시 다운스케일(긴변 1024, PNG는 투명 보존·그 외 JPEG q.85, 초과 시 재압축). 진입 3종: 삽입 메뉴·배경 우클릭 "이미지 추가"(파일 선택), 클립보드 붙여넣기(텍스트 입력 중 제외), 캔버스 드롭(드롭 지점). 렌더=FloatLayer 이미지 분기(접기토글/편집 없음), 리사이즈 비율 고정, 속성패널 이미지 전용, PNG 내보내기 async 프리디코드 후 drawImage. 실브라우저 전 시나리오(삽입·드래그·리사이즈·영속·리로드·드롭·삭제·내보내기) 검증
- ✅ **이미지 첨부 ②(노드 이미지)** `Node.img/imgW/imgH`(항상 세트, post-dc 순수 추가) — 표시 크기는 첨부 시 긴변 180px 비율 고정. `computeMetrics`가 `imgW+패딩`/`imgH+8`만큼 박스를 키워 **레이아웃 자동 반영**(sizeOf 주입 경로 그대로). 렌더=NodeLayer 세로 스택(썸네일 위·이모지/텍스트 아래), 진입=노드 우클릭 "이미지 추가/변경/제거"(`setNodeImage`/`clearNodeImage` — clear는 키 삭제로 CRDT 필드 제거), PNG 내보내기 노드 프리디코드+동일 스택 재현. 실브라우저 검증(추가→박스확장→영속→제거→복원→내보내기 픽셀 일치)
- ✅ **새 버전 적용 게이트** 기존엔 새 배포가 잡히면 **모든 화면**(랜딩·약관·로그인·홈·에디터)에 똑같이 "새 버전이 준비됐어요" 토스트가 떴다. 토스트의 근거는 "에디터가 열린 채 리로드되면 편집이 끊긴다" 하나뿐인데 잃을 게 없는 화면까지 묻고 있던 것. `pwa/updateGate.ts` 신설 — 경로가 아니라 **화면이 자기 상태로 위험도를 신고**(`useUpdateGuard`, 같은 화면도 상태별로 다름): `safe`(랜딩·약관·손대지 않은 로그인 폼)=즉시 조용히 적용 / `defer`(홈·유휴 에디터)=탭이 백그라운드일 때만 적용 / `block`(입력·편집 중)=토스트만. 판정은 순수 함수로 분리(`auth/updateRisk.ts`—입력 시작·인증 코드 단계, `home/updateRisk.ts`—팝업·확인창·검색어·진행 중 작업). 적용 전 `prepare()`가 돌아 미저장 변경을 먼저 저장하고(에디터 `flushSave` 신설: 자동저장 flush 후 **실제 저장됐는지** 반환), 저장 실패 시 리로드를 멈추고 토스트에 알린다(로컬 복구본은 저장 성공 시에만 갱신되므로 그냥 리로드하면 유실). **탭 간 조율**: 한 탭이 적용하면 `skipWaiting`→`controlling`으로 **다른 탭까지 리로드**된다(vite-plugin-pwa register.js) → 자동 적용 전 BroadcastChannel로 피어에 질의, 편집 중인 탭이 있으면 미루고 20초 주기로 재시도(수동 클릭은 본인 선택이므로 질의 없음). 실브라우저 검증 7종(실제 SW 업데이트 사이클: safe 무토스트 자동적용 / block 토스트·무리로드·입력보존 / 수동 클릭 적용 / defer 가시=토스트 / defer 숨김=조용히 적용 / 편집 탭 있을 때 옆 탭 미룸 / 그 탭 닫히면 재시도 적용)
- ✅ **문서 공유(0009) + 실제 공동 편집 수리** 랜딩에 "실시간 공동 편집"이라 써 뒀지만 `documents`가 소유자 전용이라 **남과는 애초에 불가능**했다(M5/M5-awareness는 같은 사람의 여러 탭까지만). 공유: `document_shares`(이메일 초대 — 클라이언트는 `auth.users`를 못 읽고, 미가입자도 초대 가능) + `documents` SELECT/UPDATE 정책 확장 + 정책 재귀를 끊는 `owns_document()`/`shared_with_me()` SECURITY DEFINER + `ShareStore` 포트(Supabase/Local) + 에디터 `ShareModal` + 홈 LNB "공유받은 맵" 구획.
  **배포 후 제보 3건과 그 원인**: ① 공유받은 맵이 본문 상단을 크게 차지 → LNB 접이식 항목으로 이동(`sharedItems`/`sharedOpen`, 즐겨찾기·휴지통과 같은 꼴, 공유받은 게 없으면 구획 자체가 없음, 모바일 44px). ② **편집·접속자·커서가 한꺼번에 오지 않음** → 진짜 원인은 `private: true` 채널이 아니라 **반쪽 동기화**였다: 합류할 때 sync-request만 보내고 **자기 전체 상태는 보내지 않아서**, 각자 자기 Y.Doc을 따로 심은 두 피어의 연산 이력이 어긋나 이후 업데이트가 상대에서 **보류**됐다(부분 적용된 노드로 `children`이 undefined가 되어 상대 캔버스가 통째로 비는 것까지 실브라우저로 재현 — 기존 테스트는 원격 피어를 **빈** Y.Doc으로 만들어 이 경우를 못 잡았다). 수리: 두 전송 모두 합류 시 전체 상태를 **양방향 교환**, `yDocToDoc`이 부분 병합 상태를 **유효한 Doc으로 정규화**(필드 기본값·없는 자식 id 제거)하고 순서 배열의 중복 id를 하나로 읽음(메모가 두 개로 늘던 문제). 덧붙여 `useYjsDocSync`는 **초기 로드 전엔 붙지 않는다**(공유받은 맵을 처음 여는 기기의 빈 자리표시자가 상대 문서와 병합되는 것 차단), Supabase 전송은 private 거부 시 공개 채널로 폴백하되 `broadcast.ack`로 **발신 권한까지 확인**하고 상태를 `CollabStatus`로 올려 보내 `PresenceBar`가 "실시간 연결 끊김"/보안 경고를 표시한다(조용히 죽지 않게 — 이 사건의 핵심 교훈). ③ 초대받지 않은 계정이 URL을 열면 편집만 막힌 채 툴바·메뉴가 다 보였다 → 에디터 대신 크롬 없는 전용 화면(`MapUnavailable`). 실브라우저 검증: 2탭 양방향 편집·메모 이동·중복 없음·리로드 영속, LNB 구획(데스크톱/모바일 드로어)·열기, 전용 화면(캔버스·공유·내보내기 전무 + 저장 흔적 없음), 단독 사용 무회귀
- ✅ **협업 전송 강화(실계정 테스트 후속 3건, #226/#228/#229)** 두 계정 실사용에서 나온 제보를 차례로 수리. ① `edgeStyle`이 CRDT meta에서 빠져 연결선 스타일 변경이 상대에게 안 갔고 수신 측 로컬 미러도 갱신 안 됨 → 바인딩에 추가(옵션 필드, 골든 무회귀) + 원격 문서 도착 시 미러 갱신. 커서 이름표는 이메일 → **프로필명**(`useProfileName`: 로컬 캐시→공급자 이름→이메일 로컬파트→`profiles.display_name` 대조, 색은 이메일 시드 유지). ② **인증 레이스**: private 구독 전 `realtime.setAuth()`를 await하지 않아 한 탭만 공개 채널로 폴백 → 같은 이름이라도 private/public 채널은 메시지가 안 오가서 협업이 조용히 죽음("한 명만 경고 없음 + 동시 편집 불가"). await + 세대 카운터 + private 1회 재시도 + ack 강등 사유를 명시적 'error'로 축소(10초 지연 제거, 확인 타임아웃 4초). 콘솔에 `[geurio] build <시각>` 스탬프(PWA가 에디터 열림 중 업데이트를 미뤄 이전 번들 테스트 혼선 방지). ③ **유실 자가 치유**: join 밖 send가 REST로 우회되고(콘솔 스팸) 수신은 멎어 상대 메시지를 영영 유실 — awareness는 절대 상태라 복구되지만 Yjs는 증분이라 이후 업데이트가 전부 보류("커서는 보이는데 편집만 안 옴"). 15초 주기 상태 벡터(`ysv`) 방송→빠진 연산만 diff 응답(삭제는 delete set 경로), 합류/재합류도 SV 기반 diff 교환(전체 상태 반복 전송 제거), join 밖 send 중단. **운영 절차 교정**: `realtime.messages` 정책 수동 적용은 0009의 do 블록이 아니라 **create policy 두 개만** SQL Editor에서 실행(alter는 42501, 예외 가드가 가짜 성공을 만듦 — `backend.md` §6, 실 프로젝트 검증)
- ✅ **Realtime 사용량 절감(#231, 실기기 확정)** 하루 테스트로 무료 한도의 4%(86K건)를 소모 → 지배 항목 둘을 조임: ① 혼자 열어 둔 탭은 15초 치유 신호(ysv)를 보내지 않음(awareness 피어 없으면 skip — 탭당 하루 ~5,700건이던 유휴 낭비 제거, 체감 0) ② 커서 스로틀 50→100ms(커서 트래픽 절반). 두 계정 **실기기 테스트로 커서 자연스러움 확인 후 확정**. 부산물: y-protocols Awareness 생성자 기본 상태({})는 clock 0이라 상대에게 적용되지 않음 — 실앱은 usePresence가 정체성을 심어(clock≥1) 무관, provider 단독 테스트만 같은 순서로 교정. 협업 전체 흐름(편집·커서·연결선 스타일·프로필명·자가 치유)이 실기기 2계정에서 정상 확인됨
- ✅ **랜딩 리뉴얼(Claude Design 이식)** 인트로 페이지를 새 디자인으로 재구현(원본 `Geurio 랜딩 리뉴얼.dc.html` 루트 보존 — 수정 금지 대상). 쌍둥이 구조 유지: `public/landing.html`(크롤러용 정적 — FAQ는 `<details>`, 히어로 데모는 원문에 완성 상태 폴백 SVG + `landing-demo.js`(defer, 같은 출처) 프로그레시브 인핸서가 실브라우저에서 React와 동일한 인터랙티브 데모로 교체. 계약은 "모든 내용이 JS 없이 원문 HTML에 보인다"로 정밀화 — 프로덕션 `/`는 정적이 서빙되므로 인핸서 없인 사용자 누구도 움직이는 데모를 못 봤다) + `Landing.tsx`+`landing.css`(React — dc 원본의 인터랙티브 데모 이식: 480ms 간격 rev 1→7 등장 애니메이션·가지 토글·"처음부터" 리셋·ResizeObserver 축소 스케일(640×420 고정 좌표계), FAQ 단일 열림 아코디언 grid-rows 트랜지션, 스타일은 `.lp-root` 스코프로 충돌 차단). JetBrains Mono 라틴 서브셋 self-host(`public/fonts/jetbrains-mono/`), 로고는 export 추출본이 기존 브랜드 PNG와 바이트 동일해 재사용. SEO head(canonical·OG·JSON-LD·한글 title)와 브랜드 인증 3요소 계약(기존 테스트) 무변경 통과, CTA 세션 인지(/login↔/home) 유지. 실브라우저 검증(정적/React × 데스크톱/모바일 — 데모 자람·토글·리셋·FAQ·앵커 이동·가로 스크롤 없음)
- ✅ **홈 후속 2건(미리보기 일치 + 중첩 폴더)** ① 맵 카드 미리보기가 에디터와 다르게 그려지던 3가지(실기기 제보) 수리 — 원인은 `mapPreview`가 `n.align`을 무시(항상 중앙)하고 이모지를 본문 크기로 1행에 접두해(에디터는 22/17px 별도 요소를 좌측 고정 + gap 7) 접두 폭만큼 1행이 도형을 벗어난 것. 에디터 NodeLayer의 가로 배치를 그대로 재현: 패딩(24/15/13, 평행사변형 22) 안쪽에서 clip 도형은 `tw` 폭 중앙 스트립, [이모지][gap][텍스트 블록(남은 폭, align 정렬)]. 실브라우저 에디터↔카드 비교 검증. ② **폴더 안 폴더(중첩 폴더)** 신규: `FolderData.parent`(없으면 최상위 — 기존 데이터 무회귀, SpaceStore 블롭 통과), 폴더 안 "새 폴더"=현재 폴더가 부모, 계층별 폴더 카드 표시, 뒤로가기는 한 계층씩, 브레드크럼 "스페이스 / 상위 / …" 경로, 이동 메뉴는 경로 이름("상위 / 하위")으로 전체 폴더 대상(현재 폴더 제외), 폴더 타일 맵 수는 하위 재귀 집계, 삭제는 직접 맵 0+하위 폴더 0일 때만. 실브라우저 검증(생성·진입·뒤로·영속)
- ✅ **썸네일 비용 절감(무료 egress)** 홈 썸네일이 문서 **전문**을 매 방문 재다운로드 — 이미지 첨부가 본문 jsonb 안 base64라 카드당 수백 KB~수 MB. 포트 `DocStore.loadPreview` 신설: Supabase는 `preview_doc` RPC(0012, security invoker — RLS 그대로)로 **이미지 데이터만 'stripped'로 바꾼** 본문(크기 필드 유지)을 받고, `(id, version, updatedAt)` 키 localStorage 캐시(`previewBodyCache.ts`, 용량/개수 상한·LRU)로 같은 판 재방문 시 네트워크 0. 로컬 모드는 localStorage 그대로(스트립·캐시 불필요). 미리보기는 스트립된 이미지 자리에 같은 크기의 회색 자리표시자(박스 확장 불변). **동시 편집 검토**: version은 낙관적 잠금이라 (id,version)→본문 유일, 유일성이 깨지는 단 하나의 경로(prevVersion 없는 강제 저장=version 1 재설정)는 서버가 항상 새로 찍는 updatedAt이 캐시 키에 있어 무효화. 미저장 CRDT 변경은 원래 썸네일에 없음(무회귀). RPC 미적용 서버는 전문 load() 폴백(배포 순서 안전). `backend.md` §7
- ✅ **마크다운 서식 1단계(노드)** 사용자 요청("텍스트 편집에 마크다운") → 서식 모델 확장 선택. 코어 `RichRun`에 `i`(기울임)·`s`(취소선) 추가 — **true일 때만 직렬화**(옛 문서·골든 무회귀, CRDT 제네릭 통과). `applyPartialStyle` kind `'i'|'s'`(굵게와 같은 토글 규칙), `stripRichStyle` 키 확장, 신규 `applyMarkdownShortcuts`(순수): 커밋 시 `**굵게**`/`__굵게__`/`*기울임*`/`_기울임_`/`~~취소선~~` 마커 제거+서식 적용, 기존 부분 색상 보존, 단일 마커(`*`/`_`)는 단어 경계 가드(`2*3=6`·snake_case 오탐 방지, intra-word 강조 포기 트레이드오프), 줄 걸침 무시. 웹: `richtextDom`(em/i·s/del·인라인 스타일 왕복), NodeEditBox 커밋 훅, TextToolbar **I·S 버튼**(툴팁에 문법 힌트), NodeLayer/미리보기 tspan 렌더, `wrapMeasure`는 기울임을 폰트 문자열에 반영(취소선은 폭 불변). PNG는 원래 평문 렌더라 무영향. 실브라우저 검증(마커 입력→커밋 변환·렌더·저장 rich·툴바 버튼). **후속(사용자 결정)**: 툴바를 선택-기반 노출에서 **편집 세션 동안 상시 노출**로 변경(노드 박스 위 고정) — 닫히는 건 편집 종료와 다른 메뉴(우클릭) 열림뿐, 선택 없이 버튼을 누르면 **전체 텍스트에 적용**(죽은 버튼 방지). **후속 버그(제보)**: 편집 중 Ctrl/Cmd+B가 굵어지기는커녕 얇아 보이다 커밋하면 복원 — NodeEditBox가 단축키를 안 가로채 브라우저 기본 bold 토글이 발동, 루트(700)·1단계(600)처럼 박스가 이미 굵은 노드에선 "이미 굵다"로 판단해 `font-weight: normal`(400) 스팬을 심고 커밋 시 `domToRuns`가 400→b:false(무서식)로 읽어 조용히 되돌림. 수리: Ctrl/Cmd+B·I를 preventDefault 후 툴바와 같은 `applyPartial`(800 고정, 선택 없으면 전체)로 라우팅, Ctrl/Cmd+U(밑줄 — 모델에 없음)는 막기만(편집 중 보였다 커밋 때 소리 없이 사라지는 것 방지). 실브라우저 검증(루트·1단계 800 적용·저장·재토글). **후속(사용자 요청)**: 굵게·기울임·취소선을 **도형 선택 시**에도 — 속성 패널 '텍스트 스타일'에 I·S 버튼 추가(B 옆, `BoldSizeRow` 옵션 prop — 플로트 패널 무회귀). I·S는 노드에 전용 필드가 없어(굵게 `bold`와 달리) **rich 런을 전체 텍스트에 적용**하는 whole-node 토글(`mutations.toggleNodesRichStyle` — `runsToChars`/`charsToRuns` 재사용, 모델·직렬화·CRDT 무변경, 렌더/측정/미리보기는 기존 rich 경로 그대로). 다중 선택은 굵게와 같은 첫 대상 기준 규칙, 활성 표시(`nodeFullyRich`)도 같은 기준이라 버튼 상태와 동작이 일치. 부분 색상은 켜고 끌 때 모두 보존. 실브라우저 검증(적용·색보존·저장·리로드 영속·해제·undo). **프로브 함정 기록**: Playwright `addInitScript`는 리로드에도 재실행 — 시드를 가드하지 않으면 "리로드 후 서식 소실"이 재현되는 것처럼 보인다(2시간짜리 오진, 앱은 무결). **다음 단계 후보**: 메모(플로트) 서식화(rich 모델 이식 + FloatLayer/측정/썸네일/PNG 파리티)
- ✅ **속성 패널 UI 정돈(제보: 배치가 중구난방)** 원인은 flex-wrap의 어중간한 줄바꿈 3종: 텍스트 스타일 행에 I·S가 추가되며 '크게'가 홀로 다음 줄, 모양 8종이 6+2, 색상 스와치가 6+6+1(알약형 '자동' 칩이 원형 사이에 섞임). 수리 — 전부 **정방 그리드**: 텍스트 스타일 [B|I|S]/[작게|보통|크게] 3열 2행(I·S 없는 메모·선 패널은 기존 한 행 유지), 모양 4열 2행, `SwatchRow` 개수 맞춤 그리드(≤9개=한 줄, 그 외 7열 — 리셋+13색=14=7×2 정합), '자동' 칩을 색상 스와치와 같은 원형(대각선='색 없음' 관례)으로 교체, 이모지 ✕+20종=21개 7열 3행(+선택 활성 테두리 추가), LinePanel 라벨을 `SectionLabel`로 통일·2버튼 행 2열 그리드. 실브라우저 스크린샷 검증(노드 3구획·메모·모바일 바텀시트 — 그리드가 시트 폭에 맞춰 늘어남), 전체 테스트 무회귀
- ✅ **글머리 기호·번호 매기기(줄 단위 리스트)** 사용자 요청("진짜 리스트 렌더링", 대상 노드+메모+개별도형). 설계: **텍스트 마커가 곧 데이터** — `- `/`* `/`• `(글머리)·`1. `/`1) `(번호)로 시작하는 줄을 렌더러가 리스트로 그린다. 모델·직렬화·CRDT 무변경(옛 문서 호환·협업·undo·`- ` 마크다운 내보내기 공짜). 코어 `list.ts`(`parseListPrefix`/`listDisplayLine`/`continueListMarker`)가 해석의 **단일 소스**, 소비처 4곳이 같은 규칙: 에디터 렌더(`listLines.tsx`의 `ListTextBlock` — [마커|내용] flex 행으로 px 계산 없는 행잉 인덴트, 글머리는 `• ` 글리프 치환(raw와 글자 수 동일 — rich 오프셋 보존), 리스트 줄은 정렬 무관 좌측) / 박스 측정(`wrapMeasure`·`countWrappedLines` — 내용 폭 = 전체−마커 폭, **박스는 측정 기반이라 자동으로 맞게 커짐**) / 홈 썸네일(`wrapRuns`→`WrapLine{indent,list}`) / PNG(`wrapLines` 동일 확장). 자동 이어쓰기: 노드 Shift+Enter(char-model로 직접 — execCommand는 jsdom에 없고 deprecated)·메모 Enter, 글머리=같은 마커·번호=+1, **마커만 남은 빈 줄이면 마커 제거로 리스트 종료**(표준 관례). 리스트 없는 문서는 기존 렌더 경로 그대로(무회귀 가드). 코어 9 + 웹 19 테스트, 실브라우저 검증(불릿/번호 렌더·행잉 인덴트 열 정렬·오버플로 없음·이어쓰기(10→11 포함)·빈 마커 종료·메모·썸네일 파리티). **후속 제보 2건**: ① 편집 중에는 `- 항목` 원문이 보이고 확정해야 리스트가 됐다 → 편집 박스(`listEditHtml`)도 커밋 후와 **같은 [마커|내용] 구조**로 그린다. 마커는 실제 텍스트로 들어가므로(빈 스팬·의사요소 아님) 캐럿/오프셋이 그대로고 `domToRuns`가 읽는 텍스트도 화면과 일치 — 이때 글머리는 표시 글리프 `• `로 **정규화**된다(입력 규칙: `- `를 치면 곧바로 `• `. 글자 수가 같고 `• ` 자체가 유효한 마커라 왕복 안전). 재구성은 줄별 마커 구성(`listSignature`)이 **바뀐 순간에만** 실행해 캐럿·IME를 보호하고, IME 조합 중에는 미루었다가 `compositionend`에서 반영. `applyPartial`도 같은 렌더러를 쓰게 해 서식 적용에 리스트 모양이 풀리지 않게 했다. 트레이드오프: 마커 글자에 걸린 부분 서식은 사라진다(커밋 후 렌더도 마커를 평문으로 그리므로 보이는 결과는 동일). ② 리스트만 정렬 설정을 무시하고 늘 좌측이었다 → 항목([마커|내용]) **블록 전체**를 사용자 정렬대로 배치(`justifyContent`, 내용 스팬을 `flex: 0 1 auto`로 바꿔야 짧은 항목이 행을 안 채워 정렬이 산다). 감긴 줄은 마커 오른쪽 열에 그대로(행잉 인덴트 유지). 썸네일·PNG도 같은 모델(`listBlockLeft(itemW, align)` — 내용 열은 감기면 가용 폭, 아니면 제 폭). 웹 테스트 +9(즉시 렌더 4·정렬 5), 실브라우저 검증(정렬 3종 여백 실측·편집 중 마커 타이핑 즉시 리스트화·캐럿 보존·리스트 종료 후 평문·썸네일 우측 정렬 x 좌표)
- ✅ **리스트 들여쓰기·내어쓰기 + 툴바 버튼 4종(사용자 요청)** 들여쓰기도 같은 원칙 — **마커 앞 공백**이 곧 단계(`LIST_INDENT_UNIT`=2칸, 최대 6단계). 모델 무변경이고 마크다운 중첩 목록 표현과 그대로 일치한다. 코어: `ListPrefix.indent/pad`(정규식에 선행 공백 허용, **`raw`/`display` 길이 동일 계약 유지** — pad는 양쪽에 포함), `applyListOp(text, s0, s1, op)`가 선택이 걸친 줄들의 **접두 편집 목록(`TextEdit[]`)**만 돌려주고(호출부가 char-model에 splice → 부분 서식 보존) `shiftOffset`이 캐럿을 옮긴다. 연산 후 `renumber`가 같은 단계 연속 항목의 번호를 정리하되 **묶음 시작 번호는 보존**(5.로 시작하면 5,6,7). `continueListMarker`의 빈 마커 종료는 `replaceWith`로 바뀌어 **들여쓴 줄은 한 단계 내어쓰기**(표준 관례). 웹: 컨트롤러 `applyListOp`(툴바·단축키 공용 경로), 노드 편집 **Tab/Shift+Tab**(리스트가 아니어도 기본 동작은 차단 — 포커스가 나가면 blur 커밋으로 편집이 끊긴다), 메모 textarea도 동일, `TextToolbar`에 글머리·번호·내어쓰기·들여쓰기 4버튼 추가(폭이 넘쳐 `flexWrap`+`maxWidth 320`으로 2행). **정렬 모델 교정**: 항목을 하나씩 정렬하던 방식은 들여쓰기와 함께 쓰면 깨진다(가운데 정렬에서 1단계 들여쓴 줄의 마커가 상위보다 23px **왼쪽** — 계층이 거꾸로). 연속 리스트 줄을 `width: fit-content` **묶음 상자**로 묶어 그 상자만 정렬(auto margin)하고 안쪽은 항상 좌측 기준 → 마커가 한 열에 서고 들여쓰기가 오른쪽으로 쌓인다. 썸네일·PNG도 묶음 최대 `itemW`로 같은 계산. 코어 +20 / 웹 +14 테스트, 실브라우저 검증(중첩 마커 x 상위 917 < 하위 926·Tab/Shift+Tab·툴바 4버튼·저장 텍스트 2칸 들여쓰기·메모 Tab·오버플로 없음)
- ✅ **SNS 가입 이메일의 이메일 회원가입 차단(제보)** Google로 가입한 이메일로 이메일 회원가입을 시도하면 가입이 진행되는 듯 **인증번호 화면까지 넘어가는데 코드는 영영 오지 않았다**. 원인은 Supabase `auth.signUp`의 이메일 열거 방지 — 이미 가입된 주소에도 **성공을 돌려주고 메일은 보내지 않는다**(유일한 단서가 `identities`가 빈 배열인 가짜 user). 수리 2겹: ① 가입 **전에** 그 이메일의 로그인 수단을 조회(`email_signin_providers` RPC, 0013 — 0008과 같은 트레이드오프로 이메일 열거 허용, 공급자 이름만 반환)해 이미 가입돼 있으면 막고 **어떻게 가입했는지**까지 안내(소셜이면 "Google 계정으로 가입한 이메일 — 아래 Google로 계속하기", 이메일이면 로그인 탭 유도 링크). ② RPC 미배포·네트워크 실패로 확인 불가(`null`)여도 어댑터가 `identities: []`를 감지해 '이미 가입됨' 오류로 바꾸므로 **막다른 인증 화면으로는 절대 넘어가지 않는다**. UI는 비밀번호 찾기의 미가입 툴팁과 같은 톤의 콜아웃(`signupBlockedCalloutStyle`), 이메일을 고치면 해제. 어댑터 7 + 로그인 UI 5 테스트, 데모 모드 가입 흐름 실브라우저 무회귀 확인(차단 경로는 Supabase 전용이라 유닛/통합 테스트로 검증). **후속 제보**: 안내가 뜬 화면에서 'Google 계정으로 로그인'을 누르면 문구만 사라져 **아이콘만 남은 빈 콜아웃**이 됐다 — 콜아웃 본문이 `error`인데 그 흐름(및 모드 전환·비밀번호 수정)이 에러만 비우고 `signupBlocked` 플래그는 남겼기 때문. 두 값이 따로 놀 수 없게 `patch`에서 묶고(에러가 비면 플래그도 해제 — 호출부마다 챙기지 않아도 불변식 유지) 렌더도 `signupBlocked && error`로 가드. 회귀 테스트 3건(Google 클릭·탭 전환·비밀번호 수정)은 수리 전 모두 실패함을 확인
- ✅ **문서 id 충돌 42501 수리 + 새 id 이전(제보: Supabase 로그)** `row-level security policy **(USING expression)**`가 자동저장마다 쌓였다. 이 괄호 변형은 Postgres에서 **`INSERT ... ON CONFLICT DO UPDATE`가 이미 있는 행에 부딪혔는데 그 행을 UPDATE할 권한이 없을 때**만 나온다 — 즉 남의 `documents` 행에 upsert를 시도한 것. 뿌리는 **RLS가 "행이 없다"와 "행은 있는데 안 보인다"를 똑같이 빈 결과로 만든다**는 점: `load()`가 null이면 에디터가 신규 문서로 오해하고 그 id에 강제로 썼다. id가 겹치는 경로는 ① 레거시 카드가 `docId` 없이 `map=m<제목해시>`로 열려(`mapHref` 폴백) **다른 계정이 같은 제목**이면 id 동일 ② 공유 해제 뒤 브라우저에 본문 캐시가 남은 경우. 수리: 버전을 모르는 첫 저장을 **upsert→INSERT**로 바꿔(23505로 안전 실패) 이어서 그 행을 읽어 보고 — 읽히면 내 문서(`conflict`로 버전 통지), 안 읽히면 남의 문서(`idTaken` 신설). 에디터는 `idTaken`을 받으면 **새 랜덤 id로 옮겨 저장**하고(`moveToFreshId`) 이후 저장·로컬 캐시·협업 채널을 그 id로 돌리며, 주소창을 replace로 교체하고, 홈 워크스페이스를 재바인딩한다(`rebindMovedDoc` — 카드 `docId`·`mapFolders`·`recent` 키를 함께 옮기고 **레거시 카드는 `mapId(제목)`으로 매칭**, 바뀐 게 없으면 같은 참조 반환). 사용자에겐 DocChip 전광판 배너로 한 번 알린다. **RLS는 제 역할을 했다** — 정책이 없었다면 그 upsert가 남의 문서를 조용히 덮어썼을 요청이다. 어댑터 3 + 순수 헬퍼 6 + 에디터 통합 3 테스트(전체 751 통과), 로컬 모드 저장·새 맵 생성 실브라우저 무회귀 확인
- ✅ **캔버스 배경이 화면 이동 중 깨져 보이던 문제(제보)** 팬하거나 노드 추가로 화면이 따라 움직일 때 배경(색·도트)이 깨져 보였다. 도트 자체는 결백했다 — DPR 1/1.25/1.5/2에서 격자·명도 모두 균일하고 팬 전후로 불변임을 실측(첫 측정에서 "가장 어두운 값 93"이 나온 건 크롭이 하단 상태바 **글자**를 물었던 오진). 진짜 원인은 **레이어 구성**: 배경 그라디언트가 `.mf-ed-vp` 자신에 있었고 그 안의 팬/줌 레이어가 움직일 때마다 브라우저가 뷰포트 **전체를 다시 래스터**했다(CDP `LayerTree.layerPainted` 실측 — 20스텝 팬에 전면 리페인트 9회, 총 8.4M px). 매 프레임 새로 그려지는 전면 그라디언트는 실기기의 GPU/메모리 압박에서 타일이 늦거나 저해상도로 올라오며 "깨짐"으로 보인다. 수리 두 줄: ① 배경을 `data-canvas-bg` **전용 레이어**로 분리(`z-index:0`, `pointer-events:none`, `translateZ(0)`)해 **한 번만 래스터**하고 재사용 ② 팬/줌 div에 `willChange: transform` + **`inset: 0`** — 자식이 전부 absolute라 이 div는 0×0이었고, **크기 없는 요소는 승격돼도 합성기가 재활용할 래스터가 없어** 부모 레이어가 매 프레임 무효화됐다(원점이 (0,0) 그대로라 자식 좌표는 불변). 결과 8.4M px → 3.6M px, 배경 레이어 페인트 횟수 = 1. 구조 계약을 테스트로 고정(배경이 팬 레이어 밖·`.mf-ed-vp`에 배경 없음·`translateZ(0)`·`pointer-events:none`·팬 레이어 `willChange`), 테스트 헬퍼는 `div[style*="translate"]`가 배경의 `translateZ(0)`까지 잡아 `[data-pan-layer]`로 교체. 실브라우저 검증: 픽셀 파리티(차이 0.46% = 합성 레이어의 글자 안티에일리어싱뿐, 배경·노드 동일)와 인터랙션 무회귀(노드 선택·해제·우클릭 메뉴·노드 드래그·팬·더블클릭 편집 — 팬 레이어가 뷰포트를 덮게 됐지만 투명·무핸들러라 이벤트는 그대로 `.mf-ed-vp`로 버블)
- ✅ **서식 툴바 정돈(요청 2건)** ① 리스트 버튼 순서를 **번호 매기기 → 글머리 기호**로(사용자 선정). ② 텍스트 색상이 항상 새 줄에서 시작하도록 — 예전엔 한 줄 flex에 `flexWrap`만 걸어 접히는 지점이 **스와치 중간**이라 앞 두 색이 들여쓰기 버튼 뒤에 매달렸다(제보). 루트를 `flexDirection: column` **명시적 2행**(서식·리스트 / 색상·지우기)으로 바꿔 어떤 폭·테마에서도 줄이 고정된다. 회귀 테스트 2건(버튼 순서 `compareDocumentPosition`, 색상 줄이 서식·리스트 버튼을 품지 않음 + `flexDirection`), 테스트 헬퍼의 `closest('div')`는 이제 행 div를 잡으므로 `closest('.mf-tctx')`로 교체. 실브라우저 확인(데스크톱 1행 239px / 2행 297px, 390px 모바일에서도 화면 안)
- ✅ **GNB 로고 → 홈(요청)** 에디터 상단바의 브랜드 로고(마크+워드마크)를 버튼으로 만들어 독칩 홈 버튼과 **같은 `controller.goHome`**을 연결했다(저장을 먼저 태우고 이동하는 동작이 갈라지지 않게 핸들러를 공유). 시각은 그대로 — 클릭 영역용 padding을 넣고 `marginLeft: -4`로 상쇄해 정렬이 안 밀린다. 호버는 기존 `mf-ed-btn` 클래스. 접근 이름은 독칩의 "홈으로"와 겹치지 않게 `Geurio 홈으로`(툴팁은 "홈으로"). 아웃라인 보기에는 독칩이 없어 이제 로고가 유일한 홈 진입점이 된다. 테스트 2건(이동·미저장 편집이 저장된 뒤 이동), 실브라우저 데스크톱/390px 모바일 모두 `/home` 이동 확인
- ✅ **단계별 리스트 마커(요청)** 들여쓰기 단계마다 마커가 바뀌고 **네 번째는 첫 번째로 돌아간다**(3단계 주기) — 번호 `1.`→`a.`→`i.`, 글머리 `•`→`◦`→`▪`(사용자 첨부 이미지 기준). 설계는 기존 원칙 그대로 **텍스트 마커가 곧 데이터**: 들여쓰기/내어쓰기가 텍스트의 마커 자체를 그 단계 표기로 갈아 끼운다(모델·직렬화·CRDT 무변경, 저장본만 봐도 무엇이 보일지 안다). 코어 `list.ts`: `bulletGlyphFor`/`ordinalStyleFor`(3주기)·`formatOrdinal`/`parseOrdinal`(decimal/alpha bijective base-26/roman), `UL_RE`에 `◦▪` 추가, `OL_RE`가 소문자 표기도 받되 **그 단계 스타일일 때만** 인정(`isOrdinalToken` — 최상위의 `a. 그리고…` 같은 평문이 목록이 되는 오탐 차단. 숫자는 어느 단계에서나 허용해 옛 문서·마크다운 호환). `renumber`·`continueListMarker`·`applyListOp`(indent·toggle) 전부 단계 표기로 통일. **번호 규칙 2가지**: 들여쓰기/내어쓰기는 순번을 1로 리셋하고 `renumber`가 앞 형제를 보고 이어 준다(값을 그대로 옮기면 `a.`도 없이 `b.`로 시작하는 하위 목록이 생긴다), 빈 마커 줄의 Enter 내어쓰기는 **값을 유지**한다(비어 있던 `b.`를 내어쓰면 부모의 2번을 이어받는 게 자연스럽다). `raw`/`display` 길이 계약은 그대로(글리프 1자 치환, 번호는 display=raw). 소비처 4곳(에디터 렌더·박스 측정·홈 썸네일·PNG)은 `parseListPrefix().display`만 쓰므로 자동 반영. 코어 +10 / 웹 +4 테스트(기존 기대값 갱신), 실브라우저 검증(중첩 4단계 렌더 `• ◦ ▪ •` / `1. a. i. 1.`, Tab 반복 순환, 하위 목록 a·b, Shift+Tab 복귀, Shift+Enter 이어쓰기 `ii.`, 커밋 후 저장 텍스트, 홈 썸네일 파리티)
- ✅ **리스트 들여쓰기 간격 확대(제보: 너무 좁다)** 저장 단위는 그대로 두고 **표시 폭만** 넓혔다 — `display`의 들여쓰기 공백을 EN SPACE(`U+2002`, 0.5em)로 **1:1 치환**(`displayPad`). 1:1이라 `raw`/`display` 길이 계약이 그대로고, 소비처 4곳(에디터 렌더·박스 측정·홈 썸네일·PNG)이 전부 `display`를 재기 때문에 **추가 코드 없이** 네 곳의 폭이 함께 넓어진다(단계당 8.7px → 18.7px, 실측). `LIST_INDENT_UNIT`은 일반 공백 2칸 그대로여서 **이미 저장된 문서의 단계가 흔들리지 않는다**(단위를 4칸으로 키웠다면 기존 2칸 문서가 전부 최상위로 내려앉는다). 왕복 안전: 파서가 `U+2002`도 들여쓰기로 인정(방어)하고, 편집 박스를 읽는 `domToRuns`가 커밋 시 일반 공백으로 되돌려 **저장본에는 보이지 않는 문자가 남지 않는다**(마크다운 중첩·외부 복사·diff). 코어 +3 / 웹 기대값 갱신, 실브라우저 검증(에디터 4단계 계단·홈 썸네일 동일·커밋 후 저장본 EN SPACE 0개)
- ✅ **에디터 진입 시 전체가 흐릿하던 문제(제보) — 앞선 합성 힌트 되돌림** "들어가면 텍스트·객체가 흐릿한데 객체를 편집하면 그것만 선명해진다". 원인은 **직전 배경 수정에서 넣은 `will-change: transform`**이었다: 브라우저는 승격된 레이어의 래스터 배율을 첫 프레임 배율에 고정하는데, 에디터는 `scale(1)`로 마운트한 뒤 **한 프레임 뒤**(실측 154ms→167ms) 중앙 정렬 배율 1.25로 바뀐다 → 화면 전체가 1배 텍스처를 1.25배로 늘린 모습. 편집한 객체만 무효화돼 다시 그려지니 그것만 또렷해지는 것도 같은 이치. 수리: 팬 레이어에서 `will-change`와 `inset: 0`을 **모두 제거**(원래 `left/top` 0×0 기하로 복귀). **배경 재래스터 문제는 `data-canvas-bg` 분리만으로 이미 해결돼 있었다** — 20스텝 팬에서 콘텐츠 레이어는 21회 다시 칠해지지만 배경 레이어는 **1회**뿐(CDP LayerTree 실측, 힌트 유무와 무관). 앞 항목이 근거로 삼았던 "총 리페인트 면적 8.4M→3.6M"은 재현되지 않는 잡음이었다(같은 벤치를 다시 돌리면 순서가 뒤집힌다) — 레이어 **분리 효과는 페인트 횟수로 확인**되고 그것이 제보의 원인이었다. 회귀 테스트를 "`willChange`가 `transform`이다"에서 **"팬 중에도 걸리지 않는다"**로 뒤집어 고정. 실브라우저 재검증(팬 전/중/후 `will-change: auto`, 노드 선택·해제·우클릭 메뉴·드래그·팬·더블클릭 편집 무회귀)
- ✅ **번호 매기기 후속 2건(제보)** ① **들여쓰기 시 정렬이 틀어짐** — 컨트롤러가 편집 중 DOM을 다시 그릴 때 `editedNodeAlign`이 `n.align`을 **날것**으로 읽어 정렬 미지정 노드에서 `undefined`(=좌측)가 됐다. 렌더(`NodeLayer`)는 `n.align || 'center'`를 쓰므로 Shift+Enter(NodeEditBox 경로)는 가운데, Tab(컨트롤러 경로)은 좌측 — 조작할 때마다 묶음이 좌우로 튀었다. 기본값을 `listLines.nodeTextAlign(n)` **한 곳**으로 모아 두 경로가 같은 값을 쓰게 했다. ② **Backspace가 Tab처럼 보임** — 마커 `2. `에서 한 글자(공백)만 지워지면 그 줄이 리스트에서 빠지고, 남은 `2.`가 평문 정렬(가운데)을 따라 옆으로 튀어 들여쓰기처럼 보였다. 마커는 **한 덩어리**로 다룬다: 코어 `listBackspaceOp(text, caret)`(순수 — 캐럿이 들여쓰기 공백 포함 마커 안이면 들여쓴 줄은 내어쓰기, 최상위 줄은 마커 제거. 줄 맨 앞은 `null`로 두어 앞 줄과 합치는 평범한 삭제를 지킨다)를 노드 편집(`listBackspaceOpAt` → `controller.applyListOp`)과 메모 textarea가 함께 쓴다. **곁가지 버그 하나 더**: `setLinearSelection`이 오프셋을 못 풀면 편집 박스 **전체를 선택**해 버려서, 마커를 지운 뒤 빈 줄에 한 글자만 쳐도 본문이 통째로 날아갔다(`<br>`뿐인 빈 줄에 대응하는 텍스트 노드가 없다). `<br>`을 캐럿 자리로 인정하고, 그래도 못 찾으면 **마지막으로 지나온 자리**로 접히게 했다. 코어 +4 / 웹 +6 테스트(세 건은 수리 전 실패 확인), 실브라우저 검증(정렬 4단계 모두 `margin:0 auto` 유지·Backspace 후 마커 제거·이어서 타이핑하면 빈 줄에 입력)
- ✅ **빈 번호 항목 삭제 시 캐럿이 도형 하단 가운데로 떨어지던 문제(제보)** 직전 수리로 마커 안 Backspace가 **마커만** 없앴는데, 그러면 내용 없는 항목 자리에 **빈 줄**이 남는다. 빈 줄은 리스트 묶음 밖의 평문이라 노드 정렬(가운데)을 따르고, 게다가 박스 측정이 후행 빈 줄을 세지 않아(평문 노드도 마찬가지 — 기존 동작) 캐럿이 도형 하단 가운데에 떠 있는 것처럼 보였다(실측: 캐럿 x=808 = 편집 박스 중앙). 수리: **내용이 빈 최상위 항목**은 마커와 **앞의 줄바꿈까지** 지워 항목을 통째로 없애고 캐럿을 앞 줄 끝에 둔다(들여쓴 줄은 기존대로 한 단계 내어쓰기 — 사다리처럼 내려온 뒤 사라진다. 첫 줄이라 지울 줄바꿈이 없으면 마커만 제거). 가운데 항목을 지우면 `1., 3.`으로 벌어지므로 삭제 후 텍스트에서 계산한 **번호 재매김 편집을 원래 인덱스로 되돌려 함께** 돌려준다(`renumberEdits`, 서로 겹치지 않음). `listBackspaceOp`의 반환을 `{kind:'op'|'edit'}` 유니온으로 바꾸고, 컨트롤러는 `applyListOp`/`applyListEdits`가 같은 몸통(`withEditorSelection`)을 쓰게 정리했다. 코어 +2 / 웹 +1 테스트, 실브라우저 검증(삭제 후 캐럿이 앞 줄 끝·이어서 타이핑하면 그 자리에 입력·박스 높이 1줄로 정상)
- ✅ **리스트에 긴 텍스트를 쓰면 도형을 벗어나던 문제(제보 스크린샷)** 편집 중 줄은 `[마커 스팬][내용 스팬]` flex 행인데, 마커 스팬은 들여쓰기 공백을 지키려 `white-space: pre` + `flex-shrink: 0`이다. **Shift+Enter·Tab 직후 캐럿이 정확히 마커 끝 경계**에 놓이고 `setLinearSelection`이 그 경계를 앞 노드(=마커 텍스트)로 해석해, 이어지는 타이핑이 전부 마커 스팬 안에 쌓였다 → `pre`라 줄바꿈이 안 돼 박스를 뚫고 나갔다(실측: 460px 마커 스팬, 박스 밖으로 149px). 줄 구성(`listSignature`)은 그대로라 재구성도 안 걸렸다. 수리 2겹: ① 마커 스팬에 `data-list-marker`를 새기고 `setLinearSelection`이 **마커 끝 경계를 내용 쪽에 양보**(빈 내용 스팬은 `<br>` 자리로 해석) ② 그래도 마커 안에 글자가 들어가면(캐럿을 직접 옮긴 경우) `syncListStructure`가 **마커 드리프트**(`markerSignature` vs `domMarkerSignature`)를 보고 다음 입력에서 스스로 고친다. 커밋된 렌더(`ListTextBlock`)는 원래 정상이라 편집 중에만 나던 증상. 웹 +3 테스트(세 건 모두 수리 전 실패 확인), 실브라우저 검증(2·3단계 들여쓴 긴 줄 오버플로 149px → 0, 감긴 줄이 내용 열에 정렬)
- ✅ **리스트 정렬을 항목마다로 (제보: 정렬 설정이 안 먹는다)** 연속 항목을 `width: fit-content` 상자로 묶어 **묶음만** 정렬하던 모델(마커를 한 열에 세우려던 내 설계)을 걷어내고, 워드프로세서 표준대로 **항목마다** 정렬한다(`listItemJustify` — 행 flex의 `justify-content`). 묶음 모델이 틀렸다는 증거는 실측이었다: 도형이 내용 크기에 딱 맞게 커지므로 묶음이 움직일 여백이 **5px**밖에 없어(box 211 / group 163 / padding 42) 좌·중앙·우가 사실상 구별되지 않았다 — 사용자 눈에는 늘 좌측이었다. 지금은 짧은 항목이 890 → 950 → 1009px로 또렷이 움직인다. 소비처 4곳을 같은 모델로: 에디터 렌더(`ListTextBlock`)·편집 박스(`listEditHtml`)는 행마다 `justifyContent`, 썸네일(`mapPreview`)·PNG(`png.ts`)는 묶음 최대 폭으로 통일하던 `groupItemW`/정규화 루프를 제거해 각 항목이 **자기 `itemW`**로 정렬된다. **트레이드오프(사용자 선택)**: 가운데·오른쪽 정렬에서는 마커가 한 열에 서지 않고, 들여쓴 하위 항목이 상위보다 왼쪽에 놓일 수 있다 — Word·Google Docs도 같다. 정렬 테스트 6건을 새 모델로 다시 쓰고(묶음 margin → 행 justify), 실브라우저·홈 썸네일 파리티 확인
- ✅ **하이퍼링크(요청)** 선택한 **텍스트 일부**에 링크를 건다(사용자 선정 — 도형 전체 링크가 아니라 부분 서식 방식). 기존 부분 리치텍스트에 얹었다: `RichRun.href`(post-dc 순수 추가, **값이 있을 때만** 직렬화 — 옛 문서·골든·CRDT 무회귀), `applyPartialStyle` kind `'link'`(값=주소, `null`이면 해제, `'clear'`가 함께 뗌), `runsToChars`/`charsToRuns`가 href로도 런을 가른다. **보안은 저장 시점에**: 신설 `url.ts`의 `normalizeUrl`이 스킴 허용 목록(http/https/mailto)으로 좁히고 제어문자·공백 우회를 막아 `javascript:`류는 **문서에 들어가지도 못한다**(렌더에서 거르면 협업·내보내기 등 다른 경로로 샌다). 스킴 없으면 `https://`, 이메일꼴이면 `mailto:` 자동. **함정 하나**: "서식이 있는가" 판정(`r.b||r.c||r.i||r.s`)이 네 곳에 흩어져 있어 링크만 걸린 런이 평문으로 접혀 링크가 사라졌다 → 코어 `isStyledRuns` 한 곳으로 모음. 웹: `richSpans.tsx`(공용 `RichSpan` — 링크는 밑줄 + `data-href` span), 툴바에 링크 버튼 + 주소 입력 3행(입력창에 포커스가 가면 편집 박스가 blur 커밋되어 편집이 끊기므로 `pauseBlurCommit`으로 잠그고, 선택은 열 때 `selectionRange`로 잡아 둔다 → `applyPartialRange`로 적용). **여는 방법은 Ctrl/⌘+클릭**(단일 클릭=선택, 더블클릭=편집과 충돌 회피, `noopener,noreferrer`), 수정 키를 누르고 있는 동안만 손가락 커서(`.mf-ed-linkmod`). 진짜 `<a href>`를 쓰지 않은 이유: 평범한 클릭 한 번에 SPA가 통째로 그 주소로 떠난다. **실브라우저에서 잡은 버그**: 노드 본문이 평소 `pointer-events: none`(클릭을 도형 선택으로 통과)이라 Ctrl+클릭이 링크에 **닿지도 못했다** → 링크 span만 `pointer-events: auto`. 홈 썸네일도 밑줄로 표시. 코어 +13 / 웹 +8 테스트, 실브라우저 전 흐름 검증(적용·https 보정·저장·Ctrl+클릭 `window.open` 인자·평범한 클릭은 선택). **범위 밖**: 메모(플로트)는 rich 모델이 없어 링크 불가, PNG 내보내기는 원래 런 서식을 그리지 않아 링크도 평문, URL 자동 인식은 하지 않음
- ✅ **URL 자동 링크 + 링크 후속 3건(요청)** ① **자동 링크**: 코어 `findAutoLinks`(`url.ts`)가 `http(s)://`·`www.`·이메일만 잡고(맨 도메인은 일부러 제외 — `1.5`·`report.pdf`가 링크가 된다), `applyAutoLinks`(`richtext.ts`)가 char-model에서 그 구간에만 href를 심어 **손으로 건 링크는 덮지 않는다**. 붙이는 지점은 `commitNodeRichText` — 마크다운 단축(`applyMarkdownShortcuts`)과 **같은 커밋 훅**이다. 타이핑 중 실시간으로 걸지 않은 이유: 반쯤 친 `https://ex`가 링크가 됐다 풀렸다 하며 편집 박스를 재구성해 캐럿·IME가 흔들린다(리스트 마커에서 이미 겪은 함정). 대가는 "지금 친 URL은 확정해야 눌린다" 하나. ② **선택이 바뀌면 링크 입력창이 닫힌다**: `selectionRange()`가 편집 박스 **밖** 선택에 `null`을 돌려주도록 가드하고(`ed.contains`), 툴바가 `selectionchange`로 잡아 둔 범위와 어긋나면 입력 상태를 비우고 `pauseBlurCommit`을 푼다 — 남아 있던 입력창이 **엉뚱한 선택에 링크를 걸던** 상태 불일치가 사라진다. ③ **비활성처럼 보이던 버튼 색**: `clearButtonStyle`의 글자색이 `th.subtext`라 `제거`·`지우기`가 늘 회색이었다 → `th.text`. 진짜로 비활성인 `적용`(주소가 유효하지 않을 때)만 `subtext`로 남겨 **회색 = 못 누름**이라는 신호를 되살렸다. ④ **편집 중에도 Ctrl/⌘+클릭으로 열기**: `NodeEditBox`의 `onClick`이 수정 키가 눌린 클릭만 가로채 `[data-href]`를 열고(`preventDefault`/`stopPropagation`) **편집은 그대로 유지**한다. 코어 +9 / 웹 +5 테스트, 실브라우저 검증(재선택 시 입력창 닫힘·버튼 색 실측·URL 타이핑→커밋→저장본 href·편집 중 Ctrl+클릭 `window.open` 인자)
- ✅ **링크 글자를 파랑으로(요청)** 밑줄만으로는 링크임이 약하다는 요청 — 파란 글자를 더했다. 색은 **`--mf-link` CSS 변수**로 도형이 내려 주고 링크 span은 `.mf-link` 클래스만 단다: 인라인 `color`로 심으면 커밋 때 `domToRuns`가 그걸 런의 `c`로 저장해 **링크를 떼도 파란 글자가 남는다**. 값은 배경이 아니라 도형의 **글자색** 밝기로 고른다(`linkInk` — 글자색은 이미 "이 배경에서 읽히도록" 정해진 값이라 다크 테마·진한 루트 도형에서도 파랑이 묻히지 않는다). 런에 사용자가 고른 색이 있으면 안쪽 span의 인라인 색이 이겨 그 색이 보인다(색 지정이 안 먹히면 그게 더 이상하다). 소비처는 커밋 렌더(`RichSpan`)·편집 박스(`runsToHtml`)·홈 썸네일 셋 — 앞의 둘은 같은 클래스라 자동으로 일치. **실브라우저에서 잡은 것 3가지**: ① 링크 글자를 통째로 지우고 새로 타이핑하면 크롬이 그 자리의 계산된 색을 `<font color="#1a63d8">`로 굳혀 넣어(typing style) 파랑이 결국 모델에 저장됐다 → `isLinkInk`로 `style.color`·`FONT[color]` 양쪽에서 거른다(색 선택은 스와치 전용이고 두 링크색은 어느 팔레트에도 없어 잃는 게 없다). ② 썸네일이 하드 줄바꿈 분해에서 `href`를 떨어뜨려 **링크 표시가 아예 없었다**(하이퍼링크 PR의 누락) → 전달 복원. ③ 크롬은 SVG 장식을 **베이스라인 기준**으로 그리는데 썸네일 본문이 `dominant-baseline="central"`이라 글자만 아래로 밀려 **밑줄이 글자 위에 그어졌다** → 본문 텍스트만 `middle`로(20px에서 1.7px 차, 카드 축척에선 구분 불가 — 카드 스크린샷 비교로 확인). 웹 +8 테스트, 실브라우저 검증(코랄·다크 × 루트·본문·지정색, 편집 박스, 커밋 후 저장본, 홈 썸네일). **남은 것(별건)**: 썸네일의 취소선은 `text-decoration="line-through"`가 붙는데도 보이지 않는다 — 이번 변경과 무관한 기존 문제
- ✅ **썸네일 취소선 수리 + 장식을 직접 그리기(제보)** 홈 카드 썸네일에서 취소선이 `text-decoration="line-through"` 속성이 붙는데도 보이지 않았다. 원인 두 겹: ① 크롬은 SVG 장식을 **베이스라인 기준**으로 그리는데 본문 텍스트는 `dominant-baseline`으로 글자를 세로 가운데 맞추므로 장식만 제자리에 남아 글자 윗부분을 스쳤다(같은 이유로 링크 밑줄이 글자 **위**에 그어졌었다) ② 두께 `auto`가 폰트 크기에서 나오는데 카드 안에서 SVG가 0.5~0.7배로 줄어 1 CSS px 아래가 되고, 글자 위를 지나는 취소선은 안티에일리어싱에 묻혀 사라졌다(밑줄은 빈 배경 위라 살아남아 "밑줄만 보이고 취소선은 안 보인다"가 됐다). 수리: `text-decoration`을 버리고 **`decoRects`가 `<rect>`로 직접** 그린다 — 위치·두께를 우리가 정하므로 두 장식이 같은 규칙을 따르고 축척과 무관하게 보인다(두께 `max(1.8, fpx*0.09)` user unit). x는 `wrapRuns`가 이미 재고 있던 토큰 폭을 세그까지 합산해(`WrapSeg.w`/`WrapLine.w`) 줄 왼쪽부터 누적한다 — 정렬(`lineLeftOf`)·리스트 마커·다중 세그 모두 같은 계산. 한글 음절 블록은 x-height 중앙보다 살짝 위가 눈에 가운데라 취소선을 `fpx*0.04`만큼 올린다. 웹 +1 테스트(장식 rect 위치·두께 가드), 실브라우저 검증 5종(가운데/왼쪽/오른쪽 정렬·리스트 줄·취소선+링크 동시)에서 rect x·폭이 세그와 1px 내로 일치하고 에디터 렌더와 같은 모습. **에디터는 원래 정상이었다**(HTML `line-through`) — 썸네일만의 문제였다
- ✅ **도형 편집 줄바꿈 2건(제보)** ① Shift+Enter를 **두 번** 눌러야 줄이 바뀌고 ② 줄을 바꾼 뒤 **글자를 쳐야** 도형이 커졌다. 뿌리는 하나 — 리스트가 아닌 줄의 Shift+Enter를 **브라우저 기본에 맡기고 있었다**. contentEditable에서 글 끝에 넣은 `<br>` 하나는 화면에 나타나지 않고(브라우저는 자기 placeholder를 하나 더 둔다), 그 시점에 우리 측정(`updateNodeEditSize`)도 돌지 않는다. 게다가 측정이 쓰는 `domToRuns(el)`는 **후행 줄바꿈을 전부 버려서**, 텍스트가 맞더라도 빈 마지막 줄을 세지 않았다. 수리 3겹: 신설 `insertLineBreak`가 리스트 이어쓰기(`maybeContinueList`)와 **같은 char-model 경로**로 `\n`을 넣고 곧바로 `render`(=다시 그리기 + 크기 갱신)까지 태운다 / `listEditHtml`은 텍스트가 `\n`으로 끝나면 placeholder `<br>`를 하나 더 붙여 빈 마지막 줄이 보이게 한다 / `updateNodeEditSize`는 `domToRuns(el, true)`로 읽어 그 빈 줄을 센다(placeholder 하나를 접는 모드라 곧 실제 값). 리스트 이어쓰기·부분 서식·선택 대체·커밋 왕복 모두 무회귀(실브라우저 4종 확인 — 중간 삽입, 리스트, 굵게 보존, 저장본). 웹 +2 테스트(둘 다 수리 전 실패 확인: `preventDefault` 여부, 줄바꿈 직후 박스 높이 변화)
- ✅ **리스트를 끝낸 빈 줄에서 마커가 되살아나던 문제(제보)** `1. 단어` → Shift+Enter(`2. ` 이어짐) → Shift+Enter(마커 제거 = 리스트 끝) → 한 번 더 누르면 평범한 빈 줄이 되어야 하는데 **`2. `가 다시 생겼다**. 원인은 **좌표계 어긋남**: 캐럿 오프셋은 `linearize`(편집 박스의 placeholder `<br>`까지 한 글자로 셈)에서 오는데, 값은 `domToRuns(el)`(후행 줄바꿈을 **전부** 버림)로 읽고 있었다. 빈 마지막 줄이 값에서 사라지니 그 줄의 캐럿이 **앞 줄**(=리스트 줄)로 읽혀 `continueListMarker`가 다시 마커를 이어 준 것. 수리: `liveEditValue(el)` 신설 — `keepTrailing`으로 읽어 빈 줄을 값에 남기고 오프셋을 값 길이로 클램프해 두 좌표계를 맞춘다. `linearize` 오프셋과 값을 함께 쓰는 곳을 전부 이 헬퍼로 통일(`maybeContinueList`·`insertLineBreak`·`selectionRange`·`applyPartialRange`·`selectionLink`) — 같은 어긋남으로 **끝에 만들어 둔 빈 줄이 서식 한 번에 사라지던** 잠복 버그도 함께 해결(실브라우저 확인: 굵게 적용 후에도 빈 줄 유지·저장본에 `\n` 남음). 웹 +1 테스트(제보 순서 그대로, 수리 전 `1. 단어\n2. `가 나오는 것 확인)
- ✅ **빈 줄 뒤에서 새 리스트를 시작하면 마커가 부서지던 문제(제보)** 리스트 뒤에 빈 줄을 만들어 두고 그 줄에 `1. `을 치면 마커는 생기는데 이어지는 글자가 `1.새 `처럼 **마커 안에 쌓여** 리스트가 풀렸다. 원인은 `setLinearSelection`의 블록 줄바꿈 계산이 `linearize`/`domToRuns`와 **달랐던 것**: 두 곳은 앞이 이미 줄바꿈이면 블록(`<div>`)의 암묵적 줄바꿈을 더하지 않는데, `setLinearSelection`만 무조건 1을 더했다. 그래서 빈 줄(`<div><br></div>`) 하나마다 오프셋이 1씩 밀려, 마커가 생겨 DOM을 재구성한 뒤 캐럿이 마커 스팬 **안**(`1. `의 2번 자리)에 복원됐다 — 마커 스팬은 `white-space: pre`라 그 뒤 입력이 전부 거기 쌓인다. 수리: `lastNl` 상태를 두어 세 함수가 **같은 규칙**으로 세게 했다(맨 앞 블록은 줄바꿈 없음 = 초기값 `true`). 빈 줄이 있는 문서에서 서식·리스트 연산 뒤 캐럿이 밀리던 문제 전반에 해당. 웹 +3 테스트(단위 2 — 빈 줄 1개·여러 개에서 `setLinearSelection`→`linearize` 왕복, 통합 1 — 제보 흐름. 셋 다 수리 전 실패 확인), 실브라우저 검증(빈 줄 뒤 `1. 새항목` 정상 입력·저장본 `1. 단어\n\n1. 새항목`)
- ✅ **내어쓴 항목이 상위 번호를 잇지 않던 문제 + 리스트 종합 검증(제보)** `1.`·`2.` 아래에 `a.`를 만들고 빈 하위 마커에서 Shift+Enter로 내어쓰면 `3.`이 아니라 **`2.`가 다시** 생겼다. 원인: `continueListMarker`는 **한 줄만** 보므로 빈 `b.`를 내어쓸 때 "값 유지"(b→2)로 정했는데, 앞의 상위 형제(`2.`)를 이어 `3.`이 되어야 한다(Tab의 `applyListOp`는 자체 `renumber`로 이미 맞고, 이 이어쓰기 경로만 빠져 있었다). 수리: 코어 `renumberEdits`(텍스트 전체 번호 재매김)를 **export**하고, 이어쓰기/내어쓰기 직후 두 소비처(노드 `maybeContinueList`·메모 textarea)가 적용 — 캐럿은 `shiftOffset`으로 보정, 노드는 char-model에 splice라 부분 서식 보존. 덤으로 **목록 중간에 항목을 끼우면 뒤 번호가 밀리지 않던** 잠복 버그도 같은 수리로 해결(1,2,3에서 2 뒤에 끼우면 3,4로). 코어 +6 / 웹 +2 테스트. **종합 검증(요청)**: 실브라우저 배터리 2본(39 케이스 전부 통과) — 이어쓰기/종료/재개(글머리·번호·`1)` 구분자·`5.` 시작 보존), 제보 시나리오, 중간 삽입 재매김, 3단(i.→b.→2.) 내어쓰기 사슬, Tab 순환·복귀, 마커 Backspace(내어쓰기·항목 삭제·재매김), 여러 줄 선택 일괄 들여쓰기, 툴바 토글(번호↔글머리↔해제), 빈 줄 뒤 새 리스트(직전 수리 무회귀), 부분 서식(굵게)+들여쓰기 보존, 마크다운 단축+리스트 커밋, 저장 왕복, 메모 textarea 동일 규칙
- ✅ **편집 중 빈 박스가 한 글자 박스보다 넓던 문제(제보)** 도형에서 글자를 전부 지우면 박스가 78.8px, 한 글자를 치면 72.5px — **글자를 쳤는데 박스가 줄어드는** 모양이 됐다. 원인은 측정 폴백: `wrapMeasure`가 dc 원본대로 빈 텍스트를 플레이스홀더 `'주제'`(두 글자) 폭으로 잰다. 커밋된 빈 노드는 그 크기가 맞지만(커밋 시 텍스트도 `주제`로 복원되므로 내용과 일치), **편집 중 실측정**까지 부풀 이유는 없다. 수리: `computeMetrics`에 `opts.emptyAsIs` 추가 — 편집 라이브 측정(`updateNodeEditSize`)만 빈 값 그대로 재서 최소 폭(minW)으로 내려앉고, 한 글자 박스와 같은 크기가 된다(72.5 = 72.5 실측). 커밋된 노드·썸네일·PNG는 기존 플레이스홀더 크기 그대로(골든·dc 파리티 무회귀, 가드 테스트 포함). 웹 +2 테스트(수리 전 67≠58 실패 확인)
- ✅ **메모(플로트) 서식화 + 편집 키 통일(요청)** 도형에 얹은 서식 전부를 메모에 이식하고, 편집 키를 도형과 동일하게(**Enter=확정, Shift+Enter=줄바꿈** — 기존 메모는 Enter가 줄바꿈이었다). 코어: `Float.rich`(post-dc 순수 추가 — 직렬화/CRDT 제네릭 통과, 평문 메모 무회귀). 웹: `FloatEditBox`를 textarea → **contentEditable**(`.mf-richedit`)로 교체해 노드 편집 기계를 그대로 공유 — `renderListEdit` 시드, `syncListStructure`(마커 즉시 렌더·드리프트 복구), Tab/Backspace 리스트 연산(`controller.applyListOp` — `editedNodeAlign`에 플로트 분기 'left' 추가), Ctrl/Cmd+B·I 가로채기, Ctrl+U 차단, 링크 Ctrl+클릭(편집 중·커밋 렌더 모두), blur 커밋(`pauseBlurCommit` 존중), `maybeContinueList`/`insertLineBreak`(NodeLayer에서 export) 재사용. 커밋은 신설 `commitFloatRichText` — 노드와 같은 훅(마크다운 단축 → URL 자동 링크 → rich 저장, 평문이면 null). **서식 툴바가 메모 편집에서도 상시 노출**(`TextToolbar`의 editingNodeId 조건을 editingFloatId까지 확장 — 같은 richElRef 기반이라 로직 무변경). 커밋 렌더: rich 메모는 `nodeContentLines`+`ListTextBlock`(리스트+rich 동시 처리), 카드가 `--mf-link`를 내려 링크 파랑도 노드와 동일. 측정: `measureFloatHeight`가 rich면 노드와 같은 `wrapMeasure`로 줄 수를 센다(굵게/기울임 폭 반영). 썸네일: 플로트도 rich 세그 tspan(굵게/색/기울임) + `decoRects`(취소선·밑줄) + 링크 파랑. PNG는 노드와 같은 평문 렌더(기존 파리티). 기존 메모 리스트 테스트를 새 키 규칙으로 재작성(+Enter=확정 회귀 가드), 신규 `FloatRich.interactions` 10 + 썸네일/측정/직렬화 테스트. 실브라우저 배터리 13 케이스 전부 통과(Enter 확정·Shift+Enter 줄바꿈·툴바 굵게·마크다운·자동 링크·파란 링크·Ctrl+클릭 열기·리스트 이어쓰기/Tab·리로드 영속) + 에디터/편집 박스/썸네일 시각 파리티 스크린샷 확인
- ✅ **리스트 감긴 줄이 중앙 정렬로 튀던 문제(제보 스크린샷)** 리스트 항목이 길어 줄바꿈되면 **감긴 줄만** 도형 가운데로 갔다. 원인: [마커|내용] 행의 내용 스팬이 `text-align`을 지정하지 않아 도형의 기본값(`center`)을 **상속** — 첫 줄은 스팬이 꽉 차 보여 티가 안 나고, 감긴 줄에서만 드러났다. 수리: 내용 열은 **항상 좌측**(`text-align: left`) — 항목 위치는 `justifyContent`가 정하고 열 안은 행잉 인덴트 기준이라는 기존 모델(#273)의 구멍을 메운 것. 커밋 렌더(`ListTextBlock`)와 편집 박스(`listEditHtml`) 두 곳(썸네일·PNG는 원래 열 좌측 기준이라 무변경). 웹 +2 테스트, 실브라우저 검증(감긴 줄이 내용 열 좌측·편집 박스 computed `left`)
- ✅ **리스트 정렬을 "항상 좌측"으로 (사용자 선정 — Notion 방식)** "리스트에 정렬이 걸리면 이상해 보이는 경우가 많다"는 논의 끝에 정렬 모델을 세 번째로 교체. 여정: ① fit-content 묶음 정렬(마커 열 유지) — 도형이 내용에 딱 맞아 여백 5px, 정렬이 안 보였다 ② 항목마다 정렬(Word 방식, 사용자 선정) — 가운데 정렬에서 마커가 들쭉날쭉하고 하위 항목이 상위보다 왼쪽에 놓여 계층이 거꾸로 읽혔다(제보). ③ 지금: **리스트 줄은 정렬 설정과 무관하게 항상 좌측, 평문 줄만 도형 정렬을 따른다**(Notion·Slack·마크다운 렌더러 관례, 선택지 제시 후 사용자 선정) — 마커가 한 열에 서고, 제목 줄(가운데)+리스트(좌측) 혼합 문서가 의도된 레이아웃처럼 보인다. 소비처 4곳 동일 적용: 에디터 렌더/편집 박스(`LIST_ROW_JUSTIFY='flex-start'` 상수로 단순화, `listItemJustify` 제거), 썸네일·PNG(`listBlockLeft` 제거 — 줄 왼쪽 = 텍스트 열 왼쪽). 트레이드오프: 리스트를 일부러 가운데/우측에 두는 옵션이 사라짐(예측 가능성 우선). 정렬 테스트를 새 모델로 재작성, 실브라우저 검증(제보 스크린샷 문서 — 마커 x 한 열 실측·계단 들여쓰기·행잉 인덴트)
- ✅ **서식 파리티 완성 3종(요청)** 서식 기능이 안 닿던 마지막 세 곳을 메움. ① **PNG 내보내기에 서식** — `png.ts`의 평문 `wrapLines`를 rich 세그 모델(`wrapRichLines`)로 교체: 세그별 폰트(굵게 800/기울임 italic)로 측정·렌더하고, 캔버스엔 `text-decoration`이 없어 취소선·밑줄을 **직접 그린다**(`drawRichLine` — 썸네일 `decoRects`와 같은 위치 규칙, 링크 파랑은 `linkInk`). 노드·메모 모두, 평문은 무서식 세그 하나로 기존과 동일 출력. 정렬은 세그 단위 렌더라 시작 x를 줄 폭으로 직접 계산. ② **메모 속성 패널 I·S** — `floatFullyRich`/`toggleFloatsRichStyle`(노드 `toggleNodesRichStyle`의 플로트 짝, FIRST 대상 기준 규칙 동일) + `toggleFloatRichStyle` 컨트롤러 + FloatPanel의 `BoldSizeRow`에 I·S prop 연결. ③ **마크다운 내보내기에 서식** — 코어 신설 `richToMarkdown`(`applyMarkdownShortcuts`의 역방향): `**굵게**`·`*기울임*`·`~~취소선~~`·`[텍스트](주소)`, 색은 문법이 없어 평문, 마커가 공백을 감싸면 무효라 런 가장자리 공백을 마커 밖으로 뺀다. `toMarkdown`이 노드 라벨·메모에 적용(rich 없으면 그대로 — 무회귀). 코어 +4 / 웹 +2 테스트(전체 코어 202 + 웹 818), 실브라우저 검증(PNG 다운로드 픽셀 — 굵게/기울임/취소선/빨강/리스트 굵은 세그/링크 파랑+밑줄이 에디터와 동일, 패널 I 토글 저장·렌더, 내보낸 .md에 `- **굵게** *기울임* [링크](…)`)
- ✅ **맵 안 텍스트 검색(#20 요청)** 툴바 🔍 버튼(모바일 44px) 또는 **Ctrl/⌘+F**(브라우저 페이지 찾기를 가로챈다 — 캔버스 텍스트는 그걸로 못 찾는다)로 여는 검색 바(`SearchBar.tsx`). 노드는 **트리 순서(DFS)** + 메모(이미지 제외), 대소문자 무시 부분 일치. 일치 전부에 앰버 링(`searchMarks` — Node/FloatLayer가 boxShadow에 얹음, 선택 accent·원격 피어색과 구별), **Enter/버튼으로 이동한 현재 항목만** 실제 선택 + 뷰포트 중앙 이동(`panToCanvasPoint`) — 타이핑마다 화면이 튀지 않게. 첫 Enter는 다음이 아니라 **첫 번째 일치로**(브라우저 찾기 관례), Shift+Enter 역방향, Esc 닫기(링 정리), n/m 카운터(없으면 빨간 '없음'). 접힌 가지 안 노드는 제외(자동 펼침 = 문서 변경이라 하지 않음). 데스크톱=상단 중앙 384px, 모바일=전폭·40px 버튼·**입력 16px**(iOS 자동 확대 방지). **함정 하나**: `controller`는 렌더마다 새 객체라 effect deps에 넣으면 `setSearchMarks(새 Set)`→리렌더 무한 루프 — 일치 구성 시그니처가 바뀔 때만 밀어 넣는다. 웹 +5 테스트, 실브라우저 검증(데스크톱/모바일 스크린샷·이동·선택 링)
- ✅ **단축키 도움말(#23 요청)** `?` 키(편집 중엔 그냥 물음표) 또는 보기/모바일 ☰ 메뉴의 "단축키 도움말"로 여는 모달(`ShortcutHelp.tsx`). 실제 구현된 단축키만 나열(일반/선택·이동/텍스트 편집/리스트/서식 입력 5구획), Mac이면 ⌘ 표기, 데스크톱 2열·모바일 바텀 시트(스크롤). Esc·배경·✕로 닫힘. 웹 +3 테스트, 실브라우저 검증(데스크톱 모달·모바일 시트 스크린샷)
- ✅ **문서 버전 히스토리(#21 요청)** 이전 시점으로 되돌리기. 저장소는 **이 기기 로컬**(localStorage `mindflow_hist_<docId>`, `versionHistory.ts`) — 서버 테이블을 두지 않은 이유는 본문에 base64 이미지가 인라인이라 판마다 서버에 쌓으면 무료 티어 저장량/egress가 감당 불가(썸네일 비용 절감과 같은 제약)이고, 로컬이면 오프라인·데모 모드에서도 동작. UI가 "이 기기에서"임을 밝힘. **알갱이**: 저장 성공마다 기록하되 3분 안의 연속 저장은 마지막 항목을 **갱신**(IntelliJ 로컬 히스토리 방식 — 자동저장 0.9초마다 판이 쌓이는 것 방지), 같은 본문은 dedupe, 상한 30개·3M자(넘치면 오래된 것부터, 쿼터 초과 시 절반 버리고 재시도·그래도 실패면 조용히 포기 — 히스토리가 저장을 방해하면 안 됨). 기록 지점은 `persistDoc`의 **저장 성공 분기**(실패한 본문은 기록 안 함). 모달(`VersionHistory.tsx`, 편집/☰ 메뉴 "버전 기록"): 좌측 시점 목록(M/D HH:MM · 상대시간 · 노드 수), 우측 홈 카드와 같은 실제 렌더 미리보기(`realPreview` 재사용), "이 버전으로 복원"은 **복원 직전 현재 상태를 강제 스냅샷**으로 남기고(돌아올 길 — 단 직전 판과 동일 본문이면 dedupe) `commitDoc`이라 **undo 한 번**으로도 복귀. 모바일=바텀 시트(목록 상단 38%·복원 버튼 44px). 웹 유닛 8(dedupe·간격 갱신·force·상한·쿼터 폴백·왕복·손상 방어) + 통합 3(메뉴→모달·빈 안내·복원+undo+강제 스냅샷), 실브라우저 검증(자동저장 기록→모달→옛 판 미리보기→복원→undo, 모바일 시트)
- ✅ **공유 보기 전용 초대(#22 요청)** 0009가 미리 깔아 둔 `role='view'`(스키마·RLS·Realtime 정책 전부 준비돼 있었고 UI만 없었음)를 실사용으로. **마이그레이션 불필요** — documents SELECT는 view로도, UPDATE·채널 발신은 edit만이라는 서버 게이트가 이미 있었다. 웹 3겹: ① **ShareModal** 초대 행에 권한 셀렉트(편집 가능/보기 전용, 기본 edit) + 참가자 행에서 소유자가 바로 변경(add=upsert 재사용) + **자기 자신 초대 차단**(소유자 자기 행이 생기면 아래 판별이 소유자를 잠근다) ② **에디터 읽기 전용 모드**: 판별은 `shareStore.list(docId)`에서 **내 행**의 role(RLS의 결 — 초대받은 사람은 자기 행만 보이고 소유자 목록엔 자기 행이 없다. 실패 시 edit 유지 — 진짜 게이트는 서버라 안전). 차단은 **chokepoint 한 곳**(`commitDoc` 이른 return — 드래그 미리보기까지 전 변이가 이 문을 지난다) + persistDoc(판별 전 창의 42501 소음 방지) + 편집 세션 진입 5종(start*Edit — 선택은 허용)·addChild/Sibling(no-op 커밋 뒤 유령 편집 방지)·컨텍스트 메뉴. UI는 편집/삽입/스타일 메뉴·속성 패널·모바일 선택 바·저장 버튼·버전 기록을 감추고 GNB에 눈 아이콘 "보기 전용" 배지 + DocChip 상태를 "보기 전용"으로. **협업은 붙지 않는다**(collabDocId='') — 0009 발신 정책이 edit 전용이고 Yjs 프로토콜(합류 SV 교환·15초 치유)이 양방향 발신 전제라, 발신 막힌 채 붙으면 증분이 영영 보류된다(#229의 교훈). 뷰어는 저장된 최신 판을 열람(리로드=새 판) ③ **홈 LNB**: 공유받은 맵의 "보기" 배지가 실권한을 표시 — 배지는 원래 있었는데 role이 늘 edit 폴백이었다(`listSharedWithMe`를 hydrate에 합류시켜 실제 role 매핑). 웹 읽기전용 4 + 공유 모달 role 3 테스트(전체 844), 실브라우저 10종(배지·메뉴 숨김·더블클릭/드래그/우클릭/Delete 차단·저장본 불변·view 초대·행에서 변경·자기 초대 차단·모바일 ☰) 검증. 참고: 로컬 모드도 같은 판별 경로라 데모에서 재현 가능
- ✅ **도형 겹침 자동 정리 2건(제보)** 기존 마그넷 장치(`overlap.ts`의 `computeFreeNudge` — 조작한 도형 **자신**을 비켜나게 함 + 리플로우 후 전 자유 도형 밀어내기)를 확장. ① **붙여넣기**: 붙여넣은 자유 도형이 원본과 24px만 어긋난 채 겹쳤다 → `pendingNudgeRef`를 **목록**으로 바꿔(붙여넣기는 한 번에 여럿) 붙여넣은 자유 루트들을 순서대로 마그넷(뒤 도형은 앞 도형이 옮겨 간 자리를 보는 reflow 규칙). 노드의 자식으로 붙는 경우는 레이아웃이 자리를 정하므로 해당 없음, 메모/선/영역은 원래 자유 겹침 허용 유지. ② **크기 조절 확정 시 밀어내기**: 조절한 도형은 사용자가 방금 자리·크기를 정한 것이므로 **anchor로 고정**하고 겹친 개별(자유) 도형들이 밀려난다 — `pendingReflowNudgeRef`를 `{anchor?}`로 확장(리파런트/디태치의 기존 전체 패스는 anchor 없음), anchor는 movers에서 제외하고 마지막에 **여전히 겹칠 때만**(움직일 수 없는 트리와 겹친 경우) 자신이 비켜나는 폴백. 자유 도형을 키워도, 트리 노드(루트 포함)를 키워도 같은 경로 하나로 동작. **잠복 버그 수리**: 컨텍스트 메뉴 붙여넣기에서 선택/후처리가 통째로 빠지고 있었다 — `commitDoc` updater 안에서 결과를 받는 "동기 실행" 가정은 같은 핸들러의 앞선 setState(메뉴 닫기)가 큐를 채우면 React가 즉시 평가를 건너뛰어 깨진다(키보드 Ctrl+V는 우연히 동작). 결과를 updater 밖(`pasteClipboard(docRef.current, …)`)에서 계산하도록 교정 + readOnly 가드. 웹 +3 테스트(붙여넣기 무겹침·자유 도형 크기 조절 anchor 고정+이웃 밀림·루트 크기 조절 밀림 — 최소 탈출 축이 세로일 수 있어 방향 불문 이동만 단정), 실브라우저 4종(우클릭 붙여넣기·Ctrl+V 중앙·자유/루트 크기 조절) 검증
- ✅ **밀어내기 깜빡임 제거 + 네이티브 드래그 고스트 차단(제보 2건)** ① 붙여넣기/크기 조절의 겹침 밀어내기가 **깜빡였다** — nudge가 일반 effect(페인트 **후**)에 있어 겹친 자리가 한 프레임 그려진 뒤 옮겨졌다. 두 nudge effect를 **`useLayoutEffect`**로 바꿔 페인트 전에 동기 재렌더 — 겹친 프레임이 화면에 나가지 않는다. 이때 `geomRef`는 일반 effect에서 갱신돼 layout effect에선 낡을 수 있으므로 **렌더 스코프의 `geom`**을 직접 읽는다. 실브라우저 rAF 프레임 샘플링으로 검증(붙여넣기 37프레임·크기 조절 확정 30프레임 중 겹친 프레임 **0**). ② 미선택 객체를 잡고 끌면 "브라우저 전체가 이미지화되어 이동"하는 네이티브 드래그 고스트(페이지 스냅샷) — 근원은 남아 있는 텍스트 선택 위에서 드래그가 시작되는 것(캔버스 객체는 전부 `userSelect:none`이지만 **Ctrl+A가 페이지 텍스트 146자를 통째로** 선택했고, 패널 텍스트 등도 선택 가능). 수리 2겹: 에디터 루트 `onDragStartCapture` preventDefault(근원 불문 차단 — 파일 드롭은 drop/dragover라 무관) + **Ctrl/Cmd+A를 가로채 캔버스 객체 전체 다중 선택**(마인드맵 관례, 루트 제외·편집 중엔 그대로). **부수 성과**: 이전 PR의 `pasteClipboard`를 updater 밖으로 뺀 교정 덕에 이번 layout effect 전환이 안전했다. 웹 +3 테스트(dragstart 차단·Ctrl+A 선택→일괄 삭제·편집 중 무간섭), 실브라우저 4종(프레임 샘플링 2·Ctrl+A·dragstart)
- ✅ **도형 겹침 후속 3건 + 강화 배터리(제보)** ① **노드에 붙여넣기**(자식 편입) 시 트리가 새 자식 주위로 재배치되며 자유 도형 위에 얹혔다 → 리파런트/디태치와 같은 리플로우 패스 연결(pasted root에 parent가 있으면 `pendingReflowNudgeRef={}`). ② **그룹 이동**(마퀴/Ctrl+A) 드롭에 밀어내기가 없었다 → 단일 드롭과 같은 원칙으로 그룹 멤버(자유 루트들)를 `pendingNudgeRef` 목록에 — 움직인 쪽이 비켜난다. 멤버끼리는 드래그 전 무겹침+평행이동이라 서로 겹칠 일 없음. ③ **붙여넣기→이동→undo 시 엉뚱한 좌표로 튀며 겹침** — 뿌리는 밀어내기가 undo 기록에 없는 정규화(plain setDoc)라는 것: 다음 커밋이 **밀어내기 전**(겹친) 좌표를 기준점으로 스택에 실어 undo가 그 자리로 되돌렸다. 코어 `HistoryStack.amend(next)` 신설(새 undo 단계 없이 기준점만 교체 — `record`의 coalesce는 직전 기록도 continuous여야 해서 discrete 커밋(붙여넣기) 뒤엔 불가) + 웹 `amendDoc`으로 두 nudge effect를 라우팅. undo는 직전 동작 이전으로, 직전 동작만 undo하면 **마그넷 후 좌표**로 복원된다. **강화 배터리**(요청 — "겹치지 않는지 강하게 테스트"): 실브라우저 18케이스 — 배경 붙여넣기 연속 3회·Ctrl+V 중앙 2회·노드(자식) 붙여넣기·그룹 이동을 루트 위로·자유/루트 크기 확대·단일 드롭·붙여넣기→이동→undo(좌표 실측 일치)→redo→undo 2회(사본 제거)·텍스트 성장 커밋·디태치, 각 단계 후 "노드 박스 전 쌍 무겹침" 불변식 검증. 코어 +2 / 웹 +3 테스트(전체 853+204). **프로브 함정 기록**: 배터리 7단계가 계속 실패한 건 앱이 아니라 프로브였다 — 붙여넣은 사본이 뷰포트 오른쪽 가장자리 1px 밖(x=1501>1499)에 걸려 `elementFromPoint`가 NONE, 드래그가 허공을 잡고 팬으로 빠졌다(붙여넣기 지점을 화면 안쪽으로 옮겨 해결)
- ✅ **자식을 가진 자유 도형 이동 — 엉뚱한 좌표 튐 + 하위 겹침(제보 2건)** 원인 하나: wasFree 드롭의 **인라인 nudge**가 이동 **전** geom으로 서브트리 박스를 쟀다 — 루트는 새 위치(doc), 자식들은 옛 위치(geom)를 읽어 "새 루트 ~ 옛 자식"으로 늘어난 **유령 박스**가 있지도 않은 충돌을 만들었다(빈 곳에 놓아도 튀고, 튄 기준이 틀려 하위가 겹친 채 남음 — detach 주석이 이미 문서화한 함정인데 wasFree 드롭만 인라인이 남아 있었다). 수리: nudge가 layout effect(페인트 전)로 옮겨진 지금은 인라인일 이유가 없다 — 드롭/디태치는 **이동 커밋만** 하고 마그넷은 pendingNudge/리플로우 패스에 위임(다음 렌더의 새 레이아웃 geom으로 잼, 깜빡임 없음). 두 effect의 박스 조회를 **`nudgeBoxOf` 규칙 하나로 통일**: 어떤 id든 최상위 조상이 자유 루트면 그 루트가 geom 대비 옮겨 간 만큼(delta) 자기 geom 위치를 평행이동해 읽는다 — 루트 자신·자식들·이번 패스에서 먼저 밀린 다른 서브트리까지 전부 맞는다. 웹 +2 테스트(빈 곳 정확 착지 — 수리 전 실패 확인 / 하위 포함 무겹침), 실브라우저 3종(빈 곳 이동 픽셀 일치·이웃 위 이동 무겹침·연속 5회+undo 되감기) + 기존 배터리 18/18 무회귀
- ✅ **그룹 이동 고스트화 + 업데이트 dim 오버레이(요청 2건)** ① 다중 선택 드래그가 매 이동마다 문서를 커밋해 멤버 실물 전부가 실시간으로 끌려다녔다 → **단일 드래그와 같은 고스트 모델**로: 드래그 중에는 멤버들의 점선 윤곽(`GroupGhostLayer` — 노드 geom 박스·메모 x/y+w(h 폴백)·선 bbox)만 (dx,dy)로 따라오고, 실물은 드롭에서 **한 번에** 커밋된다(부수 효과: undo가 항상 한 단계 — 예전 연속 커밋은 1.2초 coalesce 창을 넘기면 조각났다). onUp에서 delta 계산→translate 커밋→루트 앵커 갱신→멤버 마그넷(pendingNudge)은 그대로. ② PWA 업데이트 적용 중 **전체 화면 dim + 중앙 로딩 카드**(`UpdateOverlay`, z 2000 — 모든 모달(≤400) 위): 적용 절차(저장 flush→skipWaiting→리로드)가 도는 동안 클릭을 막는다(그 사이 클릭이 만든 편집은 리로드로 유실될 수 있다). `blocking` 상태는 `applying`과 별개 — 자동 적용의 피어(다른 탭) 확인에서 일찍 물러나는 왕복까지 dim을 켰다 끄면 깜빡이므로, **피어 확인을 통과한 진짜 적용부터** 켠다. `UpdatePrompt`가 전 화면에 마운트되므로 홈/에디터 어디서든 덮인다. 웹 +3 테스트(그룹 고스트 드래그 중 실물 불변+고스트 수·드롭 단일 delta·undo 한 번 / 오버레이 렌더 2), 실브라우저 검증: 그룹 고스트 3종(도형2+메모1 고스트·드롭 delta 일치·undo 원위치) + **실제 SW 업데이트 사이클**(v1 설치→재빌드→update()→토스트 새로고침 클릭→MutationObserver로 오버레이 목격+스크린샷→리로드 후 적용 알림)
- ✅ **드래그 고스트에 하위 도형 포함(요청)** 단일 노드 드래그의 점선 고스트가 잡은 노드 박스 하나만 그렸다 → **서브트리 전체**가 같은 이동량(delta = ghost 중심 − geom 중심)만큼 점선 윤곽으로 따라온다(NodeLayer ghost 블록을 서브트리 순회로 확장, `data-drag-ghost`). 접힌 가지의 자식은 geom에 없어 자연히 빠지고, 트리 노드를 끌어내는 디태치 드래그에도 동일 적용. 웹 +1 테스트(부모+자식=2, 자식 없으면 1 무회귀), 실브라우저 검증(부모+자식2+손자 = 고스트 4, 좌표가 서브트리 배치 그대로, 드롭 후 0)
- ✅ **그룹 고스트 서브트리 + 자식 추가 밀어내기(제보 2건)** ① 다중 선택 이동에서 멤버의 **하위 도형** 고스트가 빠졌다(#295가 단일 드래그만 서브트리 고스트로 바꿔 그룹은 루트 윤곽뿐) → `GroupGhostLayer`의 노드 멤버를 서브트리 순회로 확장(실측: 자식 2+선 2 문서에서 고스트 5→7). ② **노드에 자식 추가** 시 트리가 커지며 개별 도형과 겹침 → 리파런트/노드 붙여넣기와 같은 리플로우 밀어내기를 `addChild`/`addSibling`/`outlineAdd*`에 연결. 편집 세션이 열려 있는 동안은 effect가 대기했다가 **이름 확정(편집 종료) 후 최종 크기로** 민다. 같은 규칙을 **텍스트 확정**에도: 트리 노드가 커지면 리플로우(자유 도형만 자기가 비켜나는 기존 단일 마그넷 유지) — `commitNodeRichText`/`outlineCommitEdit`의 parent 분기. 웹 +2 테스트, 실브라우저 검증(그룹 고스트 7개·Tab 자식 추가+긴 이름 확정 시 옆도형 밀려남·연속 추가에도 무겹침)
- ✅ **자식 추가 밀어내기를 즉시로(제보: 여전히 겹침)** 직전 수리가 밀어내기를 "이름 확정 후"로 미룬 탓에, 자식 추가 직후 **이름 입력 중 내내** 겹쳐 보였다(자식 추가는 곧바로 편집이 열리고 리플로우 effect가 편집 중에는 통째로 대기했다). 가드를 정밀화 — 대기가 필요한 건 **자유 루트를 편집 중일 때뿐**이다(movers가 자유 루트들이라 편집 중인 자유 루트가 밀리면 편집 박스째 움직인다). **트리 노드 편집**(자식 추가 직후가 바로 이 경우)은 편집 대상이 movers가 아니므로 안전 → 즉시 실행: 추가 순간 placeholder 크기로 밀고, 확정 시 커밋 훅이 다시 리플로우를 걸어 최종 크기로 재조정한다. 웹 테스트를 "입력 시작 전 무겹침" 단정으로 강화, 실브라우저 검증(Tab 직후 겹침 [] → 긴 이름 확정 후에도 [])
- ✅ **방향키 이동이 도형을 건너뛰던 문제(제보 영상)** ↓로 이동하면 바로 아래 도형을 건너뛰고 두 칸 아래가 선택되는데 ↑로는 정상 — 원인은 `nearestInDirection`이 **중심점**으로 거리·방향을 재던 것. 자식 열은 좌측 정렬·가변 폭이라 넓은 도형의 중심이 오른쪽으로 치우쳐, 바로 아래의 넓은 도형이 "대각선"으로 읽혔다(타이트 콘 `perp<=along`에서 탈락하거나, 통과해도 perp×2.2 점수 벌점으로 두 칸 아래 좁은 도형에게 짐 — 실측 문서는 후자였다). 반대 방향은 중심 기하가 달라 멀쩡했던 것이 "간헐적"의 정체. 수리: `NavPoint`에 선택적 `w`/`h`를 더해 **박스 엣지 기준**으로 — `along`=마주 보는 변 사이 간격(겹치면 1로 클램프), `perp`=교차축 **투영 겹침 간격**(x 범위를 공유하면 0 — 바로 아래 넓은 도형이 정확히 0이 된다). 크기 없이 부르면 기존 중심 모델로 축퇴(기존 유닛 테스트 무변경 통과), 호출부는 geom이 이미 w/h를 실어 배선 불요. 방향 판정(`ok`)은 중심 기준 유지. 웹 +3 테스트(제보 기하 재현 — 수리 전 실패 확인), 실브라우저 검증(제보 문서 그대로 이히히히→↓=우왘→↓=이ㅏㅓㅣㅏㅇ→↑↑ 대칭 복귀, 건너뛰기 없음)
- ✅ **에디터 UI 3건(제보)** ① 검색 바 ↑/↓ 사이가 벌어져 보임 — 각자 30px 정사각 버튼이라 작은 글리프 사이 여백이 컸다 → 화살표 쌍만 폭 22px(모바일 32)로 좁혀 나란히(높이=클릭 타깃 유지, 실측 중심 간격 32→24px). ② 공유 버튼 클릭 시 화면 상단에 여백이 아래에서 위로 차오르며 dim — `mf-fade` 키프레임에 `translateY(6px)`가 있는데 이걸 **fixed inset:0 dim 배경**에 걸어 배경 레이어 전체가 슬라이드했다(상단 6px 띠가 차오르는 게 그대로 보임) → 배경 전용 순수 페이드 `mf-dim-in` 신설(editor.css — mf-fade는 home.css/login.css에만 있어 에디터 직행 시 애니메이션이 없을 수도 있던 우연한 의존도 해소), 같은 패턴이던 홈 `ToastModal`(확인창) 배경도 함께 교체(home.css에 동일 정의). 다른 모달들은 카드에만 mf-fade라 무관. ③ 공유 안내 문구를 요청안대로 권한별 두 줄로("편집 가능 권한은 서로의 커서와 편집이 실시간으로 보여요." / "보기 전용 권한은 저장된 최신 맵을 열람만 할 수 있습니다."). 웹 +2 테스트(화살표 폭<닫기 폭·배경 mf-dim-in+문구 2줄), 실브라우저 검증(간격 실측, 모달 열림 직후 14프레임에서 상단 픽셀 항상 커버+transform 없음, 2줄 렌더 스크린샷)
- ✅ **새 배포 감지 가속(제보: 반응이 너무 늦다)** 새 버전 **확인** 시점이 페이지 로드·화면 진입(30초 스로틀)·**1시간 주기**뿐이라, 화면 이동 없이 머무는 탭(홈·에디터)은 배포를 최대 1시간 몰랐다. 적용 정책(updateGate의 safe/defer/block)은 그대로 두고 **감지만** 빠르게: ① 주기 폴링 1시간 → **5분**(확인 비용 = sw.js 몇 KB fetch, 변경 없으면 304) ② `startWakeChecks` 신설 — 탭 복귀(`visibilitychange`→visible)·창 포커스·네트워크 복귀(`online`)에 즉시 확인. 배포를 확인하러 **앱 탭으로 돌아오는 바로 그 순간**이 자연스러운 확인 타이밍인데 브라우저는 이때 스스로 확인하지 않는다. 연타(포커스+visibility 동시 발생)는 기존 30초 스로틀이 거른다. 웹 +3 테스트, 실브라우저 검증(실제 SW 사이클: v1 로드→재빌드→`reg.update()` 없이 focus 이벤트만→**0.9초** 만에 토스트, 트리거 전에는 무반응 확인)
- ✅ **리스트 마커 선택 불가(제보 스크린샷 2장)** 메모·도형 편집 중 전체 선택을 하면 리스트 마커(`1.`·`•`)와 들여쓰기까지 선택된 것처럼 보였다 — 마커는 편집기가 관리하는 장식인데 "내가 지울 수 있는 글자"로 읽힌다. 수리 **2겹**(둘 다 필요): ① 마커 스팬(`listEditHtml`·커밋 렌더 `ListTextBlock`)에 `user-select: none` — 드래그로 선택이 마커에서 시작/확장되는 것을 막는다 ② `.mf-richedit [data-list-marker]::selection { background: transparent }`(editor.css) — Ctrl+A처럼 **범위가 마커를 물면** 크롬은 user-select:none이어도 하이라이트를 칠한다(실측: ①만으로는 여전히 파랗게 칠해짐, toString에도 포함). **복사는 값에서 보존**: 마커가 곧 데이터라(텍스트 마커 = 리스트) 기본 복사에 맡기면 마커 유실 위험 — 신설 `selectedRawText`(richtextDom, 선택 경계를 `linearize`→값 좌표로)로 노드/메모 편집 박스 `onCopy`가 원문(마커·들여쓰기 포함)을 싣는다. 한 줄 일부 선택은 경계가 내용 안이라 마커를 물지 않음(무변경), 리스트 없는 박스는 기본 복사 그대로. 웹 +5 테스트, 실브라우저 검증(2x 픽셀 스크린샷 — 노드·메모 전체 선택에서 마커·들여쓰기 하이라이트 없음 / 복사 클립보드에 `1. `·`  a. ` 보존 / Shift+Enter 이어쓰기→커밋 저장·마커 뒤 Backspace 내어쓰기 무회귀)
- ✅ **리스트 마커 앞 캐럿 침범 수리(직전 배포 후속 제보)** 마커 선택 불가 배포 뒤 "키보드 커서가 리스트 앞을 침범해 마커 구역에 텍스트 편집이 가능해졌다" — 원인은 마커 스팬의 `user-select: none`: 크롬이 마커 텍스트 **안**에는 캐럿을 놓지 않는 대신 **마커 앞**(행 시작)에 캐럿 자리를 만들었고, 거기서 친 글자가 마커 앞에 쌓여 그 줄이 리스트에서 풀렸다(`ㅇㅇㅇ5. 오케이`). 수리 — 마커 구역(들여쓰기 포함, 줄 시작~마커 끝 직전)을 **캐럿 불가 지대**로: 신설 `snapCaretOffListMarker`(richtextDom — 접힌 캐럿을 `linearize`→값 좌표로 읽어 마커 구역이면 `setLinearSelection`으로 내용 시작에)를 노드/메모 편집 박스의 **selectionchange + keydown 두 곳**에서 호출(클릭 직후 빠른 타이핑 등 이벤트 순서 편차 대비 이중화, IME 조합 중엔 보류 — `composingRef`). 스냅만 있으면 ArrowLeft가 마커 구역에서 되튕겨 캐럿이 그 줄에 갇히므로 `listArrowLeft` — 내용 시작의 ArrowLeft는 마커를 통째로 건너 **앞 줄 끝**으로(첫 줄은 제자리). Home도 selectionchange 경유로 내용 시작에 안착(Notion 감각). Backspace 내용-시작 규칙(내어쓰기/마커 붕괴)은 스냅 후에도 같은 오프셋이라 무변경. 웹 +6 테스트, 실브라우저 검증(마커·들여쓰기 클릭→캐럿 inMarker=false→타이핑이 내용에(`2. ㅇㅇㅇ오케이`), 들여쓴 줄 동일, Home→ArrowLeft→앞 줄 끝 입력, 커밋 저장 구조 온전)
- ✅ **줄바꿈 시 캐럿이 리스트 앞에 깜빡이던 문제(제보)** "커서를 줄바꿈시킬 때 리스트 앞으로 이동했다가 리스트 뒤쪽으로 빠르게 이동(깜빡이듯)" — 원인은 **한글 IME 조합 중의 Shift+Enter**: 한글 타이핑 직후엔 마지막 글자가 늘 조합 중이라 keydown이 `isComposing`으로 오고, 우리 Enter 처리가 전부 `!composing` 가드 뒤에 있어 **브라우저 기본 줄바꿈**이 실행됐다. 기본 줄바꿈은 [마커|내용] flex 행을 **쪼개서**(CDP IME 시뮬레이션으로 재현 — 마커와 내용이 딴 줄로 분리) 캐럿이 리스트 앞에 그려지고, 곧이어 compositionend의 `syncListStructure` 재구성이 캐럿을 제자리로 — 두 프레임이 "깜빡임"의 정체. 수리 3겹: ① keydown — 조합 중 Enter/Shift+Enter는 `preventDefault`(맨 Enter는 IME 확정만 — 관례상 한 번 더 눌러 커밋), Shift는 `pendingBreakRef`로 의도만 기억 ② compositionend — IME가 글자를 확정한 뒤 `doBreak`(마커 이어쓰기 `maybeContinueList` → 아니면 `insertLineBreak`, **줄바꿈은 언제나 이 단일 경로**)로 잇는다 ③ `beforeinput`(insertLineBreak/insertParagraph) 차단 안전망 — keydown preventDefault를 엔진이 무시하는 경우 대비. Tab도 조합 중 기본 동작(포커스 이탈=blur 커밋) 차단. 노드/메모 동일 적용, 비조합 Shift+Enter도 doBreak로 통일(기존 기본-줄바꿈 폴백 제거). 웹 +4 테스트, 실브라우저 CDP IME 검증(조합 중 Shift+Enter → 기본 줄바꿈 없음 → 확정 시 `2. ` 이어쓰기·행 쪼개짐 0회·이어 타이핑 정상·번호 재매김)
- ✅ **방향키/클릭 캐럿이 마커 위에 한 프레임 그려지던 깜빡임(제보 정정 — 진짜 시나리오)** "리스트 1에서 →를 누르면 리스트 2 **마커 앞에 잠깐 머물렀다가** 내용 뒤로 이동" — 방향키·클릭의 **기본 동작**(캐럿 이동)은 keydown 핸들러 **뒤에** 실행되는데, 교정이 비동기 selectionchange(태스크)뿐이라 태스크와 페인트의 경합에 따라 마커 위에 선 캐럿이 **한 프레임 화면에 나갔다**(계측: capture→setTimeout(0)→rAF로 "그 프레임에 페인트될 상태"를 기록 — 수리 전 ↑ 이동에서 IN_MARKER 1프레임 실측). 수리 2겹: ① `scheduleSnap` — keydown(비조합)·mousedown·mouseup에서 **rAF 스냅 예약**. rAF 콜백은 같은 프레임의 페인트 **전에** 돌므로 기본 동작이 마커에 떨어뜨린 캐럿이 화면에 나가기 전에 교정된다(밀어내기 깜빡임을 useLayoutEffect로 잡은 것과 같은 원리 — "잘못된 상태가 페인트되기 전에 고친다"). ② `snapCaretOffListMarker` 보강 — ↑/↓ 세로 이동은 캐럿 DOM 앵커를 **마커 노드 안 끝 경계**(값 좌표는 내용 시작과 동일)에 두는데, 픽셀상 안 보여도 다음 타이핑이 마커 스팬(white-space:pre)으로 들어간다 → 값 오프셋이 같아도 앵커가 마커 안이면 내용 쪽으로 **재앵커**. 노드/메모 동일. 웹 +3 테스트(키/클릭 예약 스냅 — 기본 동작을 흉내내 keydown 후 캐럿을 마커에 넣고 rAF 2틱 뒤 교정 단정), 실브라우저 계측(→·마커 클릭·↑·↓ 전부 "마커 위치로 페인트된 프레임" 0, 수리 전 1) + 기능 무회귀(마커 클릭 타이핑·들여쓴 줄·Home→ArrowLeft·커밋 저장 구조)
- ✅ **리스트에서 ↑/↓ 캐럿이 줄을 못 건너던 문제(제보)** "리스트에서 ↑를 눌러도 캐럿이 위로 안 올라간다" — 범인은 우리 코드가 아니라 **크롬의 세로 캐럿 이동 자체가 [마커|내용] flex 행 경계를 건너지 못하는 것**(앱 JS가 전혀 없는 정적 contenteditable로 재현 — 마커 user-select 여부와 무관하게 ↑가 이전 행으로 못 가고 같은 행 마커 끝 경계에 떨어짐). 리스트 도입 때부터 잠복했고, #304의 경계 재앵커가 그 위치를 내용 시작으로 옮기며 "같은 줄에서 한 칸 왼쪽"으로 보이게 되자 발견됐다. 수리: 신설 `listArrowVertical`(richtextDom) — 리스트 행이 있는 편집 박스의 맨 ↑/↓를 **우리가 직접** 처리한다. 이동 좌표는 **픽셀 기준**(`caretRangeFromPoint` — 캐럿 rect에서 한 줄 높이 위/아래 지점의 캐럿 자리): **감긴 항목(한 행 = 여러 시각 줄) 안 이동이 자연스럽고**(실측: 3시각줄 항목을 y 906→881→856으로 한 줄씩 통과) 팬/줌 스케일과도 무관(둘 다 뷰포트 좌표). 도착점이 마커 구역이면 내용 시작으로 클램프, 편집 박스 밖(첫 줄 위/끝 줄 아래)은 첫 줄 내용 시작/텍스트 끝(관례). 픽셀 API 없는 환경(jsdom)은 내용-시작 기준 **열 보존** 텍스트 모델 폴백. 리스트 없는 박스는 개입하지 않음(평문 `<div>` 줄은 크롬이 잘 다님 — 무회귀 가드). 노드/메모 동일. 웹 +4 테스트, 실브라우저 검증(↑ 2→1→0 상승·↓ 하강·감긴 줄 시각 줄 단위 이동·페인트 깜빡임 프로브 CLEAN 유지)
- ✅ **메모 접기/펼치기 토글을 회전 셰브론으로(요청)** 기존 토글은 코럴 원 안의 `＋`/`−` — "추가/삭제"로 읽히는 모호한 기호였다(제보: 직관적인 아이콘으로, 가능하면 더 예쁘게). 표준 디스클로저 관례인 **회전 셰브론**으로 교체: 펼침=아래(내용이 아래로 이어짐)·접힘=오른쪽(더 있음), `transform: rotate(-90deg)` + 0.16s 트랜지션. 시각은 **메모의 글자색에서 파생한 은은한 칩**(배경 7%·테두리 14% 알파, 22px 라운드 사각) — 커스텀 배경/글자색·다크 카드에서도 톤이 자동으로 맞는다. 평소 55% 투명도로 내용을 방해하지 않다가 호버에서 또렷해지고(editor.css `.mf-float-fold`, `:active` 축소 피드백), 터치 기기는 호버가 없어 처음부터 85%(`@media (hover: none)`). 접근성: `role="button"`·`aria-expanded`·`aria-label`. 동작(토글·저장·이미지 플로트 제외)은 무변경. 웹 +2 테스트(셰브론 SVG·회전·aria 왕복 / 접기 저장), 실브라우저 검증(라이트·다크·커스텀 색 스크린샷, 접기→한 줄+회전, 리로드 영속)
- ✅ **피드백 수집 창구(요청 — "무료로 어떻게 쌓을까?")** 외부 서비스 없이 **기존 Supabase 무료 티어**로: `feedback` 테이블(0014) — **insert 전용 우편함**(RLS: 로그인 사용자가 자기 uid로 insert만, 조회 정책 없음 — 운영자는 Supabase Studio에서 조회. 텍스트 행이라 저장량·egress 사실상 0). 탈퇴해도 내용은 남고 연결만 끊김(`on delete set null` + 이메일 스냅샷). 포트 `FeedbackStore.submit`(Local=localStorage `mf_feedback` 누적 + 모달이 데모 안내 / Supabase=insert, 테이블 미적용 서버는 사용자 문구로 실패 — 배포 순서 안전). UI `FeedbackModal`(공용, `src/components/`) — 카테고리 칩(불편해요/오류 제보/아이디어/기타) + 본문(≤4000자) + 완료 화면, `meta`에 빌드 스탬프·userAgent 자동 첨부(재현 조사용). 진입점 2곳: 홈 프로필 메뉴 "피드백 보내기" + 에디터 보기/☰ 메뉴(에디터는 uiTheme를 넘겨 다크에서도 톤 일치 — `FeedbackTheme` 구조적 프롭). 운영 문서 `backend.md` §8(조회 SQL 포함). 어댑터 6 + 모달 5 + 진입점 통합 2 테스트(전체 906), 실브라우저 검증(홈·에디터 진입→카테고리→제출→완료 화면→`mf_feedback` 누적·meta.build 포함)
- ✅ **홈 피드백 진입점을 LNB 최하단으로(요청)** 프로필 메뉴의 "피드백 보내기"를 사이드바 **최하단 고정** 항목으로 이동 — `marginTop: auto`로 남는 공간을 밀어 올려 항상 바닥에 붙고(공간 부족 시 휴지통 아래로 이어짐), 위에 구분선. 기존 nav-item 스타일(말풍선 아이콘, 모바일 44px 터치 타깃) 그대로, 프로필 메뉴에서는 제거(진입점은 LNB 하나). 통합 테스트를 새 위치로 갱신(프로필 팝오버에 없음 단정 포함), 실브라우저 검증(데스크톱 바닥 고정 14px·프로필 메뉴 부재·제출 흐름·모바일 드로어 44px)
- ✅ **피드백 완료 아이콘을 SVG로(요청)** 완료 화면의 💌 이모지(플랫폼마다 다르게 그려짐)를 **테마 accent를 따르는 SVG 배지**로 교체 — 원형 칩(accent 12% tint 배경) + 종이비행기 스트로크 아이콘(앱의 라인 아이콘 언어와 일치, 에디터 다크 테마에서도 자동으로 톤 맞음). `data-done-icon` 가드 테스트 추가, 실브라우저 스크린샷 확인
- ✅ **에디터 피드백 진입점을 GNB 상시 아이콘으로(요청 — UI/UX 검토)** 보기 메뉴 안에 숨어 있던 "피드백 보내기"를 밖으로. 검토한 선택지: 툴바 텍스트 버튼(공유/내보내기 같은 1차 작업과 경쟁 — 소음), 구석 플로팅 버튼(미니맵·줌·독칩·범례와 충돌) → **GNB 우측 아이콘 버튼**(말풍선 + 툴팁, 검색 아이콘과 같은 버튼 문법·크기 34px·열림 상태 하이라이트) 채택. 데스크톱은 아이콘이 유일 진입점(보기 메뉴에서 제거 — 화면당 하나), **모바일은 툴바 폭이 빠듯해 ☰ 메뉴 항목 유지**. 통합 테스트 갱신(보기 메뉴 부재 + 아이콘 열림), 실브라우저 검증(데스크톱 아이콘 노출·클릭→모달·보기 메뉴 부재 / 모바일 390px 아이콘 없음·☰ 항목 유지·열림)
- ✅ **에디터 피드백 버튼을 좌측 하단으로(사용자 선정 — GNB 아이콘은 어색)** 직전의 GNB 말풍선 아이콘을 제거하고, **좌측 하단 마우스 제스처 범례**("좌드래그 선택 · 우클릭/휠클릭 드래그 이동 · …") 자리를 피드백 버튼에 내줬다(사용자 제안). 범례는 단축키 도움말(`?`) 모달과 내용이 겹치는 정적 안내라 상시 화면을 차지할 이유가 약했다 — 보기 전용 변형 범례도 함께 제거(보기 전용은 GNB 배지가 이미 알림). 새 버튼: 알약형(말풍선 아이콘 + "피드백 보내기", panel 배경·테두리·그림자, `mf-ed-btn` 호버), 데스크톱 전용(모바일은 이 구석이 없어 ☰ 메뉴 유지). 통합 테스트 갱신(버튼 1곳뿐·범례 부재), 실브라우저 검증(좌하단 16px 앵커·범례 제거·버튼 1개·클릭→모달, 스크린샷)
- ✅ **홈 카드 "변경일" → "수정일"(요청)** 맵 카드 하단의 시각 라벨 문구 교체(MapCard + 테스트 기대값)
- ✅ **"공간" → "스페이스" 용어 통일(제보: LNB는 스페이스인데 생성은 새 공간)** 홈에서 같은 개념이 두 낱말로 갈려 있었다 — LNB 헤더·브레드크럼·문서는 "스페이스", 생성/삭제/이동 UI는 "공간". **"스페이스"로 통일**(변경 범위가 작고 Notion·Slack처럼 제품 용어로 자연스러움, "작업 공간"과의 혼동도 없음). 사용자 노출 문구 전수 교체: LNB `새 스페이스`, 생성/편집 모달(`새 스페이스 만들기`/`스페이스 이름 변경`/`스페이스 이름` 라벨·aria/설명문), 삭제 확인창, 스페이스 메뉴(aria·`스페이스 삭제`·삭제 불가 안내 2종), 툴바 `스페이스로 돌아가기`, 폴더 모달 설명, 토스트 3종(가져오기 `현재 스페이스에`·이동·복원), 휴지통 배지 `내 스페이스`. **기본 스페이스 시드명도 `일반 공간`→`일반 스페이스`**(신규 사용자만 — 기존 사용자는 저장된 이름 유지, 언제든 이름 변경 가능한 데이터라 마이그레이션 없음). 코드 주석의 옛 명칭도 동기화. 테스트 기대값 갱신(라벨 단정 14곳 + 시드 의존 3곳, 픽스처로 심은 `일반 공간` 데이터는 그대로), 전체 906 통과, 실브라우저 검증(LNB·생성 모달·스페이스 메뉴에 "공간" 잔존 0)
- ✅ **최근 항목 카드의 선택 하이라이트가 잘리던 문제(제보)** 홈 "최근 항목"에서 맵 카드를 선택하면 테두리 하이라이트가 잘려 보였다 — 그리드(리스트) 카드와 같은 표시(2px 테두리 + 카드 **밖으로 3px** 번지는 글로우)를 쓰는데, 최근 트레이는 가로 스크롤 컨테이너라 **`overflow-x: auto`가 다른 축까지 auto로 만들고**(CSS 규칙 — 실측 `overflowY: auto`) 여유가 0이라 위·아래·왼쪽이 각 4px씩 잘렸다. 수리: 스크롤 박스에 `padding: 4`(RING_SLACK)를 주고 **같은 크기의 음수 마진으로 상쇄** — 카드 위치·트레이 여백·한 줄에 노출되는 카드 수(fit 계산은 트레이 기준)는 그대로인 채 링만 안쪽 여유에 들어간다. `scrollPaddingLeft`도 같이 줘 스냅 지점이 여유 안쪽에 선다. 실측: 링 여유 -4 → **0**(잘림 없음), 첫 카드 위치 불변, 그리드 카드와 테두리·글로우 계산값 동일. 웹 +1 회귀 가드(스크롤 박스 padding ≥ 3 && margin = -padding)
- ✅ **홈 색상 테마(요청: "재미있을 것 같아서")** 홈 화면의 색을 다섯 벌 중에서 고른다 — **에디터 팔레트를 그대로 재사용**(코랄·오션·포레스트·그레이프·모노)해서 홈에서 고른 "오션"이 에디터의 "오션"과 같은 파랑이다. 홈의 색은 대부분 인라인 스타일에 박혀 있었으므로 **CSS 변수 한 겹**을 끼워 한 번에 갈아 끼운다: 강조색 계열 4종(`--mf-accent`/`-rgb`/`-soft`/`-strong`/`-mute`)과 면·경계 계열 5종(`--mf-bg`/`-sunken`/`-panel2`/`-border`/`-border-soft`)으로 홈 크롬 130여 곳을 치환(SVG는 presentation attribute에 var()를 기대할 수 없어 `currentColor`+부모 color로). **다크는 제외** — 글자·패널 같은 중립색 170여 곳이 아직 하드코딩이라 배경만 어둡게 하면 흰 패널에 검은 글씨가 그대로 남는다(중립색 변수화 후의 별건). 파생색은 규칙(soft=흰색 12%, strong=검정 12%, mute=흰색 45%, borderSoft=흰색 20%)으로 계산한 값을 표에 적어 두되 **코랄만 오늘 쓰이던 값으로 고정**해 기본 테마는 색이 한 톤도 안 바뀐다(유닛 테스트로 고정). **저장은 두 겹**: 정본은 스페이스·최근 항목과 같은 per-user 워크스페이스 블롭(`SpaceStore` — 기기 간 동기화), 첫 페인트용 캐시는 localStorage(`mf_home_theme`)이고 `main.tsx`가 **렌더 전에** 입혀 기본 코랄로 한 프레임 그려지는 깜빡임이 없다. 진입점은 LNB 최하단 피드백 위의 접이식 `색상 테마` 항목(공유받음·휴지통과 같은 문법) — 적용 버튼 없이 **누르는 즉시** 화면이 바뀌고(고르는 것이 곧 미리보기), 지금 색은 접혀 있을 때도 오른쪽 점으로 보이며, 모바일은 40px 터치 타깃. 곁가지로 두 모달의 비활성 1차 버튼 색이 따뜻한 베이지(`#e7d6ca`)로 혼자 남아 있던 것을 강조색 계열(`--mf-accent-mute`)로 통일했다. 코어 무관·에디터 무영향(에디터 크롬은 자기 팔레트를 인라인으로 쓴다). 유닛 6 + 홈 통합 3(선택→즉시 반영·캐시·블롭 / 다른 기기에서 고른 색 하이드레이트 / 불러온 그대로면 무저장) 테스트, 실브라우저 검증(다섯 테마 변수·배경·트레이 실측, 리로드 직후 첫 페인트, 선택 표시, 카드 선택 링, 모달, 모바일 드로어 스크린샷)
- ✅ **다크 모드 + 테마 메뉴를 설정으로(요청 2건)** ① 직전 색상 테마에서 미뤄 뒀던 **다크**를 추가했다. 미룬 이유가 "중립색 170여 곳이 하드코딩이라 배경만 어둡게 하면 흰 패널에 검은 글씨가 남는다"였으므로, 이번엔 **글자·면·상태색까지 변수로 뽑았다**: `--mf-panel`·`-veil`·`-grey`, `--mf-text`/`-subtext`/`-muted`/`-faint`/`-faint2`(글자 계단), `--mf-accent-ink`(강조색 **위** 글자), `--mf-danger`/`-soft`/`-bg`/`-line`/`-mute`, `--mf-info`/`-star`/`-success`/`-soft`/`-ink`, `--mf-hairline`/`-scroll`/`-skel-sheen`/`-overlay-veil`/`-hover-bright`. 밝은 다섯 벌은 이 글자·상태색을 **공유**하고(`LIGHT_INK`) 다크만 뒤집으므로, 테마 표에서 달라지는 건 여전히 면·경계·강조색뿐이다. 다크 전용 판단 셋: 활성 행 글자(`accentStrong`)는 밝은 테마와 **반대로 밝아지고**, 강조색 위 글자는 흰색 대신 어두운 잉크(코랄 위 대비), hover 밝기 필터도 0.97→1.12로 **방향을 뒤집는다**(어두운 면에서 더 어두워지면 반응이 아니라 사라지는 것처럼 보인다). 홈과 로그인이 함께 쓰는 로더(`LoadingOverlay`)는 CSS 변수를 직접 읽지 않고 **홈이 막·글자색을 프롭으로 넘긴다**(로그인까지 다크가 따라가면 안 된다). ② 테마 선택은 LNB 최하단에서 **프로필 → 설정 모달**로 이동(사용자 요청) — 3열 그리드의 6칸, 각 칸은 그 테마의 **면 위에 강조색 점**을 얹은 미리보기 칩(이름만으로는 "모노"·"다크"가 얼마나 다른지 알 수 없다) + 이름, 선택 칸만 강조색 테두리. 모달이 열린 채로 뒤 화면까지 즉시 바뀌므로 고르는 것이 곧 미리보기다. 밝은 테마의 색은 그대로(코랄 기본값 유닛 테스트로 고정), 곁가지로 남아 있던 따뜻한 잔색 몇 개(#8a7365·#5c4f44·#f4f0eb·#f0c24a 등)를 같은 역할의 변수로 흡수했다. 유닛 7(코랄 값 고정·다크 반전 밝기 단정·변수 집합 일치 등) + 홈 통합 2(설정에서 선택→즉시·캐시·블롭 / 다크는 면과 글자가 함께 뒤집힘), 전체 917 통과. 실브라우저 검증(다크 홈·설정·모달·모바일 드로어 스크린샷, 계산 스타일 대비 실측 — 가장 낮은 3.18은 빈 안내 문구로 밝은 테마(2.0)보다 오히려 높다, 리로드 첫 페인트, LNB에서 사라짐, 밝은 테마 무회귀)
- ✅ **폴더에 들어가자마자 맵이 열리던 문제(제보)** 폴더를 한 번 눌러 들어간 뒤 **곧바로 한 번 더** 누르면, 더블클릭하지 않았는데도 그 자리에 새로 그려진 맵 카드가 에디터로 열렸다. 원인은 우리 코드가 아니라 브라우저의 더블클릭 판정이다 — 크롬은 두 클릭의 **대상이 달라도**(폴더 카드 → 그 자리의 맵 카드) 같은 지점·시간이면 `dblclick`을 **두 번째 클릭 대상**에 쏜다(CDP로 `clickCount:2`를 재현해 `click → DIV.folder`, `click → A.맵`, `dblclick → A.맵` 순서를 실측). 카드는 `onDoubleClick`만 믿고 열고 있었으니 그대로 열린 것. 같은 이유로 **빈 배경을 누른 뒤 카드를 한 번** 눌러도 열렸다. 수리: 카드가 **자기가 직전 클릭도 받았는지**를 스스로 세고(`sawOwnFirstClickRef`, 창 800ms — 판정은 브라우저가 하므로 우리는 트리거가 아니라 문지기), 아니면 `dblclick`을 무시한다(그 클릭은 이미 '선택'으로 처리됐다). 모바일은 원래 자체 탭 카운터라 무관. 웹 +1 테스트(제보 순서 그대로 — "안 열렸다"를 URL이 아니라 **로더가 뜨지 않았다**로 즉시 판정, 수리 전 실패 확인), 실브라우저 5종(폴더 진입 후 1회=안 열림 / 같은 카드 더블클릭=열림 / 배경 클릭 후 1회=안 열림·선택만 / 최근 항목 카드 더블클릭=열림 / 1초 간격 두 번=안 열림)
- ✅ **폴더도 한 번 = 선택 / 두 번 = 진입(요청)** 폴더만 한 번 클릭에 들어가서, 같은 그리드 안에서 카드마다 클릭의 뜻이 달랐다(맵은 한 번=선택). 규칙을 맞추면서 두 카드가 쓰던 활성화 로직을 훅 하나로 모았다 — `useCardActivation`(모바일 탭 카운터 + 데스크톱 dblclick 문지기. #317에서 알아낸 "크롬은 두 클릭의 대상이 달라도 같은 지점·시간이면 dblclick을 두 번째 대상에 쏜다"는 함정이 폴더에도 그대로 적용되므로, 맵을 누른 뒤 폴더를 한 번 눌러 들어가지는 일도 함께 막힌다). 폴더 선택 표시는 맵 카드와 같은 강조색 2px 테두리 + 바깥 글로우(2px가 되는 상태에선 패딩 1px 보정 — 드롭 대기와 같은 방식), 선택 상태는 `selectedCard` 한 칸을 맵과 나눠 쓰므로 그리드에서 선택된 것은 언제나 하나다(키는 메뉴가 쓰던 `folder:<id>` 규칙을 그대로 — `folderCardKey`). 키보드 Enter/Space는 한 번에 진입 유지(포인터의 "두 번"에 대응하는 관용구가 없고, 활성화 키는 곧 실행). 폴더 ☰ 메뉴·드래그 드롭 무회귀. 웹 +2 테스트(선택→진입 / 맵 클릭 후 폴더 1회 무진입) + 기존 폴더 진입 테스트 9곳을 더블클릭으로 갱신, 실브라우저 검증(데스크톱 한 번=선택 테두리·두 번=진입·맵 클릭 후 1회 무진입·☰ 메뉴 정상, 모바일 한 번 탭=선택·더블탭=진입)
- ✅ **홈 카드에 마지막 수정자 이름(요청·검토 후 채택)** 공동 편집이 실사용에 들어가며 "이 맵을 마지막으로 건드린 사람"이 정보가 됐다. 시각(`updated_at`)은 이미 있었으니 **사람**만 더한다 — 카드 하단이 `수정일 · 3시간 전 · 홍길동`. **표시 규칙(설계의 핵심)**: 마지막 저장자가 **내가 아닐 때만** 이름을 붙인다. 혼자 쓰는 사람은 카드마다 자기 이름이 반복돼 정보가 아니라 잡음이 되고, 그런 사용자는 이름 조회 요청 자체가 나가지 않는다(왕복 0회). 서버(0015): `documents.updated_by uuid`를 **트리거가** `auth.uid()`로 찍는다(`updated_at`과 같은 이유 — 클라이언트가 보내는 값이면 남의 이름으로 위장할 수 있다. 편집자 탈퇴 시 `on delete set null`로 이름만 사라지고 문서는 남는다) + `document_editors(doc_ids[])` RPC. 클라이언트는 `auth.users`도 남의 `profiles`(RLS `profiles_select_own`)도 못 읽으므로 0010 `share_participants`와 같은 SECURITY DEFINER 조인이 필요하고, DEFINER는 RLS를 우회하므로 가드를 직접 건다(소유 또는 공유받음 + **본인은 반환하지 않음**). 이름 우선순위는 앱의 프로필명 규칙과 동일(`display_name`→OAuth 이름→이메일 로컬파트)이고 **이메일 전체는 내려가지 않는다**. 웹: `DocMeta.editedByMe`(uid를 앱 상태로 들이지 않고 `ownedByMe`와 같은 방식으로 판정만 실어 보냄) + 포트 `DocStore.listEditorNames`(Local=`{}`, RPC 미배포 서버는 조용히 `{}` — 배포 순서 안전), 홈 상태 `editorNames`, 카드 툴팁은 "…님이 마지막으로 저장". 어댑터 4 + 홈 통합 2 테스트(전체 926), 실브라우저 검증(라이트/다크 카드 표기·툴팁·긴 이름 말줄임 실측·390px 모바일 오버플로 없음 — 로컬 어댑터를 임시 스텁으로 바꿔 렌더만 확인하고 되돌렸다. 실제 이름 경로는 Supabase 전용이라 유닛/통합으로 검증). 운영 문서 `backend.md` §9
- ✅ **협업 중 중복 저장·잘못된 충돌 경고 수리(실계정 2인 제보, 심각)** A(소유자)와 B가 같은 맵을 열고 **B가 편집했는데 저장은 A가 실행**되고 **B에게 "다른 기기/탭에서 먼저 저장됨" 경고**가 떴다. 원인은 자동저장이 `doc`의 **모든** 변화를 자기 변경으로 본 것: 상대의 편집이 CRDT로 도착해 `setDoc`되면 받은 쪽도 저장을 걸어, 같은 행에 두 명이 stale한 `prevVersion`으로 써서 서로를 밀어냈다(직전에 붙인 `updated_by`도 편집하지 않은 사람으로 찍혔다 — 카드 이름이 틀리는 원인). 수리 3겹: ① **편집한 쪽이 저장한다** — `onRemoteDoc`이 그 상태의 서명(`remoteSigRef`)을 남기고 자동저장 효과가 그 서명과 같으면 저장도, 저장 표시도 건드리지 않는다(내 편집이 섞이면 서명이 달라져 곧바로 정상 경로). ② **협업 중 충돌은 경고가 아니다** — 함께 편집한 세션에서는 내 문서가 상대 편집과 이미 수렴해 있으므로, 새 버전을 기준으로 같은 내용을 한 번 더 쓰고 조용히 넘어간다. 판정 기준을 "지금 접속자가 있는가"가 아니라 **"이 맵에서 함께 편집한 적이 있는가"**(`collabSessionRef`)로 둔 이유는 실브라우저에서 드러났다 — 상대가 떠난 직후의 **인수 저장**은 접속자 0인데 내 버전 기준이 상대의 저장으로 낡아 첫 시도가 반드시 충돌하고, 그걸 "다른 기기"로 읽으면 인수 저장이 실패하고 경고까지 떴다. ③ **인수 저장 안전망** — 마지막 상대가 떠났을 때 아직 저장되지 않은 상대의 편집이 남아 있으면 남은 쪽이 저장한다(상대가 0.9초 자동저장 전에 창을 닫은 경우). 곁가지로 **탭을 닫으면 즉시 떠났다고 알린다**(`usePresence`의 `pagehide` → `removeAwarenessStates`, origin은 provider가 밖으로 내보내는 리터럴 `'local'`이어야 한다 — 다른 문자열은 상태만 지우고 상대에게 안 알려진다): awareness의 30초 타임아웃을 기다리던 탓에 인수 저장이 **32초** 뒤에 돌았고(실측) 접속자 목록에도 유령이 남았다 → **1초**. 웹 +6 테스트(원격 편집은 받는 쪽이 저장 안 함/내 편집은 저장/인수 저장/협업 충돌 무경고 재시도/단독 충돌은 기존대로 경고/pagehide 이탈 — 앞의 둘은 수리 전 실패 확인), 실브라우저 2탭 검증(B 편집 시 저장 1회·양쪽 배너 없음·내용 동기화 / 저장 전 닫기→1초 인수 / 교대로 8회 동시 편집→양쪽 10노드·저장본 10노드·경고 0)
- ✅ **닫기 직전 강제 저장 + 마지막 수정자 이름 진단(요청·제보)** ① #320에서 "남은 한계"로 적어 둔 것 — 자동저장은 0.9초 뒤에 도는데 그 사이 탭을 닫으면 마지막 편집이 아무 곳에도 남지 않았다. 신호 두 개를 쓴다: `visibilitychange`(hidden)에서 실제 저장을 하고(탭 전환·최소화·닫기 시작 시점이며 **페이지가 아직 살아 있어** 네트워크 요청이 끝까지 간다), `pagehide`에서는 **동기 localStorage 복구본**을 먼저 남기고 서버 저장도 한 번 던진다. 예약된 자동저장 타이머는 취소해 같은 내용을 두 번 쓰지 않고, **상대가 만든 상태는 여기서도 저장하지 않는다**(저장은 편집한 쪽 책임 — 탭 전환마다 남의 편집을 내 이름으로 저장하면 카드 수정자가 틀린다). 실브라우저 3종: 편집 후 0.15초에 닫기·편집 후 즉시 탭 전환·**둘이 각각 편집하고 동시에 닫기**(양쪽 편집 모두 저장본에 남음 — #320의 마지막 구멍) ② "마지막 수정자가 타인인데도 이름이 안 보인다" 제보 조사: SQL은 결백했다 — 로컬 Postgres에 0001/0009/0010/0015를 실제로 적용해(`auth.uid()`/`auth.jwt()`를 GUC로 대체) 트리거 스탬프·소유자가 보는 편집자 이름·자기 자신 제외·제3자 차단 네 가지를 모두 확인했다. 원인은 **#320에서 고친 중복 저장 레이스**였다: A의 탭이 B의 편집을 받아 저장하고 그 저장이 마지막이면 `updated_by`가 A(=나)로 찍혀 이름을 붙일 이유가 없어진다(B가 충돌 배너를 본 것과 같은 뿌리 — 저장 순서만 반대). 재발 시 원인을 구분할 수 있게 관측도 붙였다: RPC 실패는 콘솔에 경고(`[geurio] document_editors RPC 실패`), 그리고 `updated_by` 칼럼이 없는 서버(마이그레이션 지연)에서는 **칼럼 없이 한 번 더 읽어** 목록 전체가 실패하지 않게 했다(#319가 만든 배포 순서 위험 — 이름만 생략된다). 웹 +4 테스트(숨김 시 저장/저장할 게 없으면 무저장/원격 상태는 무저장/칼럼 폴백), `backend.md` §9에 진단 절차 추가
- ✅ **속성 패널 제목을 첫 줄만(요청)** 여러 줄 도형을 고르면 "선택한 주제" 아래에 내용이 **전부 한 줄로 나열**됐다 — 제목 줄이 `white-space: nowrap`이라 줄바꿈이 공백으로 접혀서다. 제목은 "지금 무엇을 고치는가"를 가리키는 **이름**이지 내용 미리보기가 아니므로 첫 줄만 쓴다. 신설 `panelTitleLine(text)`(순수)을 공용 `PanelTitle`에 끼워 도형·메모·선·영역 패널이 같은 규칙을 쓰게 했다(메모는 이미 `f.text.split('\n')[0]`로 첫 줄만 넘기고 있어 무변화 — 규칙이 한곳으로 모인 것). 첫 줄이 비어 있으면(줄바꿈으로 시작하는 텍스트) 다음 비지 않은 줄을 쓰고, 전체 텍스트는 **툴팁**에 남긴다(첫 줄만 보이니 나머지를 확인할 길은 남겨 둔다). 웹 +5 테스트(순수 함수 4 + 통합 1 — 통합은 수리 전 실패 확인), 실브라우저 검증(4줄 리스트 도형 → 제목 `결정 사항`·툴팁에 전체 / 빈 줄로 시작하는 도형 → 첫 비지 않은 줄)
- ✅ **모바일웹 3건(제보·요청)** ① **편집 중 텍스트를 선택하면 선택 바가 사라지고 캔버스 메뉴만 떴다** — 모바일에서 텍스트를 선택하는 제스처는 길게 누르기인데, 그게 브라우저의 `contextmenu`로 올라와 `.mf-ed-vp`의 핸들러가 캔버스 메뉴를 열었다(앵커 없는 메뉴 → `Editor.tsx` 규칙대로 선택 바가 숨고, `openCtxAt`의 `setTextCtx(null)`로 서식 툴바까지 내려갔다). 편집 박스는 `pointerdown`·`mousedown`·`click`은 막고 있었지만 `contextmenu`는 아니었다. 수리: `onContextMenu`가 **편집 표면(`.mf-richedit`/`input`/`textarea`) 안에서 온 요청이면 손대지 않고 빠진다**(`preventDefault`도 하지 않아 기기의 선택·붙여넣기 메뉴가 그대로 뜬다). 데스크톱 우클릭도 같은 이유로 텍스트 필드의 기본 메뉴가 맞다 — 편집 중 서식은 상시 노출되는 서식 툴바가 이미 맡는다. ② **편집 중에는 모바일 선택 바를 숨긴다**(요청) — 그 시간대엔 서식 툴바가 떠 있어 좁은 화면에 떠 있는 도구 모음이 둘이 되고, 바의 동작(하위/형제/삭제)은 편집을 끝낸 뒤에야 뜻이 통한다. ③ **GNB 공유를 모바일 ☰ 메뉴로**(요청) — 좁은 바에서 아이콘만 남아 뜻이 약했고, 자리를 비운 만큼 남은 버튼의 44px 타깃이 여유로워진다. `ShareGlyph`를 `ToolbarMenus`로 옮겨 데스크톱 버튼과 메뉴 항목이 같은 아이콘을 쓴다(순환 import 회피). 웹 +4 테스트(앞의 셋은 수리 전 실패 확인), 실브라우저 390px 검증: 수리 전 빌드로 **제보 재현**(롱프레스 → 바 사라짐 + `이미지 추가/텍스트 정렬/복사…` 메뉴) → 수리 후 같은 제스처가 실제 텍스트 선택(`window.getSelection()`이 노드·메모 모두 비지 않음)으로 끝나고 서식 툴바 유지, ☰ 공유 → 모달, **빈 캔버스 롱프레스는 여전히 메뉴를 연다**(무회귀)
- ✅ **모바일웹 ① 소프트 키보드 회피** 키보드가 올라와도 브라우저는 **레이아웃 뷰포트를 줄이지 않는다**(`window.innerHeight`·`100dvh` 불변, 줄어드는 건 `visualViewport`뿐) — 그래서 화면 아래쪽 도형·메모를 편집하면 캐럿이 키보드 뒤로 숨었고 CSS로는 알 길이 없었다. 신설 `useKeyboardInset`(순수부 `keyboardInsetOf` — `innerHeight - (vv.height + vv.offsetTop)`, iOS가 레이아웃 뷰포트를 밀어 올린 `offsetTop`도 가려진 높이에 포함, 120px 미만은 액세서리 바 잔변동으로 무시)가 가려진 높이를 재고, 에디터가 편집 중이면 그만큼을 "아래 예약 영역"으로 넘겨 **속성 시트를 피할 때 쓰던 `centerObjectAboveSheet`를 그대로 재사용**해 대상을 키보드 위로 올린다(가려진 높이만 다를 뿐 계산이 같다). iOS가 포커스 때 레이아웃 뷰포트째 밀어 올린 것은 `scrollTo(0,0)`으로 되돌린다(우리 고정 배치·팬 좌표계가 통째로 어긋나므로). 팬 뒤에는 서식 툴바도 따라가야 해서 앵커 계산을 `textCtxAnchor` 한 곳으로 모으고(노드·메모 공용 — 기존 `closeCtxMenu` 복원 경로는 `[data-node-id]`만 봐서 **메모 편집에서는 폴백 좌표로 떨어지던 잠복 버그**도 함께 해결) `refreshTextCtxAnchor`(rAF 뒤 재측정)를 붙였다. 웹 +9 테스트(순수 5 + 통합 4 — 회피 테스트는 수리 전 실패 확인), 실브라우저 390×780에서 `visualViewport`를 키보드처럼 줄여 검증(도형 top 407 → 207로 키보드 위, 서식 툴바 315 → 115로 동반 이동, 편집 세션·이어 타이핑 유지)
- ✅ **모바일웹 ② 확대 제스처와의 공존** 세 갈래. ① 조작 요소(button·[role=button]·a·input·select·textarea·label)에 `touch-action: manipulation` — **탭 두 번이 확대로 읽히던 것**을 막고 300ms 탭 지연도 사라진다. **두 손가락 확대는 일부러 남겼다**: 화면 전체를 `none`으로 막으면 저시력 사용자가 UI를 키울 길이 없어진다(접근성). 캔버스(`.mf-ed-vp`)만 우리가 핀치 줌을 직접 처리하므로 `none` 유지. ② `html/body`에 `overscroll-behavior-y: contain` — 안드로이드 크롬의 **당겨서 새로고침이 리로드**로 이어지던 경로를 끊는다(캔버스는 원래 `touch-action:none`이라 무관했지만, 바텀시트·메뉴·홈 목록의 맨 위에서 아래로 끄는 제스처가 그대로 새어 나갔다). 에디터의 리로드는 실행취소 기록·클립보드·선택·팬/줌, 그리고 아직 저장 안 된 편집까지 잃는다. 트레이드오프: 홈에서도 당겨서 새로고침이 안 된다. ③ **①(키보드 회피)과의 상호작용 수리** — 페이지를 확대하면 시각 뷰포트도 그만큼 작아져 **키보드와 구분되지 않는다**. 글씨를 키워 읽는 중에 캔버스가 저절로 움직이지 않도록 `keyboardInsetOf`가 `scale > 1.05`면 0을 돌려준다. 웹 +1 테스트, 실브라우저 검증(에디터 canvas=none·버튼=manipulation·body/html=contain, 속성 시트 스크롤 무회귀(scrollTop 120), 홈도 동일)
- ✅ **모바일웹 ③ iOS 입력창 자동 확대** 사파리는 글씨가 **16px보다 작은 입력**에 포커스하면 화면을 확대하고 되돌려 주지 않는 경우가 있다 — 로그인(14.5)·홈 검색(13)·이름 변경(14)·피드백(13.5)·공유 초대(12~13)처럼 흔한 자리가 전부 해당됐다(검색 바만 예전에 `isMobile ? 16 : 13.5`로 개별 대응돼 있었다). 자리마다 고치는 대신 **전역 규칙 한 줄**로: `@media (max-width: 767px)`에서 `input/textarea/select`의 글씨를 16px로 올린다(값이 전부 인라인 스타일이라 `!important` 필요 — 대신 앞으로 추가되는 입력도 자동으로 지켜져 "이 자리도 16px이었나"를 기억할 필요가 없다). **제외는 `.mf-edit`** — 캔버스의 선/영역 이름표와 아웃라인 행의 인라인 편집기, 그리고 도형·메모 본문(`.mf-richedit`)이다. 그 글씨 크기는 주변 텍스트·문서의 값이고 박스 측정에도 쓰여서, 키우면 편집하는 동안에만 글자가 커졌다 작아진다(대신 이쪽은 확대돼도 잃을 게 적은 짧은 한 줄이고, ②의 `scale` 가드가 그 사이 캔버스가 저절로 움직이는 것을 막는다). 실브라우저 검증(규칙 직접 확인: 390px에서 인라인 12.5px 입력 → 16px, `.mf-edit`·`.mf-richedit`는 12.5px 유지 / 1200px는 전부 12.5px 그대로, 로그인 실제 입력 14→16·데스크톱 14 유지, 모바일 로그인 레이아웃 스크린샷)
- ✅ **모바일웹 ④ 홈 화면에 추가 안내** PWA로 만들어 뒀지만 **설치로 이어지는 길이 없었다** — 안드로이드 크롬은 `beforeinstallprompt`를 던져 주고(버튼 한 번으로 설치), iOS 사파리에는 그 이벤트도 설치 배너도 없어 공유 시트의 "홈 화면에 추가"를 사용자가 스스로 찾아야 한다. 판단부(`installHint.ts` — 순수 `installHintMode`)와 표현부(`InstallHint.tsx`)를 나눠, 모바일 + 홈 화면 실행 중이 아님 + 이 기기에서 닫은 적 없음일 때 `prompt`(추가 버튼)나 `ios`(공유 절차 안내)를 띄운다. 닫으면 `mf_install_hint`로 **다시 뜨지 않고**, 추가를 누르면 브라우저 프롬프트를 띄운 뒤 같은 이유로 안내를 접는다(거절한 안내를 계속 띄우는 건 소음). `appinstalled`도 잡아 정리. 홈에만 두는 이유: 로그인·랜딩은 아직 "쓰기로 한" 화면이 아니다. 새 버전 토스트와 같은 껍데기를 쓰되 **양옆 고정 바**로 편다 — 가운데 정렬(`left:50%`+translate)은 폭을 안 정하면 뷰포트 절반 안에서만 줄어 문구가 접혔다(실측). 웹 +9 테스트(순수 5 + 컴포넌트 4), 실브라우저 검증(iPhone UA=공유 절차·버튼 없음 / 안드로이드=추가 버튼이 `prompt()` 호출 / 닫으면 리로드 후에도 안 뜸 / 데스크톱 폭·`navigator.standalone`이면 안 뜸, 두 모드 스크린샷)
- ✅ **모바일웹 ⑤ 오프라인** 표시만 붙이려다 **진짜 구멍**을 찾았다: 저장 실패는 `dirty`로 되돌려 다음 자동저장을 기다리는데, 그 계기가 **다음 편집**뿐이라 편집을 멈춘 채 연결이 끊기면 영영 대기였고, 로컬 복구본은 **저장 성공 시에만** 갱신돼 오프라인 편집은 리로드 한 번에 사라졌다. 세 겹으로 수리: ① `useOnline` + 표시 — 에디터 문서 칩이 `오프라인`/`오프라인 · 저장 대기`(빨간 점)로, 홈은 하단 `OfflineBar`(설치 안내는 이때 접는다 — 같은 자리를 두 카드가 다투지 않게). ② **재연결 즉시 재시도** — `online` 이벤트에서 미저장분을 바로 올린다(상대가 만든 상태는 여전히 상대 몫 — #320 규칙 유지). ③ **못 올린 편집 보존** — 저장 실패 시 로컬 사본을 쓰고 `mindflow_doc_<id>__pending` 표시를 남긴다. 다음에 열 때 이 표시가 있으면 **서버 판을 채택하지 않고**(그게 더 옛것이다) 로컬을 기준으로 곧바로 올린다 — 표시가 없으면 기존 채택 경로 그대로(무회귀). 저장 성공이 표시를 지운다. 웹 +5 테스트(넷은 수리 전 실패 확인), 실브라우저 검증(홈 바 등장·복귀 시 사라짐 / 칩 `저장됨`→`오프라인`→편집 후 `오프라인 · 저장 대기`→복귀 후 `저장됨`)
- ✅ **모바일웹 ⑥ 가로로 돌린 폰** 세로 기준으로 만든 UI가 낮은 화면에서 화면을 다 먹었다 — 667×375에서 55dvh 바텀시트는 206px, 캔버스가 169px만 남는다. 신설 `useIsShortScreen`(`max-height: 500px`)으로 **남는 축(가로)으로 돌린다**: 속성 시트가 오른쪽 **사이드 시트**(288px, `top/bottom:0`)가 되고, 닫기 손잡이도 시트 왼쪽 변에 세로로 붙으며, 시트를 피하는 재중앙 정렬은 아래가 아니라 **오른쪽**을 예약한다(`centerObjectAboveSheet`에 `reserveRightPx` 추가 — 가려진 축만 다를 뿐 계산은 같다). ☰ 드롭다운은 `max-height: calc(100dvh - 64px)` + 스크롤(가로에서 항목이 화면을 넘었다). 세로 폰은 전부 그대로(무회귀 테스트). **테스트 하네스 교정**: `mockMatchMedia(true)`가 **모든** 질의에 참을 돌려주던 탓에 세로 폰 테스트가 전부 "낮은 화면"으로 판정됐다 — 높이 질의는 jsdom의 실제 창 높이로 답하게 고쳤다(폭 스위치만 인자로). 웹 +4 테스트(셋은 수리 전 실패 확인), 실브라우저 검증(667×375: 시트 x=379·w=288·h=375, 캔버스 379px 남고 노드 보임, ☰ 메뉴 스크롤 / 390×780: 기존 바텀시트 h=429 그대로)
- ✅ **모바일웹 ⑦ 길게 누르기 감각** 허용치가 `|dx|+|dy| ≤ 10`(맨해튼)이라 **대각선 흔들림에 1.4배로 엄했다** — (6,6)은 실제 8.5px인데 취소됐고, 걷거나 차 안에서 누르면 그 정도는 예사라 "길게 눌러도 메뉴가 안 뜬다"가 된다. 직선 거리(`hypot`)로 바꾸고 플랫폼 터치 슬롭(안드로이드 ~8dp)에 맞춰 **14px**로 넓혔다. 시간은 500ms 그대로(iOS·안드로이드와 같은 길이). 더해서 메뉴가 열리는 순간 **짧은 진동**(`navigator.vibrate?.(12)`, 기능 감지 — iOS 사파리는 조용히 넘어간다): 메뉴가 손가락 **아래에서** 뜨므로 시각만으로는 알아채기 늦다. 두 손가락(핀치)·pointercancel에서 취소되는 기존 경로는 이미 있었다(확인). 웹 +5 테스트(둘은 수리 전 실패 확인), 실브라우저 검증(흔들림 6px→메뉴 열림·진동 1회 / 흔들림 없음→열림 / 흔들림 20px→안 열리고 화면만 이동 / 드래그는 팬)
- ✅ **협업 배지 오해 + 모바일 Enter(제보 2건)** ① **"동기화 끊김 · 새로고침"이 혼자 쓰는 맵에서도 떴다** — 실시간 채널은 남의 편집을 받는 통로일 뿐 저장과 무관한데, 문구가 "이제 저장이 안 된다"로 읽혔다. 표시 조건을 **"이 맵이 실제로 공유돼 있거나(`sharedDoc` — 초대 목록에 행이 있다) 지금 접속자가 있을 때"**로 좁히고, 문구는 **"공동 편집 연결 끊김"**, 툴팁은 **"저장은 정상이에요"로 시작**하게 바꿨다. 더해서 **자동 재접속**을 붙였다(`scheduleRetry` — 5s→15s→45s→2m 백오프, 재시도 중에는 `connecting`이라 배지가 내려간다): 예전엔 한 번 끊기면 상태가 굳어 복구 수단이 새로고침뿐이었고, 순간적인 네트워크 끊김 하나가 남은 세션 내내 배지를 띄웠다. ② **모바일에서 Enter가 편집을 끝내 버렸다** — 소프트 키보드에는 Shift가 사실상 없어서 "Enter=확정 / Shift+Enter=줄바꿈" 규칙을 그대로 두면 폰에서는 **줄바꿈을 넣을 방법 자체가 없다**. 신설 `useIsTouchDevice`(`(hover: none) and (pointer: coarse)` — 폭이 아니라 **입력 방식**을 묻는다: 가로로 돌려 폭이 넓어져도 판정이 흔들리면 안 되고, 마우스 달린 터치 노트북은 제외된다)로 터치 기기에서는 Enter를 줄바꿈으로(도형·메모 공통, IME 조합 중 Enter도 compositionend에서 이어쓰기). 편집 종료는 바깥 탭(blur 커밋)·Escape 그대로. 웹 +11 테스트(배지 4·재접속 1·Enter 6, 여덟 건은 수리 전 실패 확인), 실브라우저 검증(터치 390px: Enter 후 편집 유지·`첫 줄\n둘째` 저장·메모 동일 / 데스크톱: Enter 확정 무회귀). 곁가지로 전체 실행에서만 간헐 실패하던 오프라인 테스트의 대기 시간을 자동저장 디바운스에 맞게 늘렸다
- ✅ **끊긴 채 양쪽이 편집할 때의 손실 차단(질문에서 드러난 구멍)** "연결이 끊긴 상태로 A/B가 각각 편집하면 문제가 생기지 않나"는 질문이 맞았다 — 서버는 CRDT 로그가 아니라 **최종 본문**만 보관하므로, 갈라진 채 저장하면 나중에 저장한 쪽이 상대 작업을 통째로 덮는다. 게다가 #320이 넣은 "협업 중 충돌은 경고가 아니다"가 **연결 여부를 보지 않아**, 끊긴 상태의 충돌까지 조용히 덮어썼다(경고조차 없이). 두 겹으로 막는다: ① **충돌 무음 재시도는 지금 실시간이 붙어 있을 때만**(`collabStatusRef`가 connected/connected-insecure) — 그때만 두 문서가 CRDT로 수렴해 있다는 전제가 성립한다. 끊겨 있으면 충돌은 진짜 충돌이므로 덮어쓰지 않고 배너로 알린다. ② **공유 맵에서 30초(`COLLAB_PAUSE_AFTER_MS`) 넘게 끊겨 있으면 편집을 멈춘다** — `commitDoc` chokepoint에서 모든 변이를 차단하고 전용 화면(`CollabPaused`)이 새로고침을 안내한다. 짧은 끊김에 멈추지 않는 이유: 자동 재접속(#331)이 5초 뒤 첫 시도를 하고 그 사이의 편집은 합류 시 CRDT diff로 서로 병합된다(같은 세션이라 연산 이력을 공유). 멈추기 직전 현재 문서를 **이 기기 버전 기록에 강제 스냅샷**해 두므로, 새로고침으로 최신 판을 받은 뒤에도 끊긴 동안 쓴 내용을 되살릴 수 있다(안내가 그 경로를 알려 준다). 연결이 돌아오면 멈춤은 저절로 풀린다. 웹 +5 테스트(전송을 조종하는 모킹 provider로 끊김/연결 상태를 만들어 검증 — 셋은 수리 전 실패 확인), 실브라우저는 오버레이 시각만 확인(라이브 Supabase가 없는 환경). 곁가지로 오프라인 테스트를 Ctrl+S 기반으로 바꿔 부하에서 간헐 실패하던 타이밍 의존을 없앴다
- ✅ **끊기면 즉시 편집 중지(질문 → 실측 → 결정)** "끊긴 15초 동안 A가 a영역을, B도 같은 a영역을 편집하면 어떻게 되나"는 질문에 답하려고 **실제 바인딩으로 병합을 재현**했다(코어 `crdt/divergence.test.ts`로 정식화). 결과: 재연결하면 두 문서는 **반드시 수렴한다**(화면 불일치 없음). 그러나 수렴 ≠ 보존 — 안전한 건 서로 **다른 노드**, 같은 노드라도 **다른 필드**(중첩 Y.Map)뿐이고, ① 같은 노드의 **같은 필드**는 한쪽 값만 남고(시간순이 아니라 Yjs의 결정적 규칙), ② 각자 자식을 추가하면 노드는 둘 다 남지만 부모의 `children`이 배열 필드라 **한쪽 자식이 트리에서 떨어지고**, ③ 삭제가 편집을 이긴다. 즉 유예를 두면 그 시간만큼 **유실 창**이 열린다 → 사용자 판단대로 **유예 0**으로 바꿨다: 공유 맵에서 끊김이 잡히면 `commitDoc`이 그 즉시 모든 변이를 막고(`collabBlocked`), 화면은 얇은 배너(`CollabBlockedBanner` — 읽기·이동·줌은 그대로)로 알린다. 30초를 넘기면 기존 전체 안내(`CollabPaused`, 새로고침 + 버전 기록 스냅샷)로 승격. 멈춰 있는 시간이 곧 재접속 지연이므로 백오프 첫 시도를 5초 → **2초**(2s→8s→30s→2m)로 당겼다. 코어 +6 / 웹 +1 테스트(전체 994 + 210)
- ✅ **A의 편집이 B에게 즉시 오지 않던 문제(제보)** 편집이 **결국은** 도착하는데 최대 15초가 걸렸다 — 그 15초가 곧 주기 치유(`ysv`) 간격이라는 게 단서였다. 범인은 **버려진 채널의 뒤늦은 콜백**: 채널을 갈아탈 때(공개 폴백·오류 재구독·자동 재접속) `removeChannel(old)`을 부르는데, phoenix의 `leave()`는 서버 phx_leave 응답을 받은 **뒤에야** close를 쏘므로 그 통지는 **새 채널이 이미 붙은 다음에** 도착한다. 그런데 subscribe 콜백은 세대(gen)로 connect/disconnect 사이만 지키고 있어 같은 세션 안의 채널 교체는 그대로 통과했고, 옛 채널의 부고가 ① 공용 불리언 `joined`를 꺼서 **살아 있는 채널의 발신을 막고** ② `removeChannel(this.channel)`로 **멀쩡한 채널을 걷어냈다**. ①이 고약한 건 조용하다는 점이다 — 수신은 정상이고 상태도 `connected`라 배지 하나 없는데 내 편집만 안 나가고, 상대의 `ysv` 요청에 답하는 핸들러에는 그 게이트가 없어서 **15초마다 한 번씩 밀린 편집이 실려 갔다**(= "즉시 반영되지 않는다"). 수리: 콜백·응답 핸들러가 전부 **"내가 아직 현재 채널인가"**를 스스로 확인하고(그래서 `this.channel` 대입을 `subscribe()` **앞으로** 옮겼다), `joined`를 불리언에서 **`joinedChannel` 채널 참조**로 바꿔 남의 부고가 내 상태를 끌 수 없게 구조로 못박았다. 덤으로 공개 채널 발신 거부 시 재시도 전에 채널을 물지 않아 같은 토픽에 채널이 겹치던 것도 정리. 웹 +3 테스트(셋 다 수리 전 실패 확인) — 그중 하나는 **제보 문장 그대로** 두 피어 수준으로: 실제 Realtime 결(비동기 join·자기 메시지 미수신·close 지연)을 흉내낸 인메모리 버스에서 A가 편집하면 B에게 즉시 보이는지. 수리 전 그 테스트의 로그가 `즉시=없음 / 15초 치유 후=A의 편집`이었다. 로컬 2탭(BroadcastChannel) 실브라우저 무회귀 확인(양방향 9~18ms)
- ✅ **커서는 오는데 편집이 영영 안 오던 문제(제보 2차)** 앞 항목을 고치고도 증상이 남았다 — 그런데 제보가 달라졌다: "커서는 서로 보이는데, 편집을 확정해도 **15초를 넘겨도** 반영되지 않는다." 15초(주기 치유 간격)를 넘긴다는 건 치유 자체가 실패한다는 뜻이고, 커서가 멀쩡하다는 건 채널·`joined`·`this.channel`이 양방향으로 건강하다는 뜻이다 — 그래서 남는 설명은 **크기** 하나뿐이었다. Realtime은 **메시지 하나에 크기 상한**이 있고(무료 250KB) 넘으면 서버가 **조용히 버린다**(발신자에겐 성공으로 보인다). 우리 문서는 이미지를 본문에 base64로 인라인하므로 첨부 한 장이면 상한을 넘는다(실측: 노드 40개 텍스트 문서는 11KB인데, 첨부 한 장이면 251KB). 그러면 **합류 시 전체 상태 전송이 통째로 사라지고** → 상대에게 내 기본 연산이 없어 이후의 **작은 텍스트 편집까지 전부 보류**되며 → 15초 치유의 응답도 같은 크기라 **매번 또** 버려져 영영 복구되지 않는다. 커서(awareness)는 작아서 멀쩡히 오가니 화면상 연결은 정상이다. 크기 상한이 있는 인메모리 버스로 **제보를 그대로 재현**했다(`커서=OK / 즉시=알파 / 30초 후=알파 / 버려진 메시지 801KB × 10`). 수리: 큰 업데이트를 **조각내 보낸다**(`yupdate-part` — 조각당 base64 128KB, 묶음 id는 보낸 쪽 `clientID`+일련번호, 받는 쪽이 모아 완성될 때만 적용). 세 발신 지점(로컬 업데이트·합류 응답·SV diff 응답)이 전부 이 경로를 쓴다. 유실된 묶음은 반쪽 적용 없이 버려지고(오래된 것부터 최대 8묶음) 다음 주기 치유가 다시 보낸다. 덤으로 **결과를 버리던 `void channel.send()`를 ack 확인으로** 바꿨다 — 앱에서 가장 중요한 메시지가 실패해도 아무도 모르던 것이 이 사고가 조용했던 이유다. 웹 +3 테스트(셋 다 수리 전 실패 확인: 이미지 문서 즉시 전파 / 조각 역순 조립 / 조각 유실 시 무적용 후 치유 복구). 로컬 2탭 실브라우저 무회귀(12~19ms). **남은 것**: 이미지가 든 문서는 합류마다 수 MB가 Realtime을 지나간다 — 사용량이 문제가 되면 이미지를 별도 스토리지로 빼는 것이 다음 수순
- ✅ **이미지 별도 스토리지 분리(요청)** 이미지를 본문(jsonb)에 base64로 인라인하던 것을 **Supabase Storage**로 옮겼다. 저장소 선택 근거: 이미 쓰는 프로젝트 안이라 새 벤더·키·요금제가 없고, 무엇보다 `storage.objects`에 RLS를 걸 수 있어 **문서 권한 헬퍼(`owns_document`/`shared_with_me`, 0009)를 그대로 재사용**한다 — 이미지 권한이 문서 권한과 **자동으로 같아진다**(공유하면 같이 보이고 끊으면 같이 막힌다). R2/S3는 서명 발급을 둘 서버가 없어 Edge Function을 새로 만들어야 하고, 권한 규칙도 손으로 다시 짜야 한다. 효과(실측): 노드 40개+첨부 1장 문서의 **합류 시 전체 상태가 251KB → 11KB** — 크기 한도(250KB) 사고의 뿌리가 사라졌다. 설계: 본문에는 `mfimg:<경로>` **참조만** 남고 필드는 그대로 `img`라 **직렬화·CRDT·undo·내보내기가 아무것도 안 바뀐다**(전부 문자열). `data:`로 시작하는 **옛 문서는 그대로 열린다**(참조가 아니면 값 자체가 소스 — `displaySrc` 한 줄 규칙). 코어 `image.ts`(순수: 참조 판별·수집·인라인 교체), 포트 `ImageStore`(Supabase/Local), 마이그레이션 0016(비공개 버킷 `map-images`, 경로 `<docId>/<uuid>.<ext>` — 첫 조각이 문서 id라 정책이 `split_part`로 문서를 안다. read=view 이상, write=edit 이상, delete=소유자). 렌더는 만료 1시간 **서명 URL**을 한 번에 받아(왕복 1회) 45분마다 갱신하고, 아직 못 받았으면 **같은 크기의 자리표시자** — 표시 크기가 문서에 있어서(`imgW`/`imgH`) 늦게 온 이미지가 레이아웃을 밀지 않는다. PNG 내보내기는 `crossOrigin='anonymous'`(없으면 캔버스가 오염돼 `toBlob`이 통째로 실패 = 사진 하나 때문에 맵 전체를 못 내보낸다). **옛 문서는 열면 자동 이전**(업로드→참조 교체→저장, 실패해도 올라간 것만 바뀌고 나머지는 인라인으로 남아 다음에 재시도). **정리는 영구 삭제 때만** — 편집 중 지웠다 undo하면 참조가 살아 돌아오므로 그때 지우면 복구가 안 된다. 저장소가 없거나 업로드가 실패하면 **예전처럼 인라인**이라 데모 모드·오프라인에서도 첨부는 언제나 성공한다. 코어 +7 / 웹 +9 테스트, 실브라우저 3종(참조 문서 자리표시자 90×70 정확·옛 인라인 문서 무회귀·데모 모드 드롭이 인라인 폴백). 운영 문서 `backend.md` §10. **미검증**: 실제 업로드·서명 URL 경로는 라이브 Supabase가 이 환경에서 막혀 있어 유닛/통합으로만 확인했다
- ✅ **이미지 용량·전송량 3종(질문 → 계산 → 수리)** "사용자가 많아지면 Storage 용량이 부족하지 않을까?" 계산해 보니 **먼저 닿는 건 저장 용량이 아니라 전송량**이었고, 직전 PR에 그걸 악화시키는 부분이 있었다. ① **서명 URL 1시간 → 12시간, 객체 `Cache-Control` 1시간 → 1년**: URL 문자열이 바뀌면 브라우저·CDN 캐시가 무효화돼 화면의 이미지를 **전부 다시 내려받는다** — 45분마다 갱신하던 탓에 맵을 하루 열어 둔 사용자 한 명이 이미지 10장(2MB)을 열 번 넘게 다시 받아 ≈20MB/일을 태웠다(그런 사용자 20명이면 무료 월 5GB 초과). 12시간이면 한 세션에 URL 하나. 경로가 uuid라 **같은 경로의 내용이 바뀌지 않으므로** 1년 캐시가 안전하다. 트레이드오프는 유출된 URL의 유효 시간이라 무한(공개 버킷)으로 가지 않고 12시간에서 끊었다(사용자 선정). ② **WebP 인코딩**(`pickImageFormat` — 순수 함수, `canvasSupportsWebp()`로 지원 판별, 미지원 브라우저는 예전대로 투명 PNG/JPEG): 실측 **48~60% 절감**(사진풍 55%·스크린샷풍 48%·그라디언트 60%) — 저장·전송이 함께 절반이라 실질 수용 인원이 두 배. WebP는 투명도도 지원해 PNG 원본도 무손실을 포기하지 않고 받는다. **주의**: 순수 노이즈 이미지로 재면 8%밖에 안 나온다(모든 코덱의 최악 조건) — 실제 첨부물로 다시 재서 확인했다. ③ **업로드 실패를 알린다**: 예전엔 실패하면 조용히 본문 인라인으로 돌아갔는데, 그러면 방금 고친 250KB 협업 사고가 재발하고 Postgres(무료 500MB)도 부푸는데 **아무도 모른다**. `imageInlined` 상태 + 독칩 전광판 배너(기존 `movedNotice`/`saveConflict`와 같은 문법). 사용자가 **직접 첨부**할 때만 알리고, 배경 이전 실패는 조용히 다음 기회에 재시도(사용자가 시킨 일이 아니라 알림이 소음이 된다). 웹 +7 테스트, 실브라우저 검증(WebP 포맷별 절감 실측·앱 첨부 경로가 webp로 저장·기존 3종 무회귀). `backend.md` §10에 한도 표와 신호 해석 추가
- ✅ **이미지 화질 + 크기 조절 핸들 2건(제보)** ① **삽입한 이미지가 깨져 보인다** — 인코딩 손실은 범인이 아니었다(같은 치수에서 MAE 1.1~1.7로 어느 설정이든 작다). 진짜 원인은 **다운스케일**이었다: `MAX_IMAGE_DIM`이 1024였고, 더 나쁜 건 `SOFT_BYTE_LIMIT`(600KB)이 **평범한 PNG 스크린샷에서 발동해 치수를 512px로 반토막** 냈다는 것(제보 스크린샷이 바로 PNG 첨부였다). 두 값 모두 이미지가 **문서 본문에 인라인**되던 시절의 것이라 — 그때는 한 장의 크기가 곧 localStorage 쿼터이자 CRDT 업데이트 크기였다 — 실물이 Storage로 나간 지금은 근거가 사라졌다. `MAX_IMAGE_DIM` 1024 → **2048**(기본 표시 260px을 최대 줌 2.4로 2배 화면에서 보면 1248px 필요 → 1.6배 키워 놓고 봐도 충분), `SOFT_BYTE_LIMIT` 600KB → **1.5MB**(정상 이미지에서는 발동하지 않게), WebP 품질 0.8 → **0.85**. 실측: 원본 413KB PNG 스크린샷 → **2048×1365 WebP 96KB**(전송량은 51KB→96KB로 2배지만 1GB에 9,000장으로 여전히 여유, WebP가 이미 JPEG 대비 절반). ② **크기 조절 핸들이 이미지 안에 박혀 보인다** — 이미지 플로트만 `overflow: hidden`을 **박스에** 걸고 있었다(둥근 모서리로 이미지를 자르려던 것). 그러면 박스 **밖**에 놓인 자식까지 잘린다: 핸들(right/bottom −6)의 바깥 절반이 잘려 안쪽 절반만 이미지 위에 남았고, 원격 피어 이름표(top −22)도 함께 잘리고 있었다. 자르기를 이미지 감싸는 안쪽 래퍼(`data-float-img-clip`, `borderRadius: inherit`)로 옮겨 박스는 `overflow: visible` — 핸들이 메모 플로트와 똑같이 모서리에 걸쳐 보인다. 웹 +2 테스트, 실브라우저 검증(원본 2048 vs 표시 323css×DPR2=646 → 3.2배 여유·박스 overflow visible·핸들 전체 노출 스크린샷). **주의**: 이미 올라간 이미지는 그 해상도로 인코딩돼 있어 다시 첨부해야 개선된다
- ✅ **이미지 글자 가독성(후속 제보: 731KB 이미지인데 안의 텍스트를 알아보기 힘들다)** 품질 손잡이를 올리기 전에 **무엇이 한계인지 먼저 쟀다.** 2560×1600 스크린샷을 세 설정(2048/q0.85 · 2048/q0.92 · 2560/q0.92)으로 인코딩해 나란히 렌더해 보니, **260px 폭에서는 셋이 똑같이 뭉갰다** — 한계는 인코딩이 아니라 **표시 크기**였다(1/10 축소라 원본을 아무리 키워도 소용없다). 표시 폭 사다리(260·360·480·620)로 읽히기 시작하는 지점을 찾아 **기본 삽입 폭 260 → 480**(260 뭉갬 / 360 아슬 / 480 읽힘 / 620 또렷). 이 변경의 저장·전송 비용은 **0이다** — 같은 파일을 크게 보여 줄 뿐. 덧붙여 요청대로 **WebP q0.85 → 0.92**(글자 가장자리, 장당 +25%: 2048 스크린샷 142KB → 178KB 실측). **`MAX_IMAGE_DIM`은 2048 그대로 뒀다** — 2560/q0.92는 255KB로 43% 비싼데 이 표시 크기에서는 차이가 보이지 않는다(실측). 원본보다 크게 늘리지는 않으므로(`defaultFloatSize`의 `min`) 작은 아이콘은 그대로. 웹 +1 테스트(기본 폭 하한), 실브라우저 검증(원본 234KB PNG → webp 55KB/2048×1280, 삽입 직후 598 CSS px로 표시되어 본문 15px 글자가 읽힘 — 스크린샷 확인)
- ✅ **내보내기/가져오기의 이미지(직접 만든 회귀 수리)** 이미지를 Storage로 옮기면서 **내보내기가 조용히 반쪽이 됐다**: 본문에는 참조(`mfimg:<원본docId>/…`)만 있으니 ① 내보낸 JSON이 **자족적이지 않고**(예전엔 파일 하나로 완결됐다) ② **가져오면 이미지가 안 보인다** — 가져오기는 새 문서 id로 저장하는데 참조 경로의 첫 조각은 원본 문서 id이고 Storage 정책이 그걸로 권한을 판단하기 때문이다. 수리: 내보낼 때 **실물을 받아 데이터 URL로 되돌린다**(`imageExport.ts` — 참조 수집 → `resolve`로 서명 URL → fetch → FileReader). **가져오는 쪽은 손댈 게 없다** — 데이터 URL이 든 문서를 열면 기존 자동 이전이 새 문서 폴더로 올려 주므로, 이미 있던 두 경로가 그대로 맞물린다. 코어 `replaceInlineImages` → **`replaceImageValues`로 개명**(함수는 원래 값-키 치환이라 방향이 양쪽 다다 — 인라인→참조(이전)와 참조→인라인(내보내기)). 받지 못한 이미지는 참조로 남기고 **몇 장인지 알린다**(텍스트까지 못 내보내는 것보다 낫고, 반쪽 파일인 걸 모른 채 보관하면 안 된다) — 이를 위해 독칩 배너를 `imageInlined: boolean` → **`imageNotice: string | null`**로 일반화해 업로드 실패·내보내기 누락 두 경우가 같은 자리를 쓴다. 에디터·홈 두 JSON 내보내기 모두 적용(마크다운은 원래 이미지 미포함, PNG는 이미 참조를 푼다). 코어 무변경(개명만) / 웹 +4 테스트 — 그중 **에디터 JSON 다운로드 내용에 `mfimg:`가 없다**는 통합 가드는 수리 전 실패 확인. 실브라우저 2종(데모 인라인 맵 내보내기에 이미지 포함·참조 못 풀 때 텍스트 온전 + 알림 표시)
- ✅ **홈 내보내기가 썸네일 본문을 담던 문제(제보: 가져오면 깨진 이미지, PNG는 빈 상자)** 앞 항목을 배포하고도 증상이 남았다 — 그런데 범인은 이미지 스토리지 쪽이 아니었다. 홈 카드의 내보내기는 `docRawForExport`가 `previewDocs`를 **먼저** 쓰는데, 그 본문은 썸네일 전송량을 아끼려고 `preview_doc` RPC(0012)가 이미지 값을 `'stripped'`로 **지운** 것이다(크기 필드만 남긴다). 그래서 내보낸 JSON에는 `"img":"stripped"`가 담겼고, 그 파일을 가져온 맵은 `<img src="stripped">`가 되어 상대 경로로 읽히며 **깨진 이미지 아이콘**이 떴다(실브라우저 재현: `naturalWidth` 0). PNG도 같은 값이라 로드 실패 → 사진 자리가 빈 상자. **이미지 스토리지 분리와 무관하게 썸네일 비용 절감 이후 계속 있던 문제**다 — 그때는 이미지가 본문 인라인이었고, 지워진 자리는 마찬가지로 비어 있었다. 수리 3겹: ① `fullDocForExport` — 지워진 흔적(`hasStrippedImage`)이 보일 때만 `docStore.load()`로 **전문을 다시 받는다**(이미지 없는 맵은 왕복 0회, 로컬/데모 본문은 애초에 지워지지 않아 그대로). JSON·PNG 두 내보내기가 같이 쓴다. ② `exportPng`가 **그리지 못한 이미지 수**를 돌려주고 호출부가 알린다(에디터 독칩 배너 / 홈 안내) — 빈 상자만 남은 PNG를 **모른 채** 보관하는 일이 없게. 덤으로 에디터 PNG는 화면용 URL 발급이 아직 안 끝났으면 **자기가 직접 받아서** 그린다(막 연 맵·느린 네트워크에서 사진이 통째로 빠지던 경로). ③ `displaySrc`가 그릴 수 있는 값(`data:`/`blob:`/`http(s):`/발급된 참조)만 통과시킨다 — **이미 만들어진 손상 문서**(그 파일을 가져온 맵)가 깨진 아이콘 대신 자리표시자로 보인다. 웹 +4 테스트(홈 전문 재요청·PNG 2·`displaySrc`), 그중 셋은 수리 전 실패 확인. 실브라우저 검증: 손상 문서 렌더가 수리 전 `<img src="stripped">`(naturalWidth 0) → 수리 후 자리표시자 1·`<img>` 0, 데모 홈 JSON 내보내기에 실물 이미지 포함(무회귀), 인라인 문서 렌더 무회귀. **이미 잘못 내보낸 파일은 다시 내보내야 한다**(파일 안에 이미지가 없다)
- ✅ **홈 우클릭 메뉴 6건(요청)** 홈에 메뉴가 **하나만** 남았다 — 맵 카드 ☰·맵 카드 우클릭·폴더 ☰·폴더 우클릭·빈 자리 우클릭이 전부 신설 `HomeContextMenu`를 연다(여는 길만 다르고 뜨는 자리만 다르다). 예전에는 카드마다 자기 메뉴를 품고 있었고 하위 메뉴(내보내기·폴더/스페이스로 이동)는 패널 안을 **갈아 끼우는 드릴다운**("‹ 뒤로")이라, 배경처럼 카드가 없는 자리는 메뉴를 걸 데조차 없었다. 지금은 에디터 우클릭 메뉴와 같은 문법이다: `position: fixed`로 **커서 자리**에 뜨고(화면 밖이면 안쪽으로 당기거나 위로 뒤집는다), 하위 메뉴는 **옆으로 뻗는 플라이아웃**이다(hover로 열리고, 터치를 위해 클릭으로도 열린다 — 클릭은 토글이 아니라 **열기만**: hover로 이미 열린 뒤의 클릭이 닫아 버리면 아무 일도 안 하는 것처럼 보인다). ① 상태를 `openMenu`+`exportFor`/`moveFor`/`moveSpaceFor` 넷에서 `ctxMenu {x,y,target}` 하나로 줄이고(하위 열림은 메뉴의 지역 상태) ② **이름 변경** 신설 — 목록의 메타 제목과 **문서 본문의 루트 글자**를 함께 고친다(`applyMapTitle`: load→루트 텍스트 교체→`prevVersion` 저장, 충돌하면 최신 판으로 한 번 재시도). 하나만 고치면 열자마자 옛 이름이 보이고 다음 저장이 그 이름을 되돌린다. **docId 없는 옛 카드에는 항목을 내주지 않는다** — 그런 카드는 제목이 곧 식별자라 이름을 바꾸면 카드를 잃는다 ③ "스페이스로 이동" 하위 목록의 격자 글리프를 **LNB와 같은 스페이스 색 점**으로(제보 — 이 앱에서 스페이스를 알아보는 표식은 색이다) ④ 빈 자리 메뉴는 `새로 만들기 · 새 폴더 · 가져오기 · —— · 설정`. 검토하고 **뺀 것**: 붙여넣기(홈에 맵 클립보드가 없다)·정렬/보기 바꾸기(그런 설정 자체가 없다)·새로 고침(브라우저가 이미 한다) — 눌러도 아무 일 없는 항목은 없느니만 못하다. 좁은 화면(폭 < 184+196+16)에서는 플라이아웃이 화면 밖으로 나가므로 하위 메뉴를 **부모 아래로 펼친다**(모바일 관례). 최근 트레이 카드는 원래 메뉴가 없는 바로가기라 우클릭도 전파만 끊는다(카드를 눌렀는데 빈 자리 메뉴가 뜨면 더 이상하다). 웹 +10 테스트(우클릭 3종·플라이아웃·색 점·Escape·이름 변경 저장 인자·옛 카드 제외·좁은 화면·최근 카드), 기존 6건은 새 구조로 갱신(전체 1036). 실브라우저 검증(데스크톱 5종 + 390px 인라인 하위 메뉴 + 다크 테마 스크린샷, 이름 변경 후 저장된 본문 루트 글자 실측). **후속 2건(검토 중 발견 → 사용자 승인)**: ① 우리 메뉴가 브라우저 기본 메뉴를 **대체**하면서 카드가 링크(`<a href>`)라서 원래 있던 **"새 탭에서 열기"가 사라졌다** → 메뉴 첫 항목으로 돌려줬다(새 탭도 '연 것'이라 최근 항목에도 기록). ② 메뉴 행이 33px로 이 프로젝트의 **44px 터치 타깃 규칙**에 못 미쳤다(예전 메뉴도 36px였으니 새 문제는 아니다) → 모바일에서 `minHeight: 44`. 실브라우저 실측(새 탭 URL·최근 항목 기록 / 390px에서 여섯 행 전부 44px)
- ✅ **폴더 ☰ 버튼이 선택 시 1px 움직이던 문제(제보)** 폴더에 마우스를 얹었을 때와 폴더를 클릭했을 때 ☰ 위치가 미묘하게 달랐다. 원인은 **선택 상태의 테두리 보정 방식**: 선택하면 테두리가 1px→2px가 되는데 폴더 카드는 그걸 **패딩을 18→17로 줄여** 맞추고 있었다. 카드 겉면 크기는 지켜지지만 **패딩 박스가 1px 안쪽으로 들어가고**, ☰은 `position: absolute`의 `top/right`로 그 패딩 박스에 붙어 있어서 버튼만 (-1, +1)px 움직였다(실측 확인 — 맵 카드는 같은 상황에서 `margin: -1`로 상쇄하고 ☰이 flex 흐름 안에 있어 이동 0이었다). 수리: 폴더도 **맵 카드와 같은 방식**으로 — 패딩은 18 고정, 늘어난 테두리는 음수 마진이 상쇄한다(테두리가 바깥으로 나가므로 패딩 박스 위치가 불변). 실측 재확인(☰ 이동 0,0 / 폴더 아이콘 등 안쪽 내용도 좌표 불변). 웹 +1 테스트(수리 전 실패 확인 — 스타일 계약: 선택 전후 패딩 동일 + 선택 시 마진 -1)
- ✅ **LNB 우클릭(질문 → 검토 후 결정)** "LNB 우클릭도 고려할 게 있나? 없으면 브라우저 메뉴를 막아 달라" — **하나 있었다**: 스페이스 행은 이미 ⋮ 메뉴(이름 변경·삭제)를 갖고 있으니, 카드·폴더와 같은 규칙으로 **우클릭도 같은 메뉴**를 열어야 한다. 그 김에 남아 있던 마지막 별도 메뉴(SpaceRow의 자체 팝오버 + `spaceMenu`/`spaceMenuAnchor` 상태)를 공용 `HomeContextMenu`로 흡수했다 — 이제 홈의 메뉴는 정말로 하나뿐이고, 삭제 불가 안내는 이미 있던 `disabled`+`hint`가 그대로 받는다. **나머지 LNB**(즐겨찾기·휴지통·공유받음·피드백·프로필)에는 항목 단위 동작이 없어 `aside`에서 브라우저 기본 메뉴만 막는다(입력창 위는 예외 — 붙여넣기 등 기본 메뉴가 맞다). 근거는 일관성이다: 본문(`main`)이 이미 우클릭을 앱 메뉴로 쓰는데 LNB만 브라우저 메뉴가 뜨면 같은 화면 안에서 우클릭의 뜻이 갈린다. **트레이드오프**: LNB에서는 뒤로·새로 고침 같은 브라우저 항목을 못 쓴다(본문·카드는 이미 같은 상태였다). 검토하고 **뺀 것**: LNB 빈 자리의 "새 스페이스" 메뉴 — 바로 위에 같은 버튼이 보이는데 한 항목짜리 메뉴를 더 두는 건 소음이다. 웹 +3 테스트(스페이스 행 우클릭·마지막 스페이스 잠금+사유·LNB 나머지는 차단만 하고 앱 메뉴도 안 엶) + 기존 스페이스 이름 변경 테스트 갱신(전체 1042). 실브라우저 검증(⋮과 우클릭이 같은 메뉴·본문 메뉴 무회귀·`contextmenu` defaultPrevented 실측)
- ✅ **설치 안내 3건(질문 2 + 제보 1)** ① **"홈 화면에 추가" 안내가 모바일 전용이던 것을 데스크톱까지** — 데스크톱 크롬·엣지도 `beforeinstallprompt`를 주고 설치하면 주소창 없는 창 하나로 열린다. `installHintMode`의 첫 줄 `if (!env.isMobile) return null`을 걷어내 **한 번에 설치되는 길이 있으면 기기를 가리지 않게** 했다(수동 절차 안내인 `ios` 분기만 모바일 전용으로 남는다 — 데스크톱 사파리에는 안내할 절차가 없다). 표현도 갈랐다: 폰은 양옆 고정 바 + "홈 화면에 추가하면 앱처럼 열려요 / 추가", PC는 **오른쪽 아래 카드**(폭 420) + "앱으로 설치하면 창 하나로 열려요 / 설치". ② **루트(`/`)에 매니페스트 링크가 아예 없었다**(제보 조사 중 발견) — `landingRootSwap`이 빌드 후 `dist/index.html`을 `public/landing.html`로 통째로 덮어쓰는데 그 원본에 링크가 없어 vite-plugin-pwa가 주입한 것이 지워졌다. 그런데 **사람들이 처음 닿는 페이지가 바로 거기**다: 링크가 없으면 브라우저가 "설치할 수 없는 사이트"로 보고 설치 메뉴가 안 뜨거나 매니페스트 없는 바로가기만 만든다. 랜딩에 `manifest`·`theme-color`·`apple-touch-icon`을 직접 걸고 **테스트로 고정**(`manifest.test.ts`가 `public/landing.html`을 읽는다). 매니페스트에 `id: '/home'`도 명시 — 없으면 브라우저가 `start_url`을 id로 쓰므로 값은 지금과 같지만(기존 설치 정체성 불변), 앞으로 `start_url`을 바꿔도 같은 앱으로 남는다. ③ **곁가지 시각 버그**: 데스크톱 카드가 뜰 때 0.18초 동안 자기 폭의 절반만큼 왼쪽에 그려졌다 제자리로 튀었다 — 공용 `mf-toast-in` 키프레임이 **가운데 정렬용 `translateX(-50%)`를 품고 있어** 인라인 `transform: none`을 덮어쓴 것(실측: 오른쪽 여백 20px여야 하는데 190px). X 이동이 없는 `mf-toast-in-corner`를 신설해 모서리 알림에 쓴다. 웹 +5 테스트(데스크톱 prompt·데스크톱 수동 안내 없음·id·랜딩 매니페스트 2), 실브라우저 검증(랜딩에서 매니페스트 fetch 성공·데스크톱 카드 우하단 20px·설치 버튼이 `prompt()` 호출·모바일 무회귀). **미해결**: 안드로이드 "안전하지 않은 앱 차단됨"은 Play Protect가 APK(WebAPK)를 막는 화면이라 앱 코드로 끌 수 없다 — 매니페스트가 걸린 페이지에서 설치해야 Google이 서명한 WebAPK가 만들어지므로 ②가 그 조건을 되살렸다. 재발 시 `chrome://webapks`로 실제 WebAPK가 만들어졌는지 확인 필요
- ✅ **링크 공유(0017) + 공유 팝업 안내를 "?" 툴팁으로(요청)** 공유가 **이메일 초대**뿐이라 상대의 이메일을 알아야 했다 — "이거 좀 봐 줘"에는 링크 하나면 충분하다. `documents.link_role`(nullable, `check`로 `'view'`만) + `link_shared()` SECURITY DEFINER + documents SELECT 정책 확장 + 포트 `ShareStore.getLink/setLink` + 공유 팝업의 `링크가 있는 사람은 열람` 토글·주소·복사 버튼. **범위를 좁힌 두 결정**: ① **보기 전용만** — 링크는 유출되면 끄기 전까지 회수할 수 없는데, 열람은 피해가 "봤다"에서 멈추지만 편집은 내용을 되돌릴 수 없게 만든다. DB의 check 제약이 막으므로 편집 링크를 열려면 **의도적인** 마이그레이션이 필요하다. ② **로그인은 필요** — `anon`에는 아무것도 열지 않았다(익명은 공개 라우트·익명 RLS·익명 서명 URL·저장/협업 비활성이 함께 필요한 별도 작업). 링크의 비밀은 **문서 id 그 자체**다(주소가 곧 링크, id는 랜덤) — 별도 토큰을 두면 주소가 둘이 되어 어느 걸 붙여넣었는지에 따라 결과가 갈린다. **클라이언트가 뷰어를 알아보는 법**: 링크로 들어온 사람은 초대 행이 없는데 **소유자도 자기 행이 없어** 둘이 구별되지 않았다 → `DocStore.load()`가 `owner`를 함께 읽어 `ownedByMe`를 실어 주고, "내 문서가 아닌데 초대 행도 없다 = 링크로 열었다 → 보기 전용"으로 판정(진짜 게이트는 RLS, 이건 "고쳐지는 척하다 저장은 안 되는" 화면을 막는 어포던스). **이미지도 함께**: 0016의 read 정책에 `link_shared`를 더하지 않으면 링크로 연 사람에게 본문은 보이는데 사진 자리마다 회색 자리표시자가 뜬다. 곁가지로 팝업 상단의 권한 안내 두 줄을 제목 옆 **"?" 툴팁**으로 옮겼다(요청) — 호버가 아니라 **클릭 토글**인 이유는 터치에 호버가 없어서다. 웹 +11 테스트(어댑터 4·공유 모달 4·읽기 전용 3), 실브라우저 검증(안내 문구 제거·툴팁 내용·링크 켜기→주소·클립보드 복사·끄기→주소 사라짐·저장 상태). 운영 문서 `backend.md` §11. **미검증**: 라이브 Supabase가 이 환경에서 막혀 있어 실제 RLS 통과 경로(다른 계정이 링크로 여는 것)는 유닛/통합으로만 확인했다
- ✅ **보기 전용 후속 3건(링크 공유 배포 후 제보)** 링크로 연 사람에게 ① 링크 토글·초대 입력이 **열려 있었고** ② **소유자가 안 보였고** ③ 내보내기를 쓸 수 있었다. ①②의 뿌리는 하나 — `share_participants` RPC(0011)의 가드가 `owns_document or shared_with_me`인데 **링크 뷰어는 둘 다 아니라** 빈 목록이 돌아오고, 그러면 클라이언트의 `isOwner` 폴백(`owner ? … : true` — "참가자 정보를 못 얻는 구 서버에서는 판단할 수 없으니 보여 준다")이 **나를 소유자로 간주**했다. 서버 RLS가 실제 쓰기는 막고 있었으니 권한이 샌 것은 아니고, **할 수 없는 일을 할 수 있는 것처럼 보여 주는** 화면이었다. 수리 2겹: **0018**이 소유자 행에만 `link_shared(doc_id)`를 더한다(초대 명단은 그대로 — 그 목록은 곧 **이메일 주소 목록**이고 링크는 누구에게나 전달될 수 있다. "이 맵이 누구 것인가"는 뷰어가 알아야 하지만 "누구누구가 초대돼 있는가"는 아니다) + 클라이언트는 참가자 목록과 **무관하게 성립하는** 신호(`controller.readOnly`)로 `canManage`를 잠근다(구 서버에서도 성립). 뷰어의 팝업은 링크 토글·복사 버튼이 비활성, 초대 입력·권한 셀렉트·초대 취소가 사라지고 "보기 전용으로 공유받은 맵이에요" 안내가 뜬다 — **나가기만 남긴다**(공유 설정을 바꾸는 게 아니라 자기 자신을 빼는 것이고, 없애면 나갈 길이 사라진다). ③ 내보내기는 보기 전용에서 **감춘다**(편집·삽입·스타일과 같은 처리 — 이 앱의 보기 전용은 "할 수 없는 것은 보이지 않는다"로 일관). **다만 보안 경계가 아니다**: 화면을 볼 수 있는 사람은 캡처할 수 있다 — 정책·마찰 장치다. 웹 +3 테스트(잠금·소유자 노출·구 서버에서도 잠김), 그중 "구 서버" 케이스는 제보 그대로 수리 전 실패 확인. 실브라우저 검증(툴바에 내보내기 없음·토글/복사 비활성·초대 입력 없음·안내 문구·나가기 유지). **미검증**: 0018의 실제 RPC 결과는 라이브 Supabase가 막혀 있어 확인하지 못했다
- ✅ **공유 안내 툴팁을 한 줄씩(제보)** "?" 툴팁의 세 문장이 중간에서 접혀, 굵게 강조한 권한 이름과 그 설명이 갈라져 읽혔다(폭이 264px 고정이었다). 폭을 키우는 것만으로는 부족했다 — 툴팁이 **"?" 버튼에 걸려 있어**(`HelpTip` 루트가 `position: relative`) 제목 "공유" 폭만큼 오른쪽에서 시작했고, 그만큼 한 줄에 쓸 폭이 모자랐다(가장 긴 문장 330px vs 폰에서 남는 324px — 8px 차이로 접혔다). 수리: 루트를 `position: static`으로 두어 툴팁이 **제목 행**(이미 relative)에 걸리게 해 모달 본문 왼쪽 끝에서 시작하고, 폭은 가장 긴 문장에 맞춘다(`width: max-content` + 아주 좁은 화면용 상한 `calc(100vw - 48px)` — 고정 px이 아니라 상한이라 문구가 바뀌어도 따라간다). 실측: 데스크톱 1280·모바일 390 모두 세 문장 **각 1줄**, 툴팁 오른쪽 끝이 모달 안(782<850 / 368<374). 웹 +1 회귀 가드(수리 전 `264px` 실패 확인 — 폭·앵커 좌표·루트 position 계약)
- ✅ **홈 카드에서 바로 공유(요청)** 맵 카드 메뉴(☰·우클릭)에 **공유**를 더했다 — 맵을 열지 않고 초대·링크 공유를 끝낸다. 팝업은 사용자 제안대로 **에디터의 그 `ShareModal` 그대로**인데, 그러려면 먼저 떼어내야 했다: 이 모달은 `EditorController` 하나를 통째로 받아 `uiTheme`·`shareOpen`·`docId`·`shareStore`·`backendMode`·`readOnly`·`goHome`을 꺼내 쓰고 있었다(홈에는 그런 컨트롤러가 없다). `FeedbackModal`이 이미 쓰던 방식으로 정리 — `src/components/ShareModal.tsx`로 옮기고 필요한 값만 **구조적 프롭**(`open`/`docId`/`onClose`/`readOnly`/`onLeft`/`theme: ShareTheme`)으로 받되, 어댑터(shareStore·mode)는 컨텍스트 훅으로 직접 집는다. 두 호출부의 차이는 둘뿐이다: 나가기 뒤 에디터는 홈으로 보내고(`onLeft=goHome` — 접근이 사라진 맵에 남겨 두지 않는다) 홈은 그냥 닫는다. **색**은 홈이 인라인 스타일로 그리지 않는 CSS 변수 세계라 `homeModalTheme(key)`(순수)로 지금 테마의 7색을 만들어 넘긴다 — 모달 안의 가라앉은 행(`canvasBg`)은 홈의 `sunken`인데 다크에서 그 값이 곧 에디터의 `canvasBg`다(두 테마 표가 같은 팔레트에서 나온다). 같은 프롭을 **피드백 모달에도** 연결해, 다크 홈에서 흰 모달이 뜨던 것(주석에 '홈은 라이트 고정'이라 적혀 있던 시절의 잔재)을 함께 고쳤다. 메뉴 항목은 이름 변경 **다음**, 아이콘은 에디터 툴바 `ShareGlyph`와 같은 사람+더하기 도형(같은 동작은 같은 표식). **docId가 없는 옛 카드·Drive 데모에는 내주지 않는다**(가리킬 서버 문서가 없다 — 이름 변경과 같은 가드, `showShareRow`). 그리드 카드는 언제나 내 맵이라 `readOnly`가 아니다(공유받은 맵은 LNB 목록에만 있다). 웹 +3 테스트(메뉴→팝업→초대→닫기가 홈에 머문 채 동작 / 다크에서 모달 면·글자색이 홈 테마 / 옛 카드엔 항목 없음), 실브라우저 검증(라이트·다크·모바일 390 — 메뉴 항목 순서, 팝업 면 rgb 실측(255,255,255 ↔ 38,32,25), 폭 420/358 화면 안, 주소는 `/home` 유지)
- ✅ **모바일에서 엔터가 편집을 끝내던 문제(제보)** 폰에서 도형을 편집하다 키패드의 줄바꿈 키를 누르면 편집이 종료됐다. 터치 기기 Enter=줄바꿈은 이미 있었는데 실기기에서 안 통한 것 — 먼저 **에뮬레이션 폰에서 앱 안의 Enter 경로를 전부 재현해 무죄를 확인**했다: 평범한 Enter, Gboard식 `keyCode 229`/`key:Unidentified`, 한글 조합 중 Enter 셋 다 편집이 유지되고 줄이 바뀐다(조합·`beforeinput` 안전망이 받는다). 편집을 끝낼 수 있는 경로는 **blur 하나**뿐이었다. 그래서 남는 설명 둘을 모두 막았다. ① **판정이 기기 종류에만 걸려 있었다** — `useIsTouchDevice()`(`hover:none and pointer:coarse`)가 “데스크톱”이라 답하는 환경이 실제로 있다(크롬 안드로이드의 데스크톱 사이트 모드, 마우스·독을 붙인 태블릿, 일부 인앱 브라우저). 그 화면에서도 키보드는 소프트 키보드고 **Shift가 없다** → 신설 `useSoftKeyboardOpen()`(기존 `useKeyboardInset` 재사용 — `visualViewport`가 실제로 줄었는가)을 더했다. 진짜 조건은 “이 기기가 터치인가”가 아니라 **“지금 Shift 없는 키보드로 치고 있는가”**다. 데스크톱은 뷰포트가 줄지 않아 언제나 false(무회귀). ② **키패드의 액션 키가 무엇인지 지정하지 않았다** — `enterkeyhint`가 없으면 IME가 스스로 고르고, “완료/이동”류를 고르면 그 키가 **키보드를 내려** 편집 박스가 blur되고 우리 blur 커밋이 편집을 끝낸다. 노드·메모 편집 박스에 `enterKeyHint="enter"`를 못박았다(물리 키보드 무영향). 곁가지 함정: `useIsTouchDevice() || useSoftKeyboardOpen()`으로 한 줄에 쓰면 **앞이 true일 때 뒤 훅이 호출되지 않아** 훅 순서가 깨진다 — 두 줄로 분리. 웹 +3 테스트(데스크톱 질의+키보드 열림에서 노드·메모 Enter가 줄바꿈 / `enterkeyhint` 계약, 셋 다 수리 전 실패 확인), 실브라우저 3종(폰=유지 · 데스크톱UA+가짜 키보드=유지 · 데스크톱=확정 무회귀). **실기기 확인 필요**: 이 환경에서는 진짜 IME를 띄울 수 없어 두 원인 중 어느 쪽이었는지는 폰에서 확인해야 한다
- ✅ **모바일 리스트에서 엔터→내어쓰기가 엉뚱한 줄을 고치던 문제(제보)** 폰에서 `b. …` 뒤에 엔터를 치고 내어쓰기 후 글자를 쓰면 `6.c.그러`처럼 마커가 겹쳐 들어갔다. 원인은 리스트 로직이 아니라 **줄바꿈 안전망이 한 번도 동작하지 않았다**는 것 — 기본 줄바꿈이 [마커|내용] flex 행을 쪼개는 것을 막으려고 둔 최후의 방어를 React의 `onBeforeInput`으로 달아 뒀는데, 그건 **네이티브 `beforeinput`이 아니라** `textInput`/`keypress`에서 합성한 폴리필이라 `inputType`이 없고 `insertParagraph`에는 뜨지도 않는다(실브라우저 실측: 네이티브 이벤트가 `prevented=false`로 통과). 평소엔 keydown이 Enter를 먼저 처리해 티가 안 났지만, **안드로이드 IME는 Enter를 `keyCode 229`로 보내** 우리 Enter 분기를 비껴가므로 그때 이 구멍이 그대로 열린다 — 브라우저가 행을 쪼개고, 그 뒤의 내어쓰기·타이핑이 어긋난 DOM/값 위에서 돌아 마커가 겹친다. 에뮬레이션 폰에서 `keyCode 229` Enter로 **제보 흐름을 그대로 재현**했다(수리 전: 엔터가 사라지고 내어쓰기가 **앞 줄**을 고침 → `6. 그럼요…그러`). 수리: 안전망을 노드·메모 편집 박스의 **네이티브 `beforeinput` 리스너**로 옮겼다(마운트 1회 등록 + 최신 `doBreak`을 ref로 참조 — 리스너가 낡지 않게). 곁들여 같은 경로의 방어 하나 더: `withEditorSelection`이 **편집 박스 밖 선택**을 그대로 쓰고 있었다 — `linearize`는 찾지 못한 위치를 텍스트 끝으로 접으므로 남의 선택으로 **마지막 줄**을 고칠 수 있었다. 박스 안인지 확인하고(`ed.contains`), 값·오프셋을 `liveEditValue`로 같은 좌표계에 맞춘다(placeholder `<br>` 한 칸 어긋남 차단). 웹 +4 테스트(네이티브 beforeinput 3 + 박스 밖 선택 1, 넷 다 수리 전 실패 확인), 실브라우저 검증(`keyCode 229` Enter → `prevented=true`·마커 이어쓰기, 제보 흐름 `  c. `→`6. `→`6. 그러`, 일반 Enter 무회귀)
- ✅ **툴바 내어쓰기가 폰에서만 어긋나던 문제(후속 제보)** 앞의 수리로 **키패드 엔터** 경로는 정상이 됐는데 (엔터 2회로 내어쓰기되는 흐름), **서식 툴바의 내어쓰기 버튼**을 쓰면 같은 증상이 남았다. 코어는 다시 한 번 결백을 확인했다 — 그 문서 텍스트에 대해 **모든 캐럿 위치**로 내어쓰기를 전수 시뮬레이션해도 마커가 겹친 결과는 나오지 않는다. 그렇다면 어긋난 것은 연산이 아니라 **연산이 받은 캐럿**이다. 두 경로의 차이가 바로 거기였다: 엔터는 편집 박스가 포커스를 쥔 채 keydown에서 캐럿을 읽지만, 툴바 버튼은 **박스 밖을 누르는 동작**이라 그 순간의 `window.getSelection()`을 믿을 수 없다. 손가락 탭은 마우스와 달리 눌리는 즉시 선택을 흔들고(합성 `mousedown`은 `touchend` **뒤에** 오므로 거기서 `preventDefault`를 해 봐야 늦다), 방금 다시 그린 DOM을 가리키는 낡은 Range가 남기도 한다. 수리 2겹: ① **탭을 근원에서 막는다** — 툴바 루트에 네이티브 **비-passive** `touchstart` 위임 리스너를 걸어 `preventDefault` 후 그 자리에서 버튼 동작을 실행한다(React의 `onTouchStart`는 루트에 passive로 붙어 `preventDefault`가 무시된다 — 그래서 직접 건다). 합성 마우스 이벤트가 아예 오지 않으므로 이중 실행도 없다(실측: 탭 후 `trusted=false` 이벤트만, 포커스는 편집 박스 유지). ② **마지막 캐럿을 기억한다** — 편집 중 `selectionchange`마다 값 좌표계로 기록해 두고(`noteEditCaret`), 탭 시점의 선택이 박스 밖이거나 쓸 수 없으면 그 기억을 기준으로 연산한다(둘 다 없으면 무동작 — 엉뚱한 줄을 고치느니 아무 일도 안 하는 게 낫다). 웹 +2 테스트(박스 밖 선택에서도 마지막 캐럿으로 내어쓰기 / 기억도 없으면 무동작, 앞의 것은 수리 전 실패 확인), 실브라우저 검증(폰 탭: 동작 1회·포커스 유지·`  c. `→`6. `→`6. 그러`, 데스크톱 클릭 무회귀)
- ✅ **에디터 용어 통일(요청)** 마인드맵의 기본 요소를 부르는 말이 화면마다 갈려 있었다 — **주제**(속성 패널 `선택한 주제`·우클릭 `자식 주제`)·**도형**(삽입 메뉴 `도형 추가`·패널 `도형 스타일`)·**노드**(단축키 도움말)·**객체**(모바일 선택 바) 넷. 사용자 선정으로 **주제**로 통일하고, **여러 종류를 아우를 때만 `객체`**를 남겼다(모바일 선택 바의 `객체 메뉴`는 대상이 주제든 메모든 뜨는 메뉴라 그대로가 맞다). 마인드맵 앱의 관례이기도 하고, `도형`은 모양 이야기라 생각의 단위를 가리키기엔 약하며 `노드`는 개발자 말투다. 바뀐 곳: 삽입 메뉴·배경 우클릭 `도형 추가`→`주제 추가`, 패널 `도형 스타일`→`주제 스타일`·`도형 N개 선택됨`→`주제 N개 선택됨`, 단축키 도움말 `이웃 노드로 선택 이동`→`이웃 주제로`·`노드 복사·붙여넣기`→`주제 복사·붙여넣기`, 버전 기록 `노드 N개`→`주제 N개`. 가지를 늘리는 두 동작도 우클릭(`자식 주제`)과 모바일 바(`하위`)가 서로 달랐다 → **하위/형제**로 통일하고 메뉴 쪽은 동작임이 드러나게 `하위 주제 추가`/`형제 주제 추가`(모바일 바는 폭이 좁아 `하위`·`형제` 유지). 덤으로 **단축키 도움말에 없던 Tab·Enter**(하위/형제 추가)를 추가했다 — 마인드맵의 가장 기본 단축키인데 목록에 빠져 있었다. 테스트 기대값 28곳 갱신(전체 1071 통과), 실브라우저 확인(삽입 메뉴·우클릭 메뉴·속성 패널·도움말 스크린샷). **범위 밖(별건)**: 랜딩의 `노드를 눌러 가지를 펼쳐 보세요`와 `노드, 도형, 메모, 연결선, 영역까지`는 정적 쌍둥이(`public/landing.html`)와 SEO 문구가 걸려 있어 손대지 않았다 — 지금은 에디터와 어긋난다
- ✅ **초대 알림 ①: 앱 안의 배지(요청, 1순위)** 맵을 공유해도 **상대는 알 길이 없었다** — 초대 행만 생기고, 상대가 우연히 홈의 "공유받음"을 펼쳐 봐야 발견한다. 직접 "링크 보냈어"라고 말해 줘야 하는 구조였다. **아직 열어 보지 않은 초대**를 홈에서 알린다: LNB `공유받음` 행의 강조색 개수 배지 + 새 항목의 점(제목도 굵게), 그리고 **모바일 ☰ 버튼의 점**(폰에서는 LNB가 서랍이라 그 안의 배지는 닫힌 문 뒤에 있다 — 알림이 아니게 된다). **seen 상태는 서버에 둔다**(0019 `document_shares.seen_at`): 기기마다 따로 기억하면 폰에서 확인한 배지가 PC에서 또 뜬다. 이 앱은 이미 스페이스·최근 항목을 per-user로 동기화하므로 알림만 기기별로 노는 것이 오히려 어긋난다. **정책을 넓히는 대신 좁은 RPC**(`mark_shares_seen`, SECURITY DEFINER)를 낸 이유: 초대받은 사람이 자기 행을 UPDATE하게 열면 `seen_at`만이 아니라 **`role`까지** 바꿀 수 있다(RLS는 컬럼 단위로 못 좁힌다) — 보기 전용으로 초대받은 사람이 스스로 편집 권한으로 승격하는 길이 열린다. **지우는 시점은 "열었을 때"**다: 목록을 펼치기만 해도 지우면 훑어본 것과 실제로 본 것이 구분되지 않는다(우편함 관례). 배지가 근거를 잃는 두 경우를 막았다 — 0019가 안 간 구 서버는 `seen_at` select가 통째로 실패하므로 **컬럼 없이 한 번 더 읽어** 목록은 그대로 뜨게 하고(배지만 사라진다), 그렇게 `seenAt`을 모르는 항목은 "안 봤다"로 치지 않는다(**없는 알림을 만들어 내는 쪽이 더 나쁘다**). 곁가지로 로컬 어댑터의 `listSharedWithMe`가 하드코딩 `[]`이던 것을 Supabase와 **같은 규칙**(내 이메일로 온 초대)으로 바꿔 데모 모드도 같은 길을 타게 했다. 어댑터 +3 / 홈 통합 +4 테스트(전체 1078), 실브라우저 검증(데스크톱 배지 `1`·강조색 실측·항목 점 / 모바일 ☰ 점과 접근 이름 / 열고 나면 배지 사라지고 저장소에 `seenAt` 기록). **미검증**: 0019 RPC의 실제 동작은 라이브 Supabase가 막혀 있어 유닛/통합으로만 확인했다. 로컬 모드에는 "남의 문서"가 없어 실브라우저 확인은 로컬 어댑터에 임시 스텁을 넣어 렌더만 보고 되돌렸다
- ✅ **초대 알림 ②: 초대 메일(요청, 2순위)** 배지(①)만으로는 **이미 앱을 열어 본 사람**에게만 닿는다 — 처음 초대받는 사람(아직 가입도 안 했을 수 있다)에게는 앱 밖에서 닿아야 한다. Edge Function `share-invite`(Deno) 신설. **왜 함수인가**: 메일 서비스 API 키는 서버에만 있어야 한다 — 클라이언트 번들에 넣으면 누구나 우리 도메인 이름으로 메일을 보낼 수 있다(피싱). **클라이언트를 믿지 않는다**: 요청자가 그 문서의 소유자인지, 그리고 그 초대가 실제로 `document_shares`에 있는지를 서버에서 다시 확인한다(없으면 로그인한 아무나 임의 주소로 "당신이 초대됐다"는 메일을 우리 이름으로 보낼 수 있다). 본문의 사용자 입력(맵 제목·초대자 이름)은 이스케이프. **보내는 시점은 처음 초대 한 번**뿐 — `add`는 upsert라 권한 변경과 구분되지 않으므로 목록을 들고 있는 모달이 판단한다(같은 알림 반복은 스팸으로 읽힌다). 포트 `ShareStore.notifyInvite`(Supabase=함수 호출 / Local=no-op), 실패는 전부 삼킨다 — 초대는 이미 걸렸고 배지가 그 사실을 알린다. **설정 전까지 아무 일도 하지 않는다**: `RESEND_API_KEY` 시크릿이 없으면 함수가 `{sent:false, reason:'not-configured'}`로 조용히 넘어가고 공유는 지금과 똑같이 동작한다(키를 넣는 순간부터 발송). 공급자는 Resend(무료 3,000통/월, Supabase 공식 문서의 조합) — **가입·도메인 DNS 인증·`supabase functions deploy`는 소유자가 직접** 해야 하며 절차는 `backend.md` §12. 어댑터 +2 / 에디터 통합 +1 테스트(전체 1081). **미검증**: 실제 발송 경로는 시크릿·도메인이 없어 확인하지 못했다 — 함수 코드는 배포 후 `supabase functions logs share-invite`로 확인
- ✅ **알림 배지 색을 테마에서 분리(제보)** 홈 색상 테마를 바꾸면 초대 알림 배지 색까지 함께 바뀌었다 — 배지가 강조색 변수(`--mf-accent`)를 쓰고 있었기 때문. 배지는 "무엇을 강조하는 UI"가 아니라 **알림**이다: 테마마다 색이 달라지면 그때그때 다른 뜻으로 읽히고, 모노처럼 강조색이 회색(#2b2b2b)인 테마에서는 알림처럼 보이지도 않는다. `UNREAD_BADGE_BG`/`UNREAD_BADGE_INK` 상수를 `home/theme.ts`에 두고(테마 표 **밖** — 테마마다 달라지는 값이 아니라는 뜻이 코드에 드러난다) 세 곳(LNB 개수 배지·새 항목 점·모바일 ☰ 점)이 함께 쓴다. 값은 기본(코랄) 강조색 그대로라 대부분의 사용자에게는 보이는 색이 그대로다. ☰ 점의 테두리(`--mf-bg`)는 배경을 오려 내는 역할이라 테마를 계속 따른다. 웹 +1 회귀 가드(오션 테마에서 배지 배경 = 고정 색 · `var(` 미사용 · 테마 강조색과 불일치), 실브라우저 4테마 실측(코랄·오션·모노·다크 모두 배지 `rgb(240,102,63)`·글자 흰색, 테마 강조색은 각각 다름)
- ✅ **맵 템플릿(요청)** 설계의 핵심은 **템플릿을 그냥 `Doc` 하나로** 둔 것이다 — 별도 포맷을 두지 않으니 갤러리 썸네일(`realPreview`, 홈 카드·버전 기록이 쓰는 그 렌더러)·저장·협업·undo·내보내기가 전부 **기존 새 맵 경로 그대로**다. 새로 짠 것은 데이터(`src/templates/mapTemplates.ts` — 순수)·갤러리 모달·씨앗 분기 셋뿐이고, 문서를 만드는 것은 **홈이 아니라 에디터**다(주소에 `tpl=<id>`만 실리고 `loadOrSeedDoc(mapId, title, buildTemplateDoc(tpl))`이 받는다 — 홈이 조립해 저장하면 저장 경로가 둘이 된다). 저장된 본문이 있으면 템플릿을 **무시**한다(같은 주소를 다시 열어도 쓴 내용이 남는다). **진입(사용자 선정)**: 세 곳의 "새로 만들기"(툴바·빈 상태 CTA·빈 자리 우클릭)가 전부 갤러리를 열고 **첫 칸이 빈 맵**이다 — 만드는 길이 하나라 어디서 시작하든 같은 선택지를 본다(툴바 버튼은 `<a href>` → `<button>`: 누를 때는 어떤 맵을 만들지 모르므로 가리킬 주소가 없다). **내장 6종**(업무 중심): 브레인스토밍·회의록·주간 계획·프로젝트 기획·의사결정·학습 정리. 내용은 **기능 안내서를 겸하게** 짠다 — 이모지·리스트 마커(`1. 할 일 · 담당 · 기한`)·메모가 한 번씩 들어가 열자마자 "이런 것도 되는구나"를 보여 준다. **좌표를 적지 않는다**: 트리 x/y는 `layout()`이, 가지 색은 `colorOf`가 테마 팔레트 순서대로 정한다. 값이 필요한 것은 자유 도형인 메모뿐이라, 메모는 **`right` 레이아웃 템플릿의 x < 0**에만 둔다(그 모드는 트리가 반드시 x > 0에 놓이므로 왼쪽이 비어 있다 — 박스 크기는 글자 실측에 따라 달라져 미리 알 수 없고, 겹친 채 열리면 첫인상이 나쁘다). 이 계약을 **실제 `layout()`으로 재는 테스트**로 고정했다. 노드 id는 `t1`, `t2` … 결정적(문서마다 저장이 갈리고, 에디터가 만드는 `x1_<타임스탬프>`와 모양이 달라 한 문서 안에서 부딪힐 일도 없다). 갤러리는 `auto-fill minmax(200px,1fr)`라 폭에 따라 3→2→1열로 줄어 모바일 분기가 따로 없고, 본문만 스크롤해 헤더·닫기가 항상 보인다. 코어 무변경, 웹 +17 테스트(템플릿 순수 10 · 갤러리 통합 4 · 에디터 씨앗 3, 전체 1099), 실브라우저 검증(갤러리 7칸·썸네일 실렌더 확인 / 회의록 선택 → 노드 8·메모 1·**겹침 0**·번호 목록 렌더 / 빈 맵 → 노드 1·`tpl` 없는 주소 / 390px 다크 1열·가로 스크롤 없음). **후속 제보(깜빡임)**: 갤러리가 뜰 때 살짝 깜빡이는 느낌 — 프레임 단위로 재 보니 원인이 둘이었다. ① **dim 배경에만 애니메이션이 없어** 어두운 막이 한 프레임에 툭 깔린 뒤 내용이 0.2초에 걸쳐 따라 떴다(실측: t=55ms에 배경 0.42 완성, 카드는 t=220ms까지 opacity 0) → 배경도 `mf-dim-in`으로 함께 페이드(#331과 같은 이유로 `mf-fade`가 아니다 — translateY를 fixed inset:0에 걸면 레이어가 통째로 슬라이드한다). ② **첫 열기에만 55·60ms 롱태스크 두 개** — `realPreview`가 6개 문서를 레이아웃·측정하는 비용이고 결과는 모듈 캐시에 남아 두 번째부터는 12ms다. 즉 느린 것은 갤러리가 아니라 **첫 한 번**이라, 그 한 번을 클릭 순간이 아니라 **한가할 때**로 옮겼다(`requestIdleCallback`으로 미리 데우기, hue가 캐시 키라 테마 변경 시 재실행. 직렬화 본문도 모듈 캐시). 실측: 클릭→첫 프레임 **55ms → 8ms**, 롱태스크 **2개 → 0개**(새 탭 3회 재현). 웹 +2 테스트(둘 다 수리 전 실패 확인). **후속(요청)**: 카드 제목 앞의 이모지를 **SVG 아이콘**으로 교체 — 이모지는 기기마다 다른 그림으로 그려지는데(안드로이드·iOS·윈도우가 서로 다른 세트를 쓴다) 이 자리는 앱의 크롬이므로 우리가 그린 것이 맞다(피드백 완료 아이콘 💌 → SVG와 같은 판단). 전구·문서·달력·로켓·저울·책 6종을 앱의 다른 아이콘(가져오기·새 폴더·공유)과 같은 선(stroke) 언어로(24 뷰박스·1.8 두께·`var(--mf-subtext)`). **문서 안의 이모지는 그대로 둔다** — 루트 도형의 `emoji`는 크롬이 아니라 사용자가 이모지 고르기로 바꿀 수 있는 내용이고 캔버스는 그걸 글자로 그린다(썸네일에 이모지가 보이는 이유). 웹 +1 테스트(제목 줄 textContent가 이름과 정확히 같다 = 이모지 글자 0)
- ✅ **홈 본문 검색(요청)** 홈 검색이 제목만 보던 것을 **제목+본문**으로 넓혔다. 본문은 **썸네일이 이미 받아 둔 그 문자열**(`previewDocs` — 없으면 이 기기의 localStorage 사본)이라 **검색을 위해 새로 내려받는 것이 없다**(활성 스페이스의 맵 본문은 썸네일 때문에 이미 프리페치돼 있다 — 검색 범위를 그 스페이스로 잡은 이유이기도 하다). 신설 `searchIndex.ts`(순수)가 원문 JSON을 훑지 않고 **글자 필드만 골라 모은다** — 주제 `text`·`note`, 메모 `text`, 영역 `label`. 원문을 통째로 훑으면 로컬 모드 본문에 인라인된 이미지 데이터 URL 때문에 `png`·`iVBOR` 같은 질의가 엉뚱한 맵을 물어 온다(실브라우저로 확인 — 지금은 셋 다 결과 0). 파싱 결과는 docId로 캐시하되 원문 문자열이 바뀌면 다시 읽는다(키 입력마다 재파싱 방지 + 저장 후 새 내용 반영). **검색 중에는 폴더 경계를 넘는다** — 어디 있는지 알면 찾을 이유가 없다. 그래서 결과 카드에 **위치 줄**(폴더 경로)이 붙고(평소엔 `pathLabel`이 비어 줄 자체가 없다 = 레이아웃 무변화, 검색 중엔 모든 결과가 같은 조건이라 행 높이가 어긋나지 않는다), 폴더 카드도 계층 대신 **이름이 걸리는 폴더**를 보여 주며, 상단에 "'질의' 검색 결과 N개 · 제목과 내용에서 이 스페이스 전체를 찾았어요" 안내가 뜬다(다른 폴더의 맵이 섞여 나온 게 오작동으로 보이지 않게). **결과 없음은 전용 안내**로 갈랐다 — 예전엔 0건일 때 "아직 만든 맵이 없어요" + 새로 만들기 CTA가 떠서 맵이 있는데도 없다고 말하고 있었다. **범위 밖**: 다른 스페이스는 본문이 프리페치돼 있지 않아 제외(전 스페이스 팬아웃은 썸네일 비용 절감에서 일부러 피한 것). 웹 +12 테스트(순수 8 + 통합 4, 통합 3건은 수리 전 실패 확인, 전체 1114), 실브라우저 검증(메모 낱말·영역 이름으로 찾기, 폴더 안 맵이 최상위 검색에 잡히고 위치 줄 `기획`, 폴더 이름 검색, base64 3종 결과 0, 결과 없음 안내, 지우면 원상 복귀, 390px 다크). **후속(질문 → 측정 → 디바운스)**: "실시간 검색이 성능에 부담 아닐까, 입력 후에 결과가 나오게 바꿀 수 있나"는 물음에 **먼저 쟀다**. 맵 20/60/150개(노드 40개씩, 본문 130KB/391KB/978KB)로 재 보니 ① **검색 계산 자체는 맵 수와 거의 무관하게 싸다** — 결과 집합이 바뀌지 않는 키 입력은 150개에서도 33ms로 60개와 같고, 그 33ms는 rAF 두 번의 **프레임 대기 바닥값**이다(본문 파싱은 docId 캐시라 한 번뿐) ② 값비싼 것은 **결과가 바뀔 때 카드 목록을 다시 그리는 것**이고 이건 검색 방식과 무관하다(150장 재마운트 = 1.3초, 제목만 검색하던 때도 같았다). 사람이 치는 속도(90ms 간격)로는 150개에서 롱태스크 1개(59ms), 60개에서는 0개였다 — 즉 **지금 규모에서 실질적인 문제는 아니었다**. 그래서 Enter 전용으로 바꾸는 대신(비용의 원인이 아니고 즉각 피드백을 잃는다) **180ms 디바운스**를 넣었다: 입력값(`searchInput`)과 적용값(`search`)을 갈라 글자는 즉시 보이되 목록은 손을 뗀 뒤 한 번만 다시 그린다. Enter·포커스 아웃은 기다리지 않고 바로 적용(`flushSearch`), **비우기는 즉시**(지웠는데 결과가 남으면 고장으로 읽힌다). 실측: 150개 빠른 입력에서 롱태스크 **1개 → 0개**, 목록 변경 배치 **3회 → 1회**. 웹 +3 테스트(중간 결과 안 그림·Enter 즉시·비우기 즉시) + 기존 즉시 단정 1건을 `waitFor`로 갱신
- ✅ **전역 검색(모든 스페이스) + 검색 화면 분리(요청)** 검색을 활성 스페이스에서 **전 스페이스**로 넓혔다. 요청에 함께 담긴 물음 — "그러면 검색창 위치와 결과 화면 UI가 달라져야 하지 않나" — 이 설계의 뼈대가 됐다. **자리가 뜻을 말한다**: 검색창을 스페이스 헤더 **위**(홈 최상단)로 올렸다. 헤더 아래에 두면 "이 스페이스 안에서 찾는다"는 거짓말이 된다. LNB가 아니라 본문 최상단인 이유는 폰에서 LNB가 서랍이라 검색하려면 문을 먼저 열어야 하기 때문 — 데스크톱·모바일이 같은 자리를 쓰고, 모바일 ☰도 이 줄로 와서 앱 바 노릇을 한다(툴바에서 제거). **결과는 화면을 바꾼다**: 질의가 있는 동안 스페이스 그리드 대신 `SearchResults`가 뜨고, 결과는 **스페이스별로 묶여**(색 점 + 이름 + 개수) 카드 그리드로 그려진다 — 카드·썸네일·메뉴를 그대로 재사용하므로 새로 짠 것은 묶음 껍데기뿐이다. 카드에는 폴더 경로가 붙어 스페이스 안 위치까지 알려 준다. **메뉴 가림**: 다른 스페이스의 결과 카드는 **폴더 이동/폴더에서 빼기**를 내주지 않는다 — `mapFolders`는 폴더 id만 들고 있어서 A의 맵을 B의 폴더에 배정하면 어느 목록에도 없는 미아가 된다. 스페이스 이동은 안전하다(`moveMapToSpace`가 key로 원본을 스스로 찾는다). `findCard`가 `searchGroups`도 뒤지게 넓혔다(안 그러면 결과 카드의 ☰가 빈 메뉴). **본문 데이터(사용자 선정)**: 썸네일 프리페치는 활성 스페이스만 받으므로 **첫 검색 때** 나머지를 마저 받는다 — 검색은 의도적인 행동이라 그때 값을 치르는 게 맞고, `loadPreview`의 (id, version, updatedAt) 캐시 덕에 사실상 기기당 한 번이다(홈 진입마다 미리 받으면 검색하지 않는 사용자도 전송량을 낸다). 받는 동안 제목 결과가 먼저 뜨고 헤더가 "내용에서 더 찾는 중…"이라 말한다. **서버 검색은 미뤘다**: 우리 검색은 부분 일치인데(한국어에선 이게 맞다 — "이탈"로 "이탈률") Postgres FTS는 토큰 단위라 안 되고, `body::text ILIKE`는 본문의 base64 이미지에 걸린다. 서버에서도 글자만 뽑는 장치를 새로 만들어야 하고 데모 모드는 어차피 클라이언트 검색이 필요하다. 웹 +4 테스트(다른 스페이스 본문 매치·묶음 헤더 / 폴더 이름 / 폴더 이동 가림 + 스페이스 이동 유지 / 지우면 복귀), 기존 기대값 갱신(플레이스홀더 `모든 스페이스에서 검색`, 모바일 액션 행), 전체 1121 통과. 실브라우저 검증(두 스페이스 본문 매치가 각 묶음으로·헤더 개수·다른 스페이스 영역 이름·폴더 이름·결과 없음 안내·지우기 복귀·390px 다크 ☰ 유지·가로 스크롤 없음). **후속 롤백(제보: "최근 항목 위에 검색이 있으니 UI가 엉망")**: 검색창을 **원래 자리(툴바 안)**로 되돌렸다 — 전역이라는 사실은 자리가 아니라 **문구와 결과 화면**이 말한다(플레이스홀더 `모든 스페이스에서 검색` + 결과 헤더 `… · 모든 스페이스` + 스페이스별 묶음). ☰도 툴바로 복귀(`SearchBar.tsx` 삭제, 툴바는 변경 전 판에서 되살린 뒤 지우기 버튼·Esc만 다시 얹음). **툴바는 검색 중에도 남는다** — 검색창이 사라지면 질의를 고칠 수도 지울 수도 없다. 스페이스 제목이 남는 것은 오히려 맞다: "지금 어디에 있는가"=검색을 지웠을 때 돌아갈 자리이고, 결과가 전역이라는 것은 헤더와 묶음이 말한다(구글 드라이브도 같은 꼴). 최근 항목만 감춘다 — 질의로 걸러지지 않는 목록이 결과 위에 남으면 무엇이 결과인지 흐려진다. 웹 +1 테스트(검색 중 검색창 값·스페이스 헤더 유지·최근 항목 부재), 기존 모바일 액션 행 계약 원복. **실수 기록**: 로컬에서 `apps/web` 안에서만 lint를 돌려 루트 eslint가 잡는 미사용 prop(`onOpenNav`)을 놓쳐 CI가 한 번 실패했다 — CI와 같은 **루트 `pnpm lint`**로 확인할 것
- ✅ **마크다운 가져오기(요청)** 홈의 가져오기는 원래 `.md`를 받고 있었지만 **우리가 내보낸 형식만** 읽었다(`# 제목`/`- 항목`/`> 노트`/`## 개별 주제`/`## 메모`). 바깥에서 만든 문서를 넣으면 `##` 이하 제목은 통째로 버려지고 번호 목록·문단도 사라져, 사실상 "우리 파일 되읽기"였다. `parseOutline`을 `markdownImport.ts`로 떼어 내며 **평범한 마크다운**까지 읽게 넓혔다: `##`~`######` 제목이 계층이 되고(제목 스택 + 목록 스택 분리 — 목록은 가장 가까운 제목 아래에 쌓인다), 번호 목록(`1.`/`1)`), 체크박스(`- [ ]` → 표시만 제거), **문단은 바로 앞 노드의 노트**로(버리면 남의 문서에서 정작 내용인 산문이 사라진다 — 노트는 캔버스에 펼쳐지지 않아 화면도 어지럽지 않다), 코드 펜스 안은 **통째로 건너뛴다**(그 안의 `- `가 가지가 되면 구조가 어그러진다), YAML 프런트매터·수평선도 건너뛴다. **인라인 서식**은 코어로 되살린다 — 신설 `applyMarkdownLinks`(`richToMarkdown`의 역방향, `normalizeUrl` 통과분만 링크) + 기존 `applyMarkdownShortcuts`. 링크를 **먼저** 걷어내는 이유는 주소 안의 `*`·`_`가 강조 마커로 오탐되지 않게 하기 위해서다. **의도한 동작 변경 하나**: 예전에는 우리 것이 아닌 `##`을 무시하고 그 아래 불릿을 루트에 쌓았는데, 이제 계층이 된다(회귀 테스트를 새 기대값으로 갱신하며 이유를 주석에 남겼다). `toMarkdown`이 쓰는 두 문구(`개별 주제`·`메모`)만 예전처럼 섹션 전환이라 **왕복은 그대로 보장**된다. **지킨 규칙 하나**: "구조(항목·제목 계층·메모)가 하나도 없으면 가져오지 않는다" — 테스트가 이 규칙을 무심코 뒤집은 것을 잡아냈고, 문단만 있는 파일은 여전히 거절한다(문단은 구조에 딸려 오는 살이다). 코어 +5 / 웹 +13 테스트(전체 1135 + 222), 실브라우저 검증(프런트매터·체크박스·번호 목록·코드 펜스·수평선·링크가 섞인 외부 문서 → 계층 12노드·노트 3개·굵게/링크 rich·코드 안 내용 유입 0)
- ✅ **주제 댓글(0020)** 맵에 논의를 남기는 자리. **본문(jsonb)이 아니라 별도 테이블**에 둔 것이 설계의 핵심이다 — ① 댓글은 본문과 **수명이 다르다**(버전 기록으로 되돌려도 논의는 남아야 한다) ② 본문에 넣으면 CRDT 병합 대상이 되어 끊긴 채 양쪽이 달면 한쪽이 사라진다(#332에서 확인한 배열 필드의 한계) ③ 본문은 자동저장마다 통째로 오가는 값이라 댓글이 늘수록 저장·전송이 무거워진다. 붙는 자리는 **주제(노드) 하나**로 통일 — 문서 전체 댓글은 루트 주제의 댓글로 대신한다. **권한(RLS)**: 읽기=소유자+초대받은 사람, 쓰기=읽을 수 있는 사람(**보기 전용도 댓글은 쓴다** — 리뷰를 받으려고 보기 권한으로 부르는 일이 흔한데 의견을 남길 길이 없으면 초대의 목적이 반쯤 사라진다. 구글 문서의 '댓글 작성자'와 같은 생각), 지우기=작성자 또는 소유자, **수정은 열지 않는다**(남긴 말이 조용히 바뀌면 기록으로서 믿을 수 없다 — 고치려면 지우고 다시 쓴다). **링크 공유(0017)로 들어온 사람은 제외** — 링크는 누구에게나 전달될 수 있는데 댓글은 내부 논의라 본문과 같은 무게로 다룰 수 없다. 클라이언트도 같은 판단을 해서(`canComment`) 메뉴 항목과 배지까지 감춘다("열리는 척하다 빈 목록"이 되지 않게 — 이 앱의 보기 전용 원칙). 작성자 이름은 **스냅샷**(`author_name`): 조인을 쓰면 클라이언트가 남의 `profiles`를 못 읽어 0010처럼 SECURITY DEFINER RPC가 또 필요해진다. 대가는 "이름을 바꾸면 옛 댓글은 옛 이름". 주제가 지워져도 댓글은 남고(참조 무결성을 걸 수 없는 jsonb 키다) 앱이 "사라진 주제"로 보여 준다. **UI**: 포트 `CommentStore`(Supabase/Local) + 데스크톱 우측 패널(속성 패널이 좌측이라 함께 떠도 부딪히지 않고, 아래로는 미니맵/줌 묶음을 피한다)·모바일 바텀 시트(속성 시트와 하나만 남긴다), 진입점 3곳=보기 메뉴·☰ 메뉴·**주제의 개수 배지**(노트 배지와 같은 문법이되 노트가 있으면 옆으로 비켜선다, 눌러서 그 주제로 열기 — 보기 전용에서도 동작). 대상은 고른 주제를 따라간다(어느 주제의 논의인지 흐려지지 않게). 입력은 **Enter=줄바꿈 / Ctrl·⌘+Enter=등록**(소프트 키보드에는 Shift가 없어 Enter를 등록으로 쓰면 폰에서 여러 줄을 못 쓴다 — #333과 같은 이유). 아이콘은 말풍선 **안에 줄**을 넣어 피드백(빈 말풍선)과 구별. **실시간이 아니다** — 본문이 아니라 협업 채널을 타지 않으므로 상대의 새 댓글은 패널을 열 때 다시 읽어 본다(지금은 왕복 하나가 더 싸다). 테이블 미적용 서버는 조용히 빈 목록(콘솔 경고, 배포 순서 안전). 웹 +5 테스트(작성·삭제·배지·선택 따라가기·링크 뷰어 차단 — 마지막은 게이트를 풀면 실패함을 확인), 실브라우저 9종(패널 자리와 줌 묶음 무겹침·작성·주제 전환·배지 좌표·배지로 열기·리로드 영속·삭제·다크+노트 배지 나란히·모바일 시트) 검증. 운영 문서 `backend.md` §13. **미검증**: 실제 RLS 통과 경로는 라이브 Supabase가 이 환경에서 막혀 있어 유닛/통합으로만 확인했다
- ✅ **댓글 확장(0021): 진입점·답글·멘션·해결·실시간(요청 3건)** ① **진입점** — "보기 > 댓글뿐이라 너무 숨어 있다"는 지적대로 **노드 우클릭 메뉴에 `댓글 (N)`**을 추가했다(대상이 주제이니 진입점도 주제에 — 모바일 길게 누르기 메뉴도 동일). 덤으로 **보기 전용 사용자도 우클릭이 열린다**: 예전엔 "메뉴 항목이 전부 변이라 열 것이 없다"고 통째로 막았는데 이제 댓글이 있으므로, readOnly면 **댓글 항목만 있는 메뉴**를 연다(변이 항목은 여전히 없음 — 테스트로 고정). 기존 진입점(보기/☰ 메뉴·개수 배지)은 유지. ② **답글·멘션·해결**(이후 알림의 기반) — 전부 같은 테이블의 컬럼이다(0021): 답글=`parent_id`(**단층 스레드**, 대댓글 없음 — `check_comment_parent` 트리거가 같은 문서·뿌리 여부를 DB에서 강제, 뿌리 삭제 시 답글 cascade), 해결=`resolved_at/by/by_name`(뿌리에만, **좁은 RPC `set_comment_resolved`** — UPDATE 정책을 열면 RLS가 컬럼을 못 좁혀 남의 body까지 고칠 수 있게 되므로 0019와 같은 RPC 패턴. 권한은 댓글 쓰는 사람 전원=구글 문서 규칙), 멘션=`mentions jsonb`([{email,name}] — 본문에는 "@이름"이 글자로 남고 이메일 명단이 이후 알림의 겨냥 대상. 명단은 공유 참가자(0011)에서 고른 것이라 새로 노출되는 정보 없음). UI: 스레드(뿌리+들여쓴 답글+답글 입력), **배지·패널은 미해결 스레드 중심**(해결되면 배지에서 빠지고 "해결된 스레드 N개" 접힘 구획으로 — 남은 논의만 눈에 들어오게), 멘션은 입력창 `@`에 참가자 자동완성(`mentionTokenAt` 순수 함수, 후보 mousedown 처리 — blur가 클릭을 삼키는 서식 툴바의 함정 회피), 본문 렌더는 멘션된 "@이름"만 강조색. **실브라우저에서 잡은 레이스**: 멘션 선택 후 캐럿 복원이 rAF라 그 사이 타이핑이 텍스트 끝에 붙었다(`@friend 인 부탁확`) → draft 커밋 직후 effect로 이동. ③ **실시간** — 안 했던 이유는 문제가 아니라 비용 절약이었음을 확인하고 진행(사용자 조건 충족). 설계는 **내용 없는 신호(ping) 채널**: 공개 broadcast `mindflow-comments:<docId>`에 "바뀌었다"만 싣고 받는 쪽이 `list()`로 다시 읽는다(그 select에 RLS — 채널이 비밀을 나르지 않는다). **공개 채널인 이유**: 0009 발신 정책이 edit 전용인데 댓글은 보기 전용도 쓴다 — private면 그 사람의 신호만 막혀 반쪽이 된다(#228의 교훈). postgres_changes를 피한 이유: DELETE 이벤트는 RLS로 못 거르고(유출 경로) replica identity·publication 설정이 더 붙는다. **사용량(#231)**: 구독은 공유된 문서에서만(혼자 쓰는 맵 채널 0), 메시지는 실제 동작당 1건. 로컬/데모는 BroadcastChannel(같은 설계). 0021 미적용 서버는 확장 컬럼 select 실패 시 **기본 컬럼으로 재시도**(배포 순서 안전, #321과 같은 폴백). 어댑터 4 + 웹 5(답글·해결·멘션·실시간 2탭·우클릭/보기 전용 메뉴) 테스트(전체 1150), 실브라우저 검증(우클릭 진입·답글 저장 parentId·멘션 자동완성→저장→강조·해결→배지 1↓→접힘 구획→다시 열기·**2탭 실시간 82ms 도착 + 해결 표시 동기화**). **미검증**: Supabase 실채널 경로는 라이브가 막혀 있어 유닛으로만 확인 — 실계정 2인 테스트 권장
- ✅ **알림 센터(0022) — 홈 벨 + 우편함(요청: "알림 종류가 많아지는데 홈에 알림 센터를")** 종류가 셋을 넘은 알림(공유 초대·멘션·답글·새 댓글)을 배지를 늘리는 대신 **우편함 테이블 하나**로 모으고, 홈 툴바의 **종(벨) 버튼 + 드롭다운**이 읽는다(데스크톱=우측 액션 묶음 맨 앞 — 만들기 동작들과 성격이 달라 떼어 둠, 모바일=제목 줄 오른쪽 끝 44px — 액션 줄은 이미 꽉 차 있다). **설계의 핵심: 클라이언트는 알림을 만들지 못한다** — insert 정책이 없고 전부 **DB 트리거**가 만든다(댓글 insert → 멘션>답글(스레드 뿌리 작성자)>새 스레드(문서 소유자) 우선순위·같은 사람 1회·자기 행동 제외 / 공유 insert → 초대받은 사람. upsert의 UPDATE(권한 변경)는 insert 트리거를 안 타서 **처음 초대에만** — share-invite 메일과 같은 규칙이 공짜로 성립). 클라이언트 insert를 열면 아무나 남의 우편함에 가짜 멘션을 꽂을 수 있다(0022 주석). 행에는 actor_name·preview·doc_title **스냅샷**을 박아 목록이 select 하나로 끝난다(0020 author_name과 같은 이유). RLS는 전부 `recipient = auth.uid()`인데 UPDATE를 (0019·0021의 RPC 패턴과 달리) **직접 열었다** — 이 행에는 남의 것이 없어 어떤 컬럼을 바꿔도 피해자가 자기 자신뿐이라 컬럼을 좁힐 이유가 없다(같은 패턴이라도 이유가 다르면 결론이 다르다는 기록). **읽음 규칙**: 센터를 열면 전부 읽음(0019 공유 배지의 "열었을 때" 규칙 — 목록이 한 화면이라 열었으면 본 것), 방금 읽은 항목은 패널이 열려 있는 동안만 점으로 남는다. **항목 클릭 = 딥링크**: 맵으로 이동하고 댓글류는 `?comments=<nodeId>`를 실어 에디터가 로드 후 그 주제의 댓글 패널을 바로 열고 화면을 그 주제로 맞춘다(알림을 눌렀는데 맵만 열리면 무엇 때문에 왔는지 다시 찾아야 한다). 갱신은 마운트 + 탭 복귀(30초 스로틀 — 새 배포 감지 #302와 같은 생각)·열 때마다. 포트 `NotificationStore`(list/markAllRead), 로컬/데모는 `mf_notifications` + 로컬 어댑터들이 트리거 역할(LocalCommentStore의 멘션, LocalShareStore의 첫 초대 — **수신자를 이메일로** 적어 데모 세션 전환으로 실브라우저 검증 가능). 0022 미적용 서버는 조용히 빈 벨(콘솔 경고, 배포 순서 안전). 벨은 홈 컨트롤러 블롭에 얹지 않고 **자족 컴포넌트**(자기 훅+스토어)로 — 1,800줄 컨트롤러에 상태 다섯을 더 꽂을 이유가 없다. 유닛 3(멘션 생성·자기 제외 / 첫 초대만 / list 필터·markAllRead 경계) + 벨 4(배지·열면 읽음·딥링크 2·빈 안내) + 에디터 딥링크 1 테스트(전체 1158), 실브라우저 검증(friend→me 멘션·초대 시나리오: 배지 2 → 열기 → 목록·저장소 읽음·배지 소멸 → 멘션 클릭 → `?comments=c1` 딥링크·패널이 그 주제·해당 댓글 표시 / 모바일 390px 벨 44px·패널 화면 안). **미검증**: 0022 트리거의 실제 발화는 라이브 Supabase가 막혀 있어 SQL 검토 + 로컬 짝으로만 확인. **다음 후보**: 멘션 메일(0021 mentions + Resend 인프라 재사용 — Edge Function 한 장)
- ✅ **멘션 후보에서 나 자신 제외(제보: "멘션에 나도 보여서 좀 이상해")** 댓글 `@` 자동완성이 공유 참가자 전체(소유자+초대)를 그대로 보여 줘 자기 자신도 후보에 떴다. 멘션은 남을 부르는 도구고, 알림 트리거(0022)도 자기 멘션은 알리지 않으므로 골라 봐야 아무 일도 없다 — 후보 로드 시점에 내 이메일(`useAuthUser`)을 걸러낸다(이메일은 비동기로 도착하므로 effect deps에 넣어 도착 시 재필터). 웹 +1 테스트(수리 전 실패 확인), 실브라우저 검증(참가자 3명 문서에서 맨 `@` 후보 2명·`@me` 질의 후보 0명)
- ✅ **공동 편집 유령 세션(제보 2건: 새로고침마다 접속자 +1 · 이탈자가 목록에 잔류)** 로컬 2탭(BroadcastChannel)은 결백했다 — 새로고침 3연속에도 1명 유지, 닫기 즉시 정리(실브라우저 실측). 문제는 **Supabase 전송**: 새로고침/크래시는 pagehide의 "떠남" 방송(#320)이 **소켓과 함께 죽어 유실**되고, 그러면 유령은 y-protocols의 30초 `outdatedTimeout`에만 기댄다. 그 사이 ① 새 탭이 합류하면 기존 탭의 awareness 전체 응답이 유령을 **새 탭에 되심어** 접속자가 +1로 보이고(새로고침한 본인 화면에도 자기 유령이 뜬다) ② 이탈자도 30초(백그라운드 탭은 타이머 스로틀로 더) 남는다. 수리: **소켓 죽음을 즉시 아는 유일한 주체는 서버** — 채널에 `presence: { key: awareness clientID }`를 설정하고 announce에서 `track()`으로 살아 있음을 등록, 서버가 쏘는 **presence leave를 받아 그 자리에서** 그 클라이언트의 awareness를 정리한다(`removeAwarenessStates`, origin은 'local'이 아니어야 — 각자 자기 서버 이벤트로 받으므로 되방송 금지). 0009 정책은 extension을 가리지 않아 presence도 같은 read/write 정책을 탄다 — **마이그레이션 불필요**. track 실패 시(정책 변형 서버) 경고만 남기고 30초 타임아웃 폴백으로 동작, 순간 재접속의 leave는 재합류 announce가 상태를 다시 알려 복귀. 부수 효과: 크래시 이탈의 **인수 저장**(#320)도 30초 → 즉시. **실험 함정 기록**: fake timer로 30초 타임아웃을 재려 했더니 유령이 62초에도 남았다 — `lib0/time`이 모듈 로드 시점에 진짜 `Date.now` **참조**를 잡아 둬(fake가 못 바꿈) lastUpdated는 실시간, 나이 계산은 가짜 시간이 되는 불일치였다(라이브러리는 결백). 테스트는 presence 흉내 버스(track한 소켓이 소리 없이 죽으면 서버가 leave를 쏨)로: 하드 이탈 즉시 정리 / 새로고침 +1 없음(수리 전 A=3·B′=3 실패 확인) / 정상 disconnect 무회귀, 기존 채널 mock 전부에 `track` 스텁 추가(웹 +3, 전체 1162). 곁가지: dev 서버가 만드는 `apps/web/dev-dist`(PWA dev SW)가 루트 lint를 반복적으로 깨뜨려 eslint ignore에 추가. **실기기 확인 필요**: 라이브 Supabase가 막혀 있어 실채널 leave 타이밍은 두 계정으로 확인해야 한다
- ✅ **홈 맵 카드 "공유 중" 표식(요청: "공유된 맵을 카드에서 알 수 있게 — 어떻게 표현할까")** Google Drive 관례를 따랐다 — **제목 옆 작은 사람 아이콘**(에디터 공유 버튼·카드 메뉴와 같은 글리프 계열, 상태 표시라 더하기(+) 없이 사람 둘만). 자세한 내용은 툴팁·접근 라벨로: 초대는 "N명과 공유 중"(링크도 켜져 있으면 "· 링크 공유 켜짐"), 링크만이면 "링크가 있는 사람이 열람 가능" — 초대와 링크는 회수 가능성이 달라 나눠 말한다. **데이터는 일괄 조회 하나**: 신설 `ShareStore.listSharedByMe()`가 내가 걸어 둔 초대(문서별 수)와 링크 공유 여부를 한 번에 돌려준다(카드마다 `list()`를 부르면 왕복이 카드 수만큼 난다). Supabase는 `document_shares` 전체 select(RLS가 내 문서의 초대 + 나에게 온 초대만 보여 준다 — 후자는 그리드 카드가 아니어서 무해하고, 최근 트레이에 있다면 실제로 공유된 맵이니 표식이 맞다) + `documents`의 `link_role` 켜진 행. 조회 실패는 빈 객체 — 표식만 빠지고 홈은 그대로(구 서버 배포 순서 안전). 홈 hydrate에 합류시키고, **공유 팝업이 닫힐 때 다시 읽는다**(방금 초대를 걸었거나 링크를 켰을 수 있다 — 실브라우저로 초대→닫기→표식 등장 확인). 그리드·최근 트레이·검색 결과 카드 세 곳 모두(`sharedLabelOf` 순수 함수 하나를 셋이 공유). 어댑터 1 + 홈 통합 2 테스트(전체 1165), 실브라우저 검증(초대 2명 카드 "2명과 공유 중"·링크 카드 "링크가 있는 사람이 열람 가능"·공유 없는 카드 무표식·팝업 초대 후 갱신·다크 테마)
- ✅ **최근 항목 미리보기가 스켈레톤에 갇히던 문제(제보: 간헐적)** 최근 트레이의 "주간 계획" 카드가 로딩 애니메이션인 채로 남았다 — 썸네일 본문을 **한 번도 요청하지 않아서**다. 원인은 프리페치와 트레이의 **자르는 시점 불일치**: 프리페치는 원시 `recent` 목록의 **앞 32개**(RECENT_RENDER_MAX)를 잘라 대상으로 삼는데, 트레이는 해석(별칭→카드)→휴지통 제외→별칭 접기(중복 카드 하나로)를 **거친 뒤** 32개를 자른다. recent는 100개까지 보관되므로 머리에 휴지통·중복 별칭·사라진 문서 항목이 쌓이면(테스트 맵을 많이 만들고 지운 사용자가 정확히 이 상태) 화면에 **보이는** 카드의 원시 항목이 32번째 밖으로 밀려 프리페치에서 빠진다. 특히 **비활성 스페이스의 맵**(제보의 1212 스페이스)은 활성 스페이스 프리페치로도 구제되지 않아 `previewResolved`가 영영 서지 않고 스켈레톤 조건(`docId && !raw && !resolved`)에 갇힌다 — "간헐적"의 정체는 목록 상태다. 수리: 신설 `recentTrayDocIds`(viewModel, 순수)가 **트레이와 같은 파이프라인**으로 실제로 그려질 카드의 docId를 골라 프리페치가 그걸 쓴다(Drive 데모 항목은 슬롯을 세지 않아 오차가 나도 **더 받는 쪽** — 덜 받아 스켈레톤이 남는 일은 없다). 웹 +3 테스트(단위 2 — 머리에 쌓인 40개 항목·휴지통/별칭 제외, 통합 1 — 비활성 스페이스 맵이 40개 뒤에서도 스켈레톤을 벗음. 통합은 수리 전 실패 확인), 전체 1168 통과. 마이그레이션 없음 — 프론트 배포만
- ✅ **PDF/SVG 내보내기(#62 요청)** 내보내기 형식에 **SVG(벡터)**·**PDF(단일 페이지)** 추가 — 에디터 내보내기 메뉴와 홈 카드 메뉴 둘 다(PNG/JSON/MD와 같은 자리·같은 가드). 설계의 핵심은 **장면 순회를 하나로**: `png.ts`의 그리기 패스를 `exportScene.ts`의 `paintScene(Painter)`로 추출해 PNG(캔버스)·SVG(문자열)·PDF(캔버스 래스터)가 레이어 순서·기하·서식을 구조적으로 공유한다(서식 기능마다 "소비처 N곳"을 고치던 드리프트를 늘리지 않고 줄이는 방향). 도형은 전부 SVG 경로 문자열(`d`)로 만들고 캔버스는 `Path2D(d)`로 같은 문자열을 소비 — **리팩터 회귀는 구 빌드와 픽셀 비교로 확인**(변경 픽셀 0.036%, 점선 위상 서브픽셀뿐). **SVG**: 측정 기반 절대 좌표 세그(`textLength`로 측정 폭 강제 — Pretendard 없는 시스템에서도 겹침·열 어긋남 없음, 실측), 이미지는 JSON과 같은 규칙으로 **데이터 URL 인라인**(서명 URL은 만료되므로), 한국어 폴백 폰트 스택, jsdom에서도 문자열이 생성돼 단위 테스트가 실제 구조를 검증. **실브라우저에서 잡은 함정 2개**: ① XML이 `<text>` 앞뒤 공백을 접어 rich 세그 경계 공백(" & ")이 사라짐 — 크롬은 `xml:space`를 무시하고 CSS `white-space:pre`만, 옛 뷰어는 반대라 **두 겹**으로 실음 ② 캔버스 `textBaseline:middle` ↔ SVG 기준선 계수는 관례값(0.35)이 아니라 **실측 0.30em**(같은 글자 잉크 중심 비교, `dominant-baseline`은 일러스트레이터가 무시해 기준선 직접 계산). **PDF**: PNG와 같은 2배 래스터를 JPEG로 임베드한 단일 페이지를 **의존성 없이 직접 조립**(`buildPdfFromJpeg` 순수 함수 — xref 바이트 오프셋까지 단위 테스트). 벡터 PDF를 안 한 이유: 한글 폰트 서브셋 임베드는 파서 의존성+수 MB 자산이고 용도(인쇄·공유)에는 래스터가 충분하며 벡터가 필요하면 SVG가 있다. 페이지 크기는 A4가 아니라 장면 크기 그대로(1px=0.75pt — 넓은 맵의 여백/잘림 판단을 우리가 대신하지 않는다, 인쇄 대화상자가 축소해 준다). 배경이 불투명해 JPEG로 알파 잃을 것 없음(q0.95). 웹 +21 테스트(SVG 구조 11·PDF 바이트 6·에디터 통합 2·홈 통합 2, 전체 1187), 실브라우저 검증: PNG 구/신 파리티·SVG 2배 래스터 vs PNG(mae 0.20, 텍스트 AA 수준)·폴백 폰트 렌더·**pypdf 엄격 파서 통과**(MediaBox=px×0.75, 임베드 JPEG 치수 정확)
- ✅ **홈 카드 렌더 비용 절감 — 가상화 대신 `content-visibility`(중간 단계, 사용자 선정)** 맵 150개에서 검색 해제 시 카드 재마운트 ~1.3초 문제. 전면 가상화는 반응형 `auto-fill` 그리드의 열/행 계산·검색 결과의 가변 높이 스페이스 묶음·화면 밖 카드 부재(접근성·폴더 드래그 오토스크롤)·jsdom 뷰포트 목킹까지 파급이 커서, **CSS 두 줄짜리 중간 단계부터 측정→적용**: 맵 카드에 `content-visibility: auto` + `contain-intrinsic-size: auto 220px`(compact 130px) — 화면 밖 카드의 스타일·레이아웃·페인트를 브라우저가 건너뛴다(썸네일 SVG가 카드마다 수백 노드라 효과가 큼). **DOM·테스트·드래그·접근성·Ctrl+F 전부 무변경**이고, MapCard 한 곳이라 그리드·검색 결과·최근 트레이가 함께 받는다. `auto` 키워드가 한 번 그려진 카드의 실제 크기를 기억해 스크롤바 널뜀 없음. 실측(150맵·40노드 본문): 검색 해제 재마운트 **1221ms → 347ms**(3.5배), 첫 로드 2047ms → 1019ms, 롱태스크 551+668ms → 254+91ms. 실브라우저 시각 검증: 하단 스크롤 렌더·선택 링+글로우(paint containment는 자기 box-shadow를 자르지 않음 — 카드 밖 3px 글로우 온전)·최근 트레이. 웹 +1 스타일 계약 가드(전체 1188). **전면 가상화는 후순위(낮음)로 백로그 유지** — 이 단계로 부족해지고 스페이스당 맵이 수백 개 규모가 되면 메인 그리드+검색 결과 한 세트로 진행
- ✅ **댓글 작성 중 멘션 강조(요청)** 골라 넣은 멘션(@이름)이 **제출 전 입력창에서도** 강조돼 보인다. textarea는 글자를 부분 색칠할 수 없으므로 **백드롭 오버레이**: 입력 글자를 투명하게 하고(캐럿은 `caretColor`로 남긴다) 같은 글자 상자(폰트·패딩·줄높이 동일)의 오버레이가 전체 텍스트를 그리며 멘션 구간만 강조색+연한 배경 칩으로. **굵게는 쓰지 않는다** — 렌더된 댓글의 강조(700)와 달리 굵게는 글리프 폭을 바꿔 오버레이와 캐럿이 어긋난다(색·배경은 메트릭 불변). 강조 대상은 제출 규칙과 동일("골라 넣었고 본문에 아직 살아 있는" 멘션 — 이름 글자를 지우면 강조도 함께 해제), 구간 분해는 신설 `splitMentions`(순수)를 렌더된 댓글(`renderBody`)과 공유. 곁가지 함정 둘: ① 크롬은 placeholder 색을 글자색에서 계산해 투명 글자면 placeholder까지 사라진다 → CSS 변수로 복원 ② textarea 스크롤바가 차지하는 폭만큼 줄바꿈이 백드롭과 어긋난다 → 스크롤바 숨김(스크롤 동작은 유지) + onScroll/렌더 후 scrollTop 동기화. 새 스레드·답글 입력 공용(같은 CommentComposer). 웹 +1 테스트(강조·폭 불변·이름 깨지면 해제, 전체 1189), 실브라우저 검증(2배 스크린샷 — 겹침/어긋남 없음·줄바꿈 정렬·스크롤 동기 83/83·placeholder 복원·제출 후 렌더 강조 무회귀)
- ✅ **댓글을 모든 객체로(요청 ①)** 댓글 대상을 주제(노드) 하나에서 **모든 객체**(메모·이미지·연결선·영역)로 확장. 설계의 핵심은 **서버 무변경**: 객체 id가 컬렉션 불문 같은 팩토리(`x1_…` 꼴, 공유 카운터+타임스탬프)라 문서 안에서 유일하므로 `comments.node_id`가 그대로 "대상 id"가 된다 — 마이그레이션도, RLS 변경도 없다(0022 알림 딥링크도 그대로 동작). 웹 4겹: ① 선택 추종을 전 종류로(어떤 객체를 골라도 패널이 따라간다, 해제만 유지) ② 우클릭 메뉴에 댓글 항목(메모·선·영역 — 공용 `commentItem` 헬퍼로 주제와 같은 문법, **보기 전용은 어떤 객체 위에서든 댓글만 있는 메뉴**로 확장) ③ 개수 배지(`CommentBadge` 공용 컴포넌트 — 메모 우상단(접기 토글이 좌상단), 선은 곡선 중점 위(라벨 있으면 그 위), 영역은 라벨 알약과 대칭인 우상단. 누르면 그 대상의 패널) ④ 패널 제목이 대상 종류를 말한다(`commentTargetLabel` — 주제는 기존 첫 줄, `메모 · …`/`연결선 · 라벨`/`영역 · …`/`이미지`, 사라지면 "사라진 대상") + 알림 딥링크 센터링이 geom에 없는 대상(메모·선·영역)은 문서 좌표에서 중심을 구한다. 웹 +1 통합 테스트(메모 선택 추종→저장 키=플로트 id→배지, 전체 1190), 실브라우저 검증(메모 우클릭→댓글→제목 `메모 · 주간 회고 메모`→배지 1 / 선·영역 배지 표시·선 배지 클릭 시 패널이 `연결선 · 참고`로)
- ✅ **주제/메모 본문 인라인 멘션(요청 ②)** 캔버스 텍스트를 편집하다 `@`를 치면 **공유 참가자 자동완성**이 서식 툴바에 후보 행으로 뜨고(같은 토큰 규칙 `mentionTokenAt`·나 자신 제외), 고르면 "@이름"이 멘션 런으로 들어가 강조되며 **멘션된 사람에게 알림**이 간다. 코어: `RichRun.m`(이메일, 값 있을 때만 직렬화 — 옛 문서·골든·CRDT 무회귀), char 모델 전 경로(runsToChars/charsToRuns/isStyledRuns/clear) 통과, 신설 `insertMention`(토큰을 "@이름 "으로 스플라이스, 뒤 공백은 평문이라 이어 치는 글자가 멘션 안으로 안 자란다)·`mentionEmails`. 자동 링크는 멘션 구간을 건너뛴다. 웹: 편집 박스 왕복은 링크와 같은 무늬(`.mf-mention` 클래스 + `data-mention-email` — 인라인 color면 커밋 때 c로 굳는다), 커밋 렌더 `RichSpan`도 같은 클래스(색은 `--mf-link` 파이프라인 재사용: **밑줄 파랑=링크, 민짜 파랑=멘션**, 굵게는 폭이 변해 안 쓴다). 툴바 감지는 selectionchange+input 두 신호, **캐럿 전용 `editCaretOffset` 신설** — `selectionRange`는 선택이 없으면 전체를 돌려줘 못 쓴다(실브라우저에서 잡음). 알림: **0023** — documents UPDATE 트리거가 old/new 본문 멘션 이메일의 **집합 차이**만 알린다(자동저장 0.9초마다 돌아도 스팸 없음, 수신자는 소유자·초대자만=임의 이메일 차단, 자기 멘션 제외, 미확인 동일 알림 있으면 생략=undo/CRDT 재등장 억제). 벨 kind `doc_mention`("맵에서 회원님을 멘션했어요", 클릭=맵 열기), 로컬 짝은 LocalDocStore.save의 같은 diff. 코어 +4, 웹 +2(왕복·로컬 알림 diff) 테스트(전체 1192+226), 실브라우저 검증(@fr→후보 행→선택→편집 박스 멘션 span·링크 잉크 색→이어 타이핑 평문→커밋 rich `m`·알림 1건 생성·재저장 무중복). **범위 밖**: 썸네일/PNG/SVG는 멘션을 평문으로(색 없음), 0023 실발화는 라이브 Supabase 필요
- ✅ **인라인 멘션 리스트를 댓글과 동일하게(제보 스크린샷)** 캔버스 멘션 후보가 툴바 안 칩 나열이라 댓글 작성창의 리스트(굵은 이름+회색 이메일 행)와 모양이 갈렸다 → **같은 행 스타일**로 교체하고, 요청대로 **리스트가 떠 있는 동안 서식 툴바는 통째로 숨는다**(같은 컨테이너에서 내용 교체 — 리스트가 닫히면 툴바가 그대로 복귀, 위치·앵커 무변경). 알림은 이미 멘션을 쓸 수 있는 모든 곳에 배선돼 있음을 확인: 주제·메모 인라인=0023(노드+플로트 추출), 모든 객체의 댓글 멘션=0022. 선·영역 **라벨**은 rich 모델이 없어 인라인 멘션 자체가 불가(별건). 실브라우저 검증(메모 편집 @ → 리스트 노출+B 버튼 소멸 → 선택 → 리스트 소멸+툴바 복귀 → 커밋 rich `m` + 알림 1건)
- ✅ **인라인 멘션 알림이 안 가던 문제(제보) — 0023 미적용의 원인 2개** 라이브에서 멘션 알림이 오지 않았다. 원인은 0023 마이그레이션이 **한 번도 적용되지 못한 것**: ① documents의 본문 칼럼은 `body`가 아니라 **`data`**다 — 존재하지 않는 칼럼으로 `create trigger ... after update of body`가 실패해 파일 전체가 롤백됐다(마이그레이션은 파일 단위 트랜잭션이라 kind check 확장까지 함께 미적용) ② 고치고 나니 두 번째가 나왔다: 평문 노드의 `rich`는 **JSON null**이라(SQL NULL이 아니다) `coalesce`가 못 거르고 `jsonb_array_elements`가 "cannot extract elements from a scalar"로 터진다 — 칼럼명이 맞았어도 모든 저장이 에러였을 코드. 0012와 같은 `jsonb_typeof` 가드로 수리. **이번에는 로컬 Postgres 하네스로 실발화를 검증했다**(#321 방식 — auth.uid()를 GUC로 대체, 0023 원문 적용): 새 멘션 알림 1건·자동저장 재저장 무중복·플로트 멘션·미확인 중복 억제·미가입/비초대/자기 멘션 차단 6종 통과. **교훈: 트리거 마이그레이션은 스키마 실물 없이 눈으로만 검토하면 안 된다** — 0023 첫 판이 정확히 그 실수였다. 실패한 마이그레이션은 기록되지 않으므로 파일 제자리 수정 후 다음 main 푸시가 재시도한다
- ✅ **멘션이 든 저장이 "변경됨"에서 멈추던 문제(제보) — 0024 강화** 증상이 곧 단서였다: 멘션이 **든** 저장만 실패한다 = 트리거에서 새 멘션이 있을 때만 실행되는 유일한 문장(알림 INSERT)이 에러 → 그 에러가 documents UPDATE 전체를 굴려 저장 실패 → 자동저장이 같은 멘션을 영영 재시도. 유력 원인은 **kind CHECK 제약 이름 불일치**: 0023의 `drop constraint if exists notifications_kind_check`는 라이브 제약 이름이 다르면 조용히 무시되고, 옛 4종 제약이 남아 `doc_mention`을 거부한다 — 로컬 하네스에 그 상태(다른 이름의 4종 제약 공존)를 재현해 **같은 증상 확인**. 0024 두 겹: ① kind를 참조하는 check를 **이름 불문 동적으로 전부 지우고** 5종으로 재구축 ② 알림 생성 전체를 예외 가드로 — 어떤 이유로든 실패하면 `raise warning`만 남기고 **저장은 성공**(알림은 저장을 방해하면 안 된다 — 버전 기록과 같은 원칙, Postgres 로그로 진단). 하네스 3종: 수리 전 재현(제약 위반으로 UPDATE 실패)/0024 후 같은 저장 성공+알림 1건+제약 1개/가드(insert 강제 실패 시 WARNING만 남고 version 증가). **교훈: `drop constraint if exists <이름>`은 이름이 어긋나면 조용히 실패한다** — 제약 교체는 카탈로그를 뒤져 이름 불문으로
- ✅ **초대자 멘션 알림이 안 가던 마지막 원인(라이브 로그 제보) — 0025** 0024의 예외 가드가 설계 의도대로 원인을 로그로 드러냈다: `doc_mention 알림 생성 실패(…): column s.email does not exist`. `notify_doc_mentions`의 초대자 확인 게이트가 document_shares의 이메일 칼럼을 `email`로 참조했는데 실제(0009)는 **`invitee_email`**이다 — 저장은 성공(가드)하지만 소유자가 아닌 **초대자에게는 알림이 한 건도 못 갔다**(소유자 멘션만 게이트 앞에서 통과). 하네스가 이를 놓친 이유가 곧 교훈: 지난 하네스는 document_shares를 `(document_id, email, role)`로 **임의 생성**해 버그와 같은 오인을 그대로 품었다 — **트리거가 참조하는 테이블은 실제 마이그레이션 원문으로 만들어야 한다**(0023 body→data와 같은 계열의 세 번째 사례). 0025는 함수 재정의 한 장(칼럼명 수리, 0009가 insert 시 lower(trim) 정규화하므로 `lower()` 재적용 불필요, 0024의 이중 가드 유지). 재구성한 하네스(실제 0009+0023+0024 원문 `\i` 적용, auth.jwt() GUC 흉내)로 7종 검증: **수리 전 라이브 WARNING 그대로 재현**(알림 0건) → 0025 후 초대자 알림 1건·재저장 무중복·비초대 차단·초대자→소유자 알림·자기 멘션 차단·insert 강제 실패 시 WARNING만 남고 저장 성공. 배포 후 확인법: 새 멘션(기존 멘션은 old 집합에 있어 안 울린다)을 달고 상대 홈 벨 확인, Postgres 로그에 해당 WARNING이 더 안 쌓이는지
- ✅ **멘션 알림 단위를 (이메일, 객체) 쌍으로 — 0026(제보: 같은 사람을 다른 객체에 다시 멘션하면 알림이 안 온다)** 0025 배포 후 실계정 테스트에서 첫 멘션은 도착했는데 **읽고 나서 다른 객체에 같은 사람을 멘션하면 조용**했다 — 버그가 아니라 0023의 설계였다: old/new 본문을 **이메일 집합**으로 비교하므로 그 사람 멘션이 문서에 하나라도 남아 있는 한 집합에 변화가 없어 문서당·사람당 사실상 1회. 사용자 선정으로 비교 단위를 **(이메일, 객체 id) 쌍**으로 좁혔다(`doc_mention_sites` — 노드는 nodes의 키, 메모는 float id): 새 객체에 단 멘션은 새 쌍이라 다시 울리고, 자동저장 반복·같은 객체 안 재멘션·주변 텍스트 편집은 쌍이 그대로라 조용하다(트레이드오프: 같은 객체 안에서 지웠다 다시 멘션하는 경우도 조용 — 소음 쪽보다 낫다는 판단). 같은 저장에 객체 여럿이 늘어도 distinct로 사람당 한 번, 미확인 동일 알림 생략(중복 억제)·소유자/초대자 게이트·0024 이중 가드는 전부 유지. 로컬 데모 짝(`localDocStore.docMentionSites`)도 같은 규칙으로. 하네스(실제 0009+0023+0024+0025 원문 위에 0026)는 **제보 시나리오를 수리 전 그대로 재현**(읽음 후 새 객체 재멘션 = 새 행 없음) 후 7종 통과: 새 객체 재멘션 알림·같은 본문 재저장 조용·미확인 중복 억제·읽은 뒤 재멘션·비초대/자기 멘션 차단·가드 생존. 웹 +1 테스트(로컬 짝 — 수리 전 실패 확인, 전체 1193)
- ✅ **알림 벨 배지가 새 알림에 서지 않던 문제(제보)** 홈을 켜 둔 채 알림이 오면 빨간 배지가 안 뜨고, 벨을 눌러야 그때서야 목록이 갱신됐다(누르는 순간 읽음 처리라 배지는 영영 안 보인다). 원인은 갱신 계기: 벨의 목록 읽기가 **마운트 + 탭 복귀(30초 스로틀)**뿐이라 화면을 떠나지 않는 사용자에게는 새 알림을 알 방법이 없었다. 수리: **60초 주기 확인**(`POLL_MS`) — 탭이 보이는 동안만 돌고(가려지면 쉬었다가 복귀 시 기존 wake 경로가 즉시 확인), 패널이 열려 있으면 쉰다(이미 보고 있고, 읽는 중에 목록이 갈리면 항목이 움직인다). 작은 select 하나라 비용은 미미(realtime 아님 — #231 예산과 무관). 웹 +1 테스트(fake timer로 60초 전진 — 수리 전 실패 확인, 전체 1194), 실브라우저 검증(Playwright clock — 주입 직후 배지 0 → 60초 후 클릭 없이 배지 1)
- ✅ **알림 배지 즉시 표시(0027) — 폴링 대신 신호(질문: "60초 확인뿐인가? 비용 없이 즉시는?")** 댓글 실시간(0021)의 **내용 없는 신호(ping)** 설계를 알림에 이식. `notifications`에 행이 insert되는 순간 **DB 트리거가** `realtime.send()`로 수신자 전용 채널(`mindflow-notify:<uid>`)에 신호만 쏘고, 홈 벨이 받아서 RLS 걸린 `list()`로 다시 읽는다 — 채널은 비밀을 나르지 않고, 알림을 만드는 곳이 어디든(댓글·공유·본문 멘션·앞으로 늘 종류) notifications 테이블 하나를 지나므로 **신호 지점도 트리거 하나**다. 비용: 메시지는 알림이 실제로 생길 때만 1건(폴링보다 쌈), 공개 채널 트레이드오프는 0021과 동일(가짜 신호의 효과 = select 한 번). 포트 `NotificationStore.subscribe` 신설(Supabase=broadcast 구독, 로컬=BroadcastChannel `mf-notify` — `pushLocalNotification`이 쏨), 벨이 구독. **60초 폴링은 안전망으로 유지**(신호 유실 대비 — #335의 교훈: 채널은 조용히 끊긴다). 트리거는 예외 가드로 **realtime.send 없는 서버에서도 알림 생성을 막지 않음**(하네스: WARNING만 남고 insert 성공). 웹 +1 테스트(ping 즉시 배지 — 수리 전 실패 확인, 전체 1195), 실브라우저 2탭 실측 **49ms**. **미검증**: 라이브 Supabase의 realtime.send 실발화는 실계정 확인 필요 — 안 되더라도 폴링 폴백으로 최대 1분
- ✅ **소소한 이슈 3건(제보 스크린샷)** ① **멘션 후보 리스트가 편집 박스에서 멀리 떴다** — 서식 툴바 위치가 두 줄 툴바 높이 어림값(`sy - 92`) 고정이라, 멘션 리스트가 툴바를 **대체**해 내용이 짧아지는 순간 바닥이 붕 떴다. 팝업 위치를 **실측 높이 기반**으로(useLayoutEffect로 offsetHeight를 재서 바닥이 박스 위 8px — 페인트 전에 재므로 틀린 위치가 화면에 안 나간다): 리스트든 툴바든 링크 입력 행이 열려 높이가 변해도 항상 박스에 붙는다(실측: 툴바 gap 8px·리스트 gap 8px). ② **멘션을 고르면 "@이름"이 도형을 벗어났다** — `insertMentionRange`가 편집 박스를 다시 그리기만 하고 **재측정(`updateNodeEditSize`)을 안 태웠다**(타이핑은 input 이벤트가 재는데 이건 프로그램적 DOM 교체라 그 경로를 안 탄다). 삽입 직후 노드 박스를 그 자리에서 다시 잰다(메모는 편집 박스가 카드 안이라 원래 스스로 자란다). 실측: 박스 73→119px, 넘침 0. ③ **홈 검색 중 제목이 활성 스페이스명 그대로** — 결과는 전 스페이스에서 오는데 제목이 "일반 공간"이면 그 스페이스 안에서 찾은 걸로 읽힌다 → 검색 중에는 제목을 **"검색"**으로, 지우면 원래 제목으로 복귀(#361 롤백 때 "돌아갈 자리 표시"로 남겼던 결정을 사용자 제보로 뒤집음 — 기존 테스트 기대값 갱신). 웹 +1 테스트(멘션 삽입 후 박스 폭 증가 — 수리 전 실패 확인) + 검색 제목 단정 갱신(전체 1196), 실브라우저 검증(gap 8px·박스 성장·스크린샷·제목 왕복)
- ✅ **에디터 이슈 3건(제보 4건 중 — 깜빡임은 미재현)** ① **주제 2개에서 드래그 선택 후 Delete 무반응** — 마퀴/Ctrl+A가 객체를 **하나만** 물면 1개짜리 다중 선택이 만들어지는데, 그 상태는 어느 분기도 몰랐다(키보드 Delete/Escape는 `total > 1`만 받고, 단일 분기들은 `selection`을 보는데 마퀴가 그걸 비운다) — 화면에는 선택돼 보이는데 아무 키도 안 듣는 유령 상태. 마퀴·Ctrl+A 모두 **결과가 1개면 단일 선택으로 정규화**(삭제·속성 패널·방향키·F2가 평소처럼 동작). ② **메모 멘션이 파랗지 않음** — 줄 분해(`listLines.splitLines`)가 rich 런을 `LineSeg`로 옮길 때 **`m`(멘션)을 떨어뜨렸다**. 노드 평문+멘션은 RichSpan 직행이라 파랗고, 메모(와 리스트 줄 안 멘션)는 항상 이 경로라 색을 잃던 것 — `LineSeg.m` 추가 한 줄로 수리(실측 rgb(26,99,216)). ③ **모바일 댓글 커서 위치 틀어짐** — iOS 확대 방지 전역 규칙(#347)이 **textarea만 16px로 키우고** 백드롭 오버레이(멘션 강조가 글자를 대신 그리는 div)는 12.5px 그대로라, 캐럿(16px 메트릭)과 보이는 글자가 어긋났다. 같은 미디어 규칙에 `[data-mention-overlay]`를 추가해 글자 상자 계약을 모바일에서도 유지(실측 16px/16px·행높이 24px 일치). 웹 +2 테스트(①·② 모두 수리 전 실패 확인, 전체 1198), 실브라우저 3종 검증. **④ 공동 편집 맵 진입 깜빡임은 미재현**: 로컬 2탭(공유+피어/공유 단독/비공유)에서 rAF 계측·CDP 스크린캐스트 픽셀 diff 모두 커튼 페이드(설계된 공개 애니메이션) 외 프레임 변화 0 — 라이브 전용 요인(폰트 늦은 로드 후 재측정·피어의 미저장 편집 도착·이미지 서명 URL 지연)으로 추정, 재현 조건 확보 후 별건
- ✅ **공동 편집 맵 진입 시 내용이 튀던 깜빡임(제보 추가 정보로 확정)** 재현 정보("편집 이전 내용이 잠깐 보였다가 현재 저장된 버전으로")가 원인을 짚었다 — **이 기기의 로컬 캐시가 먼저 그려지고 서버 최신 판이 도착하면 교체**되는 구조. 혼자 쓰는 맵은 마지막 저장을 이 기기가 해서 캐시=서버(교체 안 보임)지만, 공동 편집 맵은 상대의 편집만큼 캐시가 낡아 매 진입마다 낡은 판→새 판 점프가 보였다. 커튼(CanvasCurtain)이 못 가린 이유: `hydrating`이 **캐시가 있으면 곧장 false**라(캐시 즉시 렌더 설계) 커튼이 서버 로드 전에 걷혔다. 수리: `backendSettled` 신설 — supabase 모드에서 **백엔드 로드가 정착(성공·실패 불문)할 때까지** 커튼 해제 조건에 추가. 캐시 즉시 렌더 자체는 유지(커튼 아래에서 그려져 fit·측정은 진행, 오프라인/실패 시 기존 오류·캐시 경로가 이어받아 커튼이 무한정 남지 않음) — 공개되는 첫 화면이 언제나 서버 판이 된다. 문서 id가 도중에 바뀌는 경우(id 이전)도 로드 effect가 재정착할 때까지 다시 가린다. 웹 +1 테스트(캐시 존재 + 지연 로드에서 커튼 opacity 1 유지 → 정착 후 서버 판으로 공개 — 수리 전 실패 확인, 전체 1199)
- ✅ **홈 4건(제보·요청)** ① **알림 패널 스크롤바** — 목록이 길어지면 기본 10px 스크롤바가 패널 오른쪽을 가려 디자인을 해쳤다 → `.notif-scroll`(LNB `.lnb-scroll`과 같은 처리: 평소 투명, 호버에서만 4px 썸). ② **모바일 벨을 고스트 아이콘으로** — 좁은 앱 바에서 박스형 버튼이 깨져 보였다 → 제목 줄의 ☰과 같은 문법(테두리·면 없음, 20px 글리프, 44px 터치 타깃 유지), 배지는 버튼 모서리가 아니라 **글리프 모서리**에 붙인다. 데스크톱은 박스형 그대로. ③ **"폴더로 이동" 대상을 현재 위치의 폴더만으로** — 예전엔 하위 폴더까지 경로 이름("상위 / 하위")으로 전부 나와 목록이 길고 계층이 흐려졌다 → 최상위에서는 최상위 폴더, 폴더 안에서는 직속 하위만(화면의 폴더 카드와 같은 목록 — "지금 보이는 저 폴더로 넣는다". 더 깊이 넣으려면 그 폴더로 들어가서 옮긴다). ④ **"새 탭에서 열기" 제거**(#345에서 넣었던 것을 사용자 결정으로 롤백 — 필요하면 카드 링크 Ctrl/⌘+클릭). 웹 +2 테스트(벨 고스트+패널 클래스 / 새 탭 부재) + 이동 대상·메뉴 목록 기대값 갱신(전체 1200), 실브라우저 4종 검증(스크롤바 10px→2px 실측·모바일 스크린샷·하위 메뉴에 최상위만·새 탭 부재)
- ✅ **알림 패널 스크롤바 화살표 조각(제보 후속)** 커스텀 스크롤바(#348) 배포 후 "위아래 화살표가 패널 라운드에 잘려 보인다" — 그 화살표는 스크롤바의 **위/아래 버튼**이다: Windows 크롬은 `::-webkit-scrollbar`를 커스텀하면 버튼 자리가 빈 조각으로 남아 둥근 모서리에 걸쳐 잘려 보인다(Linux에는 버튼이 없어 검증 스크린샷에서 안 보였던 것). 수리: `::-webkit-scrollbar-button { display: none }`으로 버튼을 없애고, `::-webkit-scrollbar-track { margin: 12px 0 }`으로 썸의 이동 구간을 둥근 모서리(반지름 14) 안쪽으로 들여놓음(제보의 "위아래 간격 좁히기"가 이 마진이다). 웹 +1 CSS 계약 가드(jsdom은 스크롤바 의사 요소를 렌더하지 않아 파일을 직접 읽는다 — vitest의 `import.meta.url`이 file: 스킴이 아니라 cwd 기준 경로 폴백, 전체 1201)
- ✅ **토스트 X가 자동 업데이트를 영구히 잠그던 문제(질문 → 조사로 발견)** "업데이트 기능이 있는데 왜 닫았다 열어야 하나"는 질문에 답하려고 경로를 추적 — 홈은 이미 유휴 시 `safe`(보고 있어도 즉시 조용히 적용)인데 실제로는 안 됐다. 원인 두 겹: ① 토스트의 **X(나중에)가 `dismissed`로 자동 적용까지 걸어 잠갔고 세션 내내 풀리지 않았다** — 편집 중 토스트를 한 번 닫은 장수 탭은 화면이 안전해져도, 이후의 **어떤 배포도** 스스로 적용하지 못했다(토스트도 다시 안 뜸) ② X가 `setNeedRefresh(false)`로 감지 플래그까지 꺼서, **같은 대기 SW로는 onNeedRefresh가 재발화하지 않아** 같은 버전은 다시 볼 근거 자체가 사라졌다. 수리: X의 뜻을 **"지금 묻지 마"(토스트 숨김)로만** 좁힘 — `dismissed`는 자동 적용 이펙트에서 빠지고, needRefresh는 끄지 않는다(화면이 safe/hidden이 되는 순간 조용히 적용). 웹 +1 테스트(가상 모듈 `virtual:pwa-register/react`를 vi.mock으로 목킹 — block에서 X → safe 전환 → 자동 적용, 수리 전 실패 확인. 전체 1202)
- ✅ **알림 패널 화살표가 새 빌드에서도 남던 진짜 원인(제보 재현 → 확정)** #389의 `::-webkit-scrollbar-button { display: none }`이 배포됐는데도 화살표가 남았다 — **크롬 121+는 표준 속성(`scrollbar-width`/`scrollbar-color`)이 지정된 요소에서 `::-webkit-scrollbar` 커스텀을 통째로 무시**한다. 우리 CSS는 둘을 같이 걸고 있어서(`.lnb-scroll`·`.notif-scroll`·`.mf-recent-scroll` 전부) 4px 폭·버튼 숨김·호버 썸이 **전혀 적용된 적이 없었고**, 실제로 그려진 건 표준/전역 스크롤바(Windows에선 화살표 달린 것)였다. 수리: 표준 속성을 `@supports not selector(::-webkit-scrollbar)`(= Firefox 전용) 블록으로 격리 — 크롬/엣지는 웹킷 커스텀, Firefox는 표준 thin(원래 화살표 없음). **검증 함정 2개 기록**: ① 이 컨테이너의 headless 크로뮴은 웹킷 커스텀 스크롤바를 아예 그리지 않아(오버레이 강제, 30px 테스트 규칙도 0px) 앞선 검증이 헛돌았다 — **xvfb headed 크로뮴**으로 재검증(수리 전 10px 전역 스크롤바 → 수리 후 4px 커스텀) ② `offsetWidth - clientWidth`에는 **테두리가 포함**된다 — 앞서 "스크롤바 2px"로 읽은 값은 패널 테두리(1px×2)였다. CSS 계약 테스트 강화(@supports 격리 + 최상위 표준 속성 규칙 부재, 전체 1202)
- ✅ **모바일 홈 이중 스크롤(제보)** 최상단↔최하단 이동 시 페이지 전체와 목록 스크롤이 따로 움직였다 — 홈 루트가 `100vh`였기 때문. 모바일에서 `100vh`는 주소창을 무시한 **큰** 뷰포트라 루트가 화면보다 길어지고, 그 차이만큼 페이지 스크롤이 하나 더 생겨 안쪽 목록(main) 스크롤과 이중이 된다. 에디터는 정확히 이 이유로 M6부터 `100dvh`를 쓰고 있었는데 홈만 남아 있었다 → `100dvh`로 통일(데스크톱에서는 100vh와 동일). 웹 +1 계약 테스트(수리 전 실패 확인, 전체 1203), 실브라우저 390px 검증(루트=뷰포트 높이·document 스크롤 불가·main만 스크롤)
- ✅ **캔버스 멘션 리스트 키보드 내비게이션(요청)** 인라인 멘션 후보 리스트를 ↑/↓로 이동(끝에서 순환)하고 Enter로 선택. 핵심은 **document 캡처 단계에서 가로채기** — 편집 박스가 포커스를 쥔 채라 그냥 두면 Enter는 편집 확정(데스크톱)으로, 방향키는 캐럿 이동으로 새어 리스트가 닫힌다. `preventDefault`+`stopPropagation`으로 NodeEditBox/전역 핸들러·beforeinput 안전망까지 차단(IME 조합 중 Enter는 확정 동작이라 제외). 활성 항목은 `data-active` + `panel2` 배경으로 강조하고 **hover도 활성을 따라가게**(키보드 강조와 hover 강조가 따로 놀면 Enter가 어느 항목을 고를지 헷갈린다), 토큰(질의)이 바뀌면 인덱스를 처음으로 리셋. 캡처 리스너는 표시 여부 전환에만 등록하고 최신 목록·인덱스는 ref로 읽는다(mentionMatches가 렌더마다 새 배열이라 deps에 넣으면 매 렌더 재등록). 웹 +1 테스트(2명 후보에서 이동·순환·Enter 선택·**편집 세션 유지** — 구현 전 실패 확인, 전체 1204), 실브라우저 검증(이동·순환·Enter로 `@buddy ` 삽입·리스트 닫힘·편집 유지·활성 배경 실측). 댓글 작성창의 멘션 리스트는 별도(textarea 기반 — 필요 시 같은 패턴 이식)
- ✅ **댓글 작성창 멘션 리스트 키보드 조작(요청 — 캔버스 리스트의 짝)** 직전에 캔버스 멘션 리스트에 넣은 ↑/↓·Enter 조작을 댓글 작성창(`CommentComposer`)에도. 캔버스와 달리 **textarea가 포커스를 쥐고 있어** document 캡처 리스너가 필요 없다 — 기존 React `onKeyDown`에 분기만 추가: 리스트 표시 중 ↑/↓=활성 행 순환 이동(`preventDefault`로 캐럿 줄 이동이 토큰을 흔들지 않게), Enter/Tab=**활성** 후보 선택(예전엔 항상 첫 후보 고정), `e.nativeEvent.isComposing` 가드(IME 조합 확정 Enter가 후보 선택으로 새지 않게). 활성 표시는 캔버스와 같은 문법(`data-active` + `th.panel2` 배경 + `onMouseEnter` 동기 — 마우스가 가리키는 행과 Enter가 고를 행이 달라 보이면 안 된다), 질의(token start/query)가 바뀌면 첫 행으로 리셋. 기존 동작 무회귀: Escape=토큰/패널 닫기, Ctrl·⌘+Enter=제출, 리스트 없을 때 Enter=줄바꿈. 웹 +1 테스트(수리 전 실패 확인 — 활성 표시 자체가 없었다, 전체 1205), 실브라우저 검증(초기 활성+배경 하이라이트·양방향 순환·hover 동기·Enter가 둘째 후보(buddy) 선택·캐럿 7=`@buddy ` 뒤·저장 멘션 buddy·Enter=줄바꿈 유지)
- ✅ **협업 끊김 배너 3건(모바일 제보) — 겹침·dim·재연결 애니메이션** ① **겹치는 알림 정리**: 끊김 상태에서 가운데 배너(`CollabBlockedBanner`, top 14)와 우상단 `PresenceBar` 배지("공동 편집 연결 끊김", top 16)가 **같은 상태를 두 번** 말하며 좁은 모바일 화면에서 겹쳤다 — 배너/`CollabPaused`가 뜨는 조건(`collabBlocked`)에서는 PresenceBar를 통째로 내린다(접속자 아바타도 끊긴 동안은 낡은 정보). 배너가 뜨지 않는 끊김(공유 안 된 맵 + 접속자 흔적)만 기존 배지가 맡는다. ② **배경 dim(요청)**: 다시 연결될 때까지 캔버스 컨테이너에 dim 베일(`data-collab-dim`, 모달 dim과 같은 잉크의 옅은 판 `.26`) — **`pointer-events: none`**으로 시각 신호만 준다(조작 차단은 `commitDoc` chokepoint가 이미 하고, 읽기·이동·줌은 그대로가 기존 설계). ③ **재연결 애니메이션(요청)**: "다시 연결 중…" 옆 회전 스피너(`.mf-collab-spin`, `mf-spin` 키프레임 신설) + dim·배너 모두 제자리 페이드 `mf-dim-in`(배너는 `translateX(-50%)` 센터링이 있어 transform 있는 키프레임 금지 — #331의 교훈). 웹 +1 테스트(dim 부재·배지 겹침 **둘 다** 수리 전 실패 확인, 전체 1206). 실브라우저: 끊김 상태는 supabase 모드 전용이라 이 환경에서 만들 수 없어(#332와 동일) 같은 마크업 주입으로 번들 CSS 검증 — 스피너 `animationName: mf-spin`·회전(transform 변화) 실측, 390px 배너 화면 안 배치, dim 시각 스크린샷. 실기기 확인 권장
- ✅ **끊김 배너·dim이 비행기 모드인데 사라졌다 다시 뜨던 문제(실기기 제보, #396 배포 직후)** 원인은 자동 재접속(#331)과의 상호작용: `scheduleRetry`가 **재시도할 때마다 상태를 'connecting'으로** 올리는데(그 자체는 #331의 의도 — 배지용), `collabBlocked`가 `collabStatus === 'offline'`을 **그대로** 읽어서 재시도 주기(2s→8s→30s→2m)마다 ① 배너·dim이 내려갔다 실패하면 다시 뜨고(제보의 깜빡임) ② 그 'connecting' 창 동안 **편집 차단까지 풀려** 유실 창이 다시 열리며 ③ 30초 승격 타이머(`CollabPaused`)가 매번 리셋돼 오래 끊겨도 전체 안내가 영영 안 떴다. 수리: **끊김은 재시도 중에도 끊김이다** — `collabDown` 상태를 신설해 offline을 한 번 보면 실제로 다시 붙을 때(`connected`/`connected-insecure`)까지 유지하고, 'connecting'은 판단을 바꾸지 않는다(첫 접속 과정의 connecting은 offline을 본 적이 없어 그대로 통과 — "접속 과정은 고장이 아니다" 원칙 유지, 빈 docId도 초기값 connecting이라 오탐 없음). 재연결 시에도 connected가 확인될 때까지 배너가 유지되다 한 번에 걷힌다(제보 후반의 "재연결 시 다시 노출됐다 사라짐"도 같은 뿌리). 웹 +2 테스트(재시도 중 배너·dim·편집 차단 유지 / 승격 타이머 무리셋 — 둘 다 수리 전 실패 확인, 전체 1208). 실기기 재확인 필요(비행기 모드 유지 중 배너 상시 유지 + 30초 후 전체 안내 승격)
- ✅ **끊김 배너 후속 2건(배포 확인 후 요청)** ① **배너 두 줄** — 첫 줄 "연결이 끊겨 편집을 잠시 멈췄어요"(경고 아이콘), 둘째 줄 스피너+"다시 연결 중…"(전용 행 `data-collab-retry-row`, subtext 색) — 무슨 일인지와 지금 무엇을 하는 중인지가 줄로 나뉜다(알약 → 라운드 14 카드). ② **dim이 상호작용을 차단** — 처음엔 `pointer-events: none`으로 읽기·이동·줌을 열어 뒀는데(#396) 객체 선택·속성 패널이 살아 있어 "고쳐지는 척"으로 읽혔다(제보: "객체를 선택할 수 있고 속성박스 등 상호작용이 가능"). dim을 `pointer-events: auto` + **z 150**(캔버스 안 최고 층인 NodeLayer 편집 박스 100보다 위, 배너 151)으로 올려 그 아래 모든 포인터 조작을 삼킨다. 문서 변이는 여전히 `commitDoc` chokepoint가 이중으로 막는다(키보드 등 포인터 밖 경로). dim 위 우클릭은 `preventDefault`(브라우저 기본 메뉴 차단). jsdom은 pointer-events를 실행하지 않으므로 테스트는 스타일 계약(auto + z>100 + 배너 z>dim)으로 고정하고, **실브라우저에서 기능을 실측**: 대조군(무dim 탭=선택 바 뜸) → dim 주입 후 같은 탭·더블클릭 모두 차단(`elementFromPoint`=dim), 두 줄 배치·칩 위 페인트 확인. **프로브 함정 기록**: 페이드인(0.25s) **도중** 스크린샷을 찍으면 배너가 반투명이라 독칩이 위에 있는 것처럼 보인다(스태킹 오판으로 1시간 조사 — `topAtChip` 실측으로 페인트 순서는 정상 확인, 촬영을 페이드 후로)
- ✅ **화이트보드 M1(코어) — `Doc.kind('board')`·루트 없는 layout·`Float.caption`** 신규 문서 종류 "화이트보드"(사용자 제안: 트리 없이 메모·이미지만 자유 배치, 윈도우 바탕화면 메모 감각, 공유는 맵과 동일)의 코어 기반. **설계(전수 조사 후 A안 채택)**: board = `nodes: {}`인 평범한 Doc — 숨은 루트를 두는 B안은 "루트를 그리지 말 것" 분기를 렌더·선택·기하 7곳+에 심어야 하고 이 코드베이스가 이미 유령 선택 상태로 여러 번 데인 이력이 있어 기각. 조사 결과 **하드 크래시는 `layout()`의 `at(nodes, ROOT_ID)` 단 한 곳**이고(직렬화 `!d.nodes` 가드는 `{}`가 truthy라 통과, CRDT `readNodesMap`은 루트를 요구도 합성도 안 함 — 실측), 나머지는 전부 "빈 결과 조기 반환" 계열. 이번 변경: ① `Doc.kind?: 'map'|'board'` — **'board'일 때만** 직렬화(`RichRun.href` 규칙 — 골든·기존 저장본 바이트 무변경), 알 수 없는 값은 파싱에서 버림 ② **CRDT meta에 `kind` 왕복**(edgeStyle 패턴 — 빠뜨리면 협업 중 원격 문서 도착 시 board가 조용히 맵으로 변해 다음 저장에 기록되는 사고, #226과 같은 계열) ③ `layout()` 루트 부재 시 조기 반환(자유 도형 하위 트리는 여전히 `layoutFreeSub`로 배치) ④ `Float.caption?: string`(이미지 제목 — 제네릭 통과라 직렬화/CRDT 무수정) ⑤ `toMarkdown(doc, title?)` — 루트 없으면 메타 제목을 H1로, 선두 빈 줄 제거(루트 있으면 무변경). 코어 +10 테스트(전체 236, 골든 무회귀), 웹 1208 무회귀. **다음**: M2 에디터 board 모드(geom/바운즈를 "노드 ∪ 메모"로 일반화 — 커튼·fitView·미니맵·export 4종을 한 번에 해결하는 지렛대, 제목 3곳을 메타 기반으로), M3 홈 갤러리 진입+썸네일(`mapPreview:391` 가드 완화), M4 그리기(획=원자 폴리라인)
- ✅ **화이트보드 M2(에디터 board 모드)** 트리 없는 문서가 에디터에서 온전히 동작한다. ① **장면 바운즈 일반화(지렛대)**: 화면 맞춤·첫 센터링·미니맵이 보던 "내용"을 노드 geom에서 **노드 ∪ 메모 박스**(`sceneBounds` — 메모 높이는 실측 `floatHeights`)로 확장 — 보드에서 커튼이 안 걷히던 문제(첫 센터링이 영영 안 돎)와 맵에서 트리 밖 메모가 화면 맞춤·미니맵 밖이던 문제를 한 번에 해결. **완전히 빈 보드**(새로 만든 직후)는 감쌀 내용이 없으므로 기본 뷰(원점 중앙, zoom 1)로 곧장 연다. 미니맵은 메모를 작은 사각(`data-minimap-float`)으로 그림. 내보내기 가드(`computeSceneBounds`/`renderExportCanvas`)는 "노드·메모·영역·선이 **모두** 비었을 때만" null로 완화. ② **제목을 메타 기반으로**: `metaTitle` 상태 신설(첫 값=URL title, 백엔드 로드가 저장된 메타 제목을 돌려주면 채택) — 저장(`persistDoc`)·내보내기 파일명·DocChip 표시(`docTitle`)가 이걸 폴백으로 쓰고(맵은 루트 텍스트가 계속 우선 — 무회귀), **board의 제목 편집**은 `commitRootTitle`(루트 없으면 no-op)로 가지 않고 메타를 고쳐 곧바로 저장한다(제목만의 변경은 doc을 감시하는 자동저장이 못 보므로). 조사에서 확인한 "저장 계층은 이미 메타 제목이 1급"이라는 결을 에디터도 따르게 된 것. ③ **UI 트리밍**(`controller.isBoard` — "할 수 없는 것은 보이지 않는다"): 삽입 메뉴·배경 우클릭=메모/이미지만, 스타일 메뉴=레이아웃·연결선 구획 숨김(테마 유지), 보기/☰ 메뉴=아웃라인 숨김. ④ **시드**: `buildTemplateDoc('board')` — 갤러리(M3)가 `tpl=board`로 연다. ⑤ **이미지 캡션**: 렌더=이미지 **아래 바깥**(top:100% — 히트 박스·핸들·겹침 계산 불변, 그림 캡션 관례로 가운데), 편집=속성 패널 '제목' 입력(blur/Enter 커밋 — 입력마다 커밋하면 타이핑마다 undo 단계가 쌓인다), 빈 값이면 키 삭제(`setFloatCaption` — clearNodeImage와 같은 규칙). 웹 +5 테스트(시드·저장 kind/빈 nodes / 메뉴 4종 트리밍 / 제목 메타 커밋 / 캡션 렌더+패널 편집 왕복 / 맵 무회귀 — 저장 단정은 Ctrl+S로 디바운스 우회), 전체 1213. 실브라우저 검증(빈 보드 열림·커튼 걷힘·삽입/우클릭/스타일 트리밍·메모 생성·board 저장·미니맵 메모 사각·캡션 렌더/편집·메모 기준 첫 센터링 화면 안 [true,true]). **알려진 갭(후속)**: 클립보드로 다른 맵의 주제를 board에 붙여넣을 수 있음(동작은 정상 — 자유 도형으로 렌더), 방향키가 메모를 순회하지 못함(맵과 동일), 단축키 도움말이 board에서도 트리 단축키를 보여줌
- ✅ **화이트보드 M3(홈 진입·썸네일·회귀)** 홈에서 화이트보드를 만들고 알아볼 수 있다 — 이 마일스톤으로 화이트보드가 **실사용 가능**해졌다(M4 그리기는 별건). ① **갤러리 진입**: "새로 만들기" 갤러리의 **둘째 칸**(빈 맵 다음, 템플릿들 앞 — 둘 다 "시작" 칸)에 `data-template="board"` 카드(메모+이미지 삽화 SVG — 내용이 비어 있어 realPreview 대신), `createFromTemplate('board')`가 `새 화이트보드` 제목으로 `tpl=board` 에디터를 연다(M2의 시드와 맞물림). ② **카드 썸네일**: `realPreview`의 `!ids.length` 가드를 "노드도 메모/영역/선도 없을 때만 null"로 완화 — 보드 카드가 장식용 miniPreview로 떨어지지 않고 실제 메모 배치를 그린다(버전 기록·갤러리 미리보기도 같은 렌더러라 함께 해결). `root`는 ids가 비면 undefined지만 노드 렌더 루프가 돌지 않아 안전. ③ **JSON 가져오기**: 유효 조건을 "루트가 있는 맵 **또는** `kind==='board'`"로 완화(보드는 루트 없음이 정상) — 보드 제목은 본문에 없으므로 파일명. ④ **버전 기록**: 보드는 "주제 0개"가 판을 구별 못 해 **메모 수**를 세고 라벨도 `메모 N개`로. 웹 +5 테스트(갤러리 칸 위치·생성 / 보드 JSON 가져오기 / 썸네일 실렌더 — **수리 전 실패 확인** / 빈 보드 miniPreview 폴백 / 버전 기록 메모 수), 전체 1218. 실브라우저 E2E: 홈 갤러리(blank→board→템플릿 순) → 보드 생성(`tpl=board` URL) → 메모 2개 → 홈 카드 썸네일이 실제 메모 배치를 렌더(rects 2) → **2탭 협업 회귀**(탭1 메모 추가 → 탭2 실시간 +1, 협업 왕복 후 저장 `kind: board` 유지). **범위 밖(후속 후보)**: 홈 카드에 문서 종류 배지(지금은 썸네일 모양으로 구별), 로컬 모드 `syncDocsToCards`가 카드 없는 보드 본문을 자동 등록하지 못함(정상 생성 경로는 홈이 카드를 만들어 무관)
- ✅ **화이트보드 후속 2건(요청) — 배경 무조건 흰색 + 메모 접기 제거** ① board 캔버스 배경을 테마와 무관하게 **흰색**으로 — 신설 `docThemeOf(doc)`(theme.ts)가 board면 `themeOf(themeKey)`에 `canvasBg '#ffffff'`·`dot '#e6e6e6'`만 덮어쓴다. 에디터의 theme 소비처(캔버스 배경·미니맵·커튼·PNG/SVG/PDF 내보내기)와 홈 내보내기 3곳(`useHomeController`)이 전부 이 함수 하나를 지나므로 화면과 내보낸 파일이 같은 흰 바탕이다(도형 테마색·UI 크롬은 그대로 — 바뀌는 건 바닥뿐). ② board에서는 메모 **접기 기능 자체를 제거**(FloatLayer — 토글 비렌더 + `collapsed` 무시: 과거 맵에서 접힌 채 board로 온 데이터도 펼쳐 그린다). 화이트보드의 메모는 훑어보는 바닥 내용이라 접기가 오히려 내용을 감춘다는 판단. 웹 +1 테스트(dark 테마 board에서 배경 255,255,255 + 토글 부재 + collapsed 본문 렌더), 맵은 무회귀(기존 접기 테스트 그대로)
- ✅ **화이트보드 M4(그리기 — 펜·획 지우개)** 검토대로 v1 범위: 자유 곡선 펜 + **획 단위** 지우개(픽셀 지우개 아님 — 픽셀 분할은 획을 쪼개는 기하가 필요해 v2). **코어**: `Stroke { id, pts: number[](평탄 [x,y,…]), color, w }` — `Doc.strokes?`는 **비어 있지 않을 때만** 직렬화(kind·caption과 같은 규칙 — 골든·기존 저장본 무변경), CRDT는 `syncEntityList/readEntityList('strokes','strokesOrder')` 제네릭 경로(획은 **원자 값** — 그린 뒤 공동 편집이 없어 노드처럼 필드 병합할 이유가 없다). 신설 `strokes.ts`: `strokeBounds`(굵기 절반 포함)·`strokeHit`(선분 거리 제곱, tol=지우개 반경+굵기/2)·`strokePathD`(SVG/캔버스 공용 d 문자열, 점 하나는 극소 선분으로 둥근 캡 점). **웹**: 하단 중앙 알약 `BoardToolbar`(선택/펜/지우개 + 펜 활성 시 색 5·굵기 3, Escape=선택 복귀, board 전용·readOnly 숨김) / `BoardDrawLayer`(도구가 켜진 동안 inset:0 z110 오버레이가 포인터를 전담 — 그 아래 선택·드래그 핸들러를 안 건드리고 얹는 가장 파급 작은 방법. 두 번째 손가락이 닿으면 진행 획을 커밋 없이 버림=핀치는 그리기가 아니다) / `StrokeLayer`(팬 레이어 안 SVG, 영역 뒤·메모 앞 = 보드 바닥의 잉크). 입력 단순화는 입력 시점(화면 1.5px/zoom 미만 점 버림 + 0.1 반올림 — 손글씨는 초당 수십 좌표라 문서가 부푼다), 펜을 떼는 순간 **커밋 한 번=undo 한 단계**, 지우개 문지르기는 continuous 커밋으로 coalesce. `docSignature`·히스토리 스냅샷 6지점에 strokes 배선(빼먹으면 자동저장이 획 변경을 못 보고 undo가 획을 못 되돌린다 — `?? []` 정규화로 부재==빈 배열). 장면 반영 4곳: `sceneBounds`(첫 센터링·화면 맞춤)·홈 썸네일(`mapPreview`)·`paintScene`(PNG/SVG/PDF — 내보내기 가드도 완화)·미리보기 가드. **실브라우저에서만 드러난 함정 2개**: ① 도구 막대·그리기 레이어가 `.mf-ed-vp`(배경 드래그 소유) 자식이라 pointerdown이 새면 배경 마퀴가 포인터를 **캡처**해 버튼이 pointerup/click을 영영 못 받는다(jsdom 테스트는 통과하는데 실브라우저에서 펜 버튼이 안 눌림) — ContextMenu·TextToolbar와 같은 처방(루트 pointerdown stop) ② SVG 스펙에서 최상위 svg의 **width/height 0은 렌더링 자체를 끈다** — DOM에 path가 있는데 화면에 잉크가 안 보였다(EdgeLayer처럼 10×10 + overflow:visible로). 프로브도 DOM 존재가 아니라 **픽셀**(빨간 px 수)로 재확인하게 강화. 기본 펜 굵기는 선택지(2·4·8) 중 하나인 4(아니면 첫 사용에서 아무 굵기도 눌린 상태가 아니다). 코어 +5(직렬화 왕복·CRDT 왕복·기하) / 웹 +4(그리기 커밋→저장→undo·지우개 선별 삭제·도구 막대 board 전용·썸네일 획 렌더 — jsdom엔 PointerEvent가 없어 기존 `firePointer` 처방), 전체 241+1223. 실브라우저 14종(펜 커밋·픽셀 페인트·저장/리로드 영속·지우개·undo 복원·Escape 복귀·홈 썸네일·PNG 다운로드에 잉크 픽셀 4,503·2탭 협업 획/지우개 양방향 전파·맵 무회귀·흰 배경·접기 부재)
- ✅ **board 메모 좌우 패딩 대칭(제보: 접기가 사라졌는데 좌측만 넓다)** 메모 카드의 좌측 패딩 32px는 **접기 토글 자리**였다 — board에서 토글을 없앤(#401) 뒤에도 자리만 남아 좌측이 우측(11px)의 세 배였다. 수리의 핵심은 **한 곳으로 모으기**: 신설 `floatPadLeft(board)`(metrics.ts — 맵 32 / board 11)를 렌더(FloatLayer)·측정(`measureFloatHeight` — 좁아진 만큼 줄바꿈 폭이 넓어져 박스 높이가 달라진다)·내보내기(`sceneFloatBox`/`paintScene` — PNG·SVG·PDF 셋 다, board에선 접기 배지(코럴 ＋/− 원)도 그리지 않는다)·홈 썸네일(`mapPreview` — innerW·textX)이 전부 쓴다. 흩어진 채 렌더만 고치면 측정·썸네일·내보내기의 줄바꿈이 화면과 어긋난다(이 코드베이스의 "소비처 N곳" 계열). 내보내기 `sceneFloatBox`는 board에서 `collapsed`도 무시(에디터 FloatLayer와 같은 규칙). 맵은 전부 기존 값 그대로(무회귀 — 토글이 여전히 그 자리에 있다). 웹 +2 테스트(board 좌=우 / 맵 32px+토글 유지, 전체 1224), 실브라우저 4종(board 패딩 11=11 실측·편집 열닫아도 높이 불변=측정 파리티·맵 32px 무회귀·홈 썸네일 textX가 board=+11/맵=+32)
- ✅ **화이트보드 후속 6건(요청)** ① **갤러리 제목·문구** "새 맵 만들기" → **"새로 만들기"**(문서 종류가 둘이 됐다), 안내는 "생각을 가지로 뻗는 마인드맵, 자유롭게 붙이는 화이트보드 중에서 골라 보세요". ② **구획 분리**(제보: 섞여 있어 구별이 힘들다) — 모달 폭 760→860, `마인드맵`(빈 맵 + 템플릿 6)과 `화이트보드`(빈 화이트보드) 두 구획에 각각 한 줄 설명(`SectionHead`)과 구분선. 카드 최소 폭을 200→**180**으로 낮춰 마인드맵 7칸이 **두 줄**에 들어가고(세 열이면 세 줄이라 화이트보드 구획이 접힌 화면 밖으로 밀린다) 860×860 화면에서 두 구획이 한눈에 보인다(실측: 모달 778px, 스크롤 없음). ③ **새 보드에 시드 메모 하나**(제보: 텅 빈 흰 화면이 허전하다) — `buildTemplateDoc('board')`가 원점을 감싸는 자리에 안내 겸 예시 메모를 담는다(첫 센터링이 장면 중심을 잡으므로 화면 한가운데 열린다). ④ **도구 단축키 V/P/E**(선택·펜·지우개) — 수정 키가 없을 때만이라 Ctrl+V(붙여넣기)와 겹치지 않고, 한글 IME에서 `e.key`가 자모로 와도 `e.code`로 잡는다(복사/붙여넣기와 같은 처방). 단축키 도움말에 **board 전용 구획**을 더했다(Esc=선택 복귀 포함). ⑤ **모바일 겹침 수리**(제보) — 하단 중앙 도구 막대와 우하단 줌·미니맵 묶음이 폰에서 부딪혔다 → 묶음이 도구 막대 높이(`BOARD_TOOLBAR_LIFT`)만큼 위로 비켜선다(390×780 실측: 겹침 0, 둘 다 화면 안). ⑥ **삽입·되돌리기를 도구 막대로**(요청: GNB 메뉴 안이라 불편하다) — [선택|펜|지우개] · [메모|이미지] · [실행취소|다시실행] 한 줄, 펜을 켜면 색·굵기가 **둘째 줄**로 펼쳐진다(한 줄에 몰면 폰에서 화면을 넘는다). 삽입 버튼은 그리기 도구가 켜져 있어도 눌리고 누르면 **선택 도구로 돌아온다**(방금 만든 것을 바로 만진다). board에서는 **GNB 삽입 메뉴를 없앴다** — 같은 동작의 진입점을 둘로 두지 않는다(배경 우클릭은 "누른 자리에 만든다"는 다른 동작이라 유지). 웹 +3 테스트(V/P/E 전환·Ctrl+V 무간섭 / 도구 막대 삽입·undo·redo / 모바일 묶음 lift), 기존 기대값 갱신(전체 1227), 실브라우저 12종(갤러리 제목·문구·구획·폭 / 시드 메모 화면 안 / V·P·E / 펜 중 메모 추가→선택 복귀 / undo·redo / GNB 삽입 부재 / 도움말 구획 / 모바일 무겹침) 검증
- ✅ **화이트보드 UI 5건(제보·요청)** ① **속성 시트가 하단 도구 막대에 가려졌다**(폰) — 모바일 속성 시트의 z를 25 → **130**(도구 막대 120 위)으로. 시트는 "지금 고른 것"을 다루는 화면이라 그 위가 맞다(데스크톱 패널은 좌측이라 겹치지 않아 15 유지 — 컨텍스트 메뉴 위로 올리는 부작용을 피한다). ② **시트 상단 '닫기' 손잡이 제거**(요청: 바깥을 누르면 닫히니 불필요) — 실제로 그런지 실브라우저로 확인하고 뺐다(배경 탭 → 선택 해제 → `propsOpen` 리셋). 가로 폰의 옆면 손잡이도 같은 이유로 함께 제거. **프로브 함정**: 시트를 열면 대상이 `centerObjectAboveSheet`로 시트 위로 재중앙 정렬되므로 고정 좌표를 누르면 그 대상 위를 누르게 된다 — "안 닫힌다"로 보였던 것의 정체(빈 지점을 그때 찾아 누르도록 프로브 교정). ③ **이미지 제목 20자 상한** — 한 줄 말줄임 자리라 길게 적어도 보이지 않는다. 입력창 `maxLength`만이 아니라 **커밋(`setFloatCaption`)에서도** 자른다(붙여넣기 등 다른 경로도 같은 규칙). ④ **폰의 줌·미니맵 묶음을 우측 상단으로**(요청: 하단 도구 막대 위에 얹혀 균형이 안 맞는다) — board+모바일에서만. 접속자 아바타(PresenceBar)도 우측 상단이라 **사람이 있을 때만** 그 높이(46)만큼 내려 나란히 선다. 맵은 하단 유지(무회귀 테스트). ⑤ **홈 카드에서 화이트보드 구별**(제보) — 두 겹으로: 썸네일 바탕이 **흰 종이**(에디터 보드 캔버스와 같은 인상, 맵은 기존 그라디언트) + 우상단 **'화이트보드' 배지**(최근 트레이의 작은 카드는 '보드'). 판별은 `isBoardRaw(raw)` — 카드마다·렌더마다 도는 자리라 본문을 다시 `JSON.parse` 하지 않고 `"kind":"board"` 문자열만 본다(썸네일은 이미 캐시된 파싱을 쓴다). 웹 +4 테스트(시트 z·닫기 부재·제목 20자 / 우측 상단 / 맵 하단 무회귀 / 홈 카드 배지+바탕) + 기존 손잡이 테스트 2건 갱신(전체 1231), 실브라우저 10종 검증. **후속 2건(제보)**: 배지가 카드 오른쪽에서 34px이나 떨어져 있었다(46 — Drive 배지가 쓰던 값인데 그 오른쪽에는 아무것도 없다. ★는 왼쪽, ☰은 하단) → **12**로 붙임(실측 여백 13px). 이름 영역을 **가라앉은 면**(`--mf-sunken`)으로 칠해 봤는데 그 색이 홈 배경과 비슷해 카드가 배경에 묻혔다(제보) → 되돌리고 **테두리 색**으로 옮겼다(사용자 제안): 보드 카드만 `--mf-info`(밝은 테마 #3f8fd0 / 다크 #5fa8e8) 1px. 선택 표시(강조색 2px + 글로우)와는 색·굵기·글로우가 모두 달라 헷갈리지 않고, 기존 토큰이라 테마·다크를 자동으로 따른다. 썸네일(흰 종이)·배지와 함께 세 겹으로 종류를 알린다
- ✅ **클립보드 이미지 Ctrl+V 붙여넣기(요청) — 우리가 스스로 막고 있던 진입점** "이미지가 클립보드에 있을 때 Ctrl+V로 삽입되게 할 수 있나"는 요청에 코드를 보니 **기능은 이미 있었다**(`paste` 이벤트 핸들러 → `firstImageFile` → 이미지 플로트). 그런데 실브라우저로 재 보니 한 번도 동작한 적이 없었다: Ctrl+V의 keydown이 `e.preventDefault()`로 객체 붙여넣기를 처리하고 있었고, **keydown에서 막으면 브라우저가 `paste` 이벤트를 아예 발생시키지 않는다**(실측: 캔버스에서 Ctrl+V → paste 이벤트 0회, `<img>` 0개). 즉 이미지 첨부의 세 진입점(삽입 메뉴·드롭·붙여넣기) 중 붙여넣기만 조용히 죽어 있었다. 수리: keydown은 **아무것도 막지 않고**, 붙여넣기 판단을 `paste` 이벤트 **한 곳**에서만 한다 — 텍스트 입력 중(노드/메모 편집·검색창·댓글 작성창)이면 그대로 통과, 클립보드에 이미지가 있으면 이미지 플로트, 아니면 앱 안 클립보드(복사한 주제·메모·선). **의도한 동작 변경 하나**: OS 클립보드의 이미지가 앱 안에서 복사한 객체보다 **우선**한다(대부분의 앱과 같은 규칙 — 방금 캡처한 이미지를 붙여넣는 쪽이 자연스럽다). keydown에는 취소 가능한 **120ms 폴백**을 남겨, `paste` 이벤트가 오지 않는 환경(제한된 브라우저·확장)에서도 객체 붙여넣기는 살아 있다(이벤트가 오면 즉시 취소 — 이중 붙여넣기 없음). 읽기 전용은 두 경로 모두 `commitDoc` chokepoint가 그대로 막는다. 웹 +3 테스트(이미지 있으면 `defaultPrevented`+객체 붙여넣기 안 함 / 이미지 없으면 객체 붙여넣기 1회, 250ms 뒤에도 중복 없음 / 텍스트 입력 중 무간섭 — jsdom은 이미지를 디코드하지 못해 **삽입이 아니라 갈림**을 단정하고 실삽입은 실브라우저가 맡는다, 전체 1234), 실브라우저 검증(`navigator.clipboard.write(ClipboardItem)`로 진짜 PNG를 심고 진짜 `Control+v`: board 플로트 1→2·map 0→1·paste 이벤트 1회·`<img>` 렌더 / 회귀 5종 — 객체 붙여넣기 무회귀·이미지 우선·저장본에 `data:image/webp` 플로트·검색창 텍스트 붙여넣기 그대로·텍스트 입력 중 이미지 미삽입)
- ✅ **화이트 테마 신설 + 보드 테마 수리 + 모바일 도구 막대 자리(제보 3건)** ① **화이트보드에서 테마를 바꿔도 아무것도 안 바뀌었다** — 원인은 M4 때 넣은 덮어쓰기(`docThemeOf`가 board면 `canvasBg`를 흰색으로 강제)였다. 보드에서 눈에 띄는 면은 사실상 캔버스 하나인데(메모는 고정 노랑, 획은 자기 색) 그 면만 테마 밖에 있었으니, 스와치를 눌러도 화면이 그대로였던 것. ② 그래서 흰 배경을 **덮어쓰기가 아니라 테마**로 만들었다 — 신설 `white`(라벨 '화이트', 순백 `appBg`/`canvasBg`, 중립 회색 도트, 강조색 `#2f7fd6`): 도형 기본 채움이 캔버스와 같은 흰색이어도 **테두리가 가지 색**이라 또렷하다(Miro·Whimsical류 흰 캔버스와 같은 결). 마인드맵에서도 고를 수 있다(요청). **새 보드의 기본값이 곧 이 테마**라 첫인상은 그대로면서 이제 바꿀 수 있다(`BOARD_THEME_KEY` — 맵의 코랄과 갈리는 유일한 지점). `docThemeOf`는 지워지고 소비처 4곳(에디터 캔버스·미니맵·커튼, 홈 내보내기 3종)이 `themeOf(doc.themeKey)`를 직접 쓴다. 곁가지로 **스타일 메뉴의 스와치를 "그 테마의 캔버스 면 + 강조색 점"**으로 바꿨다 — 강조색 원만 그리면 화이트·모노·다크처럼 면이 정체인 테마가 구별되지 않는다(홈 설정 모달과 같은 문법). 홈 썸네일이 쓰는 **복사본 표**(`mapPreview`의 `THEME_PAL`)에도 함께 추가하고, 한쪽에만 추가하면 그 테마의 카드가 조용히 코랄로 그려지므로 **드리프트 가드 테스트**를 세웠다. **주의**: 이미 만들어 둔 보드는 저장된 테마(대개 코랄)를 그대로 쓰므로 이제 그 색이 보인다 — 스타일 메뉴에서 화이트를 한 번 고르면 된다. ③ **폰의 줌·미니맵 묶음을 우측 하단으로 되돌리고**(직전 배포에서 우측 상단으로 올렸던 것 — 원래 자리가 낫다는 판단), 겹침은 **도구 막대가 비켜서** 푼다(요청): 폰에서 막대가 좌측 하단(`left: 12`)에 붙고 도구 행(선택·펜·지우개)과 동작 행(메모·이미지·실행취소·다시실행)이 갈리며, 폭을 `calc(100vw - 172px)`로 잡아 묶음 자리를 비운다(펜 옵션도 색 줄·굵기 줄로 나뉜다 — wrap에 맡기면 굵기 하나가 색 줄 끝에 매달린다). 폰 화이트보드의 관례이기도 하다(도구는 왼쪽, 보기 컨트롤은 오른쪽). 데스크톱은 하단 중앙 그대로. 웹 +4 테스트(보드 테마 적용·저장 / 맵 화이트 / 묶음 하단+막대 좌측·폭·2행 / 썸네일 미러 파리티, 첫 번째는 수리 전 실패 확인, 전체 1237), 실브라우저 13종(새 보드 흰 배경·스와치 7종 면색 구별·테마 변경 반영·저장·맵 화이트 / 390px 묶음 우측 하단·무겹침·화면 안·펜 펼침·미니맵 꺼도 무겹침·버튼 동작 / 맵 무회귀)
- ✅ **모바일 화이트보드 도구 막대 재배치 + 펜 메뉴 전환(시안 제공)** 첨부 시안대로 폰 배치를 셋으로 갈랐다: **도구 막대는 바닥 전폭 한 줄**(선택·펜·지우개·메모·이미지 — `left/right: 12`, 44px 타깃), **되돌리기·다시실행은 막대에서 떼어 자기 알약**으로 그 위 띠 왼쪽(도구가 아니라 "방금 한 일"을 다루므로 성격이 다르다), **줌·미니맵 묶음은 같은 띠 오른쪽**(우측 하단을 지키되 막대 높이만큼 위로 — `BOARD_BAR_LIFT` 상수 하나를 BoardToolbar가 내주고 ZoomControls가 쓴다. 좌우로 비켜서던 직전 판은 막대 폭을 깎아 먹었다). **펜을 누르면 막대가 메뉴를 전환한다**(요청) — 도구 목록 자리에 `[‹ 뒤로][색 5][굵기 3]`이 들어서고 행은 그대로 하나다(쌓으면 좁은 화면에서 바닥이 두꺼워진다). `‹`는 **메뉴 전환이지 도구 변경이 아니라** 돌아가도 펜은 켜져 있고(버튼이 활성 표시), 다른 도구로 가면 패널은 저절로 닫힌다(`penPanel` 로컬 상태 + `tool !== 'pen'` effect). 데스크톱은 한 줄에 다 들어가므로 기존 배치 그대로(도구·삽입·되돌리기 + 펜 옵션 행). 웹 +1/갱신 2 테스트(전폭·알약·묶음 lift·막대 구성 / 펜 전환·‹ 복귀·펜 유지, 전체 1238), 실브라우저 12종(390px 기하 실측 — 막대 12~378·알약 12~117·묶음 240~374, 세 상자 무겹침, 터치 44px, 펜 전환 후에도 높이 56 불변·그리기 동작, ‹ 복귀 시 펜 활성, 메모 추가·되돌리기 동작)
- ✅ **화이트보드 도구 막대 후속 3건(요청)** ① **도구와 삽입 사이 구분선 + 정렬** — 폰 막대의 다섯 아이콘이 한 덩어리로 보였다. `[선택·펜·지우개] | [메모·이미지]` 사이에 구분선을 넣고 간격을 `space-evenly`로 바꿔 **양 끝 여백까지 균일**하게(실측 19px 여섯 곳 동일). 구분선의 좌우 마진은 폰에서 0으로 — `space-evenly`가 이미 간격을 정하는데 마진을 더하면 그 자리만 21px로 벌어져 열이 어긋나 보인다(실측). ② **메뉴 전환 애니메이션**(사용자 제안 그대로) — 펜을 열면 펜 메뉴가 **오른쪽에서 들어오고** 도구 목록은 왼쪽으로 나가며, `‹`면 반대. "밀어내기"가 보이려면 **나가는 층을 잠깐 더 그려야** 하므로 막대 안을 두 층으로 겹치고(높이 고정 = 버튼 높이, `overflow: hidden`) `leaving` 상태가 200ms 뒤 스스로 정리한다. 키프레임 4종은 `editor.css`(`mf-board-in/out-right/left`)에 두고 길이는 `PANEL_ANIM_MS`와 같은 값, `prefers-reduced-motion`이면 사실상 즉시. ③ **폰의 미니맵 아래 버튼 줄 제거**(최소화·화면 맞춤 — 마인드맵·화이트보드 모두) — 좁은 화면에서는 지도만 남기는 편이 깔끔하고 그 자리는 이제 도구 막대·독칩이 쓴다(확대/축소는 원래 핀치, 화면 맞춤은 열 때 자동으로 한 번 돈다). 버튼이 없으면 미니맵을 껐을 때 껍데기만 남으므로 폰에서는 **아예 그리지 않는다**(`showMinimap`은 기본 켬). 데스크톱은 네 버튼 + 배율 그대로. 웹 테스트 갱신(구분선 구성·간격 규칙·전환 방향 4종·폰 묶음에 버튼 없음, 전체 1238 — 모바일 테스트의 클러스터 앵커를 `화면 맞춤` 버튼에서 `[data-zoom-cluster]`로 교체), 실브라우저 10종(390px 간격 실측 19/19·전환 중 두 층의 transform 실측·전환 후 한 층으로 정리·‹ 반대 방향·펜 유지·폰 맵/보드 묶음 버튼 0·데스크톱 무회귀)
- ✅ **그리기 획을 언제나 객체 위로(제보)** M4에서 획을 "보드 바닥의 잉크"로 보고 메모/이미지 **아래**에 깔았는데, 실제로 쓰면 메모 위에 그은 획이 그 메모 뒤로 숨어 **방금 그은 것이 사라진 것처럼** 보였다(종이에 펜으로 덧그리는 감각과도 반대). 잉크는 항상 맨 위로 올린다. 소비처 셋을 같은 순서로: 에디터 렌더(`Viewport` — StrokeLayer를 FloatLayer 뒤로 옮기고 **`zIndex: 90`**을 못박는다. DOM 순서만으로는 부족한 이유는 객체 쪽에 z-index를 쓰는 요소가 있어서다 — 노드 40·라인 25~28·메모 10/20·배지 81. 편집 박스(100)와 끌어 올린 노드(200)만 잉크 위로 남긴다: **지금 만지는 것은 보여야 한다**), 내보내기(`paintScene`의 획 그리기를 맨 끝으로 — PNG·SVG·PDF 셋이 같은 순회를 쓰므로 한 줄 이동으로 셋 다), 홈 썸네일(`mapPreview`의 `<path>`를 SVG 자식 맨 뒤로). 웹 +2 테스트(화면 z-index·DOM 순서 / SVG 문자열에서 잉크가 메모 뒤에 온다 — **둘 다 수리 전 실패 확인**), 실브라우저 3종(메모 박스 안 빨간 픽셀 0 → 1,177 · 계약 실측 `inkZ 90 > memoZ 10` · PNG 내보내기에 잉크 4,979px + 메모 31,267px 공존)
- ✅ **홈 폴더 2건(요청)** ① **내용이 있어도 폴더를 삭제할 수 있다** — 예전엔 "직접 담긴 맵 0 + 하위 폴더 0"일 때만 열렸다(막은 이유: 지우면 그 아래가 고아가 된다). 판단을 뒤집은 근거는 **폴더가 그릇이 아니라 이름표**라는 것 — `mapFolders`의 배정 하나일 뿐이라 이름표를 떼는 일이 내용을 지울 이유가 되지 않는다. 그래서 삭제는 **비파괴**다: 직접 담긴 맵과 직속 하위 폴더가 **한 단계 위**(부모 폴더, 없으면 스페이스 최상위)로 올라온다(`confirmDeleteFolderYes`가 `mapFolders` 재배정 + 하위 폴더 `parent` 승격). 휴지통으로 보내지 않은 이유: 휴지통은 맵만 담고 폴더 복원 개념이 없어, 함께 버리면 되돌릴 때 폴더를 새로 만들어 다시 채워야 한다. 확인창이 **무엇이 몇 개 어디로 가는지** 문장으로 밝힌다(`folderDeleteSummary` — "안에 있는 맵 2개와 하위 폴더 1개는 삭제되지 않고 '작업공간'(으)로 옮겨져요"). `canDelete`/비활성 사유는 통째로 제거. ② **상위로도 드래그앤드롭**(사용자 제안 채택) — 아래로는 폴더 카드에 끌어다 놓으면 되는데 위로 꺼내려면 우클릭 "폴더에서 꺼내기"뿐이라 방향에 따라 도구가 달랐다. 툴바 뒤로가기 버튼을 없애고 그 자리를 **그리드 첫 칸의 "상위 폴더" 타일**(`ParentFolderCard`, 파일 탐색기의 `..`)에 넘겼다: 점선 테두리 + 가라앉은 면으로 "내용이 아니라 길"임을 알리고, 부제로 **올라가면 닿는 곳의 이름**(상위 폴더명, 최상위면 스페이스명)을 말한다. 드롭 대상이자(`moveMapUp` — 현재 폴더의 부모로) 클릭 한 번이면 그리로 올라간다(폴더·맵 카드의 "한 번=선택 / 두 번=열기"와 다른 이유: 이 타일은 고를 대상이 아니다 — 선택도 메뉴도 속성도 없고, 대신하는 것이 뒤로가기 버튼이다). 드롭 하이라이트 키는 실제 폴더 id와 섞이지 않게 `PARENT_DROP_ID` 하나를 예약. 폴더 구획은 하위 폴더가 없어도 이 타일 때문에 열린다. 웹 +3 테스트(뷰모델 타일 계층 / 내용 있는 폴더 삭제→내용 승격 / 타일 드롭→한 단계 위, 뒤로가기 기대값 3곳 갱신, 전체 1242), 실브라우저 9종(타일이 첫 칸·이름·뒤로가기 버튼 부재·하위에서 상위명·드롭 후 저장본 `ddeep: fa`·클릭 이동·삭제 메뉴 활성·확인창 문구·삭제 후 폴더/맵 승격) + 모바일 390px·다크 스크린샷
- ✅ **상위 폴더 타일도 두 번 클릭으로(제보)** 폴더를 더블클릭해 들어가는 순간 **두 번째 클릭이 그 자리에 새로 그려진 "상위 폴더" 타일에 떨어져** 글자가 통째로 선택된 것처럼 보였다. 타일만 "한 번 = 올라가기"였던 것이 원인이자, 같은 그리드 안에서 카드마다 클릭의 뜻이 다르다는 문제이기도 했다 → **폴더 카드와 같은 `useCardActivation`**(한 번=아무 일 없음 / 두 번=올라가기, 모바일은 더블탭). 이 훅의 **문지기**(#317 — 자기가 직전 클릭도 받았는지 세어 두고 아니면 dblclick을 무시)가 그대로 붙으므로, 폴더 진입에 딸려 온 dblclick으로 **곧바로 한 계층 더 올라가는 일도 없다**. 곁들여 선택 자국을 아예 없앴다: 타일에 `user-select: none` + `.map-card` 전체에도 같은 규칙(`home.css`) — 카드는 누르는 대상이지 읽을 글이 아니라, 열기(두 번 클릭)가 제목을 파랗게 남길 이유가 없다. 웹 +1 테스트(제보 순서 그대로: 폴더 더블클릭 진입 → 타일 존재 → 한 번 클릭 무반응 → 두 번 클릭 상승. **수리 전 실패 확인**), 기존 타일 이동 단정 4곳을 더블클릭으로 갱신(전체 1243), 실브라우저 6종(진입 후 `getSelection()` 빈 문자열·`user-select: none` 실측, 한 번/두 번 클릭, 드롭 무회귀)
- ✅ **폰 화이트보드: 두 손가락 = 화면 이동(요청)** 펜/지우개를 켜면 화면을 옮길 방법이 없었다 — 도구를 '선택'으로 되돌려야 했다(M4의 v1 규칙: 두 번째 손가락은 획만 버렸다). 규칙을 **한 손가락 = 그리기·지우기 / 두 손가락 = 화면 이동·확대**로 정리. 두 겹: ① 컨트롤러에 제스처 API(`twoFingerStart/Move/End`)를 뽑고, 배경 핸들러의 핀치 코드를 그리로 옮기면서 **중점 이동을 더했다** — 예전에는 거리만 봐서 **두 손가락을 함께 끌면 아무 일도 일어나지 않았다**(맵·보드 공통이었다). 매 이동을 제스처 **시작 상태 기준으로 다시** 계산해 누적 오차가 없다. ② 그리기 오버레이(`BoardDrawLayer`)가 포인터를 전부 가져가 아래 배경 핸들러가 볼 수 없으므로, 두 번째 손가락이 닿으면 진행 중인 획을 버리고 **같은 API로 직접 넘긴다**(배경과 같은 함수라 감각이 같다). 손가락이 하나 남아도 **마지막 하나가 떨어질 때까지 그리기를 재개하지 않는다**(화면을 옮기다 낙서가 생기면 안 된다 — `gesturing` 플래그). 웹 +1 테스트(펜 켠 채 두 손가락 순수 평행이동 → 팬 레이어 transform 변화·배율 불변·저장본 획 0, **수리 전 실패 확인**: 캔버스가 꿈쩍도 안 했다), 실브라우저 CDP 실제 터치 7종(한 손가락 그리기 / 두 손가락 이동·획 0 / 벌리기 확대 1.2→2.4 / 손가락 하나 남아도 이어 그리지 않음 / 지우개도 동일 / 선택 도구 한 손가락 팬 무회귀)
- ✅ **보드 템플릿 3종(요청 — 화이트보드 다음 기능 ①)** 화이트보드는 열면 늘 빈 종이라 "무엇을 할 수 있는지"를 스스로 알아내야 했다. 맵 템플릿과 **같은 설계**로 얹는다 — **템플릿은 그냥 `Doc` 하나**라, 갤러리 썸네일(`realPreview`)·저장·협업·undo·내보내기가 전부 기존 새 문서 경로 그대로다. 새로 짠 것은 `BOARD_TEMPLATES` 데이터와 `buildTemplateDoc`의 분기뿐이고, 주소에는 `tpl=board-…`만 실린 채 **문서는 에디터가 시드한다**(홈이 조립해 저장하면 저장 경로가 둘이 된다). **어휘를 보드가 실제로 만들 수 있는 것으로 제한**했다(메모·이미지·잉크) — 사용자가 지웠다가 다시 만들 수 없는 물건(영역·연결선)을 템플릿만 슬쩍 심어 두면 "이건 어떻게 되살리지?"가 된다. 열 제목도 그냥 **색 있는 굵은 메모**(`rich: [{t, b:true}]` — 런 글자 = `text` 모델 계약, 테스트로 고정). 3종: **회고 (KPT)**(Keep/Problem/Try 3열 + 예시·빈 스티커), **칸반 보드**(할 일/진행 중/완료), **아이디어 스티커**(넓은 질문 카드 + 3×2 스티커 격자 — 맵 템플릿에 이미 '브레인스토밍'이 있어 갤러리 한 화면에 같은 이름이 둘이 되지 않게 하는 일로 부른다). 좌표는 원점을 가운데 두는 격자(`COL_X`/`HEAD_Y`/`CARD_Y`, 카드 260×96)라 첫 센터링이 전체를 화면 한가운데 놓고, **"어떤 두 메모도 겹치지 않는다"를 테스트가 지킨다**(열자마자 카드가 포개져 보이면 첫인상이 나쁘다 — 맵 템플릿의 "메모는 트리가 오지 않는 x<0에만" 계약과 같은 계열). 빈 스티커는 앱이 새 메모를 만들 때와 같은 `text: ''`라 "메모 입력…" 자리표시자가 그대로 뜬다. 갤러리는 화이트보드 구획의 빈 보드 뒤에 나란히(아이콘 3종 추가 — 순환 화살표·세 열·스티커 두 장, 이모지가 아니라 SVG인 이유는 기존과 같다), 미리보기 워밍업에도 합류. 웹 +8 테스트(템플릿 순수 6 — id 유일·board 문서 형태·직렬화 왕복·rich 계약·무겹침·id 모양 / 갤러리 1 / 에디터 씨앗 1, 전체 1254), 실브라우저 15종(갤러리 3종 노출·썸네일 실렌더·모달 무스크롤, 회고 선택 → `tpl=board-retro`·메모 9·주제 0·화면 겹침 0·전체 화면 안·제목 3개 굵게(800), Ctrl+S 저장본 `kind: board`·선/영역 0·rich 굵게 런, 홈 카드 배지+썸네일, 칸반·아이디어 스티커 열기, 390px 가로 스크롤 없음)
- ✅ **하이라이터 + 획 선택·이동(요청 — 화이트보드 다음 기능 ②)** 그리기가 "긋고 지우는" 것뿐이라, 강조할 방법도 방금 그은 것을 **고칠** 방법도 없었다(지우고 다시 긋는 수밖에). 두 가지를 더한다. ① **형광펜**: 모델은 플래그 하나 — `Stroke.hl?`(**true일 때만** 직렬화, `kind`·`Float.caption`과 같은 규칙이라 옛 저장본·골든 무변경. CRDT는 필드 제네릭 경로라 배선 불요). 값은 "무엇으로 그렸는가"만 말하고 **표현은 소비처가 정한다** — 반투명(0.42) + **곱하기 합성**(알파만 낮추면 흰 바탕에선 그럴듯해도 색 있는 메모 위에서 뿌옇게 뜬다: 형광펜은 밑을 가리는 게 아니라 걸러 낸다). 소비처 넷이 같은 상수(`boardTools.ts`의 `HL_OPACITY`)를 쓴다: 에디터 SVG(`mix-blend-mode`)·PNG(`globalCompositeOperation`)·SVG 내보내기(`style="mix-blend-mode"`)·홈 썸네일. 팔레트·심 굵기는 펜과 **따로**(형광 5색 / 12·20·30px) 기억한다 — 형광펜을 한 번 쓴 뒤 펜이 노란 20px이 되면 안 된다. 단축키 **H**, 도구 막대는 색·굵기 UI를 두 도구가 공유(폰은 기존 메뉴 전환 그대로). ② **획 선택·이동**: `SelectionKind`에 `'stroke'` 추가 — 획은 **자기 DOM 요소가 없으므로**(한 장의 SVG에 그려진 path들이고 포인터를 받지 않는다: 잉크가 객체 위에 있어도 클릭은 통과해야 한다) 배경이 받은 포인터를 **좌표로 히트테스트**해 가른다(`strokeAt` — 지우개와 같은 허용치 규칙, 나중에 그린 것 우선). **우선순위는 객체가 먼저, 획이 마지막**이다: 시각 순서(잉크가 위)를 그대로 따르면 하이라이터로 덮은 메모가 통째로 안 잡힌다 — 잉크는 대개 빈 자리에 있어 마지막 순서로도 집힌다. 이동은 `stroke-move` 드래그가 **원본 좌표 기준으로 매번 다시** 계산(코어 신설 `translateStrokePts` — 0.1 반올림이 쌓여 뭉개지지 않는다, continuous 커밋이라 undo 한 단계). 표시는 점선 상자(획에는 손잡이가 없다), 속성 패널 `StrokePanel`(그 획을 그린 도구의 팔레트·굵기 + 삭제), Delete·우클릭 삭제, 폰 선택 바는 **속성·삭제만**(글자가 없어 편집이 없고 메뉴도 삭제뿐 — 눌러도 아무 일 없는 버튼을 두지 않는다). **범위 밖(후속 후보)**: 마퀴·Ctrl+A는 획을 담지 않는다(여러 획을 한 번에 옮기려면 그 경로가 필요 — MultiSelection 확장 건), 획에는 댓글이 붙지 않는다(목록에서 가리킬 이름이 없다 — 보기 전용 우클릭도 그래서 열지 않는다). 코어 +7 / 웹 +5 테스트(전체 245 + 1259), 실브라우저 22종: H 단축키·형광 팔레트·메모 위 반투명 곱하기·펜 설정 보존·클릭 선택·드래그 이동(메모 불변)·저장본·undo 원위치·패널 색 변경·Delete·폰 옵션 전환 / 내보내기 파리티(SVG `mix-blend-mode:multiply`+`opacity=0.42`·PNG 픽셀에서 노란 띠 아래 글자 생존·홈 썸네일 동일)
- ✅ **스티커 반응·점 투표(요청 — 화이트보드 다음 기능 ③)** 회고를 화이트보드에서 하면 마지막에 "그래서 뭘 먼저 할까"가 남는데, 표를 던질 방법이 없어 메모에 손으로 별을 그려야 했다. **설계의 뿌리는 CRDT 함정 하나**다: Yjs 바인딩은 필드 값을 **통째로** 넣으므로(중첩 Y.Map이 아니다) 표를 `{ [이메일]: n }` 같은 한 값에 담으면 끊긴 채 두 사람이 누를 때 한쪽 표가 **통째로 사라진다**(#332). 그래서 **한 사람의 한 표 = 항목 하나**(`Reaction { id, target, by, byName?, emoji }`) — 획(`Stroke`)과 같은 원자 값 설계라 추가·삭제가 각각 병합된다. 이 주장을 말이 아니라 **실제 Y.Doc 분기 테스트**로 고정했다(갈라진 두 피어가 같은 스티커에 각자 투표 → 재연결 후 양쪽 모두 2표). `Doc.reactions?`는 비어 있지 않을 때만 직렬화·전파(`kind`·`strokes`와 같은 규칙 — 옛 저장본·골든 무변경). 코어 `reactions.ts`(순수): `reactionGroups`(이모지별 개수·내 표 여부·이름, **첫 등장 순** — 개수로 정렬하면 한 표에 칩이 자리를 바꾼다. 투표 점만 맨 앞)·`toggleReaction`·`pruneReactions`. **점 투표와 이모지 반응은 같은 모델**이고 약속된 한 글자(`VOTE_EMOJI` = ●)만 렌더가 다르다. 웹: 메모 카드 **아래 바깥**(top:100%)에 칩 줄(`ReactionRow`) — 카드 안에 넣으면 표 하나에 박스 측정·겹침·핸들 기하가 흔들린다(캡션과 같은 이유). 칩은 토글이고 **내 표는 강조색 테두리·연한 배경**, 툴팁은 누른 사람 이름. 선택했을 때만 (+)가 떠 5종(●·👍·❤️·😂·❓)을 고르고, 폰에서는 칩이 곧 터치 타깃이라 30px(실측 37.5). 정체성은 로그인 이메일(없으면 이 기기 표시자) — 데모에서도 자기 표를 되돌릴 수 있다. **함정 하나**: `commitDoc`의 "바뀌었나" 판정과 undo 스냅샷·자동저장 서명이 `strokes`까지만 보고 있어 **반응만 바뀐 커밋이 통째로 버려졌다**(칩이 안 생김 — 실브라우저 이전에 테스트가 잡았다). 네 곳에 `reactions`를 배선(판정·record·amend·서명 + undo 복원). 메모를 지우면 그 표도 함께 정리하되 **삭제 커밋에서만** 돌린다(모든 커밋에서 돌리면 상대가 방금 만든 메모보다 그 반응이 먼저 도착한 순간에 남의 표가 사라진다). **범위 밖**: 맵(마인드맵)에는 붙이지 않는다(회고 스티커의 어휘), 내보내기(PNG·SVG·PDF)·홈 썸네일에는 그리지 않는다(댓글 배지와 같은 판단 — 진행 중 논의의 표시지 문서 내용이 아니다), 보기 전용은 표를 던질 수 없다. 코어 +5 / 웹 +4 테스트(전체 250 + 1263), 실브라우저 11종(선택 전 무노출·투표 점·이모지 추가·저장본 항목 2개·토글·undo 복원·다른 메모와 분리·삭제 시 정리·**2탭 실시간 도착과 합쳐짐**·폰 칩 크기)
- ✅ **화이트보드 후속 3건(제보)** ① **반응 칩이 너무 강렬** — 내 표 표시가 강조색 **불투명 테두리 + 12% 배경**이라 메모 옆에서 튀었다. 칩은 곁다리 정보지 강조 대상이 아니므로 테두리를 강조색 40% 반투명으로 낮추고 배경은 6%로(거의 비치지 않을 만큼) 줄여 **개수 글자로 읽히게** 뒀다. ② **Shift+Enter가 두 줄** — 한글로 쓰다 누르면 한 칸이 아니라 두 칸이 내려갔다. 우리 처리는 이미 한 경로(`doBreak`)뿐이라 코드만 보면 두 번 불릴 데가 없었는데, **같은 물리적 Enter가 두 번 도착**하는 것이 원인이었다: 조합 중 keydown은 기본 동작을 막고 의도만 기억했다가 `compositionend`에서 줄을 바꾸는데(#303), 브라우저·IME 조합에 따라 조합이 끝난 **뒤** 평범한 keydown(또는 `beforeinput: insertLineBreak`)이 한 번 더 온다. 수리: 브라우저가 주는 줄바꿈 신호를 전부 `requestBreak` 한 문으로 모으고, IME 확정으로 줄을 바꾼 **시각을 남겨** 80ms 안에 도착한 메아리를 무시한다(사람이 두 번 누르는 간격은 보통 100ms 이상이라 연속 줄바꿈은 그대로). 도형(노드)·메모 편집이 같은 기계를 쓰므로 양쪽에 함께 넣었다. ③ **칸반 템플릿 제거**(칸반은 별도 기능으로 만들 예정 — 열을 옮기는 일은 붙였다 떼는 스티커보다 규칙이 있는 보드가 맞다) → 자리에 **우선순위 정하기**(임팩트·노력 2×2) 신설: 세 열(회고)·격자(아이디어)와 모양이 겹치지 않고, 스티커를 옮겨 담는 것 자체가 결론이 되며 점 투표와도 맞물린다. 아이콘도 세 열 → 사분면. 웹 +2 테스트(IME 메아리 — **수리 전 `br` 3개(두 줄) 확인** / 칩 알파 계약), 실브라우저 9종(칩 배경·테두리 알파 실측, 평범한 Shift+Enter 3줄→4줄·IME 메아리 4줄→5줄 저장본 확인, 갤러리에서 칸반 부재·우선순위 존재·썸네일 실렌더, 보드 열기 메모 8개·겹침 0·화면 안)
- ✅ **화이트보드 연결선·프레임(요청)** M2에서 보드 UI를 트리밍하며 감췄던 **연결선(화살표)**과 **영역(프레임)**을 보드 어휘로 편입했다. 새로 만든 것은 거의 없다 — 모델(`Line`/`Zone`)·렌더·히트테스트·속성 패널, 그리고 끝점이 메모에 달라붙는 **마그넷 앵커**(`LineAnchor{kind:'float'}`)까지 전부 이미 있었고 **진입점만 막혀 있었다**(스티커끼리 화살표로 잇고 구획으로 묶는 건 화이트보드의 기본 어휘라 감춰 둘 이유가 없다). ① **진입점 2곳**: 하단 도구 막대의 삽입 묶음이 메모·이미지 → **메모·이미지·연결선·영역** 넷으로, 배경 우클릭 메뉴도 같은 넷(보드 라벨은 '연결선 추가'). **주제(트리 노드)만은 계속 내주지 않는다** — 보드에는 레이아웃할 트리가 없다. ② **폰은 삽입도 전환 메뉴**: 도구 넷 + 삽입 넷을 한 줄에 늘어놓으면 44px 버튼 여덟에 구분선까지 353px라 폰에서 쓸 수 있는 폭(~354px)에 여백이 0이 된다 → 도구 줄에는 삽입 진입(＋) 하나만 두고 누르면 `[‹ 뒤로][메모][이미지][연결선][영역]`이 밀려 들어온다(펜 메뉴와 같은 문법·같은 애니메이션. `penPanel` 불리언을 `panel: 'tools'|'draw'|'insert'`로 일반화). 고르면 **도구 목록으로 돌아온다** — 방금 만든 것을 바로 만진다. ③ **장면 바운즈·미니맵 일반화**(진짜 구멍은 여기였다): 첫 센터링·화면 맞춤이 보는 `sceneBounds`가 노드 ∪ 메모까지만 봐서 **프레임만 있는 보드는 커튼이 영영 안 걷혔고**, 미니맵도 "노드도 메모도 없다"며 통째로 사라졌다(화면에는 보이는데 지도에는 없다) → 둘 다 영역·연결선을 포함하고, 미니맵은 영역을 테두리 사각·연결선을 가는 선으로 그린다(앵커가 걸린 선은 저장 좌표가 아니라 `controller.resolveLine`이 **해석한** 끝점 — 에디터가 그리는 값과 같다). 맵에서도 트리 밖 영역·선이 화면 맞춤에서 빠지던 것이 함께 고쳐진다. 내보내기(PNG·SVG·PDF)는 `paintScene`이 원래 영역·선을 그리므로 무변경(프레임만 있는 보드로 실측 확인). **범위 밖**: 프레임은 여전히 **담는 그릇이 아니라 표식**이다(옮겨도 안의 스티커가 따라오지 않는다 — 맵에서도 같다). 웹 +3 테스트(도구 막대 연결선·영역 커밋+속성 패널 / 프레임만 있는 보드가 미니맵에 뜸 / 폰 삽입 메뉴 전환·고르면 복귀) + 기존 보드 트리밍 계약 2건 갱신(전체 1271), 실브라우저 14종(막대 구성·연결선 생성·속성 패널·**메모에 앵커 스냅**(`{kind:'float',id,side:'right'}`)·메모를 옮기면 선이 따라옴·영역 커밋·미니맵 영역/선·배경 우클릭 넷·프레임만 있는 보드가 화면 안에 열림·커튼 걷힘·폰 44px·전환·복귀) + 내보내기 3종(SVG에 프레임 사각·라벨·연결선 라벨, PNG 32KB)
- ✅ **화이트보드 후속 3건(제보·요청)** ① **영역(프레임)을 최상위로**(요청: 그리기가 최상위였는데 이제 영역이) — 다만 올리는 것은 **테두리·라벨뿐**이다. 통째로 올려 봤더니 7% 채움이 그 안의 노란 스티커를 물들여 색이 달라 보였다(실브라우저에서 확인하고 되돌림). 그래서 영역을 **두 판**으로 나눴다: 면 + 히트(z 8, 예전 자리)와 테두리·라벨(z 95, 잉크 90 위·편집 박스 100 아래, `pointer-events: none`). 히트를 위로 올리지 않은 이유는 **영역이 덮은 넓은 사각이 안의 메모·주제 클릭을 통째로 삼키기** 때문이다(빈 영역 클릭 = 영역 선택이라는 dc 원본 규칙도 그 자리에서 깨진다) — 라벨·핸들·삭제·댓글 배지 같은 자식만 `pointerEvents: 'auto'`로 되살린다(pointer-events는 상속된다). 소비처 셋을 같은 순서로: 에디터·내보내기(`paintScene` — 면은 맨 앞, 테두리·라벨은 맨 뒤)·홈 썸네일(`zoneFills`/`zoneEls`). ② **홈 카드의 화이트보드 폴백 삽화**(제보 스크린샷) — 본문을 아직 못 받았거나 내용이 정말 빈 보드는 폴백 삽화로 떨어지는데, 그 그림이 종류와 무관하게 **가지 뻗은 마인드맵**이라 "화이트보드" 배지가 붙은 카드에 마인드맵이 그려졌다. `miniBoardPreview` 신설(스티커 두 장 + 사진 + 잉크 한 줄 — 갤러리 '빈 화이트보드' 카드와 같은 어휘)하고 `cardSketch`가 `isBoardRaw(raw)`로 갈라 쓴다(본문이 아예 없으면 종류를 알 수 없고 배지도 안 뜨므로 그때는 예전 그대로). ③ **그리기 획 마퀴 선택**(요청: 단일·다중 모두) — `MultiSelection.strokes` 신설(**필수 필드**로 둔 이유: 옵셔널이면 새로 만드는 자리마다 조용히 빠져 "화면엔 선택됐는데 삭제가 안 되는" 유령 상태가 생긴다). 마퀴는 잉크의 점 하나라도 사각에 들면 담고(연결선과 같은 규칙), 하나만 걸리면 단일 선택으로 정규화한다. Ctrl+A·컨텍스트 메뉴 멤버십·그룹 드래그(고스트 = 잉크 bbox, 드롭에서 `translateStrokePts`로 한 번에 커밋)·일괄 삭제까지 다른 객체와 같은 경로. 속성 패널은 단일·다중이 **같은 패널**이고, 다중에서는 **색·굵기 중 어느 것도 활성 표시하지 않는다**(값이 섞여 있는데 하나를 켜 두면 "이미 그 색이다"라는 거짓말이 된다 — 누르면 전부에 적용). 세터도 `setStrokeColor(ids, hex)`/`setStrokeWidth(ids, w)`/`deleteStrokes(ids)`로 **한 번의 커밋**(id마다 커밋하면 undo를 고른 개수만큼 눌러야 한다). 웹 +4 테스트(영역 두 판 계약 / 마퀴 다중 선택·패널·삭제 / 하나면 단일 정규화 / 보드 폴백 삽화), 기존 영역 press 테스트 2건을 히트 판 기준으로 갱신(전체 1272), 실브라우저 13종(영역: 시각 z·포인터 통과·안의 메모 클릭·빈 자리 클릭 / 획: 마퀴 2개·다중 패널·활성 표시 0·그룹 고스트와 함께 이동·다중 삭제·단일 정규화·단일 삭제 / 홈 빈 보드 카드 삽화). **참고**: 영역 위에서 마퀴를 시작하면 예전처럼 영역 드래그가 된다(빈 영역 클릭 = 영역 선택) — 영역 밖에서 끌어와야 한다
- ✅ **방향키로 메모 사이 이동(요청·요구 오해 후 재작업)** 처음엔 "방향키로 메모를 이동"을 **좌표를 옮기는 미세 이동**(1px/10px)으로 만들었는데, 배포 후 확인에서 원하던 것은 **메모에서 메모로 선택을 옮기는 것**임이 밝혀져 그 PR을 통째로 revert하고 다시 만들었다(#421 → revert → 이 항목). 주제(트리)에는 이미 방향키 이웃 이동이 있었지만 메모는 방향키가 죽은 키였고, 트리가 없는 화이트보드에서는 방향키가 통째로 놀았다. 구현은 주제 이동과 **같은 채점 함수**(`nearestInDirection` — 박스 엣지 기준 거리 + 방향 콘 2단계)를 그대로 재사용한다: 메모 박스(측정 높이 `floatBoxH` 포함)를 `NavPoint`로 넘겨 이웃을 고르고, 신설 `selectAndRevealFloat`이 주제와 같은 80px 여백 규칙으로 화면 밖이면 뷰포트를 민다. **메모끼리만 오간다** — 맵에서 주제로 건너뛰지 않는다(주제는 주제끼리, 메모는 메모끼리라야 "누른 방향으로 한 칸"이 예측 가능하다). 트리가 없는 문서에서 **아무것도 고르지 않은 채** 방향키를 누르면 화면 중앙에서 가장 가까운 메모를 잡는다(보드에서 키보드로 시작하는 입구 — 그러지 않으면 여전히 죽은 키다). 문서는 바뀌지 않으므로 저장·undo·협업과 무관하다. 웹 +5 테스트(2×2 배치 4방향 순회 / 좌표 불변 / 이웃 없으면 유지 / 무선택 진입 / 맵 주제 무회귀 — 앞의 셋은 기능 제거 시 실패 확인), 실브라우저 12종(4방향 순회·좌표 불변·이웃 없음·화면 밖 메모로 갈 때 뷰포트 추종·무선택 진입·맵에서 주제↔메모 분리)
- ✅ **정렬·분배 + 격자 스냅(요청)** 여러 객체를 줄 맞추고 간격을 고르게 하는 도구. ① **계산은 순수 함수 한 곳**(`arrange.ts`) — 대상은 종류마다 좌표를 담는 방식이 다르므로(메모=좌상단+측정 높이, 자유 주제=중심+geom, 연결선=두 끝점, 획=점 배열) 컨트롤러가 전부 **좌상단 기준 박스**로 환산해 넘기고, 여기서는 "얼마나 옮길지"(dx,dy)만 돌려준다. 그래서 이 파일은 DOM도 문서 모델도 모른다. 정렬 기준은 **선택 전체를 감싸는 상자**(Figma·PowerPoint 규칙 — 무엇에 맞출지 따로 고르지 않아도 되고 결과가 예측 가능), 분배는 중심이 아니라 **가장자리 사이 간격**을 균등하게(크기가 제각각인 스티커에서 중심 균등은 큰 것 옆이 좁아 보인다). 정렬은 2개, 분배는 3개부터 — 모자라면 그 줄을 아예 그리지 않는다(눌러도 아무 일 없는 항목을 두지 않는다). ② **진입점은 다중 선택 우클릭 메뉴의 `정렬 ▸` 플라이아웃**(줄 맞춤 6 + 간격 균등 2). 속성 패널이 아니라 메뉴인 이유: 패널은 **한 종류만** 담겼을 때 뜨는데(`nodesOnly`/`floatsOnly`…) 정렬은 원래 종류를 가로지르는 동작이다. 텍스트 정렬 플라이아웃이 쓰던 `ctxSub`에 `kind`를 더해 둘이 같은 기계를 쓴다. **트리에 붙은 주제는 대상도 기준도 아니다** — 레이아웃이 자리를 정하므로 옮길 수 없다(그룹 드래그와 같은 규칙, `arrangeTargetCount`가 게이트). 한 번의 커밋이라 undo도 한 단계. ③ **격자 스냅**: 메모·이미지·영역을 끌거나 크기를 조절할 때 좌표를 10px 격자에 맞춘다. 화면의 도트 배경은 팬/줌과 무관한 **장식**이라(배경 레이어는 팬 레이어 밖) 그 점들이 문서 좌표의 격자가 아니다 — 격자는 우리가 정한 문서 좌표계의 값이다. **드래그 중 Alt를 누르면 그 순간만 꺼지고**(디자인 툴 관례의 탈출구), 보기/☰ 메뉴의 `격자에 맞추기`로 끄고 켠다(기본 켬). 설정은 문서가 아니라 **이 기기의 localStorage**(`mf_snap_grid`)에 남는다 — 협업 상대와 달라도 무방한 입력 보조다. 겹침 마그넷은 정렬·분배에 걸지 않는다(방금 줄 맞춘 것을 도형이 저 혼자 비켜나면 정렬이 아니게 된다). 웹 +12 테스트(순수 8 — 좌/우/가운데·세로·분배 균등·최소 개수·멱등 / 통합 4 — 메뉴→플라이아웃→위쪽 맞춤·간격 균등, 둘만 고르면 분배 줄 없음, 격자 스냅·토글, Alt 무시), 전체 1289 통과. 실브라우저 8종(메뉴 노출·플라이아웃 8줄·위쪽 맞춤 y 일치·x 불변·간격 105=105·격자 10 배수·Alt 무시·토글 저장). **범위 밖**: 연결선·그리기 획은 정렬 대상이지만 드래그 스냅은 아직 아니다(박스 경계가 모호), 객체끼리 붙는 스마트 가이드는 다음 후보
- ✅ **스마트 가이드(맞춤 안내선, 요청)** 메모·이미지·영역을 끌면 **이웃의 기준선**에 붙고 그 자리에 얇은 안내선이 뜬다. 계산은 정렬·분배와 같은 순수 파일(`arrange.ts`)에 얹었다 — `alignGuides(moving, others, tol)`가 끌고 있는 상자의 여섯 기준선(축마다 시작·중심·끝)을 다른 상자들의 같은 기준선과 견줘 허용치 안에서 **가장 가까운 하나**에 붙이고, 그 선에 걸린 상자들을 모아 안내선이 걸치는 구간(`from`~`to`)까지 돌려준다(축마다 따로 판단하므로 가로만·세로만 붙는 것도 자연스럽다). **안내선이 격자를 이긴다**: 격자는 "어딘가 반듯한 자리"지만 안내선은 "이것과 맞춘다"는 의도 그 자체다 — 안내선이 잡지 못한 축만 격자로 채워 둘이 서로 싸우지 않는다. 허용치는 **화면 8px을 줌으로 나눈** 캔버스 단위(확대해도 손끝 감각이 같아야 한다), 이웃 후보는 화면에 보이는 **면 있는 것들**(다른 메모·영역·주제 박스 — 트리 주제는 옮길 수 없지만 맞출 기준으로는 훌륭하다. 연결선·획은 상자 경계가 모호해 뺐다). 렌더는 팬 레이어 안의 `GuideLayer`(z 97 = 잉크 90 위·편집 박스 100 아래, `pointer-events: none`) — 굵기·점선 간격을 `1/zoom`으로 줘 **어느 배율에서나 화면 1px**이다(이 선은 객체가 아니라 눈금이라 확대할수록 굵어지면 방해가 된다. 실측 zoom 1.25에서 0.8 → 1.000px). 안내선은 **끄는 동안에만** 존재한다 — 드래그가 끝나거나 취소되면(`pointerup`/`pointercancel`) 곧바로 비운다(문서에는 남지 않는다). 토글은 새로 만들지 않고 기존 것을 넓혔다(`snapGrid` 하나가 둘을 함께 켜고 끈다 — 사용자에겐 "손이 자석처럼 붙는다"는 한 가지 감각이다): 보기/☰ 메뉴 라벨을 `안내선·격자에 맞추기`로 고치고, 드래그 중 **Alt**는 그대로 둘 다 끈다(단축키 도움말에도 한 줄 추가). 웹 +7 테스트(순수 5 — 왼쪽 끝 스냅과 안내선 구간·중심 동시·허용치 밖 무동작·같은 선의 이웃이 여럿이면 선이 길어짐·가장 가까운 선 선택 / 통합 2 — 안내선 렌더와 손 뗀 뒤 소멸·격자가 아니라 23에 붙음, 허용치 밖은 안내선 없이 격자로), 전체 1296. 실브라우저 9종(세로 안내선 위치·소멸·격자(30) 아닌 23 착지·중심 맞춤 시 십자 두 줄·중심 정확·Alt는 안내선도 스냅도 없음·확대 시 1px·영역 드래그도 동일·저장본). **범위 밖**: 자유 주제(node-move)와 그룹 드래그는 고스트로 끌고 드롭에서 커밋하는 구조라 아직 스냅 대상이 아니다(격자 스냅도 같은 이유로 빠져 있다 — 두 경로를 한 번에 손보는 별건), 크기 조절은 격자만
- ✅ **프레임을 그릇으로(요청)** 영역(프레임)은 지금까지 **표식**이었다 — 스티커를 감싸고 있어도 끌면 사각형만 혼자 움직였다(회고 보드에서 "Keep 열 전체를 옆으로"가 불가능). 이제 프레임을 끌면 **안에 든 것이 함께 온다**: 메모·이미지, 안에 든 프레임(중첩), 그리기 획, 앵커 없는 연결선, 자유 주제. **설계 변경(제안 → 구현 중 뒤집음)**: 처음엔 `Float.zone` 같은 **소속 필드**를 두려 했는데, 그 값을 정하는 것은 결국 놓는 순간의 기하다 — 두 방식이 갈리는 때는 나중에 프레임을 옮기거나 줄여 **저장된 소속과 화면이 어긋날 때**뿐이고, 그때는 화면이 이기는 편이 낫다(사용자가 읽는 규칙은 "이 사각형 안에 있는 것" 하나다). 그래서 소속은 **기하로 판정**한다(`frames.ts` — 중심이 사각형 안이면 소속, 겹친 프레임은 안쪽 것이 강조 대상). 덤으로 모델·직렬화·CRDT·마이그레이션이 **하나도 늘지 않고**, 소속 필드를 빠뜨린 경로가 만드는 "화면엔 안에 있는데 따라오지 않는" 유령 상태도 없다. **소속은 잡는 순간 스냅샷**(`CarrySnapshot`) — 끄는 동안 다시 판정하면 프레임이 지나가는 자리의 객체를 주워 담았다 흘렸다 한다. 이동은 종류별 평행이동 변형에 맡기고(신설 `translateZonesBy` 포함) **원본 좌표 기준으로 매번 다시** 계산한다(획·그룹 드래그와 같은 규칙), continuous 커밋이라 **undo 한 번**에 프레임과 짐이 함께 돌아온다. 맞춤(안내선·격자)은 프레임 상자로 계산하되 **담고 가는 것들을 기준에서 뺀다**(자기 짐에 붙지 않게) — 안내선이 자리를 조정하면 짐도 같은 델타로 따라간다. **넣기·빼기는 놓는 자리가 정한다**(별도 조작 없음): 메모·획을 끄는 동안 담길 프레임의 테두리가 실선+글로우로 강조돼(`data-frame-drop`, 폴더 드롭과 같은 어포던스) 무엇이 함께 움직이게 될지 미리 보이고, 밖으로 빼면 그대로 빠진다. **앵커 걸린 연결선은 담지 않는다**(붙은 객체를 따라 저절로 움직인다 — 담으면 두 번 움직인다), **트리에 붙은 주제도 아니다**(레이아웃이 자리를 정한다). **삭제는 비파괴** — 프레임만 사라지고 내용은 남는다(기존 동작 유지, 회귀 테스트로 고정). 웹 +7 테스트(순수 3 + 통합 4 — 함께 이동·undo 한 번 / 밖으로 뺀 것은 안 따라옴 / 드롭 강조와 해제 / 삭제 비파괴, 통합 2건은 구현 전 실패 확인), 전체 1303. 실브라우저 11종(메모 둘·잉크가 같은 델타로 이동·다른 칸 불변·undo·드롭 강조와 해제·옮긴 뒤 새 프레임을 따라감·밖으로 뺀 것은 안 따라옴·삭제 비파괴). **범위 밖**: 프레임을 줄여도 밖으로 나간 것이 자동 탈퇴하지 않는다(판정이 드래그 시작 시점이라 다음에 끌 때 반영된다), 프레임 복제 시 내용 복제, 프레임 크기 자동 맞춤
- ✅ **복사/붙여넣기를 모든 객체로 + 복제 단축키(요청)** 요청을 받고 먼저 **무엇이 실제로 안 되는지** 실브라우저로 쟀다 — Ctrl+C/X/V는 이미 붙어 있었고, 빠진 것은 넷이었다: ① **그리기 획이 통째로 빠져 있었다**(단일 선택도, Ctrl+A 다중도 — 실측: Ctrl+A→C→V에서 메모는 늘고 획은 그대로) ② `selection.kind === 'stroke'`가 `else`로 흘러 **영역 id로 잘못 모이던** 잠복 버그 ③ **프레임을 복사하면 빈 사각형만** 나왔다(끌면 안의 것이 함께 오는데 — #425와 어긋난다) ④ **프레임 안에 그은 획은 클릭으로 고를 수조차 없었다**(영역의 넓은 히트 판이 pointerdown을 먼저 삼킨다 — 그러니 복사할 방법도 없다). 수리: 클립보드 페이로드에 `strokes` 추가(collect/count/origin/paste — 좌표가 곧 내용이라 `translateStrokePts`로 통째 평행이동, 기준점은 굵기 절반을 포함한 **보이는 경계**), 다중 선택 총계에 획 포함, 프레임 복사는 **이동과 같은 함수**(`idsInFrame`)로 안의 메모·획·앵커 없는 선을 함께 담는다(중첩 프레임은 담지 않는다 — 한 단계면 충분하고 재귀는 무엇이 복사됐는지 눈으로 예측되지 않는다). 소속 판정에 쓰는 높이는 **드래그와 같은 측정값**(`floatBoxH` 주입)이라 "끌면 오는데 복사하면 빠지는" 어긋남이 없다. 획 클릭은 `beginZoneDrag`가 획 위에서 **전파를 막지 않고 물러나** 배경 핸들러(획의 주인)에게 넘기고, 우클릭 히트테스트도 **획을 영역보다 먼저** 본다(영역은 배경 판 z 8, 잉크는 그 위 z 90 — 보이는 순서가 곧 우선순위). ⑤ **복제(Ctrl/⌘+D)** 신설 — 클립보드를 건드리지 않고 그 자리에서 24px 어긋난 사본(디자인 툴 관례, 복사해 둔 다른 것을 잃지 않는다). 붙여넣기 대상에 `{kind:'offset'}`을 더하고 `applyPaste`를 뽑아 붙여넣기·복제가 같은 몸통(선택·마그넷·리플로우)을 쓴다. **트리에 붙은 주제를 복제하면 형제가 된다**(같은 부모의 자식으로 — 제자리 복제는 자유 도형으로 떨어져 나가는 셈이라 마인드맵에서 뜻이 어긋난다). 컨텍스트 메뉴에도 `복제`를 넣고(모바일에는 키보드가 없다) 획 메뉴에 복사·잘라내기·복제·붙여넣기를 추가, 도움말에 Ctrl+X·Ctrl+D 줄과 대상 문구(`주제·메모·선·영역·그리기`) 갱신. 웹 +9 테스트(순수 4 + 통합 5 — 통합 4건은 수리 전 실패 확인), 전체 1312. 실브라우저 8종(프레임 안 획 클릭·획 C/V·프레임 복사에 내용 동반·Ctrl+D 24px·클립보드 불변·Ctrl+A에 획 포함·주제 C/V 무회귀·Ctrl+D 형제 편입)
- ✅ **macOS ⌘ 단축키 확인(요청)** "cmd+c·cmd+v로 동작하는지 확인" — 코드는 결백했다: 에디터 단축키가 수정 키를 보는 곳은 **전부 `e.metaKey || e.ctrlKey`**이고 ctrl만 보는 자리는 0건이다(grep). macOS 크롬으로 위장한 실브라우저(UA·`navigator.platform`)로 ⌘ 경로를 전수 확인 — ⌘C·⌘V(획 포함)·⌘X·⌘D·⌘Z·⌘⇧Z·⌘A·⌘F·⌘S, 그리고 브라우저가 실제로 쏘는 `paste` 이벤트 경로와 도움말의 ⌘ 표기까지 정상. 회귀 가드는 ⌘C/⌘V만 있었으므로(기존 테스트) **⌘X·⌘D 케이스를 추가**했다. **프로브 함정 기록**: "⌘S 저장"만 실패로 나왔는데 앱이 아니라 프로브가 틀렸다 — 로컬/데모 모드에서는 `mindflow_doc_<id>`가 곧 **저장소 자신**이라, 그 키를 지운 뒤 ⌘S를 누르면 `prevVersion`을 든 저장이 "행이 없다"로 실패한다(편집 후 ⌘S는 정상 기록됨을 따로 확인). 저장 여부를 확인하려면 키를 지우지 말고 **편집을 만든 뒤** 저장본을 비교해야 한다
- ✅ **프레임 후속 2건(요청)** ① **내용에 맞추기** — 프레임 우클릭 메뉴에서 담고 있는 것들을 감싸도록 크기를 맞춘다(여백 28, 최소 크기는 손잡이 드래그와 동일). **빈 프레임은 no-op**이다: 맞출 내용이 없는데 최소 크기로 오그라들면 방금 그려 둔 빈 칸이 사라진 것처럼 보인다. 프레임만 움직이고 **안의 것은 제자리**이며, 여백을 두므로 맞춘 뒤에도 소속(기하 판정)이 그대로 유지된다(실측: 맞춘 직후 프레임을 끌어도 안의 것이 함께 온다). ② **삭제를 두 갈래로** — `프레임만 삭제`(기존 동작 유지 = 비파괴, 내용은 제자리)와 `내용까지 삭제`(열 하나를 통째로 버릴 때). 예전엔 안의 스티커를 따로 골라 지워야 했다. 내용째 삭제는 자유 주제를 **서브트리째** 지우고(다중 삭제와 같은 규칙) 지워진 대상의 반응도 함께 정리하며(`dropOrphanReactions` — 삭제 커밋에서만 도는 기존 규칙), 한 커밋이라 **undo 한 번**에 전부 돌아온다. 모바일은 선택 바에 삭제가 있어 `프레임만 삭제`를 메뉴에서 빼되 `내용까지 삭제`는 남긴다(바에 없는 동작). 곁들여 소속 판정을 `frameMembers` **한 곳**으로 모아 옮기기·크기 맞춤·내용째 삭제 셋이 같은 것을 담는다(세 동작이 서로 다른 것을 담으면 그 자체가 버그다). 웹 +3 테스트(맞추기·빈 프레임 no-op·내용째 삭제+undo, 셋 다 구현 전 실패 확인) + 기존 영역 메뉴 계약 갱신, 전체 1316. 실브라우저 9종(메뉴 구성·여백 28 실측·메모 불변·맞춘 뒤에도 함께 이동·내용째 삭제·undo·프레임만 삭제 무회귀·빈 프레임 그대로)
- ↩️ **칸반 열 모드 프레임 롤백(#429 되돌림, 제보)** 배포 후 확인에서 "화이트보드 프레임에 칸반 템플릿이 들어간 형태"라는 지적 — 프레임 안에 칸반을 **넣는** 방식은 결국 화이트보드의 한 가지 사용법이지 칸반이 아니었다(열을 쓰려면 먼저 영역을 그리고 모드를 켜야 한다는 것부터 어긋난다). `Zone.stack`·`columns.ts`·열 템플릿·삽입 선·카드 수 배지를 전부 되돌리고, **마인드맵·화이트보드와 나란한 세 번째 문서 종류**로 다시 만든다(사용자 결정)
- ✅ **칸반 M1 — 세 번째 문서 종류(요청, 재설계)** 프레임 방식(#429)을 되돌리고 **마인드맵·화이트보드와 나란한 `kind: 'kanban'`**으로 다시 만들었다(형식·범위 모두 사용자 선정: 고정 레이아웃 / 카드는 텍스트 그대로 / 단계로 나누기). **모델(코어)**: `KanbanColumn{id,title,color?}` + `KanbanCard{id,col,pos,text,rich?,bg?}`, `Doc.columns`/`Doc.cards`는 **칸반일 때만** 직렬화(다른 종류의 저장본은 한 글자도 안 바뀐다), 읽을 때 **소속 열이 없는 카드는 버린다**(유령 방지). **순서는 `pos` 분수 인덱스** — 두 이웃의 중간값을 주고, 촘촘해지면(`needsRenumber`) 그 열만 다시 매긴다. 배열(`cardIds`)로 들지 않는 이유는 협업이다: 끊긴 채 두 사람이 카드를 옮기면 배열 필드는 한쪽이 통째로 사라진다(#332) — **갈라진 두 피어가 서로 다른 카드를 옮긴 뒤 재연결하면 둘 다 살아남는 것**을 실제 Y.Doc 테스트로 고정했다. CRDT는 기존 제네릭 엔티티 경로(`syncEntityList`/`readEntityList`) 재사용이라 배선 두 줄. **화면**: 캔버스가 아니라 **고정 레이아웃**(`KanbanBoard.tsx`) — 열이 가로로 늘어서고 화면 높이를 채우며, 열이 많으면 가로·카드가 많으면 열 안에서 세로 스크롤. 팬/줌·미니맵·그리기·좌표 개념이 없어 모바일에서도 그대로다(390px 가로 넘침 0 실측). 캔버스 전용 UI(삽입·스타일 메뉴, 검색 버튼, Viewport 계열)는 칸반에서 뜨지 않고, 좌상단 DocChip이 첫 열을 가리므로 보드가 그만큼 내려앉는다(`CHIP_CLEARANCE`). **동작(M1)**: 열 추가·이름 변경(제목 더블클릭)·삭제(안의 카드 동반), 카드 추가(＋ 카드 추가 → 곧바로 편집)·수정(더블클릭)·확정(Enter, Shift+Enter 줄바꿈)·**빈 카드는 확정 시 사라짐**, 열 머리의 카드 수. 저장·undo·공유·협업은 문서 기반이라 기존 경로 그대로. 홈: 갤러리 세 번째 구획 `칸반 보드`, 카드 배지·폴백 삽화(`miniKanbanPreview`)·흰 종이 바탕·info 테두리(화이트보드와 같은 규칙). **함정 하나**: `commitDoc`의 "바뀌었나" 판정이 `columns`/`cards`를 안 봐서 **칸반의 모든 편집이 조용히 버려졌다**(커밋은 도는데 prev를 돌려줌) — 통합 테스트가 잡았다. 코어 +12 / 웹 +7 테스트(전체 262 + 1322), 실브라우저 14종(갤러리 구획·생성·열·카드 CRUD·저장본·undo·리로드 영속·홈 배지·모바일). **다음(M2)**: 카드 드래그로 열 간·열 안 이동, (M3) 썸네일 실렌더·마크다운 내보내기·검색·댓글
- ✅ **칸반 M2 — 카드 드래그(열 간·열 안 이동)** 카드를 끌어 다른 열로 옮기거나 같은 열 안에서 순서를 바꾼다. 이동은 코어 `moveCard`(pos 분수 인덱스) 한 번의 커밋이라 **undo도 한 단계**. **판정은 순수 함수로 분리**(`kanbanDrag.ts` — DOM은 호출부가 사각형으로 재서 넘긴다): 열은 **가로 위치**로 고르고(빈 열의 아무 데나 놓아도 그 열에 들어간다), 열 안 자리는 카드의 **중심**을 넘었는지로 정하며(가장자리에 걸친 것만으로 순서가 바뀌면 손이 흔들린다 — 코어 `columnInsertIndex`와 같은 규칙), 끌고 있는 카드는 계산에서 빠진다(자기 자리 때문에 한 칸 밀리지 않게). **입력 관례는 기기별로**: 마우스는 4px 문턱(클릭·더블클릭 편집과 구분), 터치는 **320ms 길게 누르기**(그래야 평범한 손가락 스크롤이 산다 — 카드에 `touch-action: pan-y`, 누르는 동안 8px 넘게 움직이면 스크롤 의도로 보고 드래그를 포기). 화면: 손가락/커서를 따라오는 **고스트**(원본은 자리를 지키되 흐리게 — 어디서 떠났는지 보인다)와 **삽입 위치 선**, 둘 다 `position: fixed`라 보드 스크롤과 무관하다. 화면 밖 열·카드로도 끌고 갈 수 있게 가장자리에서 자동 스크롤(보드 가로 60px/14px, 열 세로 44px/12px). 웹 +9 테스트(순수 6 + 통합 3 — 통합 2건은 구현 전 실패 확인, jsdom엔 레이아웃이 없어 사각형을 심어 규칙을 검증), 전체 1332. 실브라우저 11종(고스트·선·열 간 이동·카드 수 갱신·열 안 순서·undo·짧은 클릭은 선택만·더블클릭 편집 무회귀·리로드 영속·터치 짧은 드래그는 스크롤·길게 누르면 드래그). **프로브 함정 재발 기록**: `addInitScript`가 리로드에도 재실행돼 "리로드 후 되돌아감"이 재현되는 것처럼 보였다 — 시드를 `if (!localStorage.getItem(...))`로 가드해야 한다(같은 함정 두 번째)
- ✅ **새 배포가 크롬에 안 뜨던 문제(제보)** "Vercel에는 배포됐는데 크롬에서 업데이트가 안 뜬다" — 제보자의 콘솔 값이 원인 범위를 좁혔다: `waiting: false / installing: false`(적용 대기 중인 새 SW가 **없다** = 적용이 아니라 **감지** 단계에서 멈춤)와 빌드 스탬프 `2026-08-13T14:54:33.734Z`(= 한 배포 전 판을 그대로 돌고 있었다). 서버는 결백했다 — `vercel.json`의 `/sw.js`는 `max-age=0, must-revalidate`이고 `/(.*)`→`/app.html` 리라이트는 실제 파일이 있는 `/sw.js`를 가로채지 않는다(실측). 구멍은 **확인 요청을 언제 보내는가**였다: ① **등록 직후에 확인하지 않았다** — 확인 계기가 화면 진입(`useUpdateGuard`)·5분 주기·복귀 신호(#302)뿐이라, 탭을 새로 연 뒤 한 화면에 머무르면 브라우저의 등록 시 소프트 업데이트에만 기대게 되는데 **이미 같은 SW가 등록돼 있으면 그 확인이 건너뛰어질 수 있다** → `onRegisteredSW`에서 `registration.update()`를 한 번 직접 부른다(첫 화면에서 바로 잡힌다). ② **랜딩(`/`)에는 확인하는 코드가 아예 없었다** — 랜딩은 정적 쌍둥이라 앱 번들(등록·주기·토스트)이 통째로 실행되지 않는데 **그 페이지 자체가 SW precache 대상**이다. 즉 랜딩만 열어 두면 배포가 나가도 옛 화면이 계속 뜨고 아무도 확인하지 않는다 → 인핸서(`landing-demo.js`)에 `getRegistration().then((reg) => reg && reg.update())` 한 줄(**요청 한 번뿐** — 읽고 있는 페이지를 저절로 리로드하지는 않는다. 등록이 없는 첫 방문은 조용히 끝난다). ③ **다른 탭 때문에 미뤄지면 아무 표시가 없었다** — `safe`/`hidden`이라 자동 적용하려다 편집 중인 피어가 있으면(#286의 `anyPeerBusy`) 20초 주기로 재시도만 하고 **토스트도 안 떴다**(사용자에겐 그냥 조용한 상태) → `peerBusy`를 상태로 올려 그때는 토스트를 띄운다(수동으로 지금 적용할 길을 남긴다). 웹 +2 테스트(등록 직후 확인 — `<UpdatePrompt />`를 `Guard` 없이 단독 렌더해야 한다: `useUpdateGuard`도 자기 확인을 걸어 오탐 통과 / 피어 미룸 토스트, 둘 다 수리 전 실패 확인, 전체 1334). 실브라우저 **실제 SW 사이클**(Vercel과 같은 헤더·리라이트로 dist 서빙, `ServiceWorkerRegistration.prototype.update`를 세어 확인): 앱 화면 등록 직후 1회 · 랜딩에서도 1회 · **sw.js를 바꿔 두고 랜딩만 열어도 새 SW가 `waiting`으로 선다**(제보 상황의 반대)
- ✅ **칸반 M3 — 카드 삭제(제보) + 썸네일·마크다운·검색** ① **카드 삭제**: 컨트롤러에 `deleteCard`는 M1부터 있었는데 **진입점이 하나도 없었다**(열에는 ✕가 있고 카드에는 없었다 — 제보 "추가된 카드를 삭제하는 기능은 없는 것 같다"). 진입점 둘: 카드 우상단 **✕**(열 머리의 ✕와 같은 문법 — 평소엔 숨기고 **고른 카드**나 마우스를 얹은 카드에만 띄운다. 카드마다 늘 떠 있으면 목록이 시끄럽고, 터치 기기에는 hover가 없지만 탭이 곧 선택이라 같은 조건으로 뜬다. ✕ 자리는 늘 비워 둬 버튼이 떴다 사라져도 글자가 밀리지 않는다. `pointerdown`을 삼켜 카드 드래그가 시작되지 않게)와 **Delete/Backspace 키**. 그 김에 칸반에 **자기 키 분기**를 세웠다 — 예전에는 캔버스 분기(마퀴·방향키 이웃 이동·붙여넣기)가 그대로 흘러 뜻 없는 키가 돌고 있었다: 이제 Ctrl+Z/Y·Delete·Enter/F2(편집)·Escape(선택 해제)만 받는다. ② **홈 썸네일 실렌더**: 칸반은 좌표가 없으니 마인드맵의 레이아웃·측정 기계를 태울 것이 없다 → `buildPreview`가 `kind==='kanban'`이면 전용 렌더(`kanbanPreview` — 열 사각형 + 제목 + 카드, 넘치는 카드는 "+N"으로 접는다. 열 폭·간격은 에디터 비율, 카드 글자는 실측 폭으로 한 줄 잘라 …). 열이 하나도 없으면 null → 폴백 삽화(`miniKanbanPreview`). **실브라우저에서 잡은 것**: 카드 우상단 '칸반 보드' 배지가 마지막 열 제목을 덮었다 → 열 시작 높이(`KB_TOP`)를 26으로 내려 배지 자리를 비웠다. ③ **마크다운 내보내기**: 코어 `toMarkdown`이 칸반이면 **열=H2 · 카드=목록**(빈 열도 제목을 남긴다 — "완료: 아직 없음"도 정보다), 카드 순서는 화면과 같은 `pos`(`cardsInColumn`), rich 서식은 기존 `richToMarkdown` 그대로. **PNG·SVG·PDF는 칸반에서 감춘다**(그릴 캔버스가 없어 빈 파일이 된다 — 에디터 메뉴·모바일 ☰·홈 카드 메뉴 세 곳 모두. 모바일 ☰는 PNG 자리를 Markdown에 내준다). ④ **홈 검색**: `searchIndex`가 열 제목·카드 글자도 모은다(썸네일이 이미 받아 둔 본문을 쓰므로 새로 내려받는 것이 없다). ⑤ **단축키 도움말**: 칸반 전용 구획을 세우고 캔버스 구획(선택·이동·텍스트 편집·리스트·서식)은 감춘다 — 칸반에서 뜻이 없다. 코어 +3 / 웹 +7 테스트(카드 삭제 2건은 **수리 전 실패 확인**, 전체 265 + 1341), 실브라우저 20종(데스크톱 15 — ✕ 노출 규칙·hover·삭제·Delete 키·undo·✕에서 끌어도 드래그 아님·내보내기 메뉴 구성·Markdown 내용·도움말 구획·홈 썸네일 실렌더·카드 내용 검색·홈 메뉴 / 390px 5 — 탭 선택→✕ 30px·탭 삭제·☰ 메뉴·가로 넘침 0). **프로브 함정 기록**: 최근 트레이 카드는 **일부러** 메뉴가 없는 바로가기(#345)라 우클릭 프로브가 그 카드를 잡으면 영영 안 뜬다 — 그리드 카드를 지정해야 한다. **다음(M3 후속)**: 카드 댓글
- ✅ **칸반 카드 댓글(M3 마무리)** 서버는 **한 줄도 바뀌지 않았다** — 0020의 `comments.node_id`는 "대상 id"일 뿐이고 칸반 카드 id도 같은 팩토리에서 나온 문서 안 유일 id라, 마이그레이션도 RLS 변경도 필요 없다(0022 알림 딥링크도 그대로 동작). 웹 4겹: ① `commentTargetLabel`이 카드를 알아본다(`카드 · 첫 줄`). 문서 전체 댓글은 마인드맵과 **같은 자리**(`ROOT_ID`)를 쓰는데 칸반에는 루트 주제가 없으므로 `보드 전체`라고 말한다 ② 대상은 고른 **카드**를 따라간다(캔버스의 `selection` 규칙과 같은 뜻 — 어느 카드의 논의인지 흐려지지 않게. 선택 해제는 대상을 그대로 둔다) ③ 개수 배지 — 캔버스의 주제 배지와 같은 뜻(미해결 스레드 수)이지만 좌표가 없는 카드에서는 겹칠 자리를 찾는 대신 **글 아래 줄로 흐른다**(카드가 그만큼 자란다). 누르면 그 카드의 논의가 열리고 보기 전용에서도 동작한다. ④ 에디터 칸반 분기에 `CommentPanel` — 칸반에는 속성 시트가 없어 모바일 바텀 시트가 다툴 상대가 없다(마인드맵에서 둘 중 하나만 남기던 분기가 불필요). 진입점은 보기/☰ 메뉴와 배지 둘. 카드를 지워도 댓글은 남는다(기존 "사라진 대상" 규칙 — 배지는 카드와 함께 사라진다). 웹 +2 테스트(**구현 전 실패 확인**, 전체 1343), 실브라우저 13종(데스크톱 10 — 보드 전체 → 카드로 대상 추종·작성·저장 키가 카드 id·배지 노출/부재·배지로 열기·리로드 영속·삭제 시 배지 소멸 / 390px 3 — 바텀 시트 화면 안·그 카드의 논의)
- ✅ **폰에서 잡은 카드가 세로로 끌면 풀리며 엉뚱한 자리로 가던 문제(제보)** "카드를 길게 눌러 잡은 뒤 위/아래로 끌면 드래그가 풀리면서 하위 단계로 자동 이동된다." 원인 둘이 겹쳐 있었다. ① 카드는 `touch-action: pan-y`이고(그래야 평범한 손가락 스크롤이 산다) 잡은 **뒤에도** 그대로라, 세로로 끌면 브라우저가 스크롤을 가져가며 **pointercancel**을 쏜다 — 우리가 막고 있던 것은 `pointermove`의 `preventDefault`인데 **포인터 이벤트는 밑에 깔린 터치 동작을 취소하지 못한다**(터치 스크롤을 멈추려면 비-passive `touchmove`를 막아야 한다). ② 그 `pointercancel`이 **이동 커밋 핸들러(`onUp`)에 물려 있어**, 취소된 자리에서 `dropTargetAt`을 재고 카드를 그리로 옮겼다 — 사용자 눈에는 "드래그가 풀리면서 저절로 이동". 수리: 잡힌 뒤(`started`)에만 `touchmove`를 비-passive로 막고(잡기 전 세로 이동은 여전히 스크롤 의도라 손대지 않는다), `pointercancel`은 **취소 전용 핸들러**로 갈라 카드를 제자리에 둔다(취소는 이동이 아니다 — 핀치·시스템 제스처 등 어떤 이유든 마찬가지). 웹 +2 테스트(취소 시 화면 순서 불변 / 잡기 전엔 안 막고 잡은 뒤엔 막음 — **둘 다 수리 전 실패 확인**. 취소 테스트는 처음에 저장본으로 단정했다가 **저장이 아직 안 쓰인 초기 본문이 그대로 통과**해 가드가 되지 않는 것을 발견하고 화면 순서 단정으로 교체했다), 실브라우저 CDP 실제 터치 7종(아래로 끝까지·위로 맨 앞까지·다른 열로(가로+세로 동시)·끄는 동안 고스트 유지·잡기 전 스와이프는 스크롤·두 손가락은 취소 후 제자리·데스크톱 마우스 무회귀)
- ✅ **칸반 드롭 가이드 선을 옅게(제보: 너무 진하다)** 카드를 끌 때 삽입 자리를 가리키는 선이 **3px 불투명 강조색 막대**라 카드 사이에서 튀었다. 이 선은 **자리를 가리키는 눈금**이지 강조 대상이 아니다 — 손가락/커서를 따라오는 고스트가 이미 주인공이다. 2px·강조색 **50% 알파**로 낮추고, 양끝이 배경으로 스며드는 가로 그라디언트(0%→10% 진입, 90%→100% 소멸)로 바꿨다(딱 잘린 막대는 카드 사이에 이물처럼 보인다). 색은 여전히 테마 강조색이라 어느 팔레트에서나 톤이 맞는다. 웹 +1 계약 가드(높이 ≤2 · `linear-gradient` · 어느 정지점도 alpha < 1 — **변경 전 실패 확인**), 실브라우저 before/after 2배 스크린샷 비교(데스크톱·모바일)
- ✅ **칸반 열 순서 바꾸기(요청)** 코어 `moveColumn`은 M1부터 있었고 **UI만 없었다**. 열 머리를 끌면 순서가 바뀐다 — 놓일 자리는 세로 선으로, 끌고 있는 열은 제목 알약 고스트로 보인다(원본은 자리를 지키되 흐리게, 카드와 같은 규칙). 판정은 순수 함수(`columnDropIndex`/`columnDropIndicator` — 카드와 같은 "중심을 넘어야 바뀐다" 규칙, 끌고 있는 열은 계산에서 빼므로 코어 `moveColumn`이 받는 index와 좌표계가 맞는다). **곁들여 제스처 골격을 한 곳으로 모았다**(`beginPointerDrag`) — 마우스 4px 문턱 / 터치 320ms 길게 누르기 / 잡은 뒤 비-passive `touchmove` 차단 / **취소는 이동이 아님**. 방금 고친 터치 함정(#436)이 카드와 열 두 벌로 갈라져 드리프트하는 것을 막는다. ✕·제목 편집기에서 시작한 포인터는 드래그가 아니고(`closest` 가드), 더블클릭 제목 편집은 그대로다. 웹 +4 테스트(순수 2 + 통합 2), 실브라우저 11종(데스크톱 고스트·세로 선·맨 뒤로 이동·저장·undo·카드 동반·제목 편집 무회귀·✕ 가드 / 폰 길게 누르기·순서 변경·짧은 스와이프는 스크롤)
- ✅ **칸반 카드 색 라벨(요청)** `KanbanCard.bg`는 M1부터 있었고 **고를 UI만 없었다**(카드는 통째로 직렬화되므로 코어·마이그레이션 변경 0). 고른 카드·마우스를 얹은 카드에 **팔레트 버튼**이 뜨고(✕과 같은 조건·같은 자리 규칙 — 두 버튼 자리를 늘 비워 둬 글자가 밀리지 않는다) 누르면 스와치 판(`CardLabelPicker`, `position: fixed` — 카드가 세로 스크롤되는 열 안이라 흐름에 두면 잘린다). 색은 **연한 틴트 7종 + 없음** — 카드 배경 전체를 칠하므로 진한 색을 넣으면 본문 대비가 무너진다(라벨은 갈래를 흘깃 알려 주는 표식이지 강조 장치가 아니다). '없음'을 고르면 **키를 지운다**(빈 필드가 CRDT로 계속 오가지 않게 — `clearNodeImage`와 같은 규칙). 홈 썸네일도 카드 사각을 그 색으로 칠한다. **우클릭 메뉴를 새로 두지 않은 이유**: 터치에는 우클릭이 없고 길게 누르기는 이미 드래그가 쓴다 — 버튼 하나면 데스크톱·폰이 같은 길을 쓴다. **실브라우저에서 잡은 것**: 버튼 안에 현재 색 동그라미를 그렸더니 **카드 배경과 같은 색이라 비어 보였다** → 팔레트 글리프로 바꾸고 현재 라벨 이름은 접근 이름·툴팁에 뒀다(색은 카드가 이미 말한다). 웹 +2 테스트, 실브라우저 14종(버튼 노출 규칙·스와치 8종·칠하기·저장·카드별 다른 색·바깥 클릭 닫힘·리로드 영속·없애면 키 삭제·undo·홈 썸네일 색·폰 30px 타깃·판이 화면 안)
- ✅ **칸반 카드 서식(요청)** `KanbanCard.rich`도 M1부터 있었고 쓰이지 않았다. 전용 편집기(contentEditable)를 새로 포팅하는 대신 **주제·메모와 같은 커밋 훅**을 붙였다 — 확정할 때 `applyMarkdownShortcuts`가 `**굵게**`·`*기울임*`·`~~취소선~~` 마커를 서식으로 바꾸고 `applyAutoLinks`가 타이핑한 URL을 링크로 만든다(편집 중 실시간으로 걸지 않는 이유도 같다: 반쯤 친 주소가 링크가 됐다 풀렸다 하며 캐럿·IME가 흔들린다). 렌더는 **같은 `RichSpan`**이라 굵게·기울임·취소선·색·링크가 캔버스와 한 규칙으로 보이고, 링크 파랑은 카드가 `--mf-link`를 내려 주는 기존 파이프라인(값은 배경이 아니라 **글자색** 밝기에서 고른다). **재편집의 계약**: 카드 편집기는 평범한 textarea라 서식을 그대로 보여 줄 수 없다 → 열 때 `richToMarkdown`으로 **마크다운 원문**을 seed한다(굵게였던 글자가 `**굵게**`로 보이고 그대로 확정하면 다시 굵어진다). 그래서 `text`는 늘 화면에 보이던 글자와 같고, 평문으로 고치면 `rich` 키가 사라진다. 곁들여 코어 `richToMarkdown`이 **자동 링크를 부풀리지 않게** 했다(글자 자체가 주소면 `[주소](주소)` 대신 맨 URL — 마크다운 렌더러도 맨 URL을 링크로 읽고, 재편집 원문이 그대로 보여야 왕복이 맞는다). 마크다운 내보내기도 이 경로라 서식이 함께 나간다. 코어 +2 / 웹 +3 테스트(전체 267 + 1355), 실브라우저 12종(마커 걷힘·굵게/기울임/취소선/링크 계산 스타일·링크 잉크 rgb(26,99,216)+밑줄·재편집 원문·재확정 유지·평문이면 키 삭제·.md 서식·리로드 영속). **범위 밖**: 홈 썸네일은 카드 글자를 한 줄로 잘라 그리므로 평문 그대로(10px 한 줄에서 굵기 차이는 읽히지 않는다)
- ✅ **칸반 템플릿 3종(요청)** 갤러리 칸반 구획이 빈 보드 하나뿐이라 "어떻게 쓰는 건가"를 스스로 알아내야 했다. 화이트보드 템플릿과 **같은 설계** — 템플릿은 그냥 `Doc` 하나라 썸네일(`realPreview`)·저장·협업·undo·내보내기가 전부 기존 경로 그대로고, 새로 짠 것은 `KANBAN_TEMPLATES` 데이터와 `buildTemplateDoc` 분기뿐이다(주소에는 `tpl=kanban-…`만 실리고 문서는 **에디터가** 시드한다 — 홈이 조립해 저장하면 저장 경로가 둘이 된다). 3종: **스프린트 보드**(백로그→완료 5열)·**이슈 트리아지**(들어옴→해결 5열)·**콘텐츠 파이프라인**(아이디어→발행 4열). 카드는 **보드가 실제로 만들 수 있는 것**만 쓴다 — 글자와 색 라벨(사용자가 지웠다 다시 만들 수 없는 물건을 템플릿만 심어 두면 "이건 어떻게 되살리지?"가 된다). 좌표 개념이 없어 겹침 걱정도 없다. **실브라우저가 잡은 구멍**: 데이터·빌더·갤러리 카드까지 만들고 `createFromTemplate`에 분기를 빠뜨려 **카드는 보이는데 눌러도 아무 일이 없었다**(jsdom 테스트는 주소를 직접 열어 이 경로를 지나지 않았다) → 갤러리→생성 통합 테스트로 고정. 웹 +7 테스트(템플릿 순수 5 + 갤러리 1 + 에디터 씨앗 2, 전체 1367), 실브라우저 12종(갤러리 3종·빈 칸반이 앞·썸네일 실렌더·가로 스크롤 없음 / 스프린트 열 5·카드 7·색 라벨 3장·저장본 kanban·주소 tpl / 나머지 2종 열림 / 폰 넘침 없음)
- ✅ **겹친 영역(프레임) 정책(제보 → 원인 확정 → 사용자 선정)** "화이트보드에서 영역끼리 겹치면 작은 영역을 움직일 수 없고 객체가 꼬인다." 실브라우저로 재현해 **원인이 둘**임을 확정했다: ① 모든 영역의 히트 판이 같은 z(8)라 **DOM 순서**(=배열 순서)가 승자를 정한다 — 나중에 만든 큰 영역이 위에 깔려 작은 영역의 빈 자리를 눌러도 큰 영역이 잡힌다(실측: `elementFromPoint` → `zb`) ② 소속 판정이 **중심점**이라 큰 영역이 작은 영역을 담는 것은 물론 **작은 영역도 큰 영역을 담는다**(큰 영역의 중심이 작은 영역 안에 들 때) — 부분 겹침도 서로 담는다. 그래서 어느 쪽을 끌어도 상대가 통째로 따라왔다. **정책(사용자 선정: 겹침 허용 + 규칙 정리)**: 겹침 자체는 문제가 아니므로 막지 않고(중첩은 화이트보드의 자연스러운 쓰임 — Figma·Miro도 허용) 두 규칙만 못박는다 — ⓐ **영역이 영역을 담으려면 완전히 들어가야 한다**(`fullyInside`, 중심 규칙이 아니라): 완전 포함은 한쪽으로만 성립하므로 계층이 언제나 한 방향이고, 부분 겹침은 서로 담지 않는 **형제**가 된다 ⓑ **작은 영역이 위**(`ZoneLayer`가 넓이 내림차순으로 그린다 + 히트 테스트가 `innermostFrameAt`으로 가장 작은 것을 집는다) — 클릭·우클릭·드래그가 늘 가장 안쪽 영역에 닿는다. 물건(메모·획·자유 주제)의 소속은 **중심 규칙 그대로**다(작아서 "중심이 들어갔다"가 곧 사용자가 느끼는 소속이고, 완전 포함을 요구하면 가장자리에 걸친 스티커가 어느 프레임에도 안 들어간다). 모델·직렬화 변경 0. 웹 +6 테스트(순수 4 + 통합 2, 통합 2건 모두 **수리 전 실패 확인** — DOM 순서와 반대 방향 carry 각각), 실브라우저 8종(작은 영역 히트·작은 영역만 이동·큰 영역과 중첩 동반 이동·안의 메모 동반·부분 겹침 독립·우클릭 안쪽 영역)
- ✅ **홈 카드 다중 선택 + 일괄 동작(요청)** 카드를 여러 장 골라 한 번에 옮기고 지운다. **선택 모델**: `selectedCards: string[]`가 진실의 원천이고 **단일 선택은 길이 1인 다중 선택**이다(칸이 둘이면 "화면엔 선택돼 보이는데 메뉴는 다른 카드의 것"이 생긴다). `selectedCard`는 그중 **앵커**(마지막에 누른 카드, Shift 범위의 기준)로 남고 폴더 카드도 이 칸을 쓴다. 관례는 파일 탐색기 그대로 — Ctrl/⌘+클릭(토글)·Shift+클릭(범위)·Ctrl/⌘+A(전부). 수정 키를 쥔 클릭은 **여는 동작이 아니다**(그러지 않으면 Ctrl+클릭 두 번이 맵을 연다). 범위·전체의 "순서"는 **화면에 그려진 순서**(`data-card-key`의 DOM 순서)를 읽는다 — 뷰모델의 목록을 컨트롤러가 다시 계산하면 둘이 어긋난다. **대상에서 뺀 것**: 폴더 카드(이동·삭제의 뜻이 맵과 달라 섞이면 메뉴가 모호), 최근 항목 트레이(원래 메뉴 없는 바로가기 #345). **드래그**: 잡은 카드가 선택에 포함돼 있으면 **선택 전체**, 아니면 그 한 장(`dragKeys` 한 곳에 규칙을 둬 폴더 카드·상위 폴더 타일이 같은 판단을 쓴다). 잡은 카드에만 개수 배지(브라우저 드래그 이미지는 우리가 못 그린다). **우클릭**: 선택 **안**에서 열면 선택 유지, **밖**이면 그 카드 하나로 교체(OS 표준). 일괄 메뉴는 **폴더로 이동·스페이스로 이동·삭제하기(N개)**뿐 — 한 장에만 뜻이 있는 항목(이름 변경·공유·내보내기)은 내주지 않고, **즐겨찾기도 뺐다**(사용자 결정: 켜진 것과 꺼진 것이 섞이면 결과가 예측되지 않는다). 폴더 이동은 선택이 **한 스페이스 안일 때만**(폴더 id는 그 스페이스에서만 유효 — 전역 검색 결과처럼 섞이면 미아가 생긴다). 삭제는 "맵 N개를 휴지통으로" 확인창 하나로 묶고, 한 장 삭제와 **같은 규칙**을 쓰도록 본문을 `removeCardFrom` 순수 변환으로 뽑았다(휴지통 기록·즐겨찾기 정리·중복 방지가 경로마다 갈리면 복원이 달라진다). **실브라우저가 잡은 결함 2건**: ① 확인창들이 닫혀 있어도 `role="dialog"`로 **마운트된 채**라(늘 3개) Ctrl+A 가드가 영영 걸렸다 → 실제로 보이는 것만 센다(`getClientRects().length`) ② 메뉴 행의 mousedown이 "카드 밖 클릭"으로 읽혀 선택을 비웠고, 클릭이 도착하기 전에 메뉴가 **일괄 → 단일로 갈아 끼워져** 일괄 폴더 이동·삭제가 통째로 먹지 않았다 → `.mf-home-ctx` 안은 예외. 웹 +7 테스트(②는 수리 전 실패 확인), 실브라우저 16종(Ctrl/Shift/Ctrl+A·수정 키로는 안 열림·일괄 메뉴 구성·즐겨찾기 부재·선택 유지·메뉴 폴더 이동·드래그 개수 배지·드래그 일괄 이동·삭제 확인창·단일 메뉴 무회귀·더블클릭 열기 무회귀). **범위 밖**: 모바일 다중 선택(탭=선택/더블탭=열기가 이미 있어 진입 제스처가 겹친다)
- ✅ **모바일 홈 다중 선택(요청 — 설계 논의 후 구현)** 폰에는 Ctrl/Shift가 없어 데스크톱 다중 선택(#443)을 그대로 쓸 수 없었다. **진입 제스처와 이동 방식을 먼저 논의해 정한 뒤**(사용자 선정: 길게 누르기 / 메뉴로만) 구현. ① **진입**: 카드를 500ms 길게 누르면 선택 모드. 시간을 **직접 잰다**(칸반 `beginPointerDrag`와 같은 골격 — 10px 넘게 밀리면 스크롤 의도로 포기, 성립하면 진동 12ms). 기기의 길게 누르기(`contextmenu`)도 **같은 모드**를 열고 둘 중 먼저 오는 쪽이 켠다 — iOS는 `-webkit-touch-callout: none`을 걸면 그 이벤트가 오지 않기도 하고, 반대로 브라우저가 우리 타이머보다 먼저 쏘기도 한다. 트레이드오프: 길게 누르기의 **옛 뜻(카드 메뉴)이 사라진다** — 터치에서는 ☰이 항상 보이므로(`home.css` `@media (hover: none)`) 메뉴 접근은 그대로고, 데스크톱은 무회귀. ② **상태는 플래그 하나**(`selectMode`) — `selectedCards.length > 0`을 모드로 삼으면 모바일의 평범한 탭(이미 '선택'이라 ☰·★이 드러난다)에도 선택 바가 떠 버린다. 모드 밖은 지금 그대로(탭=선택 표시, 더블탭=열기), **모드 안은 탭=토글·더블탭 열기 꺼짐**, 0개가 되면 스스로 나간다. ③ **선택 바가 툴바 자리를 대체**(`✕ · N개 선택 · 전체 선택 · ⋯`) — 하단 고정 바를 새로 만들지 않은 이유는 그 자리를 설치 안내·오프라인 바가 이미 쓰기 때문이고, 검색·만들기는 그 시간대의 일이 아니다. `⋯`는 **데스크톱 우클릭과 같은** `HomeContextMenu`(폴더 이동·스페이스 이동·삭제, 즐겨찾기 없음 — 기존 라우팅 그대로라 새 분기 0). 카드 좌상단 체크 동그라미(그동안 ★ 감춤), 카드 ☰도 감춘다(여러 장을 골라 둔 채 한 장의 메뉴를 여는 것은 뜻이 어긋난다). ④ **터치 드래그는 만들지 않았다**(결정대로) — 홈 카드는 HTML5 DnD라 터치에서 발화하지 않는다. ⑤ **구현 중 잡은 구멍**: 폴더 카드가 선택 모드에서도 반응하면 `selectCard`의 폴더 분기가 맵 선택을 비워 **"0개 선택" 바만 남는다** → 폴더 카드·상위 폴더 타일을 모드 동안 흐리게 두고 반응하지 않게(진입도 막는다 — 고른 맵을 두고 다른 목록으로 가면 무엇을 고르고 있었는지 흐려진다). 일괄 이동·삭제가 끝나면 모드도 나간다(옮기거나 지운 카드는 이 목록에서 사라진다 — `clearSelection` 한 곳). 웹 +6 테스트(타이머 진입·기기 길게 누르기 진입·스크롤 포기·폴더 무반응·선택 바 전체 선택+⋯ 폴더 이동·데스크톱 무회귀, 구현을 걷어내면 실패 확인), 실브라우저 390px **실제 터치**(CDP) 11종(짧은 탭은 진입 아님·진입 후 검색창 사라짐·토글·더블탭 무반응·전체 선택·메뉴 화면 안+행 44px·폴더 이동 저장본·이동 후 모드 종료·누르고 밀면 진입 안 함·✕/배경 탭 종료·모드 밖 더블탭은 에디터로) + 삭제 확인창·다크 테마 확인
- ✅ **칸반 디자인 이식(Claude Design `Geurio 칸반보드.dc.html`)** 사용자가 디자인한 칸반 에디터 화면을 이식했다(원본을 저장소 루트에 둔다 — 랜딩 리뉴얼과 같은 방식, 수정 금지 대상). **파일 확보**: claude_design MCP는 `/design-login`이 대화형 터미널을 요구해 이 원격 세션에서 인증 불가(URL 직접 fetch도 403) → 사용자가 올린 번들 HTML(22MB)의 `__bundler/template` 줄에서 dc 원본(52KB)을 꺼냈다. **① 카드 곁정보·상세·보드 머리**: 코어 `KanbanCard`에 `tag`·`due`·`owner`/`ownerName`·`flagged`(카드는 통째로 직렬화·전파되는 엔티티라 필드가 그대로 왕복하고 **값이 없으면 키 자체가 없다**). 원본이 고정 표로 들고 있던 값은 규칙으로 바꿨다(`kanbanMeta.ts`) — **분류 색은 이름에서** 정하고(테마 팔레트 인덱스; 색을 저장하면 테마를 바꿨을 때 옛 색이 남고 같은 이름이 문서마다 달라진다. 덕분에 직접 적은 분류도 등록 절차 없이 색을 얻는다), **담당은 공유 참가자**(0011)에서 고르고 이름은 스냅샷(댓글 `author_name`과 같은 이유 — 클라이언트는 남의 `profiles`를 못 읽는다), 아바타 색은 **접속자 커서와 같은 시드**, **진행률은 마지막 열을 완료로 본다**(원본은 `done` 고정 열 id를 봤지만 우리 열은 사용자가 만든다). 화면: 카드 앞면(분류·긴급 배지 / 기한·댓글·담당 / 빠른 동작 `‹ › ✕`, 기한은 지남·오늘내일·그 밖 톤), 열 머리(색 점·카드 수 알약·＋·⋯ 메뉴=이름 변경·색·삭제 — 열 색은 지정이 없으면 순서대로 팔레트라 원본의 단계별 색이 기본으로 재현된다), 열 안 컴포저(Enter 추가·Esc 취소), 빈 열 안내, 열 추가 타일, 보드 머리(검색·진행 바). **동작 변경 둘**: 카드 더블클릭이 인라인 편집 대신 **상세 모달**(원본 `card.onOpen`, Trello·Linear 관례 — 제목 textarea 포커스 + Enter 저장·닫기라 이름 바꾸기는 그대로 두 동작), 색 라벨은 카드 위 → 상세 안(카드 위 빠른 동작을 원본대로 셋만 남기려고). 댓글은 원본의 간단한 로그 대신 **이미 있는 댓글 기능**(0020~0021 스레드·멘션·해결·실시간)으로 보낸다 — 같은 것을 두 벌로 두면 어느 쪽이 진짜인지 흐려진다. 원본에 있었지만 두지 않은 것: 제목·저장 상태 줄(좌상단 문서 칩이 이미 말한다), 필터 버튼(원본에도 핸들러가 없다). **② 리스트·타임라인**: 보기 모드는 **문서가 아니라 보는 사람의 상태**(문서에 넣으면 한 사람의 탭 전환이 모두의 화면을 바꾼다 — 테스트로 고정). 리스트=열별 묶음 표(폰에서는 제목·기한만 — 다섯 열을 390px에 밀어 넣으면 전부 잘린다), 타임라인=오늘이 넷째 칸인 14일. 원본은 카드마다 시작·끝 날(`s`/`e`)을 들었지만 **우리 카드에는 기한 하나뿐**이라 새 필드 대신 **오늘부터 기한까지**(지났으면 기한부터 오늘까지)를 그린다 — 손대지 않아도 뜻이 통하고 늦은 일이 오늘까지 뻗어 눈에 띈다. 창 밖 기한은 가장자리로 clamp(원본과 같다), 기한 없는 카드는 놓을 자리가 없어 숨기되 **몇 장인지 밝힌다**. **마지막 열(완료) 카드는 기한이 지나도 붉게 칠하지 않는다**(끝난 일에 경고가 남으면 남은 일과 구분되지 않는다 — 원본도 완료 막대를 달리 칠했다, 리스트·타임라인 같은 규칙). 검색은 세 보기 공용(`cardMatches`). 코어 +1 / 웹 +20 테스트(전체 236 + 1405), 실브라우저 검증(배지·기한 톤 3종·검색·컴포저·상세 전 필드·‹ ›·열 색·저장본 / 탭 전환·리스트 6행·타임라인 14칸과 막대 좌표·색·행 클릭·모바일 390px). **범위 밖**: 카드 시작일(타임라인 다중일 막대), 분류 관리 화면, 담당 여러 명
- ✅ **칸반 후속 12건(요청)** 디자인 이식 배포 후 정돈. ① 열은 **내용만큼만** 높다(`alignSelf: flex-start`, 길면 화면 높이에서 멈추고 열 안이 스크롤) — 카드가 적은 열이 끝까지 늘어나면 어느 단계가 비었는지 안 보인다 ② 새 카드의 담당은 **나 자신**(만드는 사람이 대개 맡는 사람이고, 아니면 상세에서 바로 바꾼다) ③ 카드 위 빠른 동작 `‹ › ✕` 제거(셋 다 상세에 있다) ④ 칸반의 보기 메뉴는 **보드·리스트·타임라인 셋**뿐(데스크톱·모바일 ☰ 동일) — 보기 모드를 컨트롤러(`kanbanView`)로 올려 보드 머리 탭과 GNB 메뉴가 같은 값을 본다 ⑤ GNB **도움말** 메뉴 신설(디자인 원본의 편집·보기·도움말) — 단축키 도움말을 보기에서 이리로 ⑥ 검색·탭을 디자인과 같은 자리(오른쪽에 `[검색][탭]` 한 묶음)로. 보기 메뉴에서 빠진 **보드 전체 댓글**은 그 줄의 아이콘이 맡는다(디자인의 '필터' 자리 — 원본에도 동작이 없던 버튼) ⑦ 곁정보는 **비어 있어도 자리를 지킨다**(날짜 없음·댓글 0·담당 없음 점선 원) — 카드마다 높이가 들쭉날쭉하지 않고 무엇을 안 정했는지 보인다 ⑧ **분류는 문서가 목록을 들고 있다**(`Doc.tags`, 칸반일 때만 직렬화·CRDT): 카드에만 있으면 그 분류를 쓰는 카드를 다 지우는 순간 직접 만든 분류가 사라진다. 기본은 '없음'뿐이고 직접 입력해 추가, 칩의 ✕로 삭제하면 그 분류를 쓰던 카드의 `tag`도 함께 뗀다(유령 방지). **색은 만들자마자 이름에서 자동으로** 붙고(등록 절차 없음), 고른 분류 아래 스와치 줄에서 바꾸면 문서에 저장된다(항상 펼쳐 두면 분류가 늘수록 모달이 색 판으로 가득 찬다) ⑨ 드롭 표시를 선 → **점선 사각형**(자리를 실제로 밀어내지 않고 겹쳐 그린다 — 그래야 드래그 중 카드 사각형이 흔들리지 않는다) ⑩ 드래그 고스트를 **카드와 같은 얼굴**로(`CardFace`를 카드·고스트가 공유 — 글자만 보이면 무엇을 옮기는지 흐려진다) ⑪ 진행률 글자 13px ⑫ GNB를 디자인 원본처럼(메뉴는 테두리 없는 텍스트, 공유는 알약, 내보내기는 강조 알약) — **마인드맵·화이트보드 포함 전 종류**. 곁들여 마지막 열(완료) 카드는 기한이 지나도 붉게 쓰지 않는다(리스트·타임라인에 있던 규칙을 보드 카드에도). **함정**: `commitDoc`의 "바뀌었나" 판정과 undo 스냅샷·자동저장 서명이 `tags`를 안 봐서 **분류만 바뀐 커밋이 통째로 버려졌다**(칸반 열·카드에서 겪은 것과 같은 계열) — 여섯 지점에 배선. 코어 +1 / 웹 +5 테스트(전체 237 + 1409), 실브라우저 12건 전수 검증(열 높이·owner 저장·버튼 0·메뉴 3항목·도움말 모달·검색/탭 좌표·빈 곁정보·분류 추가/색/삭제·드롭 점선 44px·고스트 곁정보·13px·알약 버튼)
- ✅ **칸반 후속 7건(제보)** ① **카드 상세에서 바로 댓글** — 상세 팝업의 댓글 줄이 **패널을 여는 버튼**이라 모달을 닫고 다른 화면으로 옮겨 가야 했다. 디자인 원본처럼 그 자리에서 읽고 쓰되, 간단한 로그를 새로 만들지 않고 **이미 있는 댓글 기능**(0020~0021 스레드·답글·멘션·해결·실시간)을 그대로 얹었다: `CommentPanel`의 몸통을 `CommentThreads`로 뽑아 떠 있는 패널과 상세 모달이 **같은 코드**를 쓴다(둘이 갈라지면 한쪽에만 기능이 붙는다). ② **색 이름·톤** — `색` → `카드 배경색`(분류 색과 카드 배경색이 한 모달에 함께 있어 그냥 "색"이면 어느 쪽인지 알 수 없다), 값도 더 옅게(`#fdf0ec`류): 카드 **배경 전체**를 칠하므로 진한 색은 본문 대비를 무너뜨린다 — 라벨은 갈래를 흘깃 알려 주는 표식이지 강조 장치가 아니다. ③ **기한 자리·지우기** — 입력을 오른쪽으로 밀고 `지우기`는 **항상 그 자리에** 두되 기한이 없으면 비활성(값이 있을 때만 나타나 버튼이 생겼다 사라지며 줄이 흔들렸다). ④⑤ **드롭 자리** — 빈 열에 끌어다 놓으면 표시가 **열 제목 위**에 떴고 크기도 실제 카드와 달랐다. 자리 표시를 카드 목록(`[data-kanban-list]`) **안**으로 넣고 높이를 끌고 있는 카드와 같게 맞췄다. 그 김에 겹쳐 그리던 것을 **목록의 한 칸으로** 넣어(원본 카드는 목록에서 빼고) 열이 자리만큼 실제로 늘어난다 — 어디에 들어갈지가 크기까지 포함해 보인다. ⑥ **열 그림자** 디자인 원본의 `0 18px 40px -34px`(잉크 40%). ⑦ **진행 바를 열 색으로** — `완료/진행` 두 덩어리로 칠하던 바를 **열별 구간**으로 나누고 각 구간을 그 열 머리의 점 색으로 그린다(`boardProgress().segments`, 카드가 없는 열은 빠진다) — 사용자가 열 색을 정했으면 그 색이 곧 진행 상황의 색이 된다. 웹 +4 테스트(상세 안 댓글 작성→배지 증가 / 지우기 항상 노출·비활성 / 빈 열 자리가 목록 안·높이 = 카드 높이 / 열 그림자 + 진행 구간 색 = 열 점 색, 전체 1413), 실브라우저 검증(열 그림자 실측·진행 구간 3개가 각 열 색·상세 모달 안 댓글 작성/표시·기한 x=681(모달 중앙 640 오른쪽)·빈 열 자리 top 270 > 머리 bottom 259·높이 109 = 카드 109·자리 생길 때 열 높이 219→222)
- ✅ **칸반 후속 6건(제보·요청)** ① **진행 바가 오른쪽부터 차던 문제** — 직전 판이 **모든 열**을 board 순서대로 칠해 바가 늘 100% 차 있었고, 완료(마지막 열) 색이 **오른쪽 끝**에 놓였다: 일이 끝날수록 색이 오른쪽부터 차오르는 것처럼 읽힌다. 디자인 원본의 구성(`[완료][진행][빈 자리]`)으로 되돌리되 **색은 열 색 그대로**(직전 요청) — 세그먼트를 **완료 → 진행** 순으로 왼쪽에서 그리고 첫 열(아직 시작하지 않은 일)은 빈 트랙으로 남긴다. ② **열 추가 타일에 열과 같은 그림자**(`0 18px 40px -34px`) — 값을 상수 하나(`COL_SHADOW`)로 모아 둘이 함께 쓴다. ③ **바닥 색이 너무 진했다** — 원본의 페이지 배경(`#FDFAF7`)은 열(`#FBF6F1`)보다 **한 톤 옅다**. 테마마다 값을 새로 적는 대신 **관계**로 만들었다: 신설 `mixHex(a,b,t)`로 `mix(appBg, panel, 0.55)` — 다크·화이트에서도 같은 방향(바닥이 열보다 물러남)이 성립한다. ④ **열 드래그도 카드와 같은 고스트** — 제목 알약만 떠 있던 것을 **열의 얼굴**(머리 + 카드 최대 4장)로 바꾸고, 원본은 목록에서 **빼고** 그 자리에 같은 크기의 점선 상자를 끼운다(세로 선 인디케이터는 그 상자로 대체 — `columnDropIndicator`는 쓰지 않는다). 히트 계산이 `[data-kanban-column]`만 보므로 그 상자는 자동으로 빠지고 `columnDropIndex`의 좌표계도 어긋나지 않는다. ⑤ **검색·보기 탭·필터를 문서 칩과 같은 선에** — 칩은 떠 있는 오버레이라 자리를 차지하지 않으므로 도구 줄이 칩 폭만큼 왼쪽을 비우고 같은 띠(높이 54)에 선다(모바일은 폭이 모자라 지금처럼 칩 아래로). ⑥ **필터 신설** — 댓글 아이콘 자리에 담당·분류·긴급만 보기·초기화·"N / M개 카드 표시 중"(첨부 이미지 구성). 계산은 순수 함수(`CardFilter`/`cardPasses`/`ownerOptions`)이고 **검색어와 같은 성격**이라 화면에서만 거른다 — 문서에도 협업에도 싣지 않는다(한 사람이 필터를 걸면 모두의 화면이 바뀌는 편이 훨씬 이상하다. 보기 모드와 같은 판단). 세 보기(보드·리스트·타임라인)에 함께 걸리고, 후보는 **지금 이 보드에 실제로 있는 것**만 담는다(담당=카드에 적힌 사람, 분류=문서의 분류 목록) — 눌러도 결과가 없는 칩은 없느니만 못하다. **곁들인 결정**: 아이콘 자리를 필터에 내주면 데스크톱에 보드 전체 댓글을 남길 길이 사라지므로 **보기 메뉴**의 구분선 뒤에 `보드 전체 댓글`을 뒀다(모바일 ☰은 예전부터 [보기 3종] + [댓글] 구성이라 두 화면이 같은 꼴이 된다) — 보기 **모드**는 여전히 셋뿐이다. 웹 +7 테스트 / 기존 7건 새 기대값으로 갱신(전체 1413), 실브라우저 검증(구간 순서 `c3(완료) → c2(진행)`·합 < 100% / 열·타일 그림자 동일 / 바닥 `rgb(253,251,249)` vs 열 `rgb(250,243,238)` / 칩·검색·탭·필터 중심선 전부 y=99 / 고스트가 제목+카드 3장·자리 308×429·원본 0개·드롭 후 `c2,c1,c3` 저장·undo 복귀 / 필터 5→2→1→5→긴급 1·Esc 닫힘 / 390px 가로 넘침 없음·패널 화면 안)
- ✅ **칸반 후속 4건(제보) — 호버가 한 번도 보이지 않던 진짜 원인 포함** ① **열 배경을 한 톤 옅게** — 디자인 원본의 층은 바닥 `#FDFAF7` < 열 `#FBF6F1` < 카드 `#FFFDFB`다. 바닥과 같은 방식으로 관계를 적어(`mixHex(panel2, panel, 0.25)`) 테마가 바뀌어도 순서가 유지된다(실측 `rgb(253,251,249)` → `rgb(251,246,242)` → 흰색). ② **열 추가 타일 길이** — 원본은 이 타일을 띠 전체 높이로 늘린다(`align-self: stretch`): 열보다 짧으면 "여기에 열이 하나 더 선다"로 읽히지 않는다(실측 339 → 637px). ③ **호버 반응** — 제보는 칸반이었지만 원인은 **에디터 전체**였다: 이 앱의 버튼은 색이 테마에서 오므로 `background`를 전부 **인라인**으로 주는데 인라인은 클래스 규칙을 이기므로 `.mf-ed-btn:hover { background: … }`가 **적용된 적이 없었다**. 대신 `::after`가 글자색(`currentColor`)을 7.5% 덮는 **덧칠 레이어**로 바꿨다 — 배경이 무엇이든(투명·패널·강조색) 같은 규칙 하나로 반응하고, 강조색 버튼은 글자가 흰색이라 **밝아지며**(원본의 `brightness(1.04)`와 같은 방향) 보통 버튼은 살짝 어두워진다. 배선: GNB 편집·보기·도움말·공유·내보내기 / 열 머리 ＋·⋯ / 열 하단 카드 추가 / 열 추가 타일 / 카드(원본대로 `translateY(-2px)`+그림자, **선택된 카드는 제외** — 선택 링이 호버 그림자에 지워지면 무엇을 고른 상태인지 사라진다) / 상세 팝업 삭제(경고색 `mf-ed-danger`)·닫기 / 리스트·타임라인 행. 요청 목록에 없던 필터 버튼·보기 탭도 함께. ④ **카드 시작일** — `KanbanCard.start`(`YYYY-MM-DD`, 값이 있을 때만 저장 — 카드는 통째로 왕복하는 엔티티라 직렬화·CRDT 무변경). 상세는 담당을 한 줄로 올리고 그 아래에 [시작일][기한]을 반씩 나눈다(셋을 한 줄에 몰면 아바타가 낄 폭이 남지 않는다). **타임라인 막대가 시작일부터 기한까지** 그려지고(`timelineSpan`이 카드를 받아 기준점을 정한다), 시작일이 없으면 예전처럼 오늘부터 — 나중에 더한 필드라 적지 않은 카드가 대부분인데 그때도 "지금부터 그날까지"가 뜻이 통한다. 코어 왕복 테스트에 `start` 포함 / 웹 +7 테스트(전체 269 + 774), 실브라우저 검증(층 3색·열 추가 높이·**호버 13곳 전수**(덧칠 opacity 0 → 0.075 / 카드 transform·그림자 / 삭제 `rgb(156,139,126)` → `rgb(192,83,46)` / 행 배경 `rgba(46,42,38,.035)`)·시작일 저장본·타임라인 막대)
- ✅ **칸반 열 메뉴 정돈(제보: 메뉴 안이 구분되지 않는다)** 열 ⋯ 메뉴의 세 항목은 성격이 다른 묶음이라 **구분선과 구획 이름**으로 갈랐다 — `이름 변경`(즉시 실행) ── `열 색`(고르는 판) ── `열 삭제`(파괴적). 행의 문법은 GNB 드롭다운과 같다(아이콘 + 라벨 + `mf-ed-btn` 호버, 삭제는 `mf-ed-danger`라 호버에서 경고색). 새 스타일을 만들지 않고 `MenuDivider`·`MenuSectionLabel`을 **export해 그대로 가져다 썼다** — 앱 안의 메뉴가 한 언어를 쓰고, 나중에 메뉴 문법을 손볼 때 한 곳만 고치면 된다. 곁들여 둘: 스와치를 `flex-wrap` 대신 **9열 그리드**로(wrap에 맡기면 7+2처럼 어중간하게 접혀 어느 색인지 눈이 헤맨다 — 속성 패널 색 줄과 같은 규칙), 지정 색이 없으면 **'기본 색' 칸이 활성**으로 보인다(지정이 없으면 열 순서대로 팔레트를 따르는데 그동안 아무 칸도 활성이 아니라 무엇이 걸려 있는지 알 수 없었다). 웹 +1 테스트(구분선 2개·구획 이름·순서·호버 클래스·기본 색 활성), 실브라우저 검증(구분선 2개 실측·메뉴 폭 236·스와치 18px 한 줄·이름 변경 호버 덧칠 0 → 0.075·삭제 호버 `rgb(192,83,46)`·색 적용/활성 표시/이름 변경 진입 무회귀)
- ✅ **칸반 열 삭제 확인창(요청)** 열 ⋯ 메뉴의 `열 삭제`가 바로 지우던 것을 **한 번 묻고** 지우게 바꿨다 — 열을 지우면 **안의 카드도 함께** 사라진다(되돌릴 수는 있지만 메뉴를 잘못 눌러 여러 장이 사라지는 일은 막아야 한다). 홈의 폴더 삭제 확인창과 같은 결로 **무엇이 몇 장 사라지는지 문장으로 밝히고**(`이 열에 있는 카드 2장도 함께 삭제돼요`) 되돌릴 수 있다는 것도 함께 말한다(`실행 취소(Ctrl+Z)로 되돌릴 수 있어요`). **빈 열도 확인을 거친다** — 잃을 게 없다고 건너뛰면 같은 버튼이 상황에 따라 다르게 동작해 예측이 안 된다(문구만 `이 열에는 카드가 없어요`로 바뀐다). **초점은 취소에** 둔다 — 파괴적 버튼이 기본 초점이면 Enter 한 번에 지워진다. Esc·배경 클릭으로도 닫히고, 취소하면 아무것도 바뀌지 않는다. 모바일 44px 터치 타깃. 웹 +3 테스트(취소하면 그대로·문구 / 확인하면 열과 카드가 함께 사라지고 undo 한 번으로 복구 / Esc·빈 열 문구) + 기존 열 삭제 테스트를 확인창 경유로 갱신(칸반 59), 실브라우저 검증(확인창이 뜬 동안 열·카드 불변·초점 취소·메뉴 닫힘 / 취소·Esc·배경 클릭 무변경 / 빈 열 문구 / 확인 → 저장본 `['c2']`·카드 0 → undo 복구 / 390px 폭 342·버튼 44px)
- ✅ **칸반 가독성 정돈(질문: 폰트는 같은데 왜 원본보다 도드라져 보이나)** 폰트는 같았다 — 칸반에는 자체 `fontFamily` 지정이 한 곳도 없고 에디터 루트의 `Pretendard, 'Pretendard-fallback', system-ui, sans-serif`를 그대로 상속한다(Pretendard는 self-host 가변 폰트). 실측해 보니 원인이 셋이었다. ① **글자 안티에일리어싱** — 디자인 원본은 `body`에 `-webkit-font-smoothing: antialiased`를 거는데 우리 앱은 **랜딩에만 있고 본체에는 없었다**(계산값 `auto`). macOS 크롬·사파리에서 `auto`는 서브픽셀 렌더링이라 같은 폰트·같은 굵기라도 획이 더 굵게 그려지고, 차이는 **작고 굵은 글자**에서 가장 크다 — 제보가 지목한 자리(10.5px/700 분류, 11.5px 메타, 13px/600 카드 추가)가 정확히 그것이다. 전역에 `antialiased` + `-moz-osx-font-smoothing: grayscale`(에디터·홈 전체에 걸리는 변경 — 원인이 앱 전역 설정이었다). ② **분류 배지 글자색** — 원본은 분류마다 색쌍을 손으로 골랐고 글자는 언제나 채도를 죽인 짙은 색인데(개발 `#EAF3F6` 배경에 `#3F6C7C` 글자), 우리는 이름에서 뽑은 팔레트 **원색을 그대로 글자에** 썼다. 신설 `tagInk(color, textColor)`가 색조를 유지한 채 명도만 낮춘다(`mixHex(c, th.text, 0.45)` — 실측 `#3f8fd0` → `#3a6180`으로 원본과 근접, `#d92626` → `#8e2723`). 배경 틴트는 그대로라 색 구분은 유지된다. ③ **기한 굵기** — 원본 메타 행은 `11.5px` + 굵기 지정 없음(=400)인데 우리는 500이었고 오늘·내일/지남이면 700까지 갔다 → 400, 강조 상태 600(보드·리스트 두 곳). 댓글 수는 원래 400이라 ①만으로 해결. 곁들여 열 **안쪽** 구분선(머리 아래·바닥 위)을 바깥 테두리보다 한 톤 옅게(원본도 바깥 `#F0E6DC` / 안쪽 `#F2E8DF`로 갈라 둔다 — 실측 `#ecdfd5` → `#f3eae4`). 웹 +2 테스트(`tagInk` 순수 규칙 / 배지 글자가 원색이 아니고 기한이 400, 전체 780), 실브라우저 계산값 검증(스무딩·배지 색·기한 굵기·구분선). **미검증**: ①의 획 두께 변화는 macOS에서만 뚜렷하다 — 이 환경(리눅스)에서는 선언이 적용됐는지만 확인했고 실기기 확인이 필요하다
- ✅ **칸반 후속 6건(제보)** ① **열 추가 타일** — 띠 높이를 따르되 **화면의 절반까지만**(`maxHeight: 50%`, 실측 695 → 339px), 호버는 시안대로 코럴(배경 틴트 + 코럴 테두리·글자, `.mf-kb-addcol`). 공용 덧칠(`.mf-ed-btn::after`)은 글자색(회색)을 덮는 규칙이라 이 자리엔 약하다 — 원본은 "여기 누르면 열이 하나 더 선다"를 **강조색**으로 말한다. ② **열 배경색 신설** — `KanbanColumn.bg`(머리 점 색 `color`와 **따로** 고른다). 값은 카드 배경과 같은 **연한 틴트**뿐이다: 열 전체를 칠하므로 진한 색이면 그 안의 카드·글자가 통째로 안 읽힌다. 없애면 키를 지운다(빈 필드가 CRDT로 오가지 않게). 리스트 보기의 그룹 행에도 그 색이 나타난다. ③ **열 메뉴를 시안대로** — `이름 변경` ── `열 색상`(5열 격자, 고른 칸에 **체크**) ── `배경색` ── `열 삭제 · N개`(**함께 사라질 카드 수**를 밝힌다). 두 색 구획은 같은 격자(5열 × 28px)를 쓴다 — 칸 크기가 다르면 한 메뉴 안에서 색 고르기가 두 가지 물건처럼 보인다. '없음' 칸은 대각선이 이미 뜻을 말하므로 체크 대신 테두리로 활성을 표시. ④ **리스트 보기의 댓글 0도 표시**(예전엔 `comments > 0`일 때만 그렸다). ⑤ **리스트 면 층**을 원본과 같게(바깥 `#FFFDFB` / 헤더 `#FBF6F1` / 그룹 행 `#FDFAF7` / 구분선 `#F6EEE7`) — 보드에서 쓰던 층 계산을 `kanbanMeta`의 헬퍼로 모아(`boardSurface`·`columnSurface`·`innerLine`·`columnBg`) **세 보기가 같은 함수**를 쓴다. ⑥ **타임라인 막대가 그 열의 색**을 따른다(`hexA(열색,.16)` 배경 + `.45` 테두리, 글자는 `tagInk`로 눌러 옅은 막대 위에서도 읽힌다). **트레이드오프**: 지난 기한을 붉게 칠하던 표시가 막대에서는 사라진다(보드 카드·리스트의 기한 글자에는 그대로). 코어 무변경(모델 필드만) / 웹 +5 테스트(전체 269 + 785), 실브라우저 검증(열 추가 339/695·호버 3속성 전환 / 메뉴 문구·색 10칸·배경 8칸·체크 / 배경색 저장본 `bg:"#edf4fc"` / 리스트 `0` 표시·면 3층 실측 / 타임라인 막대 3개가 각 열 색)
- ✅ **홈 3건(제보·요청)** ① **칸반 썸네일이 실제와 달랐다** — 예전 썸네일은 열도 카드도 제목도 **전부 강조색(코럴) 틴트**라, 색 점으로 단계를 가르고 분류 배지가 색을 담당하는 실제 화면과 인상이 달랐다. 에디터 어휘로 다시 그린다: 위쪽 **진행 바**(완료부터 왼쪽으로, 열 색 구간 — 썸네일에서 "칸반"임을 가장 먼저 알리는 표식) / 열 머리의 **색 점 + 제목**, 배경은 사용자가 고른 `col.bg` 그대로 / 카드는 흰 면 + 테두리 + **분류 배지**(같은 `tagColor`·`tagInk` 규칙), 들어가는 만큼 쌓고 나머지는 `+N`(카드 높이가 분류 유무에 따라 달라지는 것까지 실제와 같게). 색은 에디터와 같은 **UI 테마**(고정 팔레트)라 카드와 화면이 같은 색으로 보이고, 이 자리만 쓰던 테마 미러(`THEME_PAL`) 의존이 하나 줄었다. ② **마퀴(드래그 사각형) 선택**(요청) — 빈 자리에서 끌면 **닿는** 카드를 한 번에 고른다(파일 탐색기 관례 — 완전히 감쌀 필요 없음). 계산은 순수 함수(`marquee.ts`의 `rectFrom`/`keysInRect`/`canStartMarquee`)로 두고 DOM은 훅이 읽어 넘긴다(에디터 `kanbanDrag`와 같은 결). **마우스에서만** — 터치에는 길게 누르기(선택 모드)가 이미 있고 손가락 드래그는 목록 스크롤이다. 카드·메뉴·버튼·입력 위에서 시작한 드래그는 마퀴가 아니라 **폴더로 끌어 넣는 기존 동작이 그대로**고, 5px 문턱을 넘겨야 시작하며(평범한 클릭과 구분), Ctrl/⌘·Shift를 쥐고 시작하면 기존 선택에 더한다. 사각형은 `pointer-events: none`이라 그 아래 카드가 hover·drop 대상을 잃지 않는다. ③ **Shift+클릭 앵커**(제보: C → B → A로 고르면 A를 고르는 순간 C가 빠진다) — 원인은 범위의 기준(앵커)으로 `selectedCard`를 쓰는데 Shift+클릭이 그 값을 **끝점으로 옮긴** 것: 두 번째 Shift의 범위가 B..A가 된다. 기준을 `selectAnchor`로 **분리**해 평범한 클릭·Ctrl 토글에서만 움직이게 했다(`selectedCard`는 계속 "지금 초점"이라 메뉴 대상은 그대로). 웹 +9 테스트(마퀴 순수 4 + 홈 통합 3 + 썸네일 1 + 앵커 회귀 1 — 회귀는 제보 순서 그대로, 전체 1446), 실브라우저 검증(썸네일 색 점 3·분류 배지·`+2`·진행 바 스크린샷 / C → B → A 3장 유지 / 빈 자리 드래그 사각형 → 닿은 카드 선택 → 놓으면 사각형만 사라짐 / 카드 위 시작은 마퀴 아님)
- ✅ **마퀴 선택 중 글자가 선택된 채 남던 문제(제보)** "다중선택 후 드래그하여 폴더로 이동 시 카드가 이동 된 후 브라우저 텍스트가 선택되어져 있다." 실브라우저로 재현해 범인이 **폴더 이동이 아니라 그 앞의 마퀴 드래그**임을 확인했다 (사각형을 끄는 동안 `window.getSelection()`이 `"\n☆\nA\nA\n☰\n☆\nB\nB\n☰\n☆\nC\nC\n☰\n"` — 카드 글자가 통째로 선택돼 있었고, 그 상태로 폴더까지 끌고 가니 이동이 끝난 뒤에도 파란 글자가 남았다). 이미 `pointermove`에 `preventDefault`를 걸고 있었지만 **글자 선택은 그 아래 깔린 네이티브 마우스 동작이라 포인터 이벤트가 취소하지 못한다** — 멈추려면 `user-select: none`이어야 한다(칸반 카드 드래그에서 같은 결을 `touchmove`로 이미 겪은 계열). 수리: 누른 순간 `document.body`에 `mf-noselect`를 걸고(본문 전체 `user-select: none !important`) 놓을 때 벗기면서 **남아 있던 선택까지 비운다**(`removeAllRanges()` — 누르기 전에 이미 있던 선택도 함께 정리). 드래그가 취소되거나(`pointercancel`) 끌던 채 페이지가 언마운트돼도 클래스가 남지 않는다(기존 `finish`/안전망 경로 공유). 웹 +2 테스트(끌는 동안 클래스가 붙고 놓으면 떨어진다 — **수리 전 실패 확인**), 실브라우저 검증(마퀴 중 선택 문자열 "" · 놓은 뒤 "" · 폴더 이동 후 "" · `mapFolders` 두 카드 모두 이동 · 클래스 잔존 없음)
- ✅ **홈 2건(제보·요청)** ① **상위 폴더로 옮긴 카드가 다중 선택된 채 남았다** — 여러 장을 골라 상위 폴더 타일로 끌어 올린 뒤 그 폴더로 올라가 보면 옮긴 카드들이 그대로 고른 상태였다. 원인은 타일이 **단일 경로**(`moveMapUp`)를 키마다 부른 것 — 폴더 카드 드롭·메뉴 이동이 쓰는 일괄 경로(`moveMapsToFolder`)는 선택을 비우는데 그 길만 빠져 있었다. 신설 `moveMapsUp(keys)`로 모아 **상태 변경도 한 번**이고 선택도 함께 끝난다. 같은 이유로 단일 폴더·스페이스 이동도 선택을 비운다 (옮긴 카드는 이 목록에서 사라진다 → 선택도 끝난다 — #443이 일괄 경로에만 놓았던 규칙을 모든 이동 경로로 넓혔다). ② **문서 종류별 카드 테두리 색**(요청) — 예전엔 화이트보드·칸반이 **같은 파랑**(`--mf-info`)을 쓰고 마인드맵은 중립 경계선이었다. 셋을 갈랐다: 마인드맵 초록 `#3f9e6a`(다크 `#5ec38b`) · 화이트보드 파랑 `#3f8fd0`(기존 값 그대로 — 배포된 인상이 바뀌지 않는다) · 칸반 보라 `#8a63d2`(`#a98ae6`). 색은 **테마를 따르지 않는다** — "무엇을 강조하는가"가 아니라 **무엇인가**를 말하는 표식이라(알림 배지 #376과 같은 판단) 밝은 다섯 벌이 같은 값을 쓰고 다크만 어두운 면에서 죽지 않게 한 단계 밝힌다(`docMap`/`docBoard`/`docKanban` → `--mf-doc-*`). 선택 표시(강조색 2px + 글로우)와는 굵기·글로우로 갈리고, **열 수 없는 카드**(Drive 데모의 비지원 파일)는 중립 경계선 그대로다 (회색 카드에 종류 색만 남으면 "열리는 맵"처럼 보인다). 웹 +2 테스트(선택 잔류 회귀 — **수리 전 `expected 0, received 2` 실패 확인** / 세 색 계약), 기존 기대값 2건 갱신(전체 1448). 실브라우저 검증(두 장 선택→타일 드롭→위 폴더에서 선택 0·`mapFolders` 둘 다 이동 / 세 종류 테두리 실측 밝은·다크 스크린샷 / 선택 시 2px 강조색으로 덮임)
- ✅ **화이트보드 디자인 이식(Claude Design `Geurio 화이트보드.dc.html`)** 새 디자인을 에디터 크롬 전반에 이식(원본은 저장소 루트에 보존 — 수정 금지 대상, 22MB 번들 HTML의 `__bundler/template`에서 추출). **설계의 핵심은 값을 컴포넌트마다 베끼지 않은 것**이다 — 신설 `editor/chrome.ts`에 토큰 네 가지만 둔다(유리질 카드=반투명+블러, 아래로 깔리는 그늘, 세로 그라디언트 강조 버튼, 등폭 글꼴). 색은 고정 헥스가 아니라 **테마 팔레트에서 파생**해 다크·모노에서도 성립한다. **바뀐 것**: 도구 막대=알약 + 원형 버튼(38/44), 켜진 도구는 강조색 그라디언트 + 아래 점, 색·굵기는 막대에 줄을 더하지 않고 **위에 뜬 팝오버**(꼬리가 그 도구 버튼을 가리킨다 — 위치를 실측해 옮기므로 도구가 늘어도 값을 손볼 일이 없다) / 독칩·줌·미니맵·속성 패널·댓글 패널·피드백 알약이 전부 유리질 카드로 통일, 미니맵은 가라앉은 면의 자기 칸(구분선)이 되고 배율은 등폭이며 **누르면 100%로**(`zoomReset` 신설) / 속성 패널 260폭·머리에 색 조각 + [무엇을 고르고 있는가 / 이름]·구획은 ▸ 글자 대신 회전 셰브론 + 접힌 채로도 읽는 등폭 값 / 메모 카드는 라운드 14 + 위쪽 흰 하이라이트·긴 그늘에 호버 시 **복제·삭제 빠른 동작**(터치는 고른 동안) / 캔버스는 도트 격자 위에 위쪽에서 스며드는 흰 빛. **모바일**은 원본에 없어 같은 시각 언어를 폰 구조에 입혔다(전폭 알약·44px 원형·그라디언트 활성) — 좁은 폭 때문에 넣은 **메뉴 전환(펜·삽입)은 그대로**다(한 줄에 여덟 버튼은 들어가지 않는다). **원본과 일부러 다르게 둔 셋**: 선택 링은 강조색 유지(원본은 파란 아웃라인이지만 도형·선·영역이 전부 강조색이라 메모만 파랗게 두면 "선택"이 두 색으로 갈린다), 메모의 작성자 아바타·시각은 제외(객체별 작성자를 저장하지 않는다 — 문서 단위 `updated_by`만 있다), 공유·피드백 모달은 범위 밖(홈과 함께 쓰는 컴포넌트라 따로 다룬다). 웹 +3 테스트(원형 버튼·그라디언트 활성·아래 점 / 팝오버가 막대 **밖 위쪽** / 메모 빠른 동작), 전체 1451 통과. 패널 머리 DOM이 바뀌어 테스트 헬퍼가 태그 모양에 기대던 것을 `data-props-panel`·`data-panel-name` 표식으로 교체. 실브라우저 검증(1440×900 · 390×780 — 도구 막대·팝오버 꼬리·속성 패널 머리·댓글 패널·메모 그립·캔버스 빛 스크린샷, 콘솔 오류 0)
- ✅ **디자인 후속 3건(요청)** ① **그리기·형광펜 색 추가** — 디자인 원본의 잉크 목록에서 가져와 5 → **8색**(펜 +바이올렛·틸·브라운 / 형광펜 +라벤더·민트·코랄). **앞 다섯은 그대로 둔다** — 값을 원본으로 갈아 끼우면 이미 그려 둔 획의 색과 스와치가 어긋나 그 획을 골랐을 때 어느 칸도 켜지지 않는다. 폰에서는 여덟이 한 줄에 안 들어가(실측 390px에서 넘침) **색 묶음만 가로 스크롤**하고 굵기·‹는 자리를 지킨다(`.mf-ed-vp`가 `touch-action: none`이라 이 묶음에서만 `pan-x`로 되살린다 — 실측 `scrollWidth 268 > clientWidth 208`). ② **접속자 표시를 원본과 동일하게** — 캔버스 위에 떠 있던 알약("N명 접속 중")을 걷어내고 **상단 바 오른쪽 묶음 앞**에 얼굴을 겹쳐 세운다(`PresenceAvatars` 신설 — 26px·-7px 겹침·2px 패널 테두리). 색은 피어 색 하나에서 **파스텔 면 + 진한 잉크** 한 쌍을 만든다(원본이 손으로 고른 색쌍 `#F5C9A8`/`#8A4A22`와 같은 관계이면서 커서·원격 선택이 쓰는 그 사람의 색과 이어진다), 넷 이상이면 `+N`, 인원수는 얼굴이 이미 말하므로 글자는 접근 이름에만. `PresenceBar`는 얼굴만으로 알 수 없는 둘(**끊김**·**보안 경고**)만 맡는다. ③ **공유·피드백 모달**(#471에서 남겨 둔 것) — 452폭·라운드 22·아래로 깔리는 그늘, 짙은 dim + blur(3px), 주 버튼(초대·보내기)은 그라디언트 + 색 그림자. 홈과 함께 쓰는 컴포넌트라 색은 **테마 프롭의 강조색에서 직접** 만든다(에디터 팔레트에 묶이지 않게). 웹 +2 테스트(팔레트 8색 + 폰 스크롤 계약 / 아바타가 상단 바 안에 있고 떠 있는 알약이 아니다) + 기존 presence 단정 5건 갱신(전체 1452), 실브라우저 검증(펜·형광 8색 실측, 폰 색 묶음 스크롤·`pan-x`, 2탭 협업에서 GNB 아바타 색쌍 실측·알약 부재)
- ✅ **댓글 좋아요·팝업 디자인·Enter 등록 + 피드백 모달(요청 5건 중 4건)** ① **해결 → 좋아요**: 해결 표시 UI(스레드 태그·"해결" 버튼·접힌 구획)를 걷어내고 좋아요를 넣었다 — 스레드는 이제 하나의 목록이고 배지도 전부를 센다. 저장은 **0028 `comment_likes`**, **한 사람의 한 표 = 행 하나**다: `likes int` 하나를 올렸다 내렸다 하면 누가 눌렀는지 몰라 토글이 불가능하고 둘이 동시에 누르면 read-modify-write로 한쪽이 사라진다(반응(reactions)에서 이미 같은 이유로 항목 모델을 골랐다). RLS는 "그 댓글을 읽을 수 있는 사람이 읽고 **자기 표만** 넣고 뺀다"(`can_read_comment` SECURITY DEFINER). 미적용 서버에서 좋아요 읽기가 실패해도 **목록은 뜬다**(곁다리 — try/catch로 0). `resolved` 서버 컬럼·포트는 되돌리기 쉽게 그대로 둔다. ② **팝업 디자인**(디자인 원본): 작성자 얼굴(이름 시드로 정한 파스텔 면 + 진한 잉크 — 같은 사람은 늘 같은 색, 접속자 아바타와 같은 결), 시각은 등폭, 답글·좋아요는 알약 버튼, 답글은 얼굴 열에 맞춰 들여쓴다. ③ **Enter = 등록 / Shift+Enter = 줄바꿈**(요청) — 다만 **폰은 예전 그대로 Enter가 줄바꿈**이다(소프트 키보드에는 Shift가 없어 Enter를 등록으로 쓰면 여러 줄을 쓸 방법이 사라진다). 판정은 #333과 같은 훅("이 기기가 터치인가"가 아니라 "지금 Shift 없는 키보드로 치고 있는가"), Ctrl/⌘+Enter도 유지. ④ **피드백 모달**을 원본 꼴로: 아이콘 칩 머리 + 제목/설명, **4열 카드**(원본 도형 그대로의 아이콘 + 라벨, 고른 갈래에 따라 입력 안내 문구가 바뀐다), 라운드 14 입력, 글자수, 취소:보내기 = 1:1.6 알약. **넣지 않은 것**: "현재 화면 스크린샷 첨부" — DOM 캡처 라이브러리가 의존성에 없다(눌러도 아무 일 없는 체크박스보다 빼는 편이 낫다). 웹 전체 1452 통과(해결 테스트를 좋아요 테스트로 교체, 어댑터 스텁 기대값 갱신), 실브라우저 검증(해결 버튼 부재·좋아요 토글 0→1·Enter 등록이 저장소에 남음·패널/모달 스크린샷). **미검증**: 0028 RLS 실동작은 라이브 Supabase가 막혀 유닛/통합으로만 확인. **다음**: 댓글을 캔버스 **객체**로(Figma 방식 — 문서에 핀 목록을 순수 추가하고 실제 댓글은 그 핀 id를 대상으로 지금 표에 그대로 저장 = 서버 변경 없음, 핀 선택 시 기존 팝업, 개수 배지, 댓글 0개면 핀 소멸)
- ✅ **댓글을 캔버스 객체로 — 댓글 핀(요청, Figma 방식)** 코어에 `Doc.commentPins`(`{id,x,y}`) 순수 추가 — 획·반응과 같은 규칙으로 **비어 있지 않을 때만** 직렬화·CRDT 전파(옛 저장본·골든 무변경). **설계의 핵심: 서버는 한 줄도 바뀌지 않는다** — 핀은 **자리만** 들고 말은 지금까지처럼 서버 `comments` 표에 **핀 id를 대상(node_id)으로** 저장된다(마이그레이션·RLS 변경 없음, 0022 알림 딥링크도 그대로). 본문이 오갈 때 댓글 내용이 딸려 다니지도 않는다. **동작**: 배경 우클릭 `댓글 추가`·화이트보드 도구 막대 삽입으로 꽂으면 곧바로 그 핀의 **기존 댓글 팝업**이 열리고, 핀에 **댓글 수 배지**가 붙으며, 다른 객체처럼 고르고 끌어 옮긴다(좌표는 원본 기준 재계산 — 누적하지 않는다). **댓글이 0개인 핀은 살아남지 않는다**(첫 댓글 없이 닫거나 마지막 댓글을 지우면 소멸, 작성 중인 핀만 예외). **함정**: 커밋 판정·undo 스냅샷·자동저장 서명에 `commentPins`를 배선하지 않으면 커밋은 도는데 "바뀐 게 없다"로 판정돼 **핀 변경이 조용히 버려진다**(획·칸반에서 겪은 계열 — 첫 구현이 정확히 이것 때문에 화면에 안 떴고 테스트가 잡았다). 코어 269 / 웹 +2(핀 생성→팝업→저장 대상이 핀 id·개수 배지 / 빈 핀 소멸) 테스트, 전체 1454 통과. **미검증**: 실브라우저 확인은 이번 세션 컨텍스트 소진으로 못 했다 — 핀 크기·물방울 모양·드래그 감각은 배포 후 확인 필요
- ✅ **댓글 핀 후속 9건(제보·요청)** 배포된 댓글 핀을 실제로 써 보고 나온 아홉 가지. ① **핀이 저장되지 않던 문제** — 뿌리는 "빈 핀 정리" effect가 `comments`/`commentsLoading`을 **렌더 클로저**로 읽은 것이다: 문서를 열면 목록을 부르는 effect가 먼저 돌고 정리 effect는 **같은 커밋의 낡은 값**(댓글 0개 + 로딩 아님)을 보므로, 저장돼 있던 핀이 열자마자 전부 고아로 판정돼 지워지고 그 상태가 자동저장됐다. 정리를 **목록을 막 받은 자리**(`reloadComments`의 성공 분기)로 옮겨 목록과 핀이 같은 순간의 값이 되게 했다 — 조회 실패에서는 아무것도 지우지 않는다(못 읽은 것과 없는 것은 다르다). ② 핀 아이콘을 팝업 머리와 **같은 말풍선**(`CommentIcon`)으로. ③ **핀을 옮길 때마다 목록을 다시 읽던 것** — 드래그 끝의 click이 그대로 "열기"로 읽혔다. 4px 슬롭으로 끈 뒤의 click을 삼킨다. ④ **도구 막대의 댓글을 삽입이 아니라 도구로**(Figma 방식, 단축키 C) — 켜면 커서가 댓글 아이콘이 되고 **누른 자리**에 첫 댓글 말풍선(`CommentDraftBubble`)이 뜬다. ⑤ 그래서 **문서에는 아무것도 넣지 않는다**: 한 마디를 저장해야 비로소 핀이 들어가므로(`submitCommentDraft` — 댓글 저장 성공 뒤 커밋) 쓰지 않고 다른 곳을 누르면 **지울 것도 없다**(취소로 undo가 더럽혀지지도 않는다). ⑥ 핀에는 이름이 없어 팝업 부제로 할 말이 없다 — "사라진 대상"을 지우고 **부제 자체를 뺐다**(칸반 카드·보드 전체 댓글만 부제를 갖는다). ⑦ 팝업이 **그 핀 옆**에 뜬다(`commentAnchor` — 문서 좌표 → 화면 좌표 계산을 팝업과 말풍선이 공유, 오른쪽이 좁으면 왼쪽으로 넘기고 화면 안으로 clamp. 간격은 핀 폭보다 넓어야 걸터앉지 않는다 — 실브라우저에서 40px로 확정). ⑧ **기존 객체의 댓글 제거** — 주제·메모·선·영역의 우클릭 항목·개수 배지·보기 메뉴 '댓글'을 걷어냈다(칸반 카드 댓글과 '보드 전체 댓글'은 그대로 — 칸반에는 캔버스도 핀도 없다). 보기 전용에서 객체 위 우클릭은 이제 **아무 메뉴도 열지 않는다**(항목이 전부 변이라 빈 메뉴가 된다 — 그들은 꽂혀 있는 핀을 눌러 논의한다). 옛 데이터는 지우지 않고 그냥 드러나지 않는다. ⑨ 메모 위 복제·삭제 빠른 동작 제거(같은 동작이 우클릭·단축키·모바일 선택 바에 이미 있다). 웹 테스트 1457 통과(댓글 테스트 전체를 핀 흐름으로 재작성 + 회귀 3건: 리로드 후 핀 생존·드래그 시 목록 무재요청·초안 취소), 실브라우저 검증(보드·맵 양쪽: 도구 커서 데이터 URL·클릭 자리 말풍선·미작성 취소·첫 댓글→핀 저장→리로드 생존·팝업 핀 옆 40px·같은 아이콘 path·드래그 후 팝업 안 열림·메모/주제 메뉴에 댓글 없음·메모 그립 0) **후속(프리뷰 확인 5건)**: ① 팝업이 핀을 **가리던** 것 — 개수 글자가 핀 본체를 늘려서 그 폭을 렌더 전에 알 수 없었다. 개수를 본체 **밖 배지**로 빼 폭을 상수(`COMMENT_PIN_W`)로 만들고, 간격을 `핀 폭 × 줌 + 12`로 잡아 확대해도 겹치지 않는다(오른쪽이 좁으면 왼쪽으로 넘기고, 좌우 어디에도 자리가 없으면 핀 **아래**로 비킨다). ② 첫 댓글을 남기면 **선택 도구로 돌아온다** — 댓글 모드에 머물면 방금 만든 핀을 만지려는 클릭이 또 새 초안을 띄운다. ③ 커서를 **꽂히는 핀 그대로**(같은 물방울 + 같은 말풍선 path, hotspot이 꼬리 끝이라 커서가 가리키는 자리에 핀이 선다) — 앱 아이콘·핀·커서가 `COMMENT_GLYPH` 한 문자열을 공유한다. ④ 초안 핀과 확정 핀의 모양·크기를 `commentPinShape` 한 곳으로 합쳤다(차이는 배지뿐). **초안은 화면 좌표라 배율이 안 걸려** 확대한 캔버스에서만 작게 보였는데(실측 34 vs 43), 줌을 곱해 맞췄다. ⑤ 배경이나 다른 객체를 고르면 **팝업이 닫힌다**(선택이 핀을 벗어나면 그 논의가 무엇에 딸린 것인지 흐려진다 — 칸반은 대상이 카드라 제외). 웹 +4 테스트(전체 1461), 실브라우저 재검증(초안=확정 43×43·커서 path·작성 후 도구 복귀·개수 3에도 본체 폭 불변·겹침 0·가장자리 핀에서 왼쪽 flip·배경/메모 선택 시 닫힘)
- ✅ **댓글 → 스레드(시안 이식) + 도구 즉시 복귀** 프리뷰 확인 후 받은 시안 2장과 요청 3건. ① **자리를 정하는 순간 손이 선택 도구로 돌아온다**(요청) — 예전엔 "첫 글을 남긴 뒤"였는데, 쓰든 말든 한 번 놓았으면 다음 클릭은 "또 하나 꽂기"가 아니라 평범한 조작이다(Figma도 같다). `startCommentDraft`가 `setBoardToolState('select')`를 직접 부르고, 사용자가 **도구를 바꿀 때만** 초안이 접히도록 취소를 `setBoardTool` 안으로 옮겼다(예전의 "boardTool이 comment가 아니면 초안 취소" effect는 이 흐름에서 자기 초안을 즉시 지운다 — 그래서 제거). ② **핀 = 하나의 스레드**(시안 ①): 꽂힌 핀에는 **첫 글을 쓴 사람의 얼굴**이 박히고(이름 시드 파스텔 — 스레드 목록·접속자 아바타와 같은 색 규칙), 우상단에 글 수 배지, 해결하면 그 자리를 **초록 체크**가 대신한다. 팝업 머리는 `스레드 · 개수 · ✓해결 · ⋯ · ✕`이고 하단 입력은 언제나 **그 스레드의 답글**이다(새 논의는 핀을 새로 꽂는다 — 여러 논의를 담는 것은 칸반 카드뿐이라 그쪽만 예전 목록 모드). **해결을 되살렸다** — 시안에 있고 서버 컬럼(0021)·포트·로컬 어댑터가 그대로 살아 있어 UI만 붙였다. ⋯는 '스레드 삭제'(각 글의 '삭제'는 그 글만) 하나 — 파괴적 동작을 닫기(✕) 옆에 상시 버튼으로 두면 잘못 눌린다. 입력은 라운드 12 + 포커스 링, 아래 줄에 내 얼굴과 `⌘ + Enter 로 등록`. ③ **초안 말풍선**(시안 ②): 머리에 내 얼굴과 "이름 · 새 댓글", 하단은 `취소`/`남기기`. 핀·커서는 **채운 말풍선**(`FILLED_BUBBLE_PATH`) 하나를 공유해 손끝의 그림과 꽂히는 물건이 같다. **넣지 않은 것**: 시안의 이미지 첨부 버튼 — 그 기능이 없어 눌러도 아무 일이 없다. 용어도 캔버스 쪽만 '스레드'로(도구·배경 메뉴·도움말·팝업 접근 이름), 칸반은 카드에 붙는 '댓글' 그대로. 웹 1461 통과(해결·모양 테스트를 새 모델로 재작성), 실브라우저 검증(도구 즉시 복귀·초안 말풍선 구성·핀 얼굴·머리 구성·해결→체크·⋯ 메뉴·답글 후 개수 2·커서 path 일치) **후속(시안 3장, 프리뷰 확인)**: 두 상태를 실제로 캡처해 시안과 나란히 놓고 고쳤다 — ① **선택하면 검은 링**이 얹혔다(브라우저 기본 포커스 외곽선). 고른 핀은 강조색 몸통과 후광이 이미 말하므로 `outline: none`. ② 실루엣이 시안과 달랐다: `999px`(원)이라 위가 완전히 둥근 **아치**로 보였다 → **왼쪽 아래만 각진 둥근 사각**(`15/15/15/3`)으로, 그 꼭짓점이 지점을 가리킨다. 커서 SVG의 몸통도 같은 비율. ③ 고른 핀의 개수 배지가 강조색이라 **강조색 몸통 위에서 묻혔다** → 그때만 잉크색으로 뒤집는다(시안 ②). ④ 고른 핀에서 흰 테두리를 뺐다 — 몸통(코럴)·흰 테두리·얼굴의 흰 링이 겹쳐 **과녁**처럼 보였다(초안 핀은 배경이 무엇이든 도드라져야 해서 그대로 두른다). ⑤ 초안 핀·커서·도구 막대가 담는 글리프를 **말풍선 윤곽**(시안 ③) 하나로 통일(채운 말풍선 → 선). 웹 +1 테스트(전체 1462), 3배 확대 캡처로 미선택·선택·초안·커서·도구 막대를 시안과 대조
- ✅ **스레드 핀 시안 재대조(제보: "프리뷰가 이전과 동일하다")** 제보가 맞았다 — 직전 수정이 **시각적으로 사실상 아무 차이를 만들지 못했다**: 34×34 상자에서 `border-radius: 999px`는 실효 17px인데 새로 넣은 값이 15px이라 **2px 차이**였고, 나머지 변경(포커스 링·배지 색)은 고른 상태에서만 보이는 것이었다. 배포는 결백함을 먼저 확인했다(푸시된 sha에 변경 포함, 그 sha에 Vercel 배포 completed, **프로덕션 빌드 산출물에서 새 값 grep으로 확인**). 원인은 배포가 아니라 내 대조가 느슨했던 것 — 지난번엔 시안을 눈으로만 봤다. 이번에는 **시안을 5배로 확대**해 실렌더와 나란히 붙여 실차이 셋을 찾았다: ① **얼굴이 너무 작았다** — 시안의 얼굴/핀 폭 비율은 0.81인데 24/34=0.71로, 흰 여백이 넓어 얼굴이 작은 점처럼 보였다 → 28. ② **고른 핀의 얼굴** — 파스텔 얼굴 + 흰 링을 코럴 몸통 위에 두니 여전히 과녁이었다(흰 테두리만 뺀 것으로는 부족했다). 시안은 얼굴이 **반투명 흰 원 + 흰 글자**로 몸통에 녹는다 → `Avatar`에 `onAccent` 추가. ③ **말풍선 글리프가 둥근 사각이었다** — 시안은 옆으로 퍼진 **타원 + 왼쪽 아래 짧은 꼬리**다(초안 핀·커서·도구 막대가 한 path를 공유하므로 한 줄 교체로 셋이 함께 바뀐다). 덤으로 초안 핀의 흰 테두리를 뺐다(시안은 몸통 + 후광만). 실루엣 반경은 시안도 아치형이라 그대로 뒀다 — **이번 대조에서 지난번 "아치처럼 보였다"는 진단이 틀렸음이 드러났다**. 회귀 가드는 "보이는 차이"에 걸었다(얼굴 크기 ≥ 핀−8, 미선택 얼굴은 이름 색, 고른 얼굴은 흰 반투명·링 없음) — 되돌리면 실패함을 확인. 실브라우저 재캡처로 시안과 위아래 대조(미선택·선택·초안·커서·도구 막대). **교훈**: 시안 대조는 원본 해상도로 하면 2px 차이를 "고쳤다"고 착각한다 — 확대해서 비율을 재야 한다
- ✅ **스레드 후속 2건 + 미니맵 시안 이식(제보·요청 3건)** ① **첫 글을 남기면 그 스레드가 열린 채로 남는다** — 예전엔 핀만 선택되고 팝업은 닫혀, 방금 쓴 말을 보거나 답글을 달려면 핀을 **다시** 눌러야 했다(`submitCommentDraft`가 선택과 함께 팝업까지 연다). ② **팝업 아래 입력칸은 답글이 아니라 새 스레드 글**(제보) — 핀에서는 이 칸이 첫 글의 답글로 들어가 `답글` 버튼으로 남긴 것과 결과가 구별되지 않았다. 이제 아래 칸은 언제나 뿌리 글이고(문구도 `스레드 남기기`), 답글은 각 글의 `답글` 버튼이 여는 칸만 맡는다(그 문구는 `답글 남기기` 그대로) — 그래서 핀 하나가 여러 스레드를 담고 목록이 블록으로 갈린다. ③ **미니맵을 시안대로**: 카드가 260×148 지도 + 50px 흰 줄로 갈리고, 지도는 **여백 없이** 카드를 채우며(예전 7px 안쪽 여백 제거) 바탕에 **도트 격자**(15px 십자, `th.dot` 0.5)가 깔려 "캔버스의 축소판"으로 읽힌다. 메모는 회색 점이 아니라 **자기 색의 둥근 카드**(기본값은 시안의 한 톤 진한 노랑 `#ead893` — 캔버스 기본 `#fff6cf`는 이 크기에서 바탕에 묻힌다), 뷰포트 사각형은 **파랑**(`#7fa6e8` + 알파 0.10)이다: 시안이 코랄 테마인데도 이 사각형만 파랑인 것은 강조색("고른 것")과 뜻이 다른 표시("지금 보고 있는 자리")라서다. 아래 줄은 구분선을 없애고 [미니맵][−][배율][＋][맞춤] 다섯을 `space-evenly`로 펴고, 배율은 mono 11.5 → **14px/600**(시안 실측), 지도 바탕은 카드보다 한 톤 따뜻하게(`mix(panel, canvasBg, .35)` = 실측 `#fdf8f4`), 아이콘은 시안 글리프(둥근 사각+속 사각 / 자름표)로 교체했다. **시안 대조는 색을 픽셀로 뽑아서** 했다(카드 반경 14·도트 3px·바 높이 50·글자 `#4a443d`) — 원본 해상도로 눈대중하면 2px 차이를 고쳤다고 착각한다(직전 핀 작업의 교훈). 웹 +3 테스트(팝업 유지 / 아래 칸=뿌리 글 + 답글 버튼 대조군 / 미니맵 계약 8가지), 전체 1464 통과. 실브라우저 검증(첫 글 뒤 패널·핀 선택·도구 복귀, 저장된 두 글 모두 `parentId` 없음, 미니맵 시안 대조 캡처)
- ✅ **스레드 팝업이 내용만큼 자란다(제보)** 팝업 상한이 `420px` 고정이라, 글이 조금만 늘어도 **그 안에서** 스크롤이 났다(화면에는 자리가 남아 있는데). 이제 높이는 내용이 정하고 상한만 **화면에서 나온다**(`vh - 여백`), 그래서 스크롤은 정말로 화면을 넘을 때만 생긴다. **자리 계산이 함께 바뀌어야 했다** — `anchoredBoxPos`에 넘기던 높이가 고정값이면 짧은 스레드는 필요 이상으로 위로 밀리고 긴 스레드는 화면을 넘으므로, `useLayoutEffect`+`ResizeObserver`로 **실측한 높이**를 넘긴다(페인트 전에 재므로 자리가 튀지 않고, jsdom에는 ResizeObserver가 없어 첫 실측만으로 동작). 그리고 **보드에서는 하단 도구 막대의 자리를 비워 둔다**(`bottomInset` 신설) — 그 막대는 z가 팝업보다 높아, 팝업이 화면 아래까지 자라면 입력칸을 덮는다(실측으로 발견: 팝업 bottom 668 < 막대 top 684로 교정). 스크롤바는 기본 10px + Windows 화살표 버튼이 팝업의 둥근 모서리를 가로질러 디자인을 깨므로 **얇은 알약 썸**(`.mf-cmt-scroll`, 평소 16%·호버 32%, 색은 인라인 테마를 CSS 변수로 내려 준다)으로 바꿨다 — 표준 속성은 `@supports not selector(::-webkit-scrollbar)`로 격리(#391의 교훈: 크롬 121+는 표준 속성이 있으면 웹킷 커스텀을 통째로 무시한다). 모바일 바텀 시트는 55dvh 그대로(관례). 웹 +2 테스트(상한이 화면 기반·height 미고정·스크롤 클래스/변수 / CSS 계약), 전체 1466 통과. 실브라우저 검증(xvfb headed — 짧은 스레드 281px 무스크롤 / 긴 스레드 576px 상한·스크롤바 폭 8px·화면 안·막대와 무겹침)
- ✅ **스레드 팝업 스크롤 포커스 2건(제보)** 스크롤이 생긴 팝업에서 ① `답글` 버튼을 눌러도 답글 칸이 **아래에 반쯤 가려** 어디에 쓰는지 보이지 않았고 ② 새 글을 남겨도 목록이 그대로라 **방금 쓴 글이 화면 밖**이었다. ① 답글 칸이 열리면 그 자리로 민다(`scrollIntoView({block:'nearest'})` — 이미 다 보이면 아무것도 움직이지 않는다), ② 내 제출이 끝나 목록이 늘어나면 **끝까지** 내린다(패널은 목록 자신이 스크롤하고, 카드 상세 모달은 바깥이 스크롤하므로 각각 `scrollTop`/`scrollIntoView`). **함정**: 끝으로 보내는 표시를 `addComment` **뒤에** 세웠더니 아무 일도 일어나지 않았다 — 그 함수가 돌아올 때는 목록이 이미 갱신돼 있어 효과가 다시 돌 계기가 없다(보내기 **전에** 세워야 한다). 표시는 **내 제출에만** 세운다: 남의 글이 실시간으로 도착할 때마다 읽던 자리가 끝으로 튀면 안 된다. 웹 +1 테스트(답글 칸 대상 확인 + 끝으로 이동 — jsdom은 레이아웃이 없어 `scrollTop` 대입이 0으로 되돌아가므로 **세터를 지켜본다**), 전체 1467 통과. 실브라우저 검증(글 12개로 스크롤을 만든 뒤: 화면에 걸친 스레드의 답글 칸이 목록 안에 완전히 들어옴 / 새 글 작성 후 `scrollTop+clientHeight = scrollHeight`로 끝 도달·마지막 블록에 그 글)
- ✅ **댓글/스레드 용어 혼용 정리(질문 → 조사 → 사용자 선정)** "용어가 혼용되는데 통일할 필요 없나"는 물음에 전수 조사했더니 **캔버스 쪽만 절반이 옛 이름**이었다: 도구·도움말·배경 메뉴·핀 배지·팝업 제목·아래 입력칸은 '스레드'인데, 같은 팝업의 닫기(`댓글 닫기`)·빈 안내(`아직 댓글이 없어요`)·글 삭제(`댓글 삭제`)·입력 접근 이름(`댓글 입력`)·초안 말풍선(`첫 댓글 남기기`/`새 댓글`)이 '댓글'로 남아 있었다. 선택지 셋을 제시해 **캔버스만 스레드로**(사용자 선정) — 칸반 카드는 핀이 없고 한 카드에 여러 논의가 붙는 목록이라 '댓글'이 맞고, 홈 알림 문구는 양쪽(핀·카드)에서 오므로 중립어가 필요하다. 구현은 갈림길을 **`thread`(핀인가) 하나**로 모았다: 팝업이 그 값으로 제목·닫기·빈 안내·placeholder를 고르고, 새로 뚫은 프롭 `inputLabel`(입력 접근 이름)·`pinMode`(글 삭제 접근 이름)를 목록·행에 내려 준다 — 두 자리를 같은 컴포넌트가 그리므로 문구를 하드코딩하면 어느 한쪽이 반드시 어긋난다. 어댑터 오류 문구는 컨텍스트를 알 수 없어 **중립어('글')**로 바꿨다(`글을 남기지 못했어요`). 웹 +1 테스트(팝업의 본문·접근 이름·placeholder를 통틀어 '댓글'이라는 낱말이 **하나도 없다**는 계약 — 낱말을 되돌리면 실패), 전체 1468 통과. 실브라우저 확인(스레드 팝업·초안 말풍선 모두 `hasComment: false`)
- 📌 **라이브 배포 절차(브랜치 지형 — 머지만으로는 안 나간다)** 이 저장소는 브랜치가 셋이다: 작업 브랜치(`claude/…`) → **기본 브랜치 `claude/mindflow-design-impl-iyxiol`**(PR이 머지되는 곳) → **`main`**(라이브 `geurio.com`이 배포되는 곳). PR을 머지하면 기본 브랜치까지만 올라가고 **`main`은 그대로 멈춰 있다** — 자동으로 따라가는 워크플로가 없다(`.github/workflows/ci.yml`이 전부고 main으로 푸시하는 잡은 없다). 그래서 "머지·CI·프리뷰 전부 초록인데 라이브는 옛 화면"이 된다(실제로 제보를 받고서야 알았다: `main`이 한 커밋 뒤인 채 이전 세션이 만든 판을 서빙하고 있었다). **라이브 반영 = `git push origin claude/mindflow-design-impl-iyxiol:main`**(fast-forward — 새 커밋 없이 이미 CI 통과한 같은 커밋을 가리키게만 하므로 내용 변화가 없고, 문제가 생기면 이전 sha로 되돌린다). 이 푸시는 **지정 브랜치 밖으로 나가는 동작**이라 사용자 확인을 받고 한다. 확인은 콘솔 스탬프 `[geurio] build <시각> (<sha7>)`로 — 라이브 sha가 방금 올린 커밋이면 반영된 것이고, 그래도 옛 화면이면 서비스 워커 캐시다(그 사이트 탭을 모두 닫았다 열거나 SW Unregister). 참고: 이 원격 세션에서는 `geurio.com`도 게이트웨이 403이라 **라이브를 직접 열어 확인할 수 없다** — 사용자에게 확인을 부탁하고, 침묵을 결론으로 쓰지 않는다(아래 항목과 같은 규칙). **덧붙여**: 머지 직전에 기본 브랜치가 **같은 기능을 다른 세션이 따로 구현한 판**을 이미 담고 있을 수 있다(이번엔 #480이 그랬다 — 파일 이름부터 `CommentDraftLayer` vs `CommentDraftBubble`이라 충돌 53곳). 그때는 **어느 판이 더 나아간 것인지**로 가르고(우리 브랜치가 그 9건 + 이후 시안·수정까지 담고 있었다), 상대 판에만 있는 것은 하나씩 골라 이식한다(핀 키보드 열기). 자동 병합이 **두 판을 동시에 import**해 놓는 경우가 있어 충돌 표시가 없는 파일도 눈으로 확인해야 한다(`Viewport.tsx`가 그랬다).
- 📌 **배포 확인 절차(에이전트 함정 기록)** 원격 세션의 프록시는 `vercel.com`·`*.vercel.app`을 막는다(응답 코드 000) — **배포 결과를 직접 볼 수단이 없다.** 그래서 GitHub에 남는 흔적(Deployments API·커밋 상태 `Vercel`·체크런 `Vercel Preview Comments`)으로 판단하게 되는데, 그 셋은 전부 **Vercel이 GitHub로 되보고하는 값**이고 **지연이 아주 클 수 있다**: 실측으로 어제는 푸시 후 **35초**였는데 다음 날 같은 브랜치에서는 **35분**이었다(빌드는 그전에 끝나 대시보드에는 완료로 보였다. 같은 시간대에 `Supabase Preview` 체크런도 함께 늦었다). 나는 30분간 0건인 것을 보고 **"배포되지 않았다"고 단정**해 불필요한 빈 커밋까지 푸시했고(되돌렸다), 그 전에는 `GET /commits/{sha}/status`의 `combined: pending`을 "빌드 중"으로 오독했다 — 그 값은 **상태가 0건일 때의 기본값**이다. **규칙**: 관측 가능한 것(GitHub Actions `verify`)과 관측 불가한 것(프리뷰 반영)을 갈라 보고하고, 간접 신호의 침묵을 결론으로 쓰지 않는다 — "확인할 수 없다"와 "되지 않았다"는 다른 말이다(RLS가 "행이 없다"와 "안 보인다"를 구분하지 못해 생긴 42501 사고와 같은 계열의 실수). 기다릴 때는 상태가 **non-pending으로 바뀔 때까지 폴링**해 두면 늦게 도착한 값도 놓치지 않는다
- ✅ **빌드 스탬프에 커밋 sha(제보 대응)** 콘솔 스탬프가 시각뿐이라 "지금 보고 있는 화면이 어느 커밋인가"를 물을 수단이 없었다 — 프리뷰는 **커밋마다 주소가 따로** 있어서 옛 주소를 열어 둔 탭은 영영 옛 빌드를 보여 주고, PWA가 에디터가 열린 동안 업데이트를 미루면 리로드해도 캐시된 셸이 그대로다(제보의 절반이 이 경로였다). `__BUILD_SHA__`를 배포 환경 변수(`VERCEL_GIT_COMMIT_SHA`/`GITHUB_SHA`)에서 받아 `[geurio] build <시각> (<sha7>)`로 찍고, 피드백 `meta.sha`에도 함께 실어 나중에 되짚을 수 있게 했다(로컬은 `dev`).
- ✅ **길게 쓴 답글로 스크롤이 가지 않던 문제(제보)** 원인 둘이 겹쳐 있었다 — ① **답글에는 표시를 아예 세우지 않았다**(새 스레드 글만 세웠다) ② 세웠더라도 "목록 끝으로"라 긴 글은 **꼬리**가 보인다(읽으려면 머리가 필요하다). 게다가 답글은 스레드 안에 끼어들어 **마지막 줄이 아니므로** 끝으로 가면 엉뚱한 자리다. 수리: 제출 **직전**의 id 집합을 남겨 두고 목록이 갱신되면 그중 없던 글이 내가 쓴 것이므로 그 글(`[data-comment-item]`)을 찾아 옮긴다 — 글이 보이는 높이보다 **길면 머리를 위에**(8px), 짧으면 아래가 걸리지 않을 만큼만. 자리는 **두 번** 잡는다(이 커밋 + 다음 rAF): 패널이 자기 높이를 재서 상태로 올리므로 첫 시점의 목록 높이가 최종이 아니고, 그러면 `scrollTop`이 옛 높이로 잘려 목표에 못 닿는다. 레이아웃을 잴 수 없으면(테스트) 예전처럼 끝으로. 웹 +1 테스트(긴 답글 → 머리 8px — **수리 전 `0`으로 실패 확인**), 프로덕션 빌드 실브라우저 검증(660px 답글: view 449·머리 오프셋 8·보임 / 짧은 새 글: 목록 안에 완전히 보임)
- ✅ **마인드맵 스레드 진입점(요청: "맵에서도 스레드를 쓸 수 있을까?")** 먼저 실브라우저로 확인해 보니 **기능은 이미 맵에서 동작했다** — 배경 우클릭 `스레드 추가` → 초안 말풍선 → 첫 글 저장 시 핀 생성 → 리로드 후에도 유지(핀·팝업·답글·좋아요·멘션·알림 딥링크는 문서 종류와 무관한 공용 경로다). 갈려 있던 것은 **진입 방식**뿐이었다: 도구 모드·단축키 **C**·도구 버튼이 하단 도구 막대(`isBoard` 가드)에만 있어 맵에서는 우클릭이 유일한 길이었다. 사용자 선정대로 둘을 더했다 — ① **삽입(⊕) 메뉴에 `스레드 추가`**(다른 삽입과 달리 문서에 바로 넣지 않는다: 화면 가운데 초안 말풍선이 뜨고 한 마디를 남겨야 핀이 들어간다. 모바일에서도 이 메뉴가 뜨므로 길게 누르기와 함께 두 갈래) ② **단축키 C로 도구 모드**(커서가 꽂히는 핀 그대로 바뀌고, 누른 자리에 초안 → 자리를 정한 순간 선택 도구로 복귀). **그리기 도구(P·H·E)는 화이트보드 전용 그대로**이고(맵에서 눌러도 아무 일 없음 — 회귀 가드 테스트), 켜 둔 도구를 끄는 **Escape는 전역 키에서 받는다**(보드에서는 도구 막대가 맡지만 맵에는 막대가 없다). 스레드 도구는 `canComment`일 때만 켜진다 — 켜 봐야 아무 일도 없으면 커서만 바뀐 채 먹통이 된다(보기 전용·스토어 없음). 단축키 도움말의 맵 '일반' 구획에도 C 한 줄. 웹 +3 테스트(삽입 메뉴·C 도구+Escape는 **구현 전 실패 확인** / 그리기 도구 비활성은 회귀 가드), 실브라우저 검증 5종(삽입 메뉴 → 초안·문서 핀 0 / C → 핀 커서 data URL·클릭 자리 초안·도구 복귀·저장본 핀 / Escape / P·H·E 무반응 / 도움말 C 줄)
- ✅ **스레드 삭제 2건(제보)** ① **⋯의 "스레드 삭제"가 글 하나씩 지웠다** — `removeComment(root.id)`로 **첫 뿌리 글 하나만** 지웠기 때문이다(아래 입력칸이 새 뿌리 글을 남기므로 핀 하나에 뿌리가 여럿 있을 수 있다). 신설 `controller.removeThread(nodeId)`가 그 핀의 **뿌리들을 모두** 지우고(답글은 0021 `on delete cascade`로 함께 사라진다) 목록 재조회는 마지막에 한 번만 한다. 남의 글이 섞여 있으면 그 글에서 실패할 수 있으므로(RLS: 작성자 또는 문서 소유자) 첫 오류를 돌려주고 확인창에 그대로 보여 준다. 그리고 **확인을 먼저 받는다** — 글 여러 개가 한 번에 사라지는데, 댓글은 본문이 아니라 별도 저장소에 살아서 **실행 취소로 돌아오지 않는다**(칸반 열 삭제 확인창과 같은 결이되 "되돌릴 수 있어요"를 말하지 않는 이유). 몇 개가 사라지는지 문장으로 밝히고 파괴적 버튼에 초점이 가지 않게 취소에 초점을 둔다. ② **다 지운 뒤 화면 우측에 옛 댓글 팝업이 남았다** — 핀이 정리돼도(`reloadComments`의 빈 핀 정리) `selection`은 여전히 그 핀이라 기존 "핀에서 벗어나면 닫는다" effect가 닫지 않았고, 팝업은 가리킬 자리를 잃어 **핀 없는 대상용 배치**(우상단, 칸반 카드·보드 전체용)로 밀려났다 — 사용자에겐 "지웠는데 다른 팝업이 떴다"로 보인다. 캔버스에서 논의는 핀에만 붙으므로(요청 ⑧) **가리키던 핀이 사라지면 팝업을 닫고 그 핀 선택도 지운다**(상대가 지운 경우·되돌리기도 같은 길). 캔버스의 옛 경로는 이것으로 닿을 수 없게 됐고, 우상단 배치와 부제는 칸반이 쓰므로 남긴다(`commentTargetLabel`은 이미 칸반 전용). **실브라우저에서 잡은 것**: 확인창을 패널 안에 두니 전체 화면 막이 **패널 크기로 오그라들었다** — 유리질 카드의 `backdrop-filter`가 `position: fixed` 자식의 기준 상자가 되고 `overflow: hidden`이 자른다. `createPortal`로 body에 그려 해결. 웹 +1 테스트(확인창 문구·취소 무변경·삭제 후 글 0·팝업/핀 소멸 — **수리 전 실패 확인**), 실브라우저 검증(글 3개 → ⋯ → 문구 "글 3개가 모두 사라지고…" → 취소 시 3개 유지 → 삭제 시 0개·팝업 없음·핀 없음·저장본 `commentPins: []`)
- ✅ **홈 리디자인(Claude Design 이식)** 첨부한 디자인 HTML을 홈 화면에 이식(원본을 저장소 루트 `Geurio 홈 리디자인.dc.html`로 보존 — 수정 금지 대상, 22MB 번들의 `__bundler/template`에서 추출). **색은 갈아 끼우지 않았다** — 디자인의 값들이 지금 코랄 토큰과 거의 같아서(bg #FBF7F3≈#fbf6f2, hover #FBF3EE≈#faf3ee) 달라지는 것은 **구조·크기·처리**뿐이었다. 그래서 여섯 벌 테마와 다크가 그대로 성립한다. 더한 토큰은 카드 입체감 여섯 개(`--mf-card-shadow`/`-hover`/`-sm`·`--mf-border-hover`·`--mf-wash`·`--mf-dot-grid`, 다크는 그늘을 검정으로 진하게·hover 경계는 반대로 밝게)이고, 반복되는 알약·원형·1차 버튼 꼴은 `home/chrome.ts` 한 곳에 모았다(에디터 `chrome.ts`와 같은 역할). **툴바**: 왼쪽은 스페이스 색 사각 + 제목(20px/800) + 등폭 개수, 오른쪽은 요청대로 디자인 순서 그대로 **알림(32 원형) · 검색(200 알약) · 구분선 · 가져오기(원형, 라벨 없음) · 새 폴더(알약) · 새로 만들기(강조색 그라디언트 알약)** — 읽는 동작이 먼저, 만드는 동작이 뒤. **hover 애니메이션**: 카드가 `translateY(-3px)` 떠오르며 그늘이 멀어지고(`--mf-card-shadow-hover`) 경계가 한 톤 진해진다, 그 위에 ★·⋯·진입 셰브론이 드러난다(예전 hover는 테두리를 강조색으로 바꿨는데 그 색은 **선택**이 쓰므로 갈랐다). `prefers-reduced-motion`에서는 떠오르지 않는다. **미리보기**: 옅은 wash + 캔버스의 점을 축소한 도트 격자(그리드 18px/최근 13px) + 좌상단 즐겨찾기(둥근 사각) + 우상단 **종류 배지**(점 + 이름) — 디자인 원본은 목업이라 추상 도형을 그렸지만 우리는 **틀만 따르고 안에는 실제 문서 렌더**(`mapPreview`)를 그대로 둔다. 카드 높이는 150/74, 반지름 18/15. **최근 항목**은 가라앉은 상자에서 **아래 선으로만 갈린 구획**이 되고(카드가 그늘로 떠 있어 면을 또 깔면 층이 둘이 된다) 카드 폭 128→158. **사이드바**: 250px, 프로필 버튼이 [아바타 32 그라디언트][이름/계정][셰브론], 구획 라벨은 넓게 벌린 대문자 꼴, 스페이스 행은 9px 사각 점 + 활성 행 강조색 점, 개수는 등폭. **폴더 카드**는 한 줄 가로 카드(46px 강조색 틴트 타일 + 이름 + 맵 수 + hover 셰브론)로 자기 그리드(minmax 300)를 쓰고, 맵 그리드는 minmax 286·gap 16. **의도한 변경 하나**: 문서 종류를 **카드 테두리 색**으로 알리던 것(이전 요청)을 **배지 점 색**으로 옮겼다 — 디자인 원본의 카드는 모두 같은 옅은 경계선이고, 색 테두리를 그대로 두면 그리드가 초록·파랑·보라로 얼룩진다(종류 신호는 배지 이름·점 색·화이트보드/칸반의 흰 종이 바탕 세 겹으로 남는다). 웹 +3 계약 테스트(툴바 순서·hover CSS·미리보기 틀) + 기존 기대값 갱신(전체 1476), 실브라우저 검증(1440×900 라이트/다크 · 390px — 디자인 원본을 같은 브라우저로 렌더해 나란히 대조, hover 실측 `translateY(-3px)`·그늘 `-26px`·경계 `#e7dacc`·★/⋯ opacity 0→1)
- ✅ **홈 디자인 개정판 이식 12건(디자인 원본 `Geurio 홈 오프라인.dc.html` — 보존, 수정 금지 대상)** 첫 이식(#490) 후 받은 개정 디자인과 수정 목록. 확인 4건은 사용자 선정대로: 종류색 카드 테두리 → **옅은 중립 경계선으로 롤백**(종류는 배지 점·이름이 말한다), 미리보기 실렌더 유지, 용어는 "맵" → **"파일"**(카드는 칸반 카드와 겹친다 — 툴바 메타·폴더 개수·구획 제목), 스크롤 페이드 없음. ① **최근 항목 그림자 잘림** — 스크롤 상자 아래쪽 여유만 18px 남기고 상쇄 마진을 걸지 않는다(위·옆은 기존 상쇄 유지 — 카드 자리는 불변). ② **hover 떠오름이 A→B 두 그림처럼 끊기던 원인**은 컴포넌트의 **인라인 `transition`**이 home.css의 transform 포함 규칙을 통째로 덮은 것 — 인라인을 걷어내고 CSS 한 곳(`.map-card`/`.tpl-card`)만 transition을 정한다(⚠️ 주석으로 못박음). 값은 디자인 선언 그대로: `transform .18s ease` + hover `translateY(-3px)`/`0 24px 44px -26px rgba(46,42,38,.42)`/`#E7DACC` + active `translateY(-1px) scale(.993)`(reduced-motion이면 떠오르지 않음). **목업 렌더 함정**: dc 템플릿이라 카드 값이 전부 인라인이라 목업 자신은 hover 그림자·경계가 안 바뀐다(인라인이 자기 hover 규칙을 덮는다) — 기준은 디자인 CSS의 **선언값**이고 우리는 `!important`로 살렸다. ③ **폴더 hover의 ">" 셰브론 제거**, ⋯를 우측 세로 중앙 flex 자식으로. ④ **⋯ 버튼은 점만**(면·테두리 없음 — 맵·폴더 공통). ⑤ **선택 = `outline: 2px solid accent` + offset 2**(디자인 그대로) — 테두리·마진 저글링이 사라지고 레이아웃 무영향(맵·폴더·최근 공통, 폴더 드롭 대기만 기존 점선 테두리). ⑥ **알림 팝업 재설계**: 352px·라운드 18·위 테두리에 벨을 가리키는 회전 꼬리, 머리 = 알림 + `N new` 등폭 알약 + 모두 읽음, **오늘/이전 그룹 헤더**, 행 = 이름 시드 파스텔 아바타 30px + 종류 미니 배지 16px(멘션 코랄/공유 파랑/댓글 초록) + 문서 칩(점+제목) + 상대시각 + 안 읽음 점. 죽은 버튼("모든 알림 보기" — 목업에만 있고 갈 화면이 없다)은 두지 않음. **곁들여 잡은 회귀**: 벨이 어댑터 배열 순서를 그대로 그려 로컬(추가순)에서 '이전'이 '오늘' 위에 섰다 → `byNewest` 정렬(회귀 테스트: 시간순 아닌 배열을 심어 수리 전 실패 확인). ⑦ **갤러리 재설계**: 1000px·라운드 24·`mf-pop` 등장, **종류 탭**(전체/마인드맵/화이트보드/칸반 — 색 점 + 등폭 개수, 열 때마다 '전체'), 구획 = 이름 + 한 줄 힌트, 카드 = wash+도트 격자 위 실렌더 112px + 점 + 이름 + 설명(min-height 34), 빈 칸은 점선 + 더하기. **마인드맵 칸을 빈 맵 포함 4개로**(브레인스토밍·주간 계획·학습 정리 — 회의록 등 데이터는 남기고 갤러리에서만 뺀다: `tpl=` 주소는 계속 동작). ⑧ LNB 활성 스페이스의 주황 점 제거. ⑨ **버튼 hover를 디자인의 `style-hover`값 그대로**: 알림·가져오기·새 폴더 = `#FBF3EE`(≈`--mf-panel2` 1/255 차) 배경, 새로 만들기 = `brightness(1.05)` — 인라인 배경을 이기려 `.mf-ctl:hover`에 `!important`. ⑩ 카드 제목부 배경 = `--mf-card`(#fffdfb, 다크 #262019 — 신설 토큰). ⑪ **종류 점 색 = 빨강/초록/보라**(`--mf-doc-map` #d9482b / `-board` #3f9e6a / `-kanban` #8a63d2, 다크는 한 단계 밝게). ⑫ LNB 274px(디자인 250 content-box = 274 border-box). 웹 테스트 갱신·추가(선택 단정 border→outline, 갤러리 4칸·탭·점, 벨 정렬 — 전체 1477), 실브라우저 22항목 계측 전부 통과(디자인 렌더와 나란히 — hover transform/그림자/경계 계산값 일치 실측, 알림·갤러리 스크린샷 대조, 모바일 390px 가로 넘침 0, 다크 무회귀)
- ✅ **홈 디자인 후속 6건(제보·요청)** ① **최근 항목 hover 그림자 잘림** — 그리드용 hover 그림자(`0 24px 44px -26px`, 하단 확장 42px)가 가로 스크롤 상자의 아래 여유(18px)를 넘어 잘린 경계가 보였다. 영역을 키우는 대신 요청대로 **같은 기하로 진해지기만** 한다: 신설 토큰 `--mf-card-shadow-sm-hover`(= sm과 같은 `0 12px 26px -22px`, 알파 .4→.62, 하단 확장 16px ≤ 18px) + `.mf-recent-scroll .map-card:hover` 특이도 오버라이드. ② **LNB 배경 = `--mf-card`(#FFFDFB)** — 디자인 aside는 순백이 아니라 카드 면이다. ③ **상위 폴더 타일에 폴더 카드와 같은 그늘**(선택 링 없음 — 고를 대상이 아니라 길이다) + 인라인 transition 제거(transform 전이를 덮어 hover 떠오름이 툭 바뀌는 함정 — 카드에서 잡은 것과 같은 계열). ④ **스페이스 제목 옆 파일·폴더 개수 제거**(구획 머리의 "파일 N / 폴더 N"이 이미 말한다). ⑤ **최근 항목이 자리가 남는데 덜 노출** — fit 계산이 옛 트레이 시절의 좌우 패딩 몫(48px)을 계속 빼고 있었다. 지금 스크롤 박스의 패딩은 음수 마진으로 상쇄돼 **카드가 쓰는 폭 = 트레이 폭 그대로**다 → 순수 함수 `recentFit(w)`로 추출해 수리(경계 폭 회귀 테스트 — 옛 계산이면 한 장 모자란다). ⑥ **LNB 리스트(공유받음·즐겨찾기·휴지통)에 문서 종류 아이콘**(첨부 이미지) — 신설 `KindMiniGlyph`: 맵=가지 다이어그램·보드=틀 안의 펜 획(대각선+꺾쇠 꼴은 13px에서 "새 탭 열기 ↗"로 읽혀 피함)·칸반=두 열, **색은 홈 카드 배지의 점과 같은 종류색 토큰(`--mf-doc-*`)** — 배지·아이콘이 같은 색으로 같은 종류를 가리킨다. 종류는 뷰모델이 `docKindOf`(cardRaw — 본문 못 받은 공유 문서는 map 폴백)로 싣는다. 곁들여 디자인의 리스트 패널을 이식: 펼친 즐겨찾기·휴지통이 **가라앉은 판**(--mf-bg + hairline, r12) 안에 들고, 두 머리에 회전 셰브론(›/⌄), 즐겨찾기 해제(★)는 휴지통 행 동작처럼 hover에 드러난다. **디자인의 "30일 후 영구 삭제" 헤더는 넣지 않았다** — 실제 30일 자동 삭제가 없어서 그 문구는 거짓 약속이 된다(원하면 자동 삭제 기능부터 별건). 웹 +5 테스트(recentFit 경계·hover CSS 계약·개수 부재·아이콘 3종+aside 배경·타일 그늘/링 없음, 전체 1482), 실브라우저 10항목 계측 + 스크린샷(hover 잘림 없음·LNB 패널·아이콘 색 실측 빨강/초록/보라·타일 그늘)
- ✅ **홈 후속 3건(제보·요청) + 라이브 배포** ① **그리드 카드 hover 그림자 완화** — 최근 항목과 같은 처방으로: `--mf-card-shadow-hover`를 24/44px 확장에서 **기본 그늘과 같은 기하(16/32/-26)로 진해지기만**(알파 .42→.56, 다크 .72→.9)으로. 갤러리 카드(`.tpl-card:hover`)의 하드코딩 그림자도 같은 토큰으로 통일. 3px 떠오름은 그대로라 입체감은 유지된다. ② **⋯ 메뉴 클릭 효과** — 쉴 때는 점만(기존 요청 유지), 손을 얹으면 panel2 원판 + 진한 글자, 누르는 순간 `scale(.86)` 눌림(맵·폴더 공통). :active 규칙은 reveal 규칙(`.map-card:hover .menu-btn` — transform !important)과 특이도가 같아 **뒤에** 둬야 이긴다(테스트로 순서 고정). 인라인 transition에 background를 더해 원판이 페이드된다. ③ **LNB 칸반 아이콘 명확화**(첨부 이미지) — "틀 안 가는 선 둘"이 13px에서 흐렸다 → **채운 세로 막대 둘**(왼쪽이 김, `fill: var(--mf-doc-kanban)`). 웹 +2 테스트(CSS 계약·아이콘 구조, 전체 1484), 실브라우저 5항목 계측(그림자 계산값·⋯ hover/active·아이콘 rects) + 스크린샷
- ✅ **프로필 팝업 + 설정 모달 디자인 이식(첨부 이미지 2장)** ① **아바타를 잉크색 사각으로**(`ProfileAvatar` — 강조색 그라디언트 → `--mf-text` 면 + `--mf-card` 글자, 다크에서는 토큰이 뒤집혀 밝은 면·어두운 글자로 대비 유지). 글자는 신설 `avatarLabel`: 한글 이름은 성을 뗀 **뒤 두 글자**("이호율" → "호율"), 그 밖은 첫 글자 대문자. ② **프로필 팝업**: 트리거는 테두리·면 없는 행(36px 아바타 + 이름/이메일 + **열림에 따라 도는 셰브론** — 테두리는 확인 후 제거 요청), 팝업은 트리거 폭에 맞춘 r16 카드 — 머리는 가장자리에서 띄운 **accent-soft 인셋 블록**(44px 아바타), 프로필명 변경·설정 행 뒤 **구분선**, 로그아웃은 danger. ③ **설정 모달**: 560 폭·r22·`--mf-card` 면, 원형 테두리 ✕(갤러리 닫기와 같은 문법), 계정 행 = accent-soft + 56px 아바타, 색상 테마 칩 = **원형 스와치**(그 테마의 면 원 안에 강조색 점 — 다크 칩은 어두운 원 + 주황 점) + 선택 칸만 강조색 테두리·soft 면, **회원 탈퇴 행 = 배경 없는 맨 행**(확인 후 수정 요청 — hover의 옅은 면만) + 휴지통 아이콘 + 잉크색 제목 + 부제 + 셰브론(문구는 이미지 그대로 "계정과 모든 보드·스페이스가 영구 삭제돼요"), 푸터 법적 링크 유지. 웹 +1 통합 테스트(아바타 글자·인셋 머리·구분선·모달 560·원형 스와치·문구, 전체 1485), 실브라우저 검증(팝업·모달 라이트/다크 스크린샷 — 첨부 이미지와 대조, 다크에서 아바타 반전 확인)
- ✅ **마인드맵 에디터 디자인 3건(Claude Design `Geurio 마인드맵 리디자인.dc.html` — 보존, 수정 금지 대상)** ① **속성 패널**: 폭 260→**296**·r18·디자인 그늘, 머리를 **강조색 틴트 띠**(`hexA(accent,.1)` — kicker 11px/이름 14.5px 800 + **✕=선택 해제**, 색 조각 제거)로, 그리고 **열릴 때마다 첫 구획이 펼쳐진 채** 시작한다(요청 — 전부 접힌 패널은 무엇을 할 수 있는지 말하지 않는다). 구현은 `useState` 초기값 하나: 패널은 선택이 바뀔 때 key로 리마운트되므로 초기값이 곧 "매번 열릴 때"다(Node=shape/Float=fbg/Line=lstyle, 아코디언 규칙은 그대로). ② **하단 도구 막대를 맵에도**(화이트보드와 같은 것) — `BoardToolbar`의 board 가드를 걷고 **도구 줄만 종류별로 갈랐다**: board=선택·펜·형광펜·지우개(기존), 맵=선택·**하위/형제 주제 추가**(디자인 원본 mmTools의 SVG 그대로, 주제 미선택이면 비활성 — 트리 어디에 붙일지 모른다) | 삽입 4종(맵 라벨은 '선 추가') | 스레드 도구 | 실행취소·다시실행. 스레드 도구 접근 이름을 '스레드 도구'로 — 댓글 팝업 aria-label('스레드')과 겹치면 보조기술이 가릴 수 없다. **모바일도 그대로 맵에 적용**(전폭 막대·되돌리기 알약·삽입 ＋ 전환 메뉴), `ZoomControls`/`MobileSelectBar`가 맵에서도 `BOARD_BAR_LIFT`(80)만큼 올라앉는다. GNB ⊕ 삽입 메뉴는 **남긴다**(디자인 원본도 GNB 메뉴를 유지한다 — 진입점 정리는 별건). ③ **캔버스 배경 그라데이션**: 신설 `canvasWash(canvasBg)`(theme.ts) — 디자인의 `radial-gradient(1200px 700px at 62% 46%)`을 **테마의 canvasBg에서 파생**(중심=흰색 0.8 믹스·중간 0.45·가장자리 원색, 어두운 캔버스는 0.08/0.04로만 밝힌다 — 다크가 잿빛으로 바래지 않게). `data-canvas-bg` 레이어의 backgroundImage가 [도트, wash] 두 겹 — **도트가 먼저**(뒤에 오는 불투명 wash가 앞이면 도트가 통째로 가려진다). 기존 배경 레이어 분리(단일 래스터) 구조는 그대로라 팬 성능 무영향. 기존 계약 갱신 7건(맵 도구 막대 부재→구성 단정, 모바일 줌 묶음 16→80, 시트 '속성 닫기' 부재→존재, 아코디언 all-collapsed→first-open, GNB 삽입 메뉴 쿼리를 드롭다운으로 스코프 — 막대와 이름 충돌) + 신규 3건(패널 296/머리/✕·첫 구획 열림·wash 두 겹), 전체 1487+269 통과. 실브라우저 검증(1440 데스크톱: wash 계산값·막대 중앙 알약 465×54·선택 그라디언트+점·하위/형제 disabled→선택 후 enabled→클릭으로 자식 추가·패널 296/강조 머리/첫 구획 open/✕ 해제·다크 wash 2겹 / 390 모바일: 전폭 막대·undo 알약(12,80)·줌 묶음(16,80) 무겹침·44px·삽입 전환→메모 추가→도구 복귀·시트 머리 ✕·가로 스크롤 없음). **참고**: 디자인 dc는 standalone 렌더에서 컨트롤러 상태가 안 풀려(`{{ it.label }}` 잔재) 픽셀 대조가 아니라 **소스에서 뽑은 수치**(폭 296·r18·막대 r26·38px 원형·아이콘 path)를 기준으로 맞췄다
- ✅ **맵 후속 2건(요청) — 메모 접기 제거 + 영역 종속을 화이트보드 전용으로** ① **메모 접기/펼치기 제거(전 종류)** — 맵에만 남아 있던 접기 토글(회전 셰브론)과 collapsed 렌더를 걷어내 메모는 언제나 펼쳐진 상태다. 토글 자리였던 **좌측 패딩 32 → 11**(우측과 대칭 — board가 #405에서 먼저 간 길). `floatPadLeft()`가 인자를 잃고 상수가 되어 소비처 4곳(렌더 FloatLayer·측정 measureFloatHeight·내보내기 sceneFloatBox/paintScene·홈 썸네일)이 자동으로 함께 좁아지고, 내보내기의 접기 배지(＋/− 원)·`SceneFloatBox.collapsed`·`toggleFloatCollapse` 컨트롤러·`.mf-float-fold` CSS도 제거. **옛 문서의 collapsed 데이터는 지우지 않고 무시**한다(항상 펼쳐 그림 — board의 #401과 같은 규칙, 모델·직렬화 무변경). ② **프레임 = 그릇을 board 전용으로**(제보: 맵에서 영역을 움직이면 안의 도형이 따라온다 — "맵과 보드는 별개") — 가드 세 곳: `frameCarry`가 board가 아니면 **영역 자신만** 담고(끌면 사각형만 움직인다 — dc 원본의 결), `setFrameDrop` 세터가 board가 아니면 담김 강조를 아예 세우지 않으며(모든 드래그 경로가 세터 하나를 지난다), `collectClipboard`의 영역 복사 동반(#426)도 board만. 맵의 영역 우클릭 메뉴에서 그릇 항목(`내용에 맞추기`·`프레임만 삭제`/`내용까지 삭제`)이 사라지고 `이름 편집·복사·잘라내기·삭제`만 남는다(삭제는 원래 비파괴라 뜻 그대로). board는 전부 기존 그대로(무회귀 테스트). 웹 테스트: 기존 접기 계약 6건을 새 계약으로 재작성 + 맵 영역 드래그/복사 2건 신설(**둘 다 가드를 되돌려 실패 확인**), 전체 1488+269 통과. 실브라우저 검증(맵: 패딩 11=11·토글 0·collapsed 문서 두 줄 렌더 / 영역 드래그 [125,86]에 메모 [0,0]·강조 없음·메뉴 구성 / board: 드래그 동반 [125,75]=[125,75] 무회귀)
- ✅ **맵 3건(제보·요청) — 영역 경계 z-순서 + 패널 첫 구획 무애니메이션 + 크기 세그먼트 디자인** ① **영역 경계 점선이 메모를 덮던 문제** — #419가 화이트보드 요청("영역을 최상위로")으로 올린 경계·라벨 판(z 95)이 맵에도 걸려 있었다. 맵에서는 그 판을 **콘텐츠 아래(9)**로(`zoneFrameZ(board)`) — 메모(10)·주제(40)가 점선을 덮고, 채움(8)보다는 위라 점선은 면 위에 남는다. **이미지 플로트는 영역보다도 아래(5)** — 배경 사진처럼 깔린다(요청). 이미지가 영역 히트 판(8) 밑으로 가면 클릭을 판이 삼키므로 `beginZoneDrag`가 누른 자리의 이미지를 찾아 **`beginFloatDrag`에 위임**(획 back-off와 같은 결 — 우클릭은 `hitTestAll`이 원래 float 먼저라 무수정), **고른 동안은 z 20으로 떠** 리사이즈 핸들이 가리지 않는다(선택 = 지금 만지는 것). 내보내기(`paintScene` — 종류별로 이미지→영역 채움→경계·라벨→콘텐츠 / board는 기존 순서)와 홈 썸네일(`mapPreview` — imgEls/memoEls 분리)도 같은 순서(소비처 파리티). board는 전부 무회귀(경계 95·잉크 위 유지). ② **속성 패널 첫 구획이 펼쳐지는 애니메이션 제거** — 열릴 때마다 max-height 전이가 재생돼 "열려 있다"가 아니라 "지금 열렸다"로 읽혔다. `PanelSection`이 **open이 처음 바뀔 때까지 전이를 끈다**(render-phase 상태 조정 — 마운트 effect로 켜면 jsdom에서 검증 불가·의미도 같다). 패널은 선택마다 key 리마운트라 매번 성립, 첫 토글부터는 기존 아코디언 전이 그대로. ③ **작게/보통/크게를 디자인 원본의 세그 트랙으로** — 예전 테두리 상자 3개 대신 **가라앉은 트랙(panel2 + border, r11, padding 3)** 위에서 활성 칸만 카드 면 + 진한 강조 잉크(`mixHex(accent, text, .2)` — 원본 #C9512A와 근접) + 작은 그늘(원본 `seg()` 그대로, 값은 테마 파생이라 다크·모노 성립). I·S 있는 노드 패널은 [B|I|S] 아래 트랙, 선 패널은 B+트랙 한 줄. 웹 +5 테스트(z·위임 / board 95 무회귀 / SVG 순서 / 무애니메이션→토글 후 전이 / 세그 계약), 전체 1493+269 통과. 실브라우저 검증(elementFromPoint — 메모 위=memo·이미지 위=zone-hit, 경계 점선이 메모 뒤로/이미지 위로 픽셀 확인, 클릭 위임→'선택한 이미지'→z 20→핸들, 첫 구획 transition none, 세그 실측 트랙 rgb(250,243,238)·활성 흰 면·rgb(202,90,57)·그늘)
- ✅ **스레드 팝업 등록 안내 문구(요청)** 입력창 아래 힌트가 "⌘/Ctrl + Enter 로 등록"이었는데 데스크톱의 실제 규칙은 **Enter = 등록**(#477)이라 문구가 동작보다 어렵게 말하고 있었다 → **"Enter로 등록"**으로(수정 키 표기 헬퍼 `mod()` 제거, Ctrl/⌘+Enter 자체는 계속 동작). 폰 문구("등록 버튼으로 남겨요")는 그대로 — 소프트 키보드에서는 Enter가 줄바꿈이다. 기존 답글 테스트에 문구 가드 추가
- ✅ **캔버스 그라데이션을 디자인 원본 색 그대로(요청 — 맵·화이트보드)** `canvasWash`가 테마 canvasBg에서 **파생**하던 것을, 기본 캔버스 두 벌 — 코랄 맵(#f5ece5)·화이트/화이트보드(#ffffff) — 은 디자인 원본 스톱 **그대로**(`#FFFDFB 0% → #FDF7F2 55% → #FBF2EB 100%`, `1200px 700px at 62% 46%`)로 바꿨다. 파생식으로는 채널별 비가 달라 정확히 같은 색이 나오지 않고, **순백 캔버스는 흰색에 흰색을 섞는 셈이라 화이트보드에는 그라데이션이 아예 보이지 않았다**(이번 요청의 절반). 나머지 테마(오션·포레스트·그레이프·모노·다크)는 기존 파생 유지 — 원본의 따뜻한 색을 그대로 얹으면 팔레트와 부딪힌다. 웹 +4 유닛(`theme.test.ts` — 코랄/화이트 정확 일치·오션 파생·다크 옅음), 전체 1497+269 통과, 실브라우저 검증(맵·보드 computed backgroundImage가 rgb 환산 스톱 3색·62% 46% 모두 일치, 보드 스크린샷에서 웜 그라데이션 확인)
- ✅ **세션 정책 3건 + 문서화(요청)** "세션 유지·동시 로그인 정책이 있나"는 물음에서 시작 — 코드에 세션 규칙이 한 줄도 없고 Supabase 기본값(무기한 유지·무제한 동시 로그인)을 그대로 쓰고 있었다. 관행(Notion·Figma·Miro)과 우리 제품의 성질(자동저장+협업이라 강제 로그아웃이 곧 유실 위험, 다기기가 전제, 민감정보 없음)을 근거로 **유지·기기 수는 그대로 두고 빠진 안전장치만** 채웠다. ① **모든 기기에서 로그아웃** — 포트 `AuthProvider.signOut(scope)` 신설(`'local'`|`'global'`|`'others'`), 설정 모달 '계정 관리'에 항목 + 확인창(되돌릴 수 있는 동작이라 문구도 그렇게). 평범한 로그아웃은 기본 `'local'`(이 기기만). ② **세션 만료 안내 + 원래 화면 복귀** — `RequireAuth`가 조용히 튕기던 것을, 신설 `features/auth/sessionNotice.ts`(순수부 `safeNextPath`·표시 저장)로 ⓐ 만료 표시를 남기고 ⓑ 지금 화면을 `?next=`로 실어 보낸다. 로그인 화면이 "로그인이 만료되었어요"를 **한 번** 보여 주고(읽으면 지운다) 성공 뒤 그 자리로 돌아간다. **"처음부터 로그아웃"과 "만료"를 가르는 근거**는 localStorage `mf_had_session`이고, 직접 로그아웃하면 지운다(안 지우면 다음 방문에 만료 안내가 거짓말이 된다). `next`는 우리 앱 안의 경로만 통과(오픈 리다이렉트 차단). ③ **비밀번호 변경 시 다른 세션 해지** — `updatePassword` 성공 뒤 `signOut({ scope: 'others' })`(지금 세션은 살아남는다). 해지 실패는 오류로 만들지 않는다(비밀번호는 이미 바뀌었다). 문서: `backend.md` §15에 정책 표(지속·JWT 1시간·자동 갱신·유휴 없음·동시 무제한)·세 장치·바꾸는 방법(대시보드 Sessions)·하지 않은 것(기기 목록)을 적었다. 웹 +10 테스트(sessionNotice 순수 5 · 어댑터 범위/해지 4 · 홈 전체 로그아웃 2 · 로그인 만료/next 3 — 셋은 구현을 되돌려 실패 확인), 전체 1511+269 통과. 실브라우저 검증(설정 행·확인창·global 로그아웃 후 마커 정리·만료 안내 노출·로그인 뒤 `/editor?map=m1`로 복귀·안내 재노출 없음). **함정 기록**: 임시 되돌리기 검증 후 `git checkout <file>`로 복구하려다 커밋 전 구현까지 날렸다 — 미커밋 파일은 checkout이 아니라 되돌린 편집을 역으로 재적용해야 한다
- ✅ **설정에 비밀번호 변경 추가(제보: 로그인 후 바꿀 자리가 없다)** 확인해 보니 `updatePassword`는 **로그인 화면의 비밀번호 찾기(복구 코드) 흐름에서만** 호출돼, 비밀번호를 바꾸려면 **로그아웃하고** 메일 코드를 받아야 했다(직전에 넣은 "변경 시 다른 세션 해지"도 그 흐름에서만 발동했다). 설정 모달 '계정 관리'의 **첫 항목**(비밀번호 변경 → 모든 기기에서 로그아웃 → 회원 탈퇴 — 위험도 순)으로 신설. 포트 `AuthProvider.changePassword(현재, 새)`가 **확인·변경·다른 세션 해지를 한 몸**으로 처리한다(불변식이 한 곳에 있다): 확인은 같은 계정으로 `signInWithPassword`를 한 번 더 하는 방식("비밀번호만 검증" API가 없다 — 성공하면 같은 사용자의 새 세션이라 사용자에겐 아무 일도 없고, 실패하면 지금 세션은 그대로), 성공 뒤는 `updatePassword`에 위임해 `scope:'others'` 규칙을 재사용. **현재 비밀번호를 묻는 이유(사용자 선정)**: Supabase는 세션만 있으면 비밀번호를 바꿔 주므로 공용 PC에 남은 로그인으로 남이 계정을 가져가는 것을 막는 유일한 장치다. **Google 전용 계정**은 바꿀 비밀번호가 없어 항목을 비활성+이유 표시(`emailSignInProviders` RPC 재사용, 확인 불가면 **비밀번호 있음**으로 본다 — 진짜 게이트는 확인 단계이므로 모르는 채 잠그는 쪽이 더 나쁘다). 모달은 현재/새/확인 3칸 + 완료 화면이 **다른 기기 로그인 해제 사실까지** 알린다. **UI 판단 하나**: 제출 버튼을 4자·일치 조건으로 비활성화하지 않고 **세 칸이 채워지면 눌리게** 두고 이유는 누른 뒤 말한다(이유 모르는 비활성 버튼 방지 — 첫 구현이 그래서 불일치 안내가 영영 안 떴다). 어댑터 4 + 홈 통합 3(불일치 무호출·인자 전달·완료 문구 / 현재 비밀번호 오류 / Google 전용 비활성) 테스트, 전체 1518+269 통과, 실브라우저 검증(설정 3행·필드 3개 password 타입·첫 칸 포커스·불일치 안내·완료 화면·닫힘). `backend.md` §15에 항목 추가
- ✅ **이메일 가입 계정에 Google이 자동 연결되던 문제(제보)** ① 이메일로 `A@…` 가입 → ② 같은 주소로 Google 로그인 하면 **한 계정에 로그인 수단이 둘** 붙었다 — Supabase가 동일 이메일·인증된 계정에 OAuth 신원을 **자동 연결**하기 때문이고(대시보드 토글 없음), 사용자가 만들지 않은 두 번째 출입구다. 이메일 가입 쪽은 이미 막고 있었으므로(#340) **대칭**을 맞췄다. 규칙은 `features/auth/googleLink.ts` 한 곳(순수): 세션 `app_metadata`의 **이번 로그인 수단**(`provider`)이 google이고 **연결된 수단**(`providers`)에 email이 있으면 거절 — 그래서 두 수단이 이미 붙은 계정의 주인이 **비밀번호로** 들어오는 것은 막지 않는다(막으면 계정을 잃는다). 모르는 값(provider 없음·목록 없음)도 막지 않는다. 포트 `AuthUser`에 `signInProvider`/`linkedProviders`를 노출(어댑터가 `app_metadata` 매핑). **거절 지점이 경로마다 다르다**: GIS 토큰 방식은 교환 **전에** ID 토큰의 `email` 클레임(신설 `emailFromIdToken` — 서명 검증 없이 읽는다, 하는 일이 '거절'뿐이라 안전)으로 `email_signin_providers`를 물어 **연결 자체를 만들지 않고** 막고, **리다이렉트 방식(현재 로그인 버튼)은 돌아온 시점에 이미 세션이 있어 사전 확인이 불가능**하므로 문지기(`RequireAuth`)가 그 세션을 보고 로그아웃+안내한다. 곁들여 만료 전용이던 안내 표시를 **일반 안내 채널**(`noteLoginNotice`/`takeLoginNotice`)로 일반화해 두 안내가 같은 자리를 쓴다. **테스트가 잡은 순서 버그**: `forgetSignedIn()`이 안내 채널까지 비우므로(같은 저장소) 마커를 먼저 지우고 안내를 남겨야 한다 — 반대로 하면 방금 남긴 문구가 사라진다. 웹 +11 테스트(규칙 4 · `emailFromIdToken` 3 · 문지기 통합 3(거절·Google 전용 통과·비밀번호 로그인 무차단) · 안내 채널 1), 전체 1528+269 통과. **한계**: 리다이렉트 경로는 DB에 연결된 신원이 남는다(로그인 문만 닫는다 — unlink는 하지 않는다). `backend.md` §1의 0013 항목에 정리
- ✅ **프로필 이미지(0031) — 홈에서 바꾸고 세 에디터에 반영(요청)** 아바타가 이름 첫 글자뿐이라 함께 쓰는 화면에서 누가 누구인지 색으로만 갈렸다. 설정 모달 **프로필** 구획에서 사진을 고르면 홈·마인드맵·화이트보드·칸반의 모든 얼굴 자리에 함께 나타난다(요청대로 **프로필명 변경도 팝오버에서 설정으로 옮겼다** — 프로필을 다루는 일이 한자리에). **저장 위치가 셋인 이유**: 실물은 Storage(`avatars/<uid>/<ts>.webp` — 본문 jsonb에 base64로 넣으면 문서마다 사진이 복사된다, 0016과 같은 판단), 내 화면이 읽는 주소는 `auth.users.raw_user_meta_data.avatar_url`(세션에 실려 와 **왕복 0회**로 첫 페인트에 뜬다), 남이 읽는 주소는 `public.profiles.avatar_url`(다른 사람의 `auth.users`는 클라이언트가 못 읽는다). 경로 첫 조각이 uid라 정책이 `split_part(name,'/',1) = auth.uid()::text`로 자기 폴더만 쓰게 강제하고, **버킷은 공개**다(0016의 서명 URL과 반대 — 프로필 사진은 함께 쓰는 사람 누구에게나 보여야 하고, 주소가 안정적이라 `Cache-Control: 31536000` 캐시가 그대로 산다. 파일명이 타임스탬프라 주소를 모르면 찾을 수 없다). **남의 사진을 찾는 길은 RPC 하나** — `share_participants`(0011)에 `avatar_url`·`user_id`를 더해(0018 링크 뷰어 가드 유지) 칸반 담당은 **이메일**로, 댓글·핀 얼굴은 **계정 id**로 잇는다. **내 얼굴은 참가자 목록에 없다**(멘션 후보에서 나를 빼는 의도된 결) → 세션에서 직접. 이름은 스냅샷(0020)이지만 **사진은 조인**이라 바꾸면 옛 댓글 얼굴까지 바뀐다(얼굴은 "지금 이 사람", 이름은 "그때 그렇게 적혀 있었다"). 접속자 아바타는 서버를 거치지 않고 awareness `PresenceUser.avatar`에 실어 보낸다(이름·색과 같은 경로). 다듬기는 클라이언트에서(`avatarImage.ts` — 정사각 crop → 256px webp q0.9, 120KB 넘으면 0.75로 재인코딩. 원본을 그대로 올리면 한 장이 수 MB고 모든 화면이 그걸 내려받는다). 반영한 자리 여덟: 홈 LNB 트리거·프로필 팝오버 머리·설정 계정 행 / 칸반 카드 담당·상세 담당 고르기·필터 담당 칩·리스트·타임라인 막대 / 댓글 행·작성창·초안 말풍선·핀 얼굴 / GNB 접속자 아바타 — 글자는 **이미지 아래에 늘 남겨** 주소가 죽어도 깜빡임 없이 폴백한다. 데모(로컬) 모드는 data URL을 `mf_profile_avatars`에 두고 참가자 목록도 그 캐시에서 읽어 **같은 길**을 탄다(프로필명 캐시와 같은 규칙) — 그래서 이 환경에서도 전 경로를 실브라우저로 검증할 수 있었다. 칼럼·버킷이 없는 서버에서도 앱은 깨지지 않는다(문구로 알리고 목록은 사진 없이 뜬다). 웹 +12 테스트(로컬 어댑터 2·홈 흐름 4·에디터 반영 6) + 기존 갱신, 전체 1591 통과. 실브라우저 검증(설정에서 고르기→256 webp 저장→LNB·팝오버 반영→기본으로 되돌리기 / 칸반 담당·필터 칩·리스트·타임라인 / 스레드 핀·댓글 행·작성창·초안 말풍선 / 2탭 접속자 아바타). **함정 기록**: 클래스 인스턴스를 스프레드한 테스트 스텁(`{ ...new LocalShareStore(), … }`)은 **프로토타입 메서드를 통째로 잃는다** — 핀 레이어가 참가자 조회를 새로 쓰자 그 스텁들이 세 파일에서 터졌다(`Object.assign`으로 교정). 운영 문서 `backend.md` §17
- ✅ **프로필 이미지 업로드 `NoSuchBucket` 진단(제보)** 프리뷰에서 사진을 올리면 `400 / "Bucket not found"`. **SQL은 결백했다** — 로컬 Postgres 하네스(Supabase 스캐폴딩: `auth.users`·`storage.buckets/objects`·역할·`auth.uid()`/`auth.jwt()` GUC 대체)에 **실제 마이그레이션 0001~0031을 순서대로 전부** 적용해 무오류를 확인하고, 0031의 효과를 기능으로 검증했다(버킷 1행·storage 정책 4개·`profiles.avatar_url` 칼럼·`share_participants` 7칼럼과 소유자/초대자/제3자 세 경로의 실제 반환값). 원인은 **배포 순서**다: `supabase/config.toml`의 `project_id`가 프로덕션 ref이고 GitHub 연동은 **`main` 머지 시점에만** 적용하므로, 프리뷰 프런트엔드가 보는 그 프로덕션 DB에는 아직 0031이 없다(→ 버킷 없음). 그래서 이번 커밋은 **드러나게** 만드는 데 집중했다: ① 원문 대신 사람이 읽을 문장(`avatarUploadError` — 버킷 없음/용량/형식/인증을 갈라 말하고 원문은 콘솔에), ② `profiles.avatar_url` 갱신 실패는 **오류로 올리지 않는다**(내 사진은 이미 메타데이터로 바뀌었다 — "실패했다"고 말하면 거짓말이 된다. 남에게 보이는 것만 늦어지고 경고는 콘솔에), ③ 버킷에 **서버 상한**을 더했다(2MB·webp/png/jpeg — 세션만 있으면 자기 폴더에 무엇이든 넣을 수 있으므로 0016과 같은 처방). `backend.md` §17에 적용 확인 3질의·수동 적용 절차(전 문장 재실행 가능)·"파일 하나가 트랜잭션이라 한 문장 실패 시 전체 롤백" 주의를 적었다. 웹 +3 테스트(전체 1594). **교훈**: 마이그레이션이 필요한 기능은 브랜치·프리뷰에서 검증할 수 없다 — "코드 완성"과 "동작 가능"이 갈리는 지점이므로 배포 절차를 함께 말해야 한다
- ✅ **설정 팝업 재구성(요청 2건)** ① **프로필 이미지 변경 + 프로필명 변경 → `프로필 설정` 한 겹 안 화면**으로 합쳤다(계정 설정과 같은 문법: 행 → 뒤로 가기 있는 화면). 첫 화면은 계정 요약 + 진입 행 둘뿐이라 가볍고, 요약 카드의 아바타는 **보여 주기만** 한다(카메라 배지·파일 고르기는 프로필 설정 화면으로 — 같은 동작의 진입점이 한 화면에 둘 있으면 어느 쪽이 진짜인지 흐려진다). ② **색상 테마가 팝업 최하단**: 순서가 [계정 요약][프로필 설정][계정 설정][색상 테마][법적 링크]다(실측 y 399 → 447 → 537). 상태는 불리언 둘을 두지 않고 **`settingsView: 'main'|'profile'|'account'` 한 칸**으로 — 둘 다 열림 같은 없는 상태가 생기지 않고, 뒤로 가기(`closeSettingsDetail`)·높이 잇기·전환 애니메이션이 "첫 화면인가"만 보면 된다(두 화면 모두 첫 화면에서만 들어온다). 가운데 구획 라벨은 **두지 않았다** — 모달 제목이 이미 '설정'이라 한 번 더 쓰면 '설정 > 설정'으로 읽힌다. 프로필명 팝업은 닫으면 프로필 설정 화면으로 돌아온다(`openProfileNameEdit`이 뷰까지 지정). **실브라우저에서 잡은 것**: `openProfileDetail`이 `avatarError: ''`로 초기화해 `?? ` 폴백을 지나가며 안내 문구("정사각형으로 잘려…")가 **빈 줄**이 됐다 → `null`. 테스트 갱신(첫 화면에는 손보는 항목이 없다 + 테마가 마지막 + 프로필 설정 안에 둘 다 있다), 전체 1594 통과. 실브라우저 검증(라이트·다크·390px 스크린샷, 진입→업로드→기본으로→뒤로→계정 설정 왕복). **후속(제보)**: 화면 전환이 **좌우 슬라이드**(들어갈 때 오른쪽에서, 뒤로 갈 때 왼쪽에서)라 어색했다 — 이 모달은 화면마다 높이가 크게 달라 상자가 동시에 줄었다 늘어나므로, **글자가 가로로 지나가는 동안 상자는 세로로 움직여** 두 움직임이 서로 어긋나 보였다. 좌우 이동을 걷어내고 **제자리 페이드**로 바꿨다(높이 전이와 같은 0.24s·같은 `cubic-bezier(.4,0,.2,1)` — 글자가 상자가 자리를 잡는 그 시간에 드러난다). 방향과 무관해졌으므로 `fwd`/`back` 두 클래스·두 키프레임을 `is-swap`/`mf-view-fade` 하나로 합쳤다(컴포넌트는 "바뀌었는가"만 안다). 실브라우저 rAF 샘플링으로 세 방향(프로필 진입·뒤로·계정 진입) 모두 **첫 줄 x 불변(464)·`transform: none`**·opacity 0→1·본문 높이 480↔296 확인, 전이를 10배 늘린 슬로 프로브로 중간 프레임도 눈으로 확인
- ✅ **Radix Dialog 도입 — 모달 12곳(요청)** 오픈소스 디자인 시스템 검토의 결론(스타일 있는 시스템은 우리 룩을 버리게 되니 **스타일 없는 접근성 프리미티브**만)을 첫 단계로 실행. `@radix-ui/react-dialog`(MIT — 브랜드·폰트·아이콘이 딸려 오지 않아 상표 노출 0) 위에 공용 `components/Modal.tsx`를 얹고, **막·카드 스타일은 기존 인라인 값을 그대로** 넘겨 시각을 한 픽셀도 바꾸지 않았다(실측: 설정 모달 첫 화면의 행 좌표가 이식 전과 동일한 399/447/537, 카드 560×567·중앙 x 720, 갤러리 1000, 공유·피드백 452, 폰 카드 상세는 여전히 바닥에 붙는 바텀시트 390×686). 이식 대상은 **진짜 모달 12곳**(설정·프로필명·비밀번호 변경/설정·회원 탈퇴·확인창 공용(11곳에서 쓰인다)·완료 토스트·템플릿 갤러리·공유·피드백·버전 기록·단축키 도움말·카드 상세·열 삭제 확인) — 앵커에 붙는 팝오버(칸반 필터·빠른 댓글)와 **비모달** 패널(댓글 패널·캔버스 초안 말풍선·CollabPaused)은 성격이 달라 손대지 않았다. **얻은 것은 행동이다**: 초점 트랩(실측 Tab 20회에 카드 밖으로 0회), 닫은 뒤 초점 복귀, 배경 스크롤 락(`body overflow hidden` → 닫으면 `visible`), `aria-modal`(18개 중 7개만 있었다), 그리고 **닫히면 언마운트**(예전에는 `display:none`으로 마운트된 채라 홈 Ctrl+A가 "보이는 dialog만" 세야 했고, 테스트도 텍스트 유무가 아니라 `display`로 판정해야 했다). 창마다 손으로 달던 `window` Escape 리스너 7곳과 막 클릭 핸들러가 사라졌다. **막 클릭으로 닫히지 않아야 하는 팝업**(편집 중인 입력·파괴적 확인창)은 `dismissOnBackdrop={false}` 한 프롭으로 규칙이 드러난다. **실브라우저가 잡은 것 둘(테스트는 통과했다)**: ① 포털이 `body` 밑이라 앱 루트(`.mf-home`)의 인라인 타이포 상속이 끊겨 모달이 **Times New Roman·검정 글자**로 떨어졌다 → `html, body`에 폰트 스택과 `color: var(--mf-text, #332820)`을 못박았다(앞으로 붙는 포털도 함께 지켜진다. 이 10px 차이가 픽셀 비교에서 처음 드러났다) ② 초점 복귀가 안 됐다 — 원인 세 겹: Radix는 `Dialog.Trigger`로 되돌리는데 우리 모달은 메뉴·행·단축키에서 열려 트리거가 없고, 초점 자리를 effect에서 재면 **자식(Content) effect가 먼저 돌아** 이미 카드 안이며(`onOpenAutoFocus`에서 재야 한다), 트랩 해제 중의 `focus()`는 되돌려진다(한 틱 뒤에 줘야 한다). 팝오버 항목으로 여는 모달은 그 항목이 사라지므로 `restoreFocusSelector`(설정 모달 → 계정 메뉴 트리거) 폴백을 뒀다. **비용**: 번들 gzip 380.1KB → 393.9KB(**+13.8KB, +3.6%**), 테스트는 포털 때문에 `container.querySelector` → `document.querySelector` 42곳과 `keyDown(window)` → `keyDown(document)` 7곳을 옮겼다(스타일 단정 385곳은 그대로 — 스타일을 우리가 계속 들고 있기 때문). 웹 +5 테스트(`Modal.test.tsx` — 포털·aria-modal·트랩·복귀·언마운트·막 클릭 규칙), 전체 1599 통과. **다음 후보**: 같은 방식으로 DropdownMenu/ContextMenu(13개 파일의 clamp/flip 손계산 제거) → Popover/Tooltip → Select/Switch/Tabs
- ✅ **Radix DropdownMenu 도입 ① — 에디터 GNB 드롭다운(요청)** Dialog에 이어 같은 방식으로 메뉴. 공용 `components/Menu.tsx`(Radix DropdownMenu 위에 **우리 패널 스타일 그대로**)를 얹고 툴바의 다섯 메뉴(편집·삽입·스타일·보기·내보내기 + 도움말·모바일 ☰)를 옮겼다. **손으로 짜던 셋이 사라진다**: ① 위치 — 트리거 아래에 붙이고 화면 밖이면 당기던 clamp 계산과 resize·scroll 리스너(파일마다 조금씩 달라 같은 버그를 여러 번 고쳤다) ② 바깥 클릭 닫기 — 창마다 달던 `window` mousedown 리스너와, 그 리스너에 안 닿게 뿌린 `stopPropagation`(그 전파 차단이 배경 드래그·마퀴와 부딪혀 또 다른 버그를 만들었다) ③ **키보드 — 아예 없었다**: 열린 메뉴에서 ↑/↓·Home/End·글자 타이핑(typeahead)이 통하지 않고 Tab만 됐다(실측으로 확인: 이제 ArrowDown이 첫 항목→다음, ArrowUp이 되돌아오고 Enter로 실행된다). 시각은 그대로 — 실측 gap 8.5px·왼쪽 정렬 dx≈0·내보내기만 우측 정렬(dx −87)·폭 190~250·면 흰색·라운드 10·그늘 `0 12px 32px`, 폰 44px 행, 가로로 돌린 폰(667×375)에서는 패널이 311px로 잘리며 스크롤(예전 `calc(100dvh - 64px)`과 같은 값). **행 스타일 두 갈래**: 목록형 메뉴는 `MenuRow`(Radix Item)로 갈아 끼우고, 자기 행 모양을 가진 내보내기 메뉴는 마크업을 그대로 두고 `MenuItemWrap`(`Item asChild`)로 감쌌다 — Radix가 Enter·Space에서 그 요소에 click을 쏘므로 기존 `onClick` 하나로 마우스·키보드가 같은 길을 탄다. **스타일 메뉴는 항목으로 감싸지 않았다** — 세그먼트·스와치가 든 **설정 패널**이라 화살표 이동이 뜻을 갖지 않는다(성격상 Popover이므로 다음 단계에서 옮기는 것이 정확하다). 호버 표시는 `.mf-ed-btn[data-highlighted]::after`로 기존 덧칠 레이어에 이어 붙였다(실측 opacity 0 → 0.075 — 마우스든 키보드든 같은 표시). **트리거에 click 폴백 하나**: Radix 트리거는 pointerdown에 여닫는데(네이티브 관례) 보조기술이 합성한 활성화와 코드가 직접 부르는 `.click()`은 click만 온다 → 직전 pointerdown을 기억해 250ms 안이면 건너뛴다(진짜 마우스에서 두 번 토글되지 않게). **비용**: 번들 gzip 393.9KB → 413.8KB(+19.9KB — popper/roving-focus가 함께 온다), 테스트는 행이 `role="button"` → `role="menuitem"`이 되어 조회 12곳을 옮겼다(스타일 단정은 그대로). 전체 1599 통과, 실브라우저 검증(다섯 메뉴 위치·크기·면·그늘, 키보드 이동·Enter 실행, 토글 재클릭 닫기, 바깥 클릭 닫기(캔버스 선택 무간섭), 메뉴로 메모 추가 후 선택 유지, 내보내기 행 5개 클릭, 스타일 패널 세그먼트·테마 스와치 동작, 390/667px 폰)
- ✅ **Radix DropdownMenu 도입 ② — 홈 우클릭 메뉴(요청)** 홈의 단 하나뿐인 메뉴(맵 카드 ☰·우클릭, 폴더, 스페이스 행, 빈 배경)를 같은 방식으로 옮겼다. **없어진 손계산**: 클릭 지점에서 화면 밖으로 나가지 않게 높이를 **행 수로 어림해**(`estimateHeight` — 힌트가 붙은 행이 있으면 어림이 틀렸다) 당기던 clamp·flip, 플라이아웃의 좌우 뒤집기(`subOnLeft`), 바깥 클릭·Escape 리스너. 트리거는 **클릭 지점에 놓인 0×0 자리표시자**이고(포인터를 받지 않는다) 메뉴는 그 자리를 기준으로 자기 **실제 크기**를 재서 선다 — 실측: (1430, 880)에서 열어도 메뉴가 화면 안(left 1248·bottom 880), 플라이아웃은 부모를 5px 물고 오른쪽에 서며 화면 안. **얻은 것**: ↑/↓ 이동·타이핑 탐색·→로 플라이아웃 열기·←로 닫기·Escape(예전에는 키보드로 아무것도 할 수 없었다 — 실측으로 전부 확인). **좁은 화면(폰)만 예외**: 플라이아웃이 양옆 어디에도 못 뻗어 부모 **아래로** 펼치던 동작(`data-inline`)은 `Sub`로 표현할 수 없어 같은 패널 안에 항목을 이어 그린다 — 부모 행은 `onSelect`에서 `preventDefault`로 메뉴를 열어 두기만 하고, 이어 그린 항목도 Radix 항목이라 화살표 이동에 함께 걸린다(실측 390px: 메뉴·하위 목록 모두 화면 안, 행 44px). **두 가드는 그대로**: 마퀴 시작 판정과 "카드 밖 클릭이면 선택 해제"가 `.mf-home-ctx`로 메뉴 안을 알아보는데, 포털로 나가도 `closest`는 DOM 조상을 보므로 판단이 성립한다(실측: 메뉴 위에서 끌어도 마퀴 0). **포털이 잡은 것 하나**: hover 하이라이트가 `.mf-home .menu-row:hover`로 `.mf-home` 안에 스코프돼 있어 포털로 나가는 순간 **닿지 않았다** → `.mf-home-ctx .menu-row:hover, …[data-highlighted]`를 새로 뒀다(`[data-highlighted]`는 Radix가 마우스·키보드 어느 쪽으로 겨눠도 붙이는 표시라 둘이 같은 모양을 얻는다. 실측 transparent → `rgb(250,243,238)`). **테스트 함정 기록**: jsdom에는 레이아웃이 없어 모든 사각형이 0이고, Radix는 "포인터가 하위 메뉴로 향하는가"를 그 사각형으로 판단한다 → **userEvent가 함께 쏘는 pointermove가 하위 메뉴를 닫아 버린다**(요소가 클릭 전에 DOM에서 떨어진다 — 실측으로 확인). 그래서 플라이아웃 안 항목은 `fireEvent.click`으로 누르고, 마우스 경로는 실브라우저 프로브가 맡는다. Escape도 `window`가 아니라 `document`에서 듣는다(window 이벤트는 document로 내려가지 않는다). 홈 374 테스트 통과(기대값 갱신: 자리는 메뉴가 아니라 자리표시자가 들고 있다·플라이아웃 조작 방식·Escape 대상), 전체 1599 통과. **여기서 멈춘 이유(범위 경계)**: 남은 메뉴 셋은 성격이 달라 이 단계에 넣지 않았다 — ① **캔버스 우클릭 메뉴**(`ContextMenu.tsx`)는 이식을 끝까지 만들어 보고 **되돌렸다**: 진입이 우클릭·길게 누르기 **포인터 전용**이라(Shift+F10 같은 키보드 진입이 없다) 키보드 이동이 닿을 길이 없어 얻는 게 거의 없는데, Radix 메뉴는 열릴 때 **초점을 가져간다** — 그러면 편집 중인 텍스트 박스가 blur되어 그 자리에서 커밋되고, "편집 중 우클릭으로 메뉴를 연 뒤 박스 안에서 텍스트를 선택한다"는 **제보에서 온 흐름**(테스트 3건이 지키고 있다)이 깨진다. 초점을 되돌리는 길은 Radix가 **private으로 감춘 `onOpenAutoFocus`**뿐이고(공개 타입에서 빠져 있다), 우회하면 focus-outside 해제·`pauseBlurCommit` 같은 장치를 겹쳐야 한다 — 잃는 것이 얻는 것보다 크다. ② **칸반 카드 우클릭 메뉴**도 같은 이유(포인터 전용). ③ **열 메뉴·스타일 메뉴·칸반 필터**는 목록이 아니라 **설정 패널**(스와치·세그먼트)이라 DropdownMenu가 아니라 **Popover**가 맞다 — 다음 단계로 넘긴다
- ✅ **A 메뉴가 열린 채 B를 누르면 B가 잠깐 열렸다 닫히던 문제(제보)** 원인이 **둘**이었고 둘 다 필요했다. ① **상태를 블라인드 토글로 뒤집고 있었다** — A가 열려 있을 때 B의 트리거를 누르면 한 번의 pointerdown에서 통지가 둘 온다(실측 로그: `B:열림` → `A:닫힘`). 툴바는 메뉴 하나를 `openMenu` 한 칸으로 관리하는데 `toggle(k)`가 "어느 메뉴의 통지인가"를 보지 않아, **뒤에 온 A의 닫힘이 이미 B를 가리키는 칸을 다시 뒤집었다**. 닫힘은 **지금 열려 있는 것이 나일 때만** 비우도록 고쳤다(`menuOpen(k)(next)` — 열림은 언제나 나로 설정, 닫힘은 `cur === k`일 때만 null). `MenuBarButton`의 `onToggle`도 없앴다: `Menu`가 열림·닫힘을 **그대로** 올려 보내고 어느 메뉴인지는 호출부가 안다(중간에서 토글로 접으면 같은 사고가 되돌아온다). ② **닫히는 A가 자기 트리거로 초점을 돌려준다** — Radix DropdownMenu의 기본 배려(Escape로 닫으면 초점이 트리거로 돌아온다)인데, 그 초점 이동이 **방금 마운트된 B에게는 "초점이 바깥으로 나갔다"로 읽혀** B가 스스로 닫혔다(실브라우저 로그 `삽입:true` → `삽입:false`, 클릭마다 열림/닫힘이 번갈아 나타난 것도 이 때문 — 앞에 닫힌 메뉴가 있을 때만 발생). 초점이 **메뉴 트리거**(`data-menu-trigger`)에 떨어진 경우만 `onFocusOutside`에서 닫힘을 취소한다 — Tab으로 메뉴 밖으로 나가면 **여전히 닫히고**(실측), Escape 후 초점이 트리거로 돌아오는 배려도 그대로다. **①은 jsdom에서, ②는 실브라우저에서만 드러났다** — 회귀 테스트는 ①을 잡고(수리 전 "B가 열리지 않는다"로 실패 확인), ②는 실브라우저 프로브가 지킨다(여섯 메뉴를 차례로 갈아 타며 매번 "열린 메뉴가 정확히 하나"를 프레임 단위로 확인). 웹 +2 테스트(전환·같은 트리거 재클릭, 전체 1601)
- ✅ **Radix Popover 도입 ① — 프로필 팝오버·알림 패널·공유 "?" 툴팁(요청)** 메뉴에 이어 **앵커에 붙는 패널**. 공용 `components/Popover.tsx`(Radix Popover 위에 우리 스타일 그대로). 이 셋이 메뉴와 다른 점은 **내용이 목록이 아니라는 것**이다 — 프로필 블록·알림 목록·설명 세 문장은 화살표로 훑는 항목들이 아니라 Tab으로 다니는 패널이다(그래서 DropdownMenu가 아니라 이 프리미티브). **없어진 손 장치 넷**: ① 자리 — `position: absolute` + `left:0; right:0`(부모가 곧 트리거일 때만 성립)·`top: calc(100% + 12px)`, 그리고 공유 툴팁이 한 줄 폭을 얻으려고 래퍼의 `position`을 static으로 두어 **제목 행에 걸치게** 하던 우회. 지금은 트리거 기준으로 서고 화면 밖이면 Radix가 당긴다(폭이 트리거와 같아야 하는 프로필 팝오버는 `--radix-popover-trigger-width` — 실측 249=249). ② 바깥 클릭·Escape — 홈 전역 mousedown의 `closest('.settings-pop,.settings-btn')` 한 줄, 벨의 document 리스너 둘, 툴팁의 window 리스너. ③ **닫힘 애니메이션을 위해 닫힌 뒤에도 마운트를 유지하던 `usePopAnim`** — Radix는 `data-state="closed"`에 걸린 CSS 애니메이션이 끝날 때까지 스스로 붙잡아 둔다(CSS를 `[data-state]`로 옮기고 훅 사용을 걷어냈다). ④ 프로필 트리거의 `div role="button"` + tabIndex + Enter/Space 핸들러 → **진짜 버튼**(초점·키보드가 공짜). **얻은 것**: `aria-expanded`, 닫은 뒤 **초점이 트리거로 복귀**(실측 Escape → `계정 메뉴`), 그리고 닫히면 언마운트. **모달 안의 팝오버**(공유 "?")는 층이 위라 **툴팁 안을 눌러도 모달이 닫히지 않는다**(실측·회귀 테스트) — 예전 구조에서는 모달의 바깥 클릭 판정에 걸릴 수 있는 자리였다. 실브라우저 검증: 프로필(트리거 폭·gap 8·r16·행 2개·바깥 클릭·Escape 초점 복귀) / 알림(352폭·gap 12·벨에 우측 정렬·목록 스크롤·오늘·이전 그룹·Escape) / 툴팁(330폭·세 문장 각 20px 한 줄·z 260·모달 유지). 테스트는 상태 단정을 클래스(`is-in`/`is-out`)에서 `data-state`로, 조회를 `container` → `document`(포털)로 옮겼다. 전체 1601 통과
- ✅ **Radix Popover 도입 ② — 스타일 패널·칸반 필터·열 메뉴** 같은 프리미티브로 **설정 패널 셋**. 이 셋이 목록이 아니라는 근거는 내용이다 — 세그먼트(레이아웃·연결선)·색 스와치 판(테마 7종·열 색 10 + 배경 8)·필터 칩. 화살표로 훑을 항목이 아니므로 DropdownMenu가 아니라 Popover이고, Tab으로 다니는 게 맞다. 에디터 GNB의 `스타일`은 `MenuBarButton` → 신설 `PanelBarButton`(트리거 모양은 `barTriggerStyle` **한 함수**를 메뉴 버튼과 공유 — 값을 두 벌로 두면 바에서 두 버튼이 달라 보인다). 없어진 손 장치: 필터·열 메뉴의 `position: absolute/fixed` + 폭을 빼서 왼쪽을 계산하던 clamp(`Math.max(8, Math.min(at.x - W, innerWidth - W - 8))`)와 **버튼 사각형을 재서 좌표를 상태에 담던 것**(`menuAt: {x,y}` → `menuOpen: boolean`), window `pointerdown`·`keydown` 리스너 세 벌. **직전 메뉴 수리와 같은 함정이 여기도 있었다** — 메뉴가 열린 채 `스타일`을 누르면 닫히는 메뉴가 자기 트리거로 초점을 돌려주고 방금 열린 팝오버가 그걸 "초점이 바깥으로 나갔다"로 읽어 스스로 닫힌다 → `Popover`에도 같은 `data-menu-trigger` 가드를 뒀다(실브라우저 실측: 편집 → 스타일 전환 후 메뉴 0·패널 열림). `ColumnIconButton`은 `forwardRef`로 바꿨다 — Radix `asChild`는 자식의 ref로 앵커 자리를 잡으므로 ref를 넘기지 않으면 메뉴가 어디에 설지 모른다. 실브라우저 검증(스타일 250폭·gap 8.5·z 200·세그먼트 동작·패널 유지·Escape 초점 복귀 / 필터 268·gap 8·우측 정렬·칩 필터링·바깥 클릭 / 열 메뉴 268·gap 6·항목 20+스와치 18·선택 후 닫힘). 테스트는 포털 조회로 옮겼다(`container` → `document`, 필터·열 메뉴·담당 칩). 전체 1601 통과
- ✅ **Radix ToggleGroup·RadioGroup·Switch 도입 — 세그먼트·라디오 카드·스위치(요청, 마지막 단계)** 손으로 짠 `<button aria-pressed>` 묶음을 의미와 키보드가 맞는 프리미티브로. 옮긴 곳 다섯: **칸반 보기 셋**(보드·리스트·타임라인)·**속성 패널 글자 크기**(작게·보통·크게)·**스타일 패널 레이아웃·연결선** → 공용 `Segmented`(ToggleGroup single), **설정의 색상 테마 6칸** → `RadioCards`(RadioGroup), **공유 모달의 링크 공유 토글** → `Switch`. `aria-pressed`("눌린 버튼")가 `role="radio"`+`aria-checked`("고른 값")로, 체크박스가 `role="switch"`("켜짐/꺼짐")로 바뀌었고 묶음 전체가 Tab 한 번(로빙 tabindex)이 됐다. **실브라우저가 잡은 것 — 화살표로 값이 바뀌지 않았다**: Radix RadioGroup은 "직전에 화살표를 눌렀는가"를 `document` keydown 리스너로 기억한 뒤 새로 초점을 받은 항목을 클릭하는데, **React 18은 이벤트를 루트 컨테이너에 위임**하므로 로빙 포커스의 초점 이동이 그 document 리스너보다 **먼저** 돈다 → 플래그가 늦게 서서 클릭이 없다(실측: 화살표는 초점만 옮기고 Space를 눌러야 선택됐다 — ToggleGroup은 원래 Space 방식이다). `role="radio"`로 알리는 이상 화살표로 값이 바뀌어야 하므로 `useArrowSelect`로 직접 처리한다(초점이 있는 칸을 기준으로 한 칸, 끝에서 순환). 실측 재검증: 크기 세그 →로 '크게'가 **저장까지**(`tsize: 'l'`), 스타일 레이아웃 →로 '조직도', 테마 →로 '오션'(강조색 `#2f7fd6`·`mf_home_theme` 저장), 칸반 보기 →로 리스트 화면 전환. **하지 않은 것**: 공유 모달의 권한 `<select>` 둘은 **native 그대로 둔다** — 이미 접근성이 온전하고, Radix Select로 바꾸면 **모바일의 OS picker**(iOS 휠·안드로이드 대화상자)를 잃는다. 얻는 것은 옵션 목록 스타일링뿐이라 트레이드오프가 거꾸로다(포인터 전용 우클릭 메뉴를 그대로 둔 것과 같은 판단). 필터의 '긴급만'·카드 필드 체크박스는 진짜 체크박스라 그대로. **후속 후보**: 색상 스와치 행(`SwatchRow` 14칸)도 하나만 고르는 묶음이라 화살표가 통해야 하는데, 지금 스와치에는 **접근 이름이 없다** — 색마다 이름을 정하는 일이 함께 필요해 별건으로 남긴다. 웹 +3 테스트(`Segmented.test.tsx` — role·화살표 순환·해제 무시·라디오 카드, 전체 1604), 번들 gzip 415.7 → 419.9KB
- ✅ **로그인 화면 새 디자인 이식 + 문구 다듬기(요청)** 디자인 원본(`Geurio 로그인 리디자인.dc.html`, 저장소 루트 보존)을 그대로 옮겼다. 왼쪽은 한 열짜리 폼(로그인·가입 / 이메일 인증 / 비밀번호 찾기 / 재설정), 오른쪽은 세 보기를 4.2초마다 번갈아 보여 주는 미리보기(`AuthPeek` — 880px 미만에서는 그리지 않는다: 좁은 화면의 일은 폼이고 미리보기는 곁다리다). 상태 기계와 실제 인증 호출(`useLoginController`)은 **한 줄도 손대지 않았다** — 화면만 바뀐다(GIS 폴백·가입 차단 콜아웃·세션 만료 안내+`?next=` 복귀·데모 코드·재전송 쿨다운·미가입 툴팁 전부 그대로). 배경은 캔버스와 같은 점 격자(24px), 필드는 48px·라운드 13에 코럴 포커스 링, 코드 칸은 등폭·자간 넓게. **제출 버튼 두 톤**: 필수 칸이 비어 있으면 옅은 코럴·비활성이고, 말해 줄 수 있는 문제(짧은 비밀번호·불일치)는 누른 뒤 문장으로 알린다 — "이유를 모르는 비활성 버튼을 만들지 않는다"는 규칙(설정의 비밀번호 변경과 같은 판단)을 지키면서 디자인의 두 톤을 얻는다. 문구는 더 친근하게: "다시 만나서 반가워요." / "몇 초면 시작해요." / "거의 다 왔어요!" / "비밀번호를 잊으셨나요?", 로딩은 "들어가는 중"·"만드는 중". **"이 브라우저에서 로그인 유지"는 죽은 체크박스로 두지 않고 실제로 동작하게 했다**(`rememberSession.ts`): 끄면 세션이 **탭 저장소**로 가서 창을 닫는 순간 사라진다(공용 PC). 읽기는 두 저장소를 다 보므로 이미 로그인해 둔 세션이 체크를 끈다고 사라지지 않고, 지우기는 두 곳 모두라 로그아웃이 반쪽으로 남지 않는다 — Supabase 클라이언트(`auth.storage`)와 로컬(데모) 어댑터가 같은 규칙을 쓴다. **실브라우저에서 잡은 함정**: `<label>` 안의 클릭은 그 라벨이 가리키는 컨트롤로 전달되고 `<button>`도 그 대상이 될 수 있어, 비밀번호 보기(눈)를 눌렀더니 같은 라벨 안의 '비밀번호 찾기'가 실행돼 화면이 넘어갔다 → 필드 묶음을 `<div>` + `htmlFor`로 바꿨다(라벨 관계는 그대로). 코드 칸의 안내 문구는 자간을 평소로 되돌렸다("메 일 로  받 은"으로 벌어졌다). 웹 +5 테스트(저장소 규칙) + 기존 기대값 갱신(문구·미리보기 노출), 실브라우저 검증(1440/390 배치 실측·네 단계 전환·보기 토글·유지 체크 저장·데모 코드 흐름·콘솔 오류 0)
- ✅ **랜딩 v2 디자인 이식(요청, 쌍둥이 둘 다)** 디자인 원본(`Geurio 랜딩 리뉴얼 v2.dc.html`, 루트 보존). 제품이 마인드맵 하나에서 셋(마인드맵·화이트보드·칸반)으로 늘어난 것을 랜딩이 처음으로 말한다 — 히어로 제목이 **"정리하는 방법은 하나가 아니에요."**다. 구성: 스티키 헤더 · 히어로(뱃지·CTA 둘·사실 셋 + 오른쪽 제품 창) · 세 가지 보기 카드 3장(미니 그림 + 요점 셋) · 협업(스레드 패널·타이핑 애니메이션) · 기능 격자 6 · FAQ 4 · 어두운 마무리 배너 · 푸터. 히어로 창은 세 보기를 6.2초마다 번갈아 보여 주고 탭을 누르면 고정된다(14초 뒤 자동 회전 복귀), 장면·문구 데이터는 `landingData.ts` 한곳. **정적 쌍둥이(`public/landing.html`)를 함께 새로 썼고, 첫 장면(마인드맵)을 원문 HTML에 그대로 넣었다** — 예전에는 "완성 상태 폴백 SVG → 인핸서가 걷어내고 처음부터 자람"이라 깜빡임을 막을 가림막(veil)이 필요했는데, 지금은 폴백이 곧 첫 화면이라 그 장치가 아예 필요하지 않다(`landing-demo.js`는 회전·탭만 얹는다. 없어도 페이지는 온전 — 계약은 "모든 내용이 JS 없이 원문에 보인다"). 지킨 계약: 브랜드 인증 3요소(앱 이름·목적 설명·개인정보처리방침 도달) · SEO head(canonical/OG/JSON-LD/한글 title) · 매니페스트 3종(#387) · CTA 세션 인지(/login ↔ /home) · 정적 FAQ는 `<details>`. **목적 설명 문구만 제품에 맞게** "마인드맵 서비스" → "마인드맵 · 화이트보드 · 칸반 보드를 한곳에서"(브랜드 인증이 보는 것은 "목적 설명이 있는가"라 문구 교체는 계약 안). 디자인에 있었지만 두지 않은 것: 푸터 '문의하기'(갈 곳이 없다), FAQ의 "30일 동안 휴지통"(그런 자동 삭제가 없어 거짓 약속이 된다). 웹 테스트를 v2 내용으로 재작성(React 8 + 정적 4), 전체 1611 + 코어 272 통과. 실브라우저 검증(정적·React × 1440/390 — 회전(6.2초)·탭 고정·FAQ 아코디언·스크롤 등장 0→14·가로 스크롤 없음·콘솔 오류 0)
- ✅ **랜딩 스크롤 등장이 라이브에서 죽어 있던 문제(제보)** "디자인은 아래로 스크롤하면 내용이 하나씩 페이드되는데 라이브는 첫 화면 말고는 전부 즉시 보인다." 원인은 **정적 쌍둥이**였다 — 그쪽 CSS가 `.lp-rv`에 즉시 재생되는 `animation: lp-rise`를 걸어 두어, 아래 섹션이 화면에 들어오기 전에 이미 등장을 끝낸 상태였다. 프로덕션 `/`는 이 정적 파일이 서빙되므로 **실제 방문자 전원이** 그 화면을 봤다(#390에서 "인핸서 없인 아무도 움직이는 데모를 못 봤다"와 같은 계열의 실수). 수리: 숨기는 규칙을 `html.lp-js` 아래로 옮기고 인핸서가 **첫 페인트 전에**(그래서 `defer`를 떼고 head 블로킹으로 되돌렸다) 그 클래스를 심는다 — JS가 없으면 클래스가 없어 모든 내용이 그대로 보인다(크롤러 계약 유지). **더 나쁜 것을 함께 잡았다**: 원래 방식(IntersectionObserver)은 교차 **상태가 바뀔 때만** 알려 주는데, 앵커를 눌러 훌쩍 건너뛴 섹션은 "아래에 있어 안 보임 → 위로 지나가 안 보임"으로 0 → 0이라 콜백이 오지 않아 **되돌아와도 영영 투명한 채로 남았다**(헤더 #faq 클릭 후 위로 올라가면 15개 중 8개가 빈 화면 — 실브라우저 재현. 디자인 원본도 같은 구성이라 같은 구멍이 있다). 관측을 버리고 rAF로 조절되는 스크롤 패스가 **접힘선(뷰포트 88%)을 넘었는가**를 직접 재서 이미 지나간 것까지 공개하고, 다 공개되면 리스너를 뗀다 — 두 쌍둥이가 같은 규칙을 쓴다. 웹 +1 회귀 가드(정적 CSS의 `opacity:0` 규칙은 반드시 `lp-js` 아래·즉시 재생 애니메이션 부재·인핸서의 클래스/공개 장치·head 블로킹 로드 — **수리 전 실패 확인**), 실브라우저 검증(첫 화면 15개 전부 숨김·첫 20프레임 깜빡임 0·스크롤에 따라 6→15 공개·앵커 점프 후 복귀 시 숨김 0·JS 끄면 숨김 0·React 쌍둥이 동일·첫 화면은 히어로 글 → 창 순서로 120ms 계단 페이드)
- ✅ **기능 격자가 "배경만 먼저" 보이던 문제(제보)** "쓰다 보면 알게 되는 것들 항목들이 디자인html과 달리 라이브에서는 배경이 먼저 보이고 컨텐츠가 하나씩 노출된다." 원인은 그 격자의 **실선 만드는 방식**이었다 — `gap: 1px` 위로 컨테이너 배경(`#EFE5DB`)이 비쳐 칸 사이 선이 되는 구조인데, 등장 대상이 **칸 하나하나**여서 칸이 투명한 동안 컨테이너의 베이지가 통째로 드러났다(실측: 컨테이너 opacity 1.00인데 칸 6개가 0.0으로 680ms — 내용 없는 둥근 사각형 하나. 디자인 원본도 칸을 대상으로 두지만 우리 접힘선 판정이 더 이른 탓에 빈 구간이 눈에 남았다). 수리: 등장 단위를 **격자 전체**로 올려 배경·실선·내용이 함께 떠오르게 했다(칸의 등장 전이도 제거 — 이제 호버 전이만 남는다). 다른 구획에는 같은 함정이 없음을 확인했다(모드 카드·스레드·FAQ 목록·마무리 배너는 각자 배경을 든 채 자기 자신이 등장 단위, 협업 띠는 화면 폭을 채우는 섹션 색이라 "빈 상자"로 읽히지 않는다). 웹 +2 회귀 가드(정적 HTML `grid lp-rv`·칸에 `lp-rv` 없음 / React 구조 — **둘 다 수리 전 실패 확인**), 실브라우저 검증(격자가 하나로 0→1 페이드·전환 중 캡처에서 빈 배경 구간 없음)
- ✅ **대시보드 ①(모델 + LNB + 보기 전용 위젯 + 피커)** 디자인 원본 `Geurio 홈 대시보드.dc.html` 이식 1단계(원본 루트 보존 — 수정 금지 대상). **스페이스 화면은 손대지 않는다**(사용자 지시 — 기존 디자인 그대로, LNB에 더한 것은 대시보드 구획과 스페이스 정렬 버튼뿐). **모델**: 대시보드는 문서가 아니라 **배치**다 — 위젯 하나 = `(docId, size)` 둘뿐(`dashboard/model.ts`, CAP 10 · 4열×152px dense · 크기 8종 · 칸반 최소 3×2), 내용은 썸네일 프리페치와 **같은 본문**(`previewDocs`)에서 그때그때 읽는다(`widgetData.ts` — 칸반=열·카드·진행바·done, 맵=루트+1단계 가지, 보드=메모 좌표 정규화). 저장은 스페이스와 같은 per-user 워크스페이스 블롭(`WorkspaceData.dashboards`, 옛 블롭은 필드가 없어 "대시보드 없음" — `coerceDashboards`가 깨진 항목을 조용히 버린다). 문서가 지워지면 위젯이 스스로 "찾을 수 없음"을 말한다(참조 무결성 강제 없음 — 복원하면 위젯도 되살아난다). **LNB**: `대시보드` 구획(행=격자 글리프+이름+위젯 수, **맨 위 = 기본** 배지, 새 대시보드) + ⠿ 정렬 토글(드래그·↑/↓, **스페이스 구획에도 같은 토글** — 요청의 두 번째 절반). 행 우클릭은 공용 `HomeContextMenu`(이름 변경 모달·삭제 확인창 — "배치만 사라지고 문서는 그대로"를 문구가 말한다). **화면**: `activeDash`면 본문이 통째로 `DashboardView`(다크 히어로 #332E29 고정 — 어두운 면이라 다크 테마에서도 성립, 인사+날짜+이름+N/10+다른 대시보드 알약+보드 추가 / 점 격자 바닥 / 4열 dense 격자, 모바일 2열로 접힘). 위젯은 전부 **보기 전용**(1단계 — 배치 편집 모드·드래그·리사이즈는 다음 단계라 히어로에 죽은 "편집" 버튼을 두지 않았다): hover `열기` 알약(터치는 상시), 클릭=에디터, 우클릭=열기·최신 내용 불러오기·맨 앞으로·크기(종류별 최소 잠금)·내리기. **피커**(보드 추가): 좌 레일(전체·스페이스들·공유받은 문서 + N/10) + 검색 + 카드 격자(홈 카드와 같은 `cardSketch` 실렌더 썸네일) + 발치 크기 선택(칸반은 3×2부터). **올린 뒤에도 피커는 열려 있고**(여러 개를 이어 올린다) 이미 올라간 카드는 "올림" 배지 — 누르면 내려간다(피커가 올리기·내리기 양쪽). 후보에서 docId 없는 옛 카드·휴지통은 뺀다(가리킬 문서가 없다 — 이름 변경·공유와 같은 가드). 웹 +16 테스트(모델 6 · widgetData 4 · 통합 6 — LNB 생성/전환·피커 올리기/CAP 안내·스페이스 정렬 저장·이름 변경/삭제·위젯 메뉴, 전체 1630), 실브라우저 29종(데스크톱 — 피커 레일/필터/크기 규칙/올림 배지, 위젯 실데이터(칸반 열·카드·1/4 done / 맵 루트·가지), hover 알약 0→1, 크기 변경 span 실측, 리로드 영속, 스페이스 정렬 블롭, 이름 변경·삭제 / 다크 / 모바일 390 — 가로 넘침 0·위젯 2열 접힘·알약 상시). **다음(합의된 계획)**: PR② 배치 편집 모드(드래그 재배치·리사이즈·등장 애니메이션), PR③ 칸반 위젯 인라인 열 이동, PR④ 모바일·테마 다듬기
- ✅ **대시보드 제보 5건 중 4건 수리(①②③⑤ — ④ 열 이동 표기는 PR③에서 기능과 함께)** ① **칸반 위젯이 실제 칸반 보드처럼 보이지 않았다** — 열 점·진행 바가 임의 고정색(KCOL_DOTS)이었고 분류 배지는 전부 강조색, 기한은 ISO 잘라 쓰기, 담당 원은 전부 코랄이었다. `widgetData`가 **에디터의 그 함수들**로 계산하도록 교체: 색 기준은 그 문서의 테마(`themeOf(d.themeKey)`), 열 점/배경=`columnColor`/`columnBg`(사용자 지정색 존중), 면 층=`boardSurface`/`innerLine`/`panel`, 분류=`tagColor`+`tagInk`(+`hexA` 틴트), 기한=`dueLabel`/`dueTone`(**완료 열은 지나도 붉지 않다** — #448 규칙), 아바타=`colorForSeed`(접속자 커서와 같은 시드), 진행 바=`boardProgress`(완료부터 왼쪽, 열 색 구간, 첫 열은 빈 트랙). 발치에 담당 아바타 겹침(디자인의 avatars)·개수 알약의 접힘 표기(`2/5`)·기한 달력 아이콘도 디자인 원본대로. **색이 문서 테마의 hex라 홈 다크에서도 실제 보드의 인상이 유지된다**(썸네일 `kanbanPreview`와 같은 계열 판단). ② **맵·화이트보드 위젯이 실제 문서와 다르게 보였다** — 디자인 원본의 목업 필(TONES 색·정규화 좌표)을 그대로 옮긴 것이 원인. 몸통을 홈 카드와 같은 **실렌더**(`realPreview` — 실제 좌표·도형·색·잉크·획)로 교체(`SceneBody`: wash+점 격자 위 중앙, hue는 피커 카탈로그의 그 값), `widgetData`의 mind/board는 지표(노드/메모 수)만 남겨 단순화(획만 있는 보드도 그릴 게 있다고 판정). ③ **오프라인 바가 브라우저 전폭을 차지** — `left/right: 12` 고정을 걷어내고 토스트 기본형(하단 중앙, 내용 폭 — 실측 315px)으로. `mf-toast-in` 키프레임이 원래 가운데 정렬(translateX(-50%)) 전제라 #387 계열 충돌도 함께 사라진다. ⑤ **정렬 토글의 "끌어서 이동" 힌트 문구 제거**(대시보드·스페이스 두 구획 모두 — ↑/↓ 버튼과 grab 커서가 이미 말한다). 웹 테스트 재작성(widgetData 3 — 에디터 규칙 드리프트 가드 포함, 전체 1629), 실브라우저 33종(진행 바 구간 색·실렌더 SVG+실제 글자·오프라인 바 폭/중앙 실측·힌트 문구 0·다크·모바일)
- ✅ **대시보드 ②(배치 편집 모드 + 런치 전환)** 디자인 원본의 editMode·ghLaunch 이식. **편집 모드**(`dashEdit` — 화면 상태, 저장 안 됨, 데스크톱 전용: HTML5 드래그·모서리 리사이즈가 마우스 전제라 모바일 다듬기는 PR④): 히어로 "편집" 토글(활성=디자인의 #F2A184+다크 잉크 — 히어로가 고정 다크 면이라 색도 고정값), 안내 띠, **고스트 격자 셀**(absolute 오버레이 그리드, `max(2, ceil(면적/열))+3`행, pointer-events none), 위젯 **드래그 재배치**(HTML5 — 잡은 위젯을 놓은 자리에 splice(`moveDashItem`=moveInList), dense가 나머지를 다시 흘림, 끌리는 위젯 .45+강조 테두리), **모서리 리사이즈**(window mousemove로 시작 사각형 기준 픽셀→칸 환산, 종류별 최소(칸반 3×2) 클램프, 라이브는 점선 오버레이+다크 mono 크기 라벨로만 보여 주고 **커밋은 손 뗄 때 한 번** — undo·저장 한 단계), 인라인 컨트롤(크기 순환 mono 버튼 + 내리기 ✕ — 1×1은 머리에 자리가 없어 아래 구석 오버레이), 편집 중엔 열기 알약·클릭 열기·보기 전용 배지 숨김, 편집 중 여유 드롭 행. **런치 전환**(위젯 클릭): 위젯의 화면 사각형에서 **화면 전체로 펼쳐지는 카드**(`mf-dash-launch` 키프레임 — CSS 변수 --lx/--ly/--lw/--lh로 from을 지정하고 **인라인 기본 스타일이 곧 도착 상태**라 reduced-motion이 애니메이션을 끄면 전체 화면 로딩 카드로 조용히 축퇴) + 베일 페이드 + 스피너 + "이름 / 종류 · 스페이스 여는 중". 실제 이동은 기존 `openWithLoader`(900ms)가 하고 오버레이는 그 위(z 260)를 덮다 홈째 언마운트된다. **실브라우저가 잡은 함정**: 오버레이를 대시보드 루트 안에 두면 그 루트의 `mf-fade`(fill both)가 **스태킹 컨텍스트를 영구로 남겨** fixed z 260이 컨텍스트째 z auto로 깔리고 LoadingOverlay(z 200)가 위에 그려진다 → `createPortal(document.body)`(#488 확인창과 같은 계열). **프로브 함정 기록**: 합성 DragEvent 셋을 한 evaluate에서 동기로 쏘면 React 상태(dragIdx) 커밋 전에 drop이 도착해 재배치가 안 된 것처럼 보인다 — 실사용자 드래그는 이벤트 사이에 프레임이 있으므로 프로브도 사이를 띄워야 한다. 웹 +5 테스트(토글 장치 일습/드래그 재배치 저장/크기 순환·내리기/리사이즈 오버레이·무변경 무커밋/런치+편집 중 무런치, 전체 1634), 실브라우저 19종(리사이즈 실기하 2×2→3×3 라벨·span·블롭, 드래그 순서 블롭, 크기 순환, 편집 중 클릭 무런치, 런치 시작 사각형→전체 화면→에디터 이동, 모바일 편집 버튼 없음)
- ✅ **대시보드 ③(칸반 위젯 인라인 열 이동 + "열 이동 가능" 배지 — 제보 ④ 해소)** 대시보드에서 유일하게 허용된 편집(디자인 원본의 카드 드래그) — 칸반 위젯의 카드를 **열 사이 드래그**로 옮기면 문서에 저장된다. **컨트롤러 `moveDashCard`**: 문서를 열지 않고 그 자리에서, `applyMapTitle`과 같은 결로 load → 코어 `moveCard`(대상 열 맨 뒤) → `prevVersion` 저장, 충돌은 최신 판으로 **한 번 재시도**(재시도에서 이미 그 열이면 성공 처리). **낙관 반영**: 위젯이 읽는 previewDocs를 먼저 옮겨 그리고, 저장 실패 시 되돌리며 ToastModal로 알린다("보기 전용으로 공유받은 보드는 옮길 수 없어요" — 진짜 게이트는 서버 RLS). 성공 시엔 저장된 실제 본문(`serializeDoc`)으로 다시 덮고 에디터 로컬 복구본(`docKey`)도 같은 판으로. **뷰**: 카드 draggable + grab 커서(dragstart `stopPropagation` — 편집 모드의 위젯 재배치 드래그와 갈라 둠), 드롭 대상 열은 점선 강조 테두리 + 옅은 틴트(`data-drop-hot`, 제 열 위에서는 안 뜸), 배지는 칸반이면 **"열 이동 가능"**(초록 `--mf-success-soft`/`-ink` + 셰브론, 툴팁 "대시보드에서 카드의 열만 옮길 수 있어요"), 맵·보드·**보기 전용으로 공유받은 칸반**은 "보기 전용" 그대로(그때는 드래그 자체를 내주지 않음 — `sharedMaps` role 판정). 배치 편집 모드 중에는 카드 드래그 꺼짐(그 시간의 드래그는 위젯 재배치). 웹 +6 테스트(배지 갈림/이동 저장 prevVersion·낙관 렌더/실패 되돌림+안내/충돌 1회 재시도/보기 전용 차단/편집 모드 차단, 전체 1640), 실브라우저 12종(배지·툴팁·드롭 하이라이트 점선 실측·이동 후 열 렌더+`2/4 done` 갱신·문서 영속·리로드 유지·제 열 무반응·편집 모드 draggable=false·연속 이동). **프로브 함정(재발 기록)**: 합성 DragEvent를 동기로 연달아 쏘면 React 상태 커밋 전에 drop이 도착한다 — 이벤트 사이를 띄울 것(PR②와 같은 함정)
- ✅ **대시보드 ④(모바일 편집 모드 + 테마 스윕 — 대시보드 마무리)** ① **편집 모드를 폰에도 연다** — PR②가 편집을 데스크톱 전용으로 뒀던 것(드래그·모서리 리사이즈가 마우스 제스처라)을, 모드 자체는 열되 **어포던스를 기기별로 갈랐다**: 폰은 위젯 드래그·리사이즈 핸들을 그리지 않고(터치에서 발화하지 않는 조작을 내주지 않는다) 크기 순환 버튼·✕가 **30px 터치 타깃**(에디터 칸반 버튼과 같은 기준)으로 커지며, 재배치는 길게 누르기 메뉴의 "맨 앞으로 옮기기"가 맡는다. 편집 안내 띠도 기기별 문구 — 폰에서는 실제로 되는 조작만 말한다("크기 버튼으로 바꾸고 ✕로 내려요. 길게 누르면 맨 앞으로"). 칸반 카드 열 이동(PR③)도 HTML5 드래그라 **폰에서는 배지가 '보기 전용'**으로 바뀐다(끌리지 않는데 '열 이동 가능'을 달면 거짓말 — 정직한 배지 원칙). ② **테마 스윕**: 여섯 테마 × 대시보드·다크 피커를 실브라우저로 실측(위젯 제목 대비 13.8~14.3 · 배지 4.8~7.3 · 히어로 11.8) — 잡힌 것 하나: **모노 테마에서 히어로의 대시보드 이름 점이 사라졌다**(히어로는 고정 다크 #332E29인데 점이 `--mf-accent`라 모노의 #2b2b2b가 배경에 묻힘) → 점에 옅은 빛 테두리(`0 0 0 1px rgba(247,239,232,.3)`) — 어느 강조색에서도 점이 산다(밝은 테마에서는 거의 안 보이는 값). 칸반 위젯 내부가 다크 홈에서도 문서 테마색인 것은 #509의 의도된 결과(실제 보드의 인상 유지)임을 스크린샷으로 재확인. **프로브 함정 기록**: 테마 스크린샷이 전부 워시로 보였다 — `[data-dashboard-view]`의 mf-fade(0.3s) 중간 프레임을 찍은 것(대비 실측은 computed color라 무관). 촬영은 페이드가 끝난 뒤로. 웹 +1 테스트(모바일 편집: 보기 전용 배지·카드/위젯 draggable 아님·모바일 배너 문구·리사이즈 핸들 없음·30px 컨트롤, 전체 1641), 실브라우저 38종(모바일 14 — 히어로 두 알약·가로 넘침 0·크기 순환 저장·길게 누르기 → 맨 앞으로 저장·✕ 내리기·편집 끝내기 / 테마 6벌 × 대비 3종 + 점 테두리 + 다크 피커). **대시보드 4단계(①→④) 완료** — 남은 것은 라이브 배포(사용자 승인 필요)
- ✅ **대시보드 칸반 위젯 = 에디터 그대로(제보: 열 크기·카드 디자인이 실제 칸반 에디터와 너무 다르다)** 예전 위젯은 디자인 목업의 **미니 렌더**(9.5px 글자·오그린 열·간이 배지)라 같은 보드가 대시보드에서 다른 물건처럼 보였다. 수리의 핵심은 다시 그리지 않고 **에디터의 그 부품을 그대로 재사용**한 것: `KanbanBoard.tsx`에서 `cardBase`/`COL_W`/`COL_SHADOW`를 export하고(이미 export던 `CardFace`와 함께 — 앱이 단일 번들이라 비용 0), 위젯 카드가 `cardBase(k, th, false)` + `<CardFace>`로 **에디터 카드와 한 코드**가 됐다(분류 배지·본문 rich 13.8px·기한 톤·댓글 수·담당 얼굴·긴급 왼 3px 테두리·빈 곁정보 자리 유지까지 전부). 열도 에디터 스펙 그대로(폭 308/모바일 264·라운드 16·COL_SHADOW·머리의 점 8px+제목 13.5/700+카드 수 알약) — 넘치면 **오그리는 대신 가로 스크롤**(에디터 보드와 같은 배치. 위젯이라고 열을 오그리면 그 순간 에디터와 다른 물건이 된다). ＋/⋯·컴포저·더블클릭 상세는 내주지 않는다(에디터 읽기 전용 모드와 같은 얼굴) — **상호작용은 카드 열 이동 하나**(요청 그대로), 놓일 자리도 에디터의 점선 자리 상자(끄는 카드 높이, 이 경로는 열 끝에 붙으므로 맨 뒤)로 통일. **곁정보도 출처가 같다**: 댓글 수는 `commentStore.list`를 위젯당 한 번 읽어 에디터와 같은 규칙(미해결 스레드 수)으로 세고, 담당 사진은 `useParticipantAvatars`(둘 다 칸반 위젯일 때만 — 다른 종류는 왕복 0). `widgetData`는 문구·색으로 미리 접던 것을 걷어내고 **실물(열·카드·분류·테마 키)을 그대로** 넘긴다 — 접은 만큼 에디터와 어긋난다는 것이 이번 제보의 교훈. 진행 바·발치 아바타는 위젯 틀의 몫이라 유지. **프로브 함정 기록**: 손으로 지은 시드의 `tags`에 `id`를 빠뜨리면 에디터가 CRDT 엔티티 동기화에서 통째로 죽는다(실앱은 항상 id를 만들어 실데이터에선 불가능한 상태 — 시드는 실물 모양대로). 웹 +1 파리티 가드(열 308·CardFace 곁정보 자리, 전체 1642) + widgetData 테스트 재작성, 실브라우저 20종(열 스펙·카드 7속성·**에디터를 같은 문서로 열어 열 폭/카드 라운드/폰트/면 색 계산값 대조**·드롭 강조+자리 상자·이동 저장·2/4 done·가로 스크롤·모바일 264/드래그 없음/페이지 넘침 0)
- ✅ **대시보드 칸반 위젯 색이 에디터와 다르던 문제(제보 후속 — "에디터에는 스타일이 없는데 왜 색이 다른가")** 그 물음이 곧 답이었다: **칸반 에디터는 스타일 메뉴가 없어 `doc.themeKey`를 아예 쓰지 않고 항상 고정 `UI_THEME`(코랄)으로 그린다**. 그런데 칸반 문서는 템플릿이 `BOARD_THEME_KEY('white')`를 관성적으로 실어 두고, 직전 판의 위젯이 `themeOf(doc.themeKey)`를 읽어 **위젯만 화이트 팔레트**(열 점·분류가 파랑 시작, 면 층도 순백 계열)가 됐다 — 홈 카드 썸네일(`kanbanPreview`)은 #446부터 UI_THEME 고정이라 멀쩡했고, 직전 프로브가 못 잡은 건 시드를 'coral'로 지어서다(coral은 UI_THEME과 같은 값 — **시드는 실물 모양대로**의 두 번째 사례). 수리: 위젯(`KanbanBody`)·`widgetData`(진행 바 팔레트)를 **UI_THEME 고정**으로, `WidgetKanban.themeKey` 필드 제거(남겨 두면 또 읽는다). 테스트 픽스처도 실세계 값('white')으로 바꿔 **수리 전 실패 확인**(위젯 점 rgb(47,127,214) ≠ 에디터 rgb(240,102,63)), 파리티 가드에 색 단정 추가. 실브라우저 23종 — themeKey 'white' 문서를 **에디터와 나란히 열어** 열 점·열 면·분류 배지 계산색 전부 일치 실측(전체 1642 무회귀)
- ✅ **홈의 첫 화면을 기본 대시보드로(요청)** 홈에 들어오면 스페이스 그리드 대신 **기본 대시보드**(목록 맨 위 = LNB의 '기본')가 열린다. 새 상태를 만들지 않고 이미 있던 **탭 세션 복원**(`mf_active_view`, 스페이스·폴더를 기억해 에디터에서 돌아오면 그 자리로)에 `activeDash`를 더해 규칙 하나로 정리했다: ① 이 탭이 기억한 화면이 없으면 = **첫 진입** → 기본 대시보드 ② **대시보드**에서 맵을 열고 돌아왔으면 그 대시보드로(예전에는 대시보드에서 나가도 스페이스 그리드로 떨어졌다 — 함께 고쳐진 구멍) ③ **스페이스**에서 나갔으면 예전 그대로 그 스페이스로(가로채지 않는다) ④ 기억한 대시보드가 사라졌으면(다른 기기에서 삭제) 기본 대시보드로 물러선다 — 없는 화면을 열지 않는다 ⑤ **대시보드가 하나도 없는 사용자는 지금 그대로 스페이스 그리드**(빈 대시보드를 지어내 첫인상을 바꾸지 않는다). 판단은 하이드레이션 안에서 **한 번만**(`landedRef`) — 하이드레이션은 마운트 + 인증 확인 뒤 재동기화로 두 번 돌 수 있어서, 매번 정하면 그 사이 사용자가 고른 스페이스가 대시보드로 되돌아간다. **함께 잡은 함정**: 폰의 ≡(서랍 손잡이)는 **스페이스 툴바 안에만** 있어서, 대시보드가 첫 화면이 되는 순간 폰 사용자는 스페이스·검색·알림으로 갈 길이 사라진다(테스트가 `메뉴 열기`를 못 찾아 드러났다) → 대시보드 히어로에도 같은 고스트 ≡(44px, 미확인 공유 점 포함, 색만 다크 히어로에 맞춤)를 뒀다. 웹 +6 테스트(첫 진입·무회귀·스페이스 복원·대시보드 복원·사라진 대시보드·폰 ≡ — 넷은 수리 전 실패 확인, 전체 1648), 실브라우저 12종(첫 진입·기본 대시보드·위젯 렌더·스페이스 왕복·대시보드 왕복·새 탭·대시보드 없는 사용자·폰 ≡ 44px·서랍 열림)
- ✅ **연결돼 있는데 "오프라인"이라 하던 문제(라이브 제보)** 로그인·문서 로드가 다 되는데 홈 하단 바("오프라인이에요")와 에디터 문서 칩("오프라인")이 떴다. 두 표시의 출처는 **한 곳뿐**이다(`useOnline` → `navigator.onLine`) — 앱이 요청 실패를 보고 판단하는 게 아니라 **브라우저 플래그를 그대로 믿고 있었다**. 그 플래그는 "랜선이 꽂혀 있다" 수준의 신호라 양방향으로 틀린다: 캡티브 포털에서는 true인데 못 나가고(그건 저장 결과가 잡는다), **VPN·가상 네트워크 어댑터가 있는 환경에서는 멀쩡히 연결돼 있는데 false로 굳는다**(제보자 환경이 이 경우). 수리: 플래그가 오프라인이라고 하면 **진짜 요청을 한 번 보내 확인한 뒤에만** 그렇게 말한다 — `HEAD /__net-check?t=…`(precache에 없고 navigate도 아니라 **서비스 워커가 가로채지 않고** 그대로 네트워크로 나간다, `cache:'no-store'`, 5초 타임아웃). 응답 내용은 보지 않는다: **서버에 닿았다는 사실 자체**가 증거라 404도 200도 좋다. 시작값은 낙관(true)이라 확인 전에는 오프라인이라 말하지 않고, 진짜 오프라인이면 요청이 즉시 실패해 곧바로 바가 뜬다. 플래그가 거짓말인 기기에서는 'online' 이벤트가 올 리 없으므로 **30초 주기로 다시 확인**해 진짜 끊김을 놓치지 않는다(그런 기기에서만 돈다). 웹 +1 회귀 테스트(제보 그대로: 플래그 false + 요청 성공 → 칩이 '저장됨' — **수리 전 실패 확인**) + 기존 오프라인 테스트를 "확인 후 표시"로 갱신(전체 1649), 실브라우저 7종(플래그를 false로 굳힌 컨텍스트에서 HEAD 확인 요청 실측·홈 바 없음·에디터 칩 '저장됨' / CDP로 진짜 오프라인 → 바 뜸 → 복구 시 사라짐)
- ✅ **대시보드 위젯은 "열기" 버튼으로만 이동(요청)** 카드 어디를 눌러도 에디터로 떠나던 것을 **버튼 하나**로 좁혔다 — 위젯 안에서 칸반 카드를 옮기거나(#511) 내용을 읽다 실수로 화면이 통째로 바뀌는 일이 없다. 바뀐 것은 셋: 위젯 루트의 `onClick` 제거, 커서를 `pointer` → `default`(열린다는 거짓 약속을 하지 않는다 — 이 프로젝트의 "정직한 어포던스" 규칙), 그리고 주석·CSS 설명을 새 규칙으로. **알약은 그대로 hover 노출**(데스크톱은 카드에 손을 얹으면 드러나고, 터치에는 hover가 없어 `@media (hover: none)`로 늘 보인다 — 유일한 문이 된 만큼 이 규칙이 그대로 발견 가능성을 맡는다). 키보드는 진짜 `<button>`이라 Tab으로 닿고 `:focus-within`이 알약을 드러낸다. 편집 모드에서는 예전처럼 알약 자체가 없다(그 시간의 클릭은 배치 조작). 웹 테스트 1건을 새 계약으로 재작성(편집 중 알약 부재 · 카드 머리·몸통 클릭 무이동 · "열기"만 런치 — **수리 전 실패 확인**), 실브라우저 10종(커서 default · 제목/몸통/칸반 카드 클릭 무이동 · hover 알약 opacity 1 · 런치 → 에디터 / 폰: 알약 상시 · 카드 탭 무이동 · 알약 탭 이동)
- ✅ **로그인 직후 홈 첫 진입이 기본 대시보드가 아니던 문제(제보)** 직전에 넣은 "첫 진입 = 기본 대시보드"가 **갓 로그인한 탭에서만** 동작하지 않았다. 원인 셋이 겹쳐 있었다(로그를 심어 순서를 실측): ① 마운트 하이드레이션이 **인증 토큰 적용 전에** 돌아 워크스페이스를 못 읽고(`ws === null` → dashboards 빈 배열) 그 상태로 판단을 끝내(`landedRef`) 곧 도착하는 인증 확인 재동기화가 손을 쓸 수 없었다 → **근거가 있을 때만 결정한다**(기억한 화면이 있거나 블롭을 실제로 읽었을 때만; 아니면 다음 하이드레이션으로 넘긴다) ② 그 사이 "보던 화면 기억" 효과가 먼저 돌아 **`activeDash: null`을 세션에 남겼고**, 재동기화가 그것을 "사용자가 스페이스를 보고 있었다"로 읽었다 → 첫 화면을 **정하기 전에는 기억하지 않는다** ③ 두 하이드레이션이 겹칠 수 있어(둘 다 비동기) 먼저 시작한 빈 조회가 나중에 끝나면 새 결과를 덮었다 → **세대 카운터**로 마지막에 시작한 요청만 적용(#335와 같은 결). 덤으로 **로그인은 새 시작**이므로 성공 시 탭의 화면 기억을 비운다(`clearActiveView` — 만료로 튕겨 나갔다 재로그인한 탭에 지난 세션의 스페이스가 남아 그리로 복원되던 경우. `?next=`로 돌아갈 **주소**는 이 값과 무관해 그대로 동작한다). 웹 +2 테스트(재동기화 착지 — **수리 전 실패 확인**, 그리고 로그인 시 기억 삭제. 전체 1651), 실브라우저 5종(실제 로그인 화면을 거쳐: 새 탭 로그인 → 기본 대시보드 / 만료 후 재로그인 → 기본 대시보드 + 기억 갱신 / 스페이스에서 나갔다 오면 스페이스 무회귀)
- ✅ **만들기 팝업 디자인 이식 + 대시보드 색상(첨부 이미지 2장)** 두 팝업이 같은 꼴이라 **껍데기를 하나로** 만들었다(`CreateDialog` — 아이콘 칩 + 제목/부제, 라벨 오른쪽의 `0/10` 등폭 카운터, 코럴 테두리 입력(열면 초점), 색 원 여섯(고른 칸에 흰 체크 + 같은 색 링), 우측 정렬 푸터). 스페이스 팝업은 이 참에 **Radix Modal로 이식**했다 — 홈에서 유일하게 `display:none`으로 떠 있던 모달이라 초점 트랩·Escape·닫힌 뒤 언마운트가 없었다. **막 클릭으로는 닫지 않는다**(이름을 적는 중이라 실수로 버려진다 — Escape·취소·✕만). ① **`새 대시보드`가 팝업을 연다** — 예전에는 이름을 자동으로 붙여(`nextDashName`) 곧바로 만들었고, 그래서 이름·색을 정할 자리가 없었다(그 함수와 테스트는 제거 — 이제 이름은 사용자가 적는다). ② **`DashboardData.color` 신설**(있을 때만 직렬화, `coerceDashboards`가 검증 — 옛 데이터는 예전처럼 강조색). 고른 색이 **보이는 곳을 세 군데** 뒀다: LNB 행 격자 글리프, 히어로의 이름 앞 점, 히어로의 다른 대시보드 알약 점 — 고르면 아무 데도 안 나타나는 색 선택은 죽은 UI다. ③ **이름 변경도 같은 팝업**이다(제목·버튼 글자만 다르고 색까지 고친다 — 스페이스가 이미 그랬다: 잘못 고른 색을 되돌릴 길이 없으면 안 된다). 상태는 `dashRename` → `dashDialog {id|null, name, color}` 하나로 합쳐 만들기·이름 변경이 한 흐름을 쓴다. 이름 상한은 두 종류 모두 **10자**(카운터가 미리 말한다 — 대시보드 이름은 예전 30자였다). 웹 +1 / 갱신 2 테스트(팝업 계약: 아이콘·부제·0/10·색 6·빈 이름 비활성 / 만들기 흐름: 색 선택 → 블롭에 이름+색 → LNB 글리프·히어로 점 실측, 전체 1651), 실브라우저 14종(2배 스크린샷을 첨부 이미지와 대조 + 폭 452·라운드 24·칩 54/16·입력 50·체크+링·우측 푸터·초점·막 클릭 무시·Escape·이름 변경 미리 채움)
- ✅ **대시보드 로딩 스켈레톤(제보: 대시보드로 착지하는데 스페이스 스켈레톤이 떴다)** 첫 화면이 대시보드가 된 뒤에도 로딩 껍데기는 예전 그대로 **스페이스 모양**(최근 항목 띠 + 카드 격자)이라, 잠깐 스페이스 화면이 그려졌다가 대시보드로 갈아 끼워졌다 — 착지 판정이 **워크스페이스 블롭을 읽은 뒤**에 나므로 로딩 중에는 어느 화면으로 갈지 아직 모른다는 것이 뿌리다. 수리: 착지를 정하는 그 자리에서 **결과를 이 기기에 적어 둔다**(`saveLandingHint('dash'|'space')`, localStorage `mf_home_landing`)와, 다음 진입의 첫 렌더가 `predictLanding()`으로 **모양을 미리 고른다**(이 탭이 기억한 화면(`mf_active_view`)이 있으면 그것이 먼저 — 스페이스에서 나갔다 온 탭은 스페이스 껍데기다). 힌트가 없는 **첫 방문은 예전 그대로 스페이스 모양** — 대시보드 껍데기를 지어내지 않는다(아직 대시보드가 있는지조차 모른다). 껍데기(`DashboardSkeleton`)는 실제 화면과 같은 뼈대다: 고정 다크 히어로(#332E29 — **진짜 색**이라 로딩이 끝나며 띠가 색을 바꾸지 않는다) + 점 격자 바닥 + 같은 행 높이·간격의 위젯 칸(데스크톱 2:1:1 / 폰 2칸 — 실제 개수를 모르므로 흔한 배치만큼만 잡는다. 많이 그리면 로딩이 끝날 때 크게 줄어든다). 히어로 자리표시자는 어두운 면 위라 공용 `mf-skel`이 아니라 흰색 알파고, 고정 폭 바에 `maxWidth: 100%`를 둬 좁은 폰에서 오른쪽 버튼 자리와 겹치지 않는다. 판정은 **첫 렌더에 한 번만**(`useRef`) — 로딩 중에 모양이 바뀌면 그 자체가 깜빡임이다. 웹 +3 테스트(대시보드 착지 → 대시보드 껍데기·스페이스 격자 부재 / 스페이스 착지 무회귀 / 착지 후 힌트 기록, 앞의 둘은 수리 전 실패 확인, 전체 1654), 실브라우저 6종(**추가된 노드 기록**으로 관찰 — 대시보드 껍데기가 떴고 스페이스 스켈레톤은 한 프레임도 없다 / 스페이스 착지 무회귀 / 로딩 끝나면 소멸 / 첫 방문 / 힌트 기록) + 껍데기 모양 캡처(데스크톱·폰). **프로브 함정 둘**: `addInitScript`는 **페이지 생성 전에** 붙여야 그 페이지에 적용되고, init script 시점에는 `document.documentElement`가 **아직 null**이라 그걸 관찰하면 조용히 실패한다(`document`를 관찰). 게다가 마운트 커밋과 데이터 커밋이 같은 태스크에서 일어나면 현재 DOM 재조회는 늘 "없었다"로 보인다 — **추가된 노드 기록**을 봐야 한다
- ✅ **대시보드에서 영역 지정 제거 + 위젯 크기 N×4(제보·요청)** ① 위젯 우측 하단으로 끌면 **영역 지정 사각형**이 떴다 — 홈의 마퀴(#446)가 `<main>`에 걸려 있어 대시보드 화면에서도 함께 돌았다. 마퀴가 고르는 것은 **맵·폴더 카드**인데 대시보드에는 그런 카드가 없고, 게다가 그 자리는 편집 모드의 **리사이즈 손잡이**라 조작이 겹친다 → 대시보드 화면에서는 마퀴를 걸지 않는다(`disabled: isMobile || !!state.activeDash` — 편집 모드든 아니든 한 규칙, 스페이스 화면은 그대로). ② 크기를 **최대 4행**까지(`DASH_ROWS_MAX` 신설 — 디자인 원본은 3행까지였다): 선택지에 `2x4`·`3x4`·`4x4`를 더하고(피커·크기 순환·우클릭 메뉴가 같은 목록을 쓴다) 모서리 드래그 클램프·`parseSize`도 4행으로, 편집 안내 문구에 "최대 4×4까지"를 적었다. **곁들여 잡은 잠복 버그**: 저장값 검증이 **선택지 목록 membership**이라, 모서리 드래그가 만든 목록 밖 조합(예: `2x3`)이 다음 로드에서 조용히 `2x2`로 되돌아갔다 → 신설 `isValidSize`(격자 안의 `CxR`인가)로 바꿔 자유 리사이즈 결과가 그대로 복원된다. 웹 +5(전체 1659 — 통합 3은 모두 수리 전 실패 확인, 모델 유닛 2), 실브라우저 12종(보기·편집 모드 드래그에 사각형 0·글자 선택 잠금 없음·리사이즈 미리보기 4×4·저장·리로드 복원·격자 span 4·메뉴 4×4·스페이스 마퀴 무회귀). **프로브 함정(세 번째 기록)**: `addInitScript`는 리로드에도 재실행돼 시드가 저장소를 다시 덮으므로, 가드하지 않으면 "리로드 후 크기가 되돌아간다"가 앱 버그처럼 보인다(실제로 이번에도 그렇게 한 번 오진했다)
- ✅ **런치 시작점을 "열기" 버튼으로 + 로딩 표시를 스피너 하나로(요청 2건)** ① 대시보드 위젯의 펼침 전환이 **위젯 사각형**에서 시작해, 손끝(누른 버튼)과 무관한 자리에서 화면이 자랐다 → 시작점을 **누른 그 버튼의 사각형**으로(`e.currentTarget`). 실측: 60×26 알약 자리에서 자란다(예전엔 539px 폭 위젯). ② 로딩 애니메이션이 화면마다 갈려 있었다 — 대시보드 런치·PWA 업데이트 적용은 **회전 링**인데 로그인·가입·스페이스에서 맵 열기·로그아웃은 dc 원본의 "코어 + 가지" 마인드맵 애니메이션이었다. 사용자가 고른 쪽(스피너)으로 통일: `LoadingOverlay`가 34px·2.5px·0.7s 링 하나를 그린다(런치·업데이트 오버레이와 같은 값). 색은 **프롭**이라 로그인은 브랜드 코럴 기본값을 쓰고 홈은 지금 테마의 강조색(`--mf-accent`/`-soft`)을 넘긴다 — 테마를 오션으로 바꾸면 맵 열기 로더도 파랑이 되어 대시보드 런치 스피너와 같은 색이다(veil·ink를 프롭으로 받던 기존 규칙 그대로: 로더는 두 화면이 함께 쓰는 부품이라 홈의 CSS 변수를 직접 읽지 않는다). 쓰이지 않게 된 `mf-core`·`mf-branch`·`mf-stem` 키프레임을 `home.css`·`login.css`에서 제거. 웹 +3 테스트(런치 시작점 — 위젯·버튼에 서로 다른 사각형을 심어 `--lx/--ly/--lw/--lh`로 구별, **수리 전 `100px` vs `620px`로 실패 확인** / 로더 스피너 계약 2), 전체 1661 통과. 실브라우저 8종(런치 시작 좌표가 알약과 1.5px 내 일치·위젯 폭과 불일치·펼침 중간 프레임 / 홈 로더 `mf-spin`·오션 강조색·트랙·옛 애니메이션 0 / 로그인 로더 코럴·문구 유지). **프로브 함정**: hover 떠오름(0.18s) 전이 중에 버튼 사각형을 재면 3px 어긋난다 — 전이가 끝난 뒤 재야 한다
- ✅ **런치 중 배경이 통째로 사라지던 문제(제보)** "열기"를 누르면 펼침 애니메이션 **뒤의 배경이 즉시 비고 로딩만 남았다". 원인이 둘 겹쳐 있었다: ① `openWithLoader`가 홈의 **전체 화면 로더**(`creatingMap` → `LoadingOverlay`, `instant`라 첫 프레임부터 불투명)를 띄웠고 ② 런치 오버레이 자신도 `var(--mf-bg)` **불투명 베일**을 함께 페이드했다 — 즉 카드가 자라기 전에 화면이 이미 비어 있었다. 수리: `openWithLoader(href, title, docId, { ownLoader: true })` 신설(호출부가 자기 전환으로 로딩을 말하면 전체 화면 로더를 생략한다) + 베일 제거. **`loaderMsg`는 그대로 남긴다** — `homeUpdateRisk`가 그 값으로 "이동 중"을 판단해 PWA 자동 적용이 끼어드는 것을 막기 때문이다(로더만 감추고 그 보호는 유지). 결과 순서가 요청 그대로다: 대시보드가 **그대로 보이는 위로** 누른 버튼에서 카드가 자라고(560ms), 그 카드 **안**의 스피너·문구가 열리며 나타나고(0.26s 지연), 다 자란 카드가 화면을 덮은 뒤 이동한다(900ms). 스페이스에서 카드를 여는 기존 경로는 그대로 전체 화면 로더를 쓴다(무회귀). 웹 +1 테스트(펼치는 중 로더 없음·오버레이 자식 하나·대시보드 내용 생존 — **수리 전 실패 확인**, 전체 1662), 실브라우저 rAF 프레임 샘플링(자라는 20프레임 전부: 로더 0·베일 0·히어로 살아 있음, 카드 60×26 → 전체 화면)
- ✅ **대시보드 진입 깜빡임 제거 + 위젯 hover 떠오름(제보·요청)** ① 스켈레톤 → 실제 데이터 전환에 **등장 페이드**(`mf-fade .3s`)가 두 겹으로 걸려 있었다 — 껍데기가 페이드하며 뜨고, 갈아 끼워진 본 화면이 **또 한 번** 페이드해 같은 자리에서 두 번 밝아지는 것이 깜빡임으로 읽혔다. 두 곳(`DashboardView`·`DashboardSkeleton`) 모두 걷어냈다(모양·뼈대가 이미 같으므로 페이드가 가리는 것도 없다 — 실측: 진입 1.4초 동안 모든 프레임 `opacity 1 · animation none`). 덤으로 그 애니메이션이 만들던 **영구 스태킹 컨텍스트**가 사라졌지만 런치 오버레이의 body 포털은 그대로 둔다(hover 떠오름의 transform이 같은 컨텍스트를 만든다). ② **위젯도 홈 카드처럼 3px 떠오른다**(요청) — `translateY(-3px)` + `--mf-card-shadow-hover` + `--mf-border-hover`, 누르는 순간 살짝 눌림, `prefers-reduced-motion`이면 그늘·경계만. 값·전이 시간이 홈 카드와 **같은 계산값**임을 실브라우저로 대조했다(둘 다 `matrix(1,0,0,1,0,-3)`·같은 그늘 문자열). **함정 하나**: 위젯에 인라인 `transition`이 있어 CSS 규칙(transform 포함)을 덮고 있었다 — 홈 카드에서 이미 겪은 그 함정이라(제보 "멈춘 두 그림만 있는 느낌") 인라인을 걷어내고 transition은 `home.css` 한 곳에서만 정한다(opacity까지 그리로 옮김). 웹 +3 테스트(진입 무애니메이션·인라인 transition 부재·CSS 계약 — 파일을 직접 읽어 hover 규칙을 고정, 전체 1665), 실브라우저 7종(진입 프레임 페이드 0·hover 떠오름/복귀·전이 속성·스페이스 카드와 값 일치)
- ✅ **문서 열기 전환 통일 — 카드도 그 자리에서 펼쳐진다(요청, 의견 합의 후)** "스페이스에서는 로딩이 화면을 덮는데 대시보드에서는 펼쳐진다"는 물음에 **규칙 하나**를 세웠다: **누른 원점이 있으면 그 자리에서 펼치고, 원점이 없거나 배경 자체가 바뀌는 동작이면 전체 화면 로더**(iOS·안드로이드의 컨테이너 트랜스폼 vs 진행 표시와 같은 구분). 통일 방향을 펼침으로 잡은 근거: 같은 동작(문서를 연다)이면 같은 모양이어야 하고, 둘 중 **정보가 더 많은 쪽**이 펼침이다 — 카드가 수십 장인 그리드에서 "내가 누른 그것이 화면이 된다"를 말해 주고 어느 카드를 눌렀는지 확인까지 겸한다. 구현: 대시보드 안에 있던 전환을 공용 `LaunchOverlay`(+`LaunchState`)로 떼어내고 상태를 **컨트롤러**(`state.launch`)로 올려 `Home`이 오버레이를 한 번만 그린다 — `controller.launchOpen(rect, {href,title,docId,kindName,space})` 하나가 입구고, 그 안에서 `openWithLoader(..., {ownLoader:true})`를 태워 전체 화면 로더를 생략한다(두 번째 클릭은 `launch`가 이미 있으면 무시). `MapCard`가 이 입구를 쓰므로 **그리드·최근 항목·검색 결과**가 한 번에 같은 전환이 됐다(문구는 카드가 아는 것으로: 종류 이름 + `spaceName`, 그리드 카드는 지금 스페이스). bfcache 복원 시 얼어붙은 펼침 카드도 로더와 함께 정리한다. **전체 화면 로더가 남는 곳**(원점이 없거나 배경이 바뀐다): 새로 만들기(배경에 카드가 꽂힌다 — 일부러 첫 프레임부터 덮는다)·로그아웃·회원 탈퇴. **아직 로더인 곳(다음 단계)**: LNB 즐겨찾기·공유받음 행, 위젯 우클릭의 "에디터에서 열기" — 원점은 있지만 스페이스 이름을 모르는 자리라 문구 규칙을 먼저 정해야 한다. 웹 +2 테스트(그리드 카드 더블클릭 → `--lx/--ly/--lw/--lh`가 그 카드 사각형·문구·전체 화면 로더 부재·그리드 생존 / 새로 만들기는 로더 유지) + 기존 "안 열렸다" 판정을 로더 문구 → 펼침 부재로 갱신(전체 1667), 실브라우저 7종(그리드 카드 원점 1.5px 내 일치·문구·자라는 19프레임에 로더 0·그리드 생존·353×213에서 시작·최근 트레이 카드 원점·새로 만들기 로더). **프로브 함정**: 프레임 샘플러의 `t0`를 init script 시점에 잡으면 관찰 창이 이미 지나 버려 1프레임만 찍힌다 — `__start()`에서 잡아야 한다
- ↩️ **열기 펼침 전환 롤백 — 문서는 어디서 열든 같은 로더로(사용자 판단)** 카드·위젯의 펼침 전환이 "아무래도 과하다"는 판단으로 걷어냈다(#525와 그 앞의 대시보드 런치까지). 지금은 **스페이스·최근·검색 결과·대시보드 위젯이 모두 같은 길**이다: 전체 화면 로더(`LoadingOverlay` — 회전 스피너 + "맵을 불러오고 있어요")가 덮고 0.9초 뒤 이동. 지운 것: 공용 `LaunchOverlay`/`LaunchState`, 컨트롤러 `state.launch`·`launchOpen`, `openWithLoader`의 `ownLoader` 옵션(쓰는 곳이 없어졌다 — 죽은 옵션을 남기지 않는다), `home.css`의 `mf-dash-launch` 키프레임과 그 reduced-motion 규칙, `MapCard`의 `launchFromCard`. `DashWidget`의 프롭도 `onLaunch(rect, …)` → **`onOpen(docId, title)`**으로 좁혔다(원점 사각형이 필요 없어졌다). **남긴 것**: 로딩 표시를 스피너로 통일한 것(#522 ②)과 위젯 hover 떠오름(#524 ②)은 그대로다 — 이번 롤백은 "펼침"만 되돌린 것이다. 대시보드 컨텍스트 메뉴의 "에디터에서 열기"·LNB 즐겨찾기·새로 만들기·로그아웃도 예전과 같은 로더라, 이제 홈에서 화면이 바뀌는 모든 길이 한 모양이다. 테스트는 런치 단정을 로더 단정으로 재작성(대시보드 "열기" 테스트에 **펼침 자국이 없다**는 가드 포함)하고, 카드 열기 전환 describe를 제거(전체 1664), 실브라우저 8종(카드 더블클릭·위젯 "열기" 모두 로더 + 오버레이 0·문구·스피너·에디터 도달·hover 떠오름 무회귀)
- ✅ **대시보드 칸반 카드도 에디터처럼 반응한다(요청)** 위젯의 카드는 이미 에디터와 **같은 부품**(`cardBase`/`CardFace`)으로 그려지는데 호버만 죽어 있었다 — 이유는 규칙이 `editor.css`에 있었고 그 파일은 **에디터 화면만 불러온다**(홈은 `home.css`뿐). 그래서 카드 호버·누름 규칙(`.mf-kb-card`)을 **컴포넌트 곁으로** 옮겼다: 신설 `editor/components/kanbanCard.css`를 `KanbanBoard.tsx`가 import하므로, 그 카드를 그리는 화면이면 어디서든 규칙이 함께 온다(중복 정의를 만들지 않는 방식 — 두 벌로 두면 값이 갈린다). 위젯 카드에 `className="mf-kb-card"`를 달고 **인라인 `transition`을 걷어냈다**(그걸 얹으면 규칙의 transform 전이가 덮여 툭 바뀐다 — 홈 카드·위젯에서 두 번 겪은 함정. 끌리는 카드가 흐려지는 `opacity` 전이는 공용 규칙에 합쳤다). 실측으로 **에디터와 계산값이 같다**(같은 문서를 두 화면에서 열어 대조: 떠오름 `matrix(1,0,0,1,0,-2)`·그늘 `rgba(46,42,38,.55) 0 12px 24px -18px`·전이 속성 목록 전부 일치). 위젯 자체의 3px 떠오름과 겹쳐 카드가 조금 더 들리는데, 그게 "카드가 위젯 안에서 반응한다"로 읽힌다. 열 이동 드래그·`prefers-reduced-motion`(떠오름 없이 그늘만)은 그대로. 웹 +2 테스트(위젯 카드의 클래스·인라인 transition 부재 / CSS가 컴포넌트 곁에 있고 `editor.css`에는 남지 않았다는 계약 — 클래스를 떼면 실패함을 확인) + 에디터 CSS 계약 테스트의 파일 경로 갱신(전체 1666), 실브라우저 7종
- ✅ **위젯 카드 이동도 에디터의 그 드래그로(요청, 검토 후 진행)** "카드 이동 애니메이션도 대시보드에 적용할 수 있나"를 검토하니 갈림의 뿌리가 **입력 방식**이었다: 에디터는 포인터 제스처로 직접 그리는 고스트(같은 얼굴·1.5° 기울기)와 정확한 자리의 점선 상자를 쓰는데, 위젯은 **HTML5 드래그**라 브라우저가 만든 반투명 스냅샷이 따라오고 슬롯은 열 **맨 뒤**에만 섰다(놓일 자리를 실제로 가리키지 못한다 = 정직하지 않은 표시). 그래서 겉모습만 흉내 내는 대신 **기계를 공유**했다: `beginPointerDrag`를 export해(에디터·위젯 공용 — 마우스 4px 문턱 / 터치 320ms 길게 누르기 / 취소는 이동이 아님, #436의 교훈 포함) 위젯이 그대로 쓰고, 자리 계산은 이미 순수 함수인 `dropTargetAt`·`edgeScroll` 재사용, 렌더는 에디터와 같은 모델(끌리는 카드는 목록에서 빠지고 그 자리에 같은 높이 점선 상자 + 손끝의 고스트). **고스트는 body 포털**이다 — 위젯이 hover에서 `transform`으로 떠오르는데 변형된 조상은 `position: fixed`의 기준 상자가 되어 고스트가 위젯 안에 갇힌다. 딸려 온 것 셋: **같은 열 안 순서 바꾸기**(`moveDashCard`에 index 추가, 바뀐 게 없으면 저장하지 않는다), **가장자리 자동 스크롤**(보드 가로·열 세로), 그리고 **터치에서도 카드 이동**(HTML5 드래그가 손가락에서 죽어 폰에서는 '보기 전용' 배지를 달았는데, 이제 배지가 `카드 이동 가능`으로 정직해졌다). 웹 +2 / 갱신 5 테스트(포인터 제스처로 재작성 — 고스트·슬롯·원본 제거·같은 열 재정렬·터치 길게 누르기, 전체 1668), 실브라우저 14종(데스크톱: 고스트 기울기·폭·원본 제거·슬롯·저장·같은 열 재정렬 / 폰: 배지·짧은 스와이프는 스크롤·길게 누르면 잡힘·열 이동 저장 / 에디터 무회귀). **프로브 함정**: 1.5° 기울인 요소는 bounding box가 부풀어 폭 비교가 3px 어긋난다 — `offsetWidth`로 재야 한다
- ✅ **드래그 고스트의 좌측 경계선이 잘려 보이던 문제(제보 스크린샷)** 카드를 옮길 때 손끝의 고스트에 두른 강조색 프레임이 **왼쪽 변만 빠져** 잘린 것처럼 보였다. 원인은 CSS가 아니라 **객체 스프레드의 키 순서**다: 고스트가 `{...cardBase(...), border: '1px solid accent'}`인데, `cardBase`에는 `border` 다음에 `borderLeft`(보통 카드는 기본 테두리, 긴급 카드는 붉은 3px)가 있다 — 스프레드는 **키의 자리를 그대로 두고 값만** 바꾸므로 내 `border`가 먼저, `cardBase`의 `borderLeft`가 **뒤에** 적용돼 왼쪽만 회색으로 남았다(실측: 위·오른쪽 `rgb(240,102,63)` / 왼쪽 `rgb(236,223,213)`). 고스트에서 `borderLeft`를 **따로** 주어 네 변을 맞추고, **긴급 카드의 붉은 3px 왼쪽 변은 그대로 둔다**(그 카드의 정체다). **에디터에도 같은 버그가 있었다** — 위젯이 드러냈을 뿐이라 두 곳을 함께 고쳤다(열 고스트는 `cardBase`를 쓰지 않아 해당 없음). 웹 +2 회귀 가드(위젯·에디터 고스트의 왼쪽 = 위쪽 색, **둘 다 수리 전 실패 확인**), 실브라우저 실측(고스트 네 변 전부 강조색 / 긴급 카드는 붉은 3px 유지 / 2배 스크린샷 대조)
- ✅ **"보드 올리기" 팝업에서 올리기 버튼이 잘리던 문제(제보 스크린샷)** 크기 선택지를 N×4까지 늘리자(#516) 발치 한 줄이 팝업을 넘어 **올리기 버튼이 모달 밖 42px에** 놓였다(실측 — 카드 오른쪽 끝 1100 vs 버튼 오른쪽 1142). 원인은 발치가 `[제목/설명][여백][크기 칩 묶음][올리기]` **한 줄**이었고 칩 묶음이 `flexShrink: 0`이라 줄지 않은 채 버튼을 밀어낸 것 — 선택지가 몇 개인지에 따라 버튼 자리가 달라지는 구조라 언젠가는 넘칠 값이었다. 수리: 발치를 **명시적 2행**으로(위=크기 칩(`flexWrap: wrap` — 넘치면 접힌다) / 아래=요약 + 올리기, 버튼은 언제나 오른쪽 끝). 폭이나 선택지 수와 무관하게 줄바꿈 지점이 고정되고 버튼이 제자리에 있다 — **서식 툴바에서 같은 이유로 이미 한 번 겪은 것**(#279: 한 줄에 `flexWrap`만 걸었더니 스와치 중간에서 접혀 앞 두 색이 들여쓰기 버튼 뒤에 매달렸다)이라 그때와 같은 처방이다. 웹 +1 회귀 가드(칩 줄과 버튼 행이 다른 행 · 발치가 `column` · 칩 11개에 `4×4` 포함 — **수리 전 실패 확인**), 실브라우저 검증(수리 전 데스크톱에서 버튼이 42px 밖 → 수리 후 1440px 칩 1행·390px 칩 2행, 두 폭 모두 버튼이 모달 안·가로 넘침 0)
- ✅ **미리보기 2건(제보) — 배경이 에디터를 안 따랐고, 첨부 이미지가 늘 자리표시자였다** ① **바탕이 그 문서의 캔버스 배경**이 된다(홈 카드·최근·검색 결과·대시보드 위젯). 예전엔 카드 썸네일 바탕이 홈 테마의 `--mf-wash` **한 값**이라 다크 맵도 오션 맵도 썸네일에서는 같은 베이지였다 — "미리보기는 캔버스의 축소판"이라는 약속이 배경에서만 깨져 있었다. 신설 `previewSurface(raw)`(본문에서 `themeKey`만 집어내는 문자열 판별 — `isBoardRaw`와 같은 규칙)가 에디터의 `themeOf(...).canvasBg`·`dot`으로 wash와 도트 색을 만들고, 카드·위젯이 그 한 함수를 함께 쓴다. **반지름은 백분율**(`85% 82%`)이다 — 에디터의 px 고정값(1200×700)을 150px 썸네일에 그대로 쓰면 상자 전체가 첫 스톱 안에 들어와 단색이 된다(`canvasWash`에 `radii` 인자 추가, 에디터 호출은 기본값 그대로). **칸반만 예외** — 칸반 화면은 캔버스가 아니라 크롬이라 에디터도 고정 팔레트로 그리므로 바탕도 `boardSurface(UI_THEME)`, 격자 없음. **의도한 변화 하나**: 화이트보드의 "흰 종이" 특례가 사라진다 — 화이트보드의 캔버스도 에디터에서는 같은 따뜻한 wash라(테마 `white`의 `canvasWash`) 바탕만으로는 맵과 갈리지 않는다. 종류는 배지가 말한다(기존 계약 테스트를 새 계약으로 갱신). ② **첨부 이미지가 보인다**. 뿌리는 `preview_doc` RPC(0012)가 `img`가 **문자열이기만 하면** 전부 `'stripped'`로 지운 것이었다 — 그 함수는 이미지가 본문에 base64로 인라인이던 시절의 것이고, 0016부터 본문에는 **50바이트짜리 참조**(`mfimg:…`)만 남으므로 지워서 아끼는 전송량이 **0인데 썸네일만 영영 회색**이었다. **0032**가 판단 기준을 값의 모양으로 바꾼다(`like 'data:%'`인 인라인만 뗀다). 로컬 Postgres에 0012·0032를 실제로 적용해 확인 — 0012는 참조를 지우고(제보 재현), 0032는 참조 보존·인라인 스트립·크기 필드 유지. 클라이언트는 썸네일 본문에서 참조를 모아 서명 URL을 발급해 그린다(`realPreview(raw, hue, urls)` — 캐시 키에 그 문서 참조의 URL을 넣어 늦게 도착한 이미지가 반영된다). **전송량은 두 겹으로 묶었다**: ⓐ **본 것만 받는다** — 카드·위젯이 화면에 닿을 때만 발급(`useVisibleOnce`, rootMargin 300px). `content-visibility: auto`가 대신 막아 줄 거라 적었다가 **실측으로 틀린 것을 확인**했다(1000×620 화면, 카드 12장 중 6장만 보이는데 12장의 이미지가 전부 요청됨 — 렌더를 건너뛰어도 리소스는 받는다). 게이트를 넣은 뒤 같은 화면에서 관측 12·보고 6, 끝까지 스크롤하면 12. ⓑ **같은 URL을 다시 쓴다** — 서명 URL은 부를 때마다 토큰이 달라져 브라우저 캐시가 통째로 빗나가므로 만료 전(11시간)까지 기기에 캐시하고(`imageUrlCache.ts`) **에디터도 같은 캐시**를 쓴다(홈에서 본 사진을 맵에서 또 받지 않는다). 웹 +11 테스트(previewSurface 4·미리보기 이미지 3·URL 캐시 5는 별 파일, 홈 통합 3 — 통합 3건 모두 수리 전 실패 확인), 전체 272+1684 통과. 실브라우저 검증(코랄·다크·오션·화이트보드·칸반 카드의 계산 배경/도트 실측이 에디터 테마 값과 일치, 사진 카드·대시보드 위젯에 실제 이미지 렌더, 게이트 6/12). **남은 것**: 버전 기록·템플릿 갤러리 미리보기는 URL을 넘기지 않아 이미지가 자리표시자다(별건). 더 줄여야 하면 업로드 때 작은 썸네일 사본을 함께 올리는 것이 다음 수순(Storage 이미지 변환은 유료 플랜)
- ✅ **공유 카드(OG 이미지)를 v2로 — 세 보기가 보이게(요청)** 랜딩 v2는 "정리하는 방법은 하나가 아니에요"라며 마인드맵·화이트보드·칸반 셋을 말하는데, 링크를 붙였을 때 뜨는 카드는 **가지 뻗은 마인드맵만** 보여 주고 있었다. 코럴 판 위에 세 종류를 나란히 놓은 구성으로 다시 그렸다(`generate-icons.mjs`) — **글자를 쓰지 않는 원칙은 그대로**다(이 스크립트가 폰트 글리프에 기대면 개발 노트북과 CI에서 다른 그림이 나온다). 그래서 종류를 알아보게 하는 일은 **실루엣**이 맡는다: 가지 뻗은 트리 / 스티커·잉크 / 열과 카드. 각 카드의 강조색은 앱이 이미 쓰는 **문서 종류색**(홈 카드 배지의 점)이라 공유 카드에서 본 색이 앱 안에서 같은 뜻으로 다시 나타난다. 그림은 카드 안쪽 **내용 상자**(272×200) 기준으로 그린다 — 첫 판은 좌표를 카드 기준으로 잡아 마인드맵의 왼쪽 자식이 카드 모서리에 잘렸다(500px 축소본·1:1 크롭까지 렌더해 확인). **파일 이름에 판을 담았다**(`og-image.png` → `og-card-v2.png`): 슬랙·카카오·트위터는 스크랩한 카드를 **URL로** 캐시하므로 같은 경로에 그림만 바꾸면 한동안 옛 카드가 계속 뜬다. 문구도 함께 맞췄다 — 제목 `그리오 Geurio — 마인드맵 · 화이트보드 · 칸반`(쌍둥이 둘 + 트위터), 앱 셸의 설명 3곳이 아직 마인드맵만 말하던 것을 랜딩과 같은 v2 문장으로. **테스트**: 제목 단정을 마케팅 문구가 아니라 **계약**(한글 표기 `그리오` + `Geurio`)으로 바꾸고(문구는 또 바뀐다), **쌍둥이 드리프트 가드** 신설 — 정적 랜딩과 앱 셸의 `og:image`·`twitter:image`가 같은 URL이고 그 파일이 `public/`에 실제로 있어야 한다(한쪽만 고치면 조용히 깨지는 자리 — 앱 셸만 옛 이름으로 되돌려 실패 확인). 전체 272 + 1685 통과
- ✅ **색상 스와치 접근성 — 이름 + 화살표 이동(요청, 프리미티브 이식의 마지막 조각)** 색 고르기 칸은 글자 없는 동그라미라 **접근 이름이 아예 없었다**(스크린리더가 "버튼"이라고만 읽었다). 보드 도구 막대만 이름이 있었는데 그게 hex라 "샵 이 비 이 비 이 비"였다. 화살표 이동도 없어 Tab이 칸마다 멈췄고, `aria-pressed`("눌린 버튼")로 알려 "고른 값"이 아니었다. **이름은 표가 아니라 hex에서 계산한다**(사용자 선정) — 팔레트는 테마마다 다르고(7벌 × 9색) 사용자가 고른 색·문서에 저장된 색도 이름이 필요하다. 손으로 지은 이름표는 테마를 늘릴 때마다 함께 고쳐야 하는 '미러 표'가 된다. 신설 `components/colorNames.ts`: `colorName(hex)`(HSL → 색조 14칸 + 밝기 수식어)와 `swatchNames(hexes)`(**같은 묶음에서 겹치면 행 순서로 번호**). **미리 렌더해 보고 규칙을 세 번 고쳤다**: ① `#8a6bd1`(중간 보라)이 "남색"으로 읽혀 남색 칸을 없앴다(진짜 남색은 어두운 파랑 → "진한 파랑") ② `#ffe14d`(형광 노랑)가 "황금"이라 밝은 노랑은 노랑으로 갈랐다 ③ 번호를 밝기 순으로 붙였더니 첫 칸이 "진한 회색 6"이라 **행 순서**로 바꿨다(화살표로 훑는 순서와 같아야 한다). 결과: 코랄 `주홍·주황·황금·연두·청록·파랑·보라·분홍·빨강`, 모노 `진한 회색 1~6·회색 1~2·빨강`, 어두운 주황은 `갈색`. 신설 `components/Swatch.tsx`의 `SwatchGroup`이 `RadioCards`(#24의 Radix RadioGroup) 위에 얹혀 **이름을 호출부가 적지 않는다** — 자리를 늘려도 "이름 붙이는 걸 잊었다"가 생길 수 없다. **시각은 호출부가 계속 들고 있다**(`style(hex, on)`) — 자리마다 크기·테두리·활성 표시가 달라 여기로 끌어오면 디자인이 한 벌로 뭉개진다. 적용 범위는 **전부**(사용자 선정): 에디터 속성 패널 8행(`SwatchRow` — 이 참에 `SectionLabel`을 컴포넌트 안으로 넣어 **보이는 글자와 접근 이름이 한 문자열**이 됐다)·보드 펜/형광펜·칸반 카드 상세(분류 색·카드 배경색 — 이름을 이미 든 팔레트는 `names` 우회로로 표의 톤 이름을 쓴다)·홈 새 스페이스/대시보드 팝업. **칸반 열 색상 메뉴는 이름만 고쳤다** — 그 칸들은 `role="menuitem"`이라 화살표 이동을 메뉴가 이미 주고, 라디오 묶음으로 바꾸면 메뉴 의미가 깨진다. 웹 +9 테스트(colorNames 8 — 그중 하나는 **모든 테마의 팔레트가 전부 구별되는지** 훑어 새 테마의 이름 충돌을 잡는다 / 패널 라디오·화살표 1) + 기존 기대값 갱신(`button` → `radio`, hex → 색 이름, `aria-pressed` → `aria-checked`), 전체 272 + 1694 통과. 실브라우저 검증(속성 패널: 묶음 이름·9칸 이름·→로 값 이동(주황→황금)·로빙 tabindex 1개·스크린샷 시각 파리티 / 보드 펜·형광펜 8색 / 칸반 상세 두 묶음 / 홈 팝업 6색 / 콘솔 오류 0). **후속**: 이름이 스크린리더 전용이 되지 않게 **호버 툴팁**도 같은 문자열로 붙였다 — 글자 없는 동그라미는 눈으로 봐도 "이게 무슨 색이라 불리는지"는 알 수 없다(툴팁이 있던 곳은 칸반 열 메뉴뿐이었다)
- ✅ **버전 기록 미리보기에 첨부 이미지(제보)** 옛 판을 고르면 사진 자리가 늘 회색 자리표시자였다 — 미리보기를 그리는 함수는 같은 `realPreview`인데 **URL을 넘기지 않아** 참조(`mfimg:…`)를 그릴 수 없었다(#531에서 홈 카드·대시보드 위젯에는 넘겼지만 이 자리는 빠져 있었다). 수리: **그 스냅샷 자신의 참조**를 본문에서 모아 발급받는다(에디터가 이미 든 `controller.imageUrls`를 재사용만 하지 않는 이유는, 그 뒤 지운 이미지가 옛 판에만 남아 있기 때문이다 — 그게 옛 판을 보는 이유다). 발급은 **캐시 먼저**(`imageUrlCache` — 지금 문서와 겹치는 참조는 왕복 0회, 홈에서 이미 본 사진도 그대로 재사용)이고 모자란 것만 `imageStore.resolve`로 한 번에 묶어 받는다. 곁들여 본문 읽기를 `useMemo`로 모았다 — 예전에는 렌더마다 `versionBody`가 **두 번** 돌아 히스토리 전체를 두 번 파싱했다(고른 판 표시용 + 참조 추출용). 신설 `useImageStore()`(BackendContext). 웹 +1 테스트(캐시가 채워진 기기에서 옛 판의 사진이 `<image href>`로 그려진다 — **수리 전 실패 확인**), 실브라우저 검증(스냅샷 본문에만 있는 참조 → 190×143 이미지 렌더·placeholder 0·리소스 타이밍에 실제 전송 477B). **범위 밖**: 템플릿 갤러리 미리보기는 템플릿 데이터에 첨부가 없어 해당 없음
- ✅ **일정 캘린더 PR1 — 기반 + 읽기(디자인 이식)** 사용자 디자인 `Geurio 일정 캘린더.dc.html`(루트 보존, 수정 금지 대상) 이식의 첫 단계. **설계 결정 셋**(논의 후 확정): ① 캘린더는 **문서가 아니라 per-user 연결된 뷰** — 구글 계정이 사람에 붙는데 문서는 스페이스·공유에 붙어 "누구의 구글인가"에 답이 없고, 저장·버전·undo·CRDT·내보내기가 전부 뜻을 잃는다. 그래서 대시보드·스페이스와 나란한 **세 번째 화면**(`activeCal` — 라우트가 아니라 상태: 홈은 `/home` 한 라우트이고 화면은 언제나 하나만 그린다). ② **저장할 곳은 우리 표, 구글은 선택적 거울** — 디자인의 "저장할 캘린더 [Geurio | Google]"이 같은 결론이다. 구글을 유일한 저장소로 두면 이메일 가입자와 OAuth 심사 전 사용자가 **일정을 아예 만들 수 없다**. ③ 칸반 마감은 **종일**(코어 `KanbanCard`에 시각 필드를 더하지 않는다 — 시각이 필요한 일정은 Geurio 일정이 맡는다), 반복은 v1 제외, 공휴일·국가 설정은 구글 연결 단계로 이월(전부 사용자 선정).
  **이번 PR**: 순수 데이터 계층 둘 — `calendar/entries.ts`(전 스페이스 칸반 카드에서 기한·시작일만 골라 모은다. 본문은 **썸네일이 이미 받아 둔 `previewDocs`**라 화면을 여는 것만으로 새로 내려받는 것이 없고, 모자란 스페이스는 검색과 **같은 프리페치 경로**를 같은 시점에 태운다. `searchIndex`와 같은 방식으로 원문 JSON을 통째로 훑지 않는다) + `calendar/model.ts`(격자·통계·목록·배지 — 날짜는 전부 로컬 `YYYY-MM-DD` 문자열). **완료(마지막) 열 카드는 달력에서 빠진다**(디자인 원본의 규칙 — 끝난 일이 달력을 채우면 남은 일이 눈에 안 들어온다). 화면: LNB `일정` 행(대시보드 구획과 스페이스 구획 **사이**, 개수 = 다가오는 마감) → 밝은 헤더(월 이동·오늘로) + **통계 칩 4종이 곧 필터** + 월 격자(항상 6주 42칸, 분류색 알약 + 열 색 점, 기간 일정은 **칸마다 바 한 조각**으로 — 시작 칸과 일요일에만 제목·양 끝만 둥근 모서리라 lane 계산이 없다) + 사이드(미니 달력 + 마감 목록/그 날 목록). 칩 색은 **에디터의 그 함수들**(`tagColor`/`tagInk`/`columnColor` + 고정 `UI_THEME`)을 그대로 부른다 — 같은 카드가 화면마다 다른 색이면 그게 곧 버그다(#513의 교훈). **눌러도 아무 일 없는 버튼은 두지 않았다**: `새 일정`·`구글 연결`은 그 기능이 오는 PR에서 함께 붙고, 지금은 항목을 누르면 그 칸반을 연다(다음 PR에서 상세 팝업으로 교체).
  **곁들여 잡은 것 둘**: ① `loadActiveView`가 필드를 화이트리스트로 걸러 새 `activeCal`을 **버려서** 일정 화면에서 맵을 열고 돌아오면 대시보드로 떨어졌다(실브라우저 프로브가 잡고 회귀 테스트로 고정 — 수리 전 실패 확인). ② 폰에서 LNB 항목을 골라도 **서랍이 닫히지 않아** 고른 화면을 가리고 있었다(스페이스·대시보드도 예전부터 그랬다 — 실측 확인) → 화면이 바뀌면 닫는 효과 하나로 세 행을 함께 고쳤다. 주말 칸은 디자인의 두 색(일=따뜻/토=차가움) 대신 **한 톤 가라앉힌 면 하나**로 묶었다 — 그만큼 옅은 두 색을 여섯 테마 × 다크에 새로 정해야 하고, 요일 구분은 숫자 색(일=danger/토=info)이 이미 말한다. 웹 +23 테스트(순수 16 + 통합 7, 전체 1718), 실브라우저 검증(데스크톱: 격자 42칸·전 스페이스 항목·완료 제외·기간 바·필터·날짜별 사이드·월 이동·오늘로·칩→에디터·세션 복원 / 390px: 사이드 접힘·서랍 닫힘·가로 넘침 0). **다음**: PR2 상세 팝업(기존 `CardDetail` 분리 + `moveDashCard` 패턴 write-back) + 드래그로 날짜 변경 → PR3 `events` 표(0033)·새 일정·시간표 → PR4 대시보드 캘린더 위젯(위젯 모델 `kind` 일반화, 크기가 보기를 정한다) → PR5 구글 읽기 겹치기(GIS 증분 승인·설정 연동·공휴일)
- ✅ **일정 캘린더 후속 3건(프리뷰 확인 제보)** ① **캘린더가 화면을 다 채우지 않고 "90% 배율"처럼 보였다** — 일정 화면이 `main`의 패딩(24/32/44) **안에** 들어 있어 사방이 밀리고 격자가 뷰포트 높이까지 자라지 못했다. 대시보드는 같은 문제를 음수 마진으로 상쇄하지만(그 화면은 세로로 흐른다), 일정은 헤더 + [달력 | 사이드] 구조에 안쪽 두 영역이 각자 스크롤하므로 **패딩을 아예 걷는 편**이 정확하다(`main`이 일정 화면에서만 `padding: 0` + `overflowY: hidden`). 실측: 1728×1000에서 뷰 높이 1000 · 격자 824 · 칸 132px · 문서 스크롤 없음. ② **통계(태그) 칩이 태그 무리처럼 보였다** — 디자인 원본의 칩은 **면도 테두리도 없다**(`chipBg: 'transparent'`, hover에서만 옅은 면): 점 + 라벨 + 등폭 숫자만으로 읽힌다. 내 판은 테두리 있는 알약이었다. **함정 하나**: 처음엔 인라인 배경으로 켜짐을 칠했는데 `.mf-ctl:hover`의 `!important`(#492)가 이겨서, 손을 얹으면 켜진 칩이 **더 옅어졌다**(hover 값이 켜짐 틴트보다 밝다). 칩 전용 클래스(`.mf-cal-chip`)로 꺼짐·hover·켜짐을 CSS 한곳에서 정하고 인라인 배경을 없앴다. ③ **오늘 칸·고른 칸 색이 부자연스러웠다** — `--mf-accent-soft`(오늘)·`--mf-accent-mute`(선택)를 그대로 써서 칸이 통째로 진하게 칠해졌다(선택 칸 실측 `rgb(242,196,179)`). 디자인 원본의 세 값(선택 `#FCF6ED` < 오늘 `#FFF3EC` < 오늘+선택 `#FDEFE4`)과 **같은 관계**를 표에 적지 않고 **강조색에서 파생**한다(`--mf-cal-today` 8% · `--mf-cal-sel` 5% · `--mf-cal-sel-today` 14% · `--mf-cal-ring` 55% 알파 · `--mf-chip-on` 13%) — 여섯 테마 × 다크에 값을 따로 정할 필요가 없다. 실측 코랄 오늘 `#fef1ec`(원본 `#FFF3EC`와 거의 같다), 고른 칸은 **안쪽 링**으로 알린다(테두리를 굵히면 격자가 1px 밀린다). **덤으로 다크에서 잡은 것**: 칩 색을 늘 흰 면(`UI_THEME.panel`)에 섞어서 다크 홈의 어두운 격자 위에 옅은 알약이 홀로 빛났다 → **hue는 칸반 고정 팔레트에서, 밝기는 놓이는 면에서**(`entryChip(e, surface)` + `homeChipSurface(theme)`) — 분류색의 정체는 그대로 두고(#513) 면만 따라간다. 코랄은 카드 면이 거의 흰색이라 눈에 보이는 변화가 없다. 웹 +3 테스트(패딩·칩 계약·칸 색 — 첫 번째는 수리 전 실패 확인) + 테마 드리프트 가드에 새 토큰 5개 추가(전체 1721), 실브라우저 재검증(채움 실측·코랄/다크 스크린샷·390px 모바일)
- ✅ **날짜별 보기의 일별 항목을 디자인 원본대로(제보 + 시안)** 시안과 대조해 보니 일별 항목이 다른 물건이었다 — 원본은 **왼쪽 3px 색 바가 붙은 납작한 행**(`padding: 6px 9px` · `radius 4/9/9/4`)에 우측 짧은 메모(`진행 중`·`3/7일째`)인데, 내 것은 마감 목록과 같은 **두 줄 카드 + D-배지**를 그대로 쓰고 있었다. 신설 `DayChip`으로 갈랐다(마감 목록의 두 줄 카드는 그대로 — 두 목록은 다른 물건이다). 색 바는 그 카드가 있는 **열 색**, 우측 메모는 열 이름이고 기간 일정이면 신설 순수 함수 `dayProgress`가 `N/M일째`를 센다(원본의 note). **시간표는 아직 두지 않는다** — 원본은 이 아래에 12AM~11PM 격자를 두지만 지금 우리 항목은 전부 종일이라(칸반 마감은 시각을 갖지 않기로 결정) 빈 24줄만 남는다. 원본도 시간 일정이 없으면 격자 대신 빈 상태(`이 날에는 시간 일정이 없어요` + `이 날에 새 일정`)를 보여 주므로, 시각이 있는 Geurio 일정과 그 버튼이 함께 오는 단계에서 붙인다(눌러도 아무 일 없는 버튼을 두지 않는다는 원칙). 헤더의 `+`(이 날짜에 일정 추가)도 같은 이유로 보류. 코어 무관 / 웹 +1(순수 `dayProgress`) + 기존 날짜별 단정 강화(색 바·radius·메모·`2/5일째`), 실브라우저 검증(행 높이 26px·왼쪽 3px 열 색·radius 4/9/9/4·메모 3종 스크린샷)
- ✅ **일정 캘린더 PR2(상세 팝업 + 드래그로 날짜 변경)** 이제 일정 화면에서 **칸반을 고친다** — 정본은 여전히 그 칸반 문서이고 일정 화면은 사본을 들지 않는다. ① **코어에 규칙을 뽑았다**: `patchCardMeta(cards, id, patch)`(값이 비면 키를 지우고, 담당은 `owner`/`ownerName` 쌍을 함께 옮기며, 분류는 24자)·`shiftCardDates(card, days)`. 에디터의 `setCardMeta`도 이 함수로 라우팅해 **두 화면이 같은 규칙**을 쓴다(카드 필드를 다루는 곳이 둘로 갈리면 그게 곧 드리프트다). ② **상세 팝업**(`CalendarDetail.tsx`) — 고칠 수 있는 것을 **일부러 좁게** 뒀다: **상태(열)·시작일·기한**, 그리고 발치의 `이 칸반 열기`. 제목·분류·담당·삭제·댓글은 칸반의 일이다(디자인 원본은 카드 상세를 통째로 열지만 그 상세는 캔버스 상태에 얽힌 댓글 패널까지 품어 홈에서 그대로 열 수 없다 — 604줄·컨트롤러 16개). 상태는 `Segmented`(Radix ToggleGroup)라 화살표로 옮겨 다니고, **마지막 열(완료)로 옮기면 달력에서 빠지므로 팝업도 닫는다**. 날짜는 native `<input type="date">`(폰에서 OS 선택기가 그대로 뜬다) + 다크에서는 `color-scheme`을 함께 뒤집는다(루트에 두면 에디터의 native 위젯까지 홈 테마를 따른다). **기한에는 `지우기`를 두지 않았다** — 기한은 이 항목이 달력에 있는 이유라 지우면 목록에서 사라진다(첫 판은 버튼만 있고 아무 일도 안 하는 죽은 버튼이었다 — 스크린샷 대조에서 잡았다). 막을 눌러 닫힌다: **아직 저장되지 않은 입력이 없으므로**("편집 중인 팝업은 막 클릭으로 닫지 않는다"는 규칙의 근거가 여기엔 없다) 흘깃 보는 팝업의 관례를 따른다. ③ **컨트롤러 write-back**: `patchCalendarCard`/`shiftCalendarCard`는 `moveDashCard`와 **같은 몸통**(낙관 반영 → 전문 로드 → 코어 변이 → `prevVersion` 저장 → 충돌이면 최신 판으로 한 번 재시도 → 실패하면 되돌리고 안내 + 에디터 로컬 복구본도 같은 판으로). ④ **드래그로 날짜 변경**: 칩·바를 끌면 손끝에 고스트(body 포털 — 격자의 `overflow: hidden`이 `fixed`를 가둔다)가 따라오고 **며칠 움직이는지 미리 말하며**(`+6일`), 놓일 칸이 강조색 링 + 옅은 면으로 뜨고, 원본은 자리에서 흐려진다. 제스처는 에디터에서 export한 `beginPointerDrag`(마우스 4px 문턱 / 터치 320ms 길게 누르기 / **취소는 이동이 아니다**)라 칸반과 감각이 같다. **기준은 잡은 칸**이다 — 기간 바의 가운데를 잡아도 그 칸에서 놓은 칸까지의 델타만큼 시작일·기한이 **함께** 평행이동한다(신설 순수 `daysBetween` — 정오 기준이라 서머타임에 한 날 어긋나지 않는다). 끌고 난 뒤의 `click`은 **한 번만** 삼킨다(놓은 자리에서 팝업이 뜨지 않게. 표식을 남기면 다음 평범한 클릭까지 먹는다). ⑤ **보기 전용으로 공유받은 보드는 끌리지도 고쳐지지도 않는다** — 칩 커서가 `grab`이 아니고 팝업은 상태·날짜 대신 안내만 띄운다(진짜 게이트는 서버 RLS, 이건 "고쳐지는 척하다 저장은 안 되는" 화면을 막는 어포던스). **곁들여 잡은 구멍**: 프리페치가 `state.spaces`만 훑어 **공유받은 칸반의 본문은 한 번도 받지 않았다** — 그러면 `공유받음` 항목이 일정 화면에 영영 뜨지 않는다(PR1의 갭). 일정 화면에서는 `sharedMaps`도 함께 받는다(검색은 원래 내 스페이스만 훑으므로 이 화면에서만). 코어 +5 / 웹 +8 테스트(상태·기한·드래그·기간 드래그·제자리 드롭·보기 전용·고스트·`daysBetween`·제목/담당/분류/삭제·댓글 열 — 구현·가드를 되돌려 실패 확인, 전체 277 + 1732), 실브라우저 15종(팝업 구성·상태 저장·세그먼트 갱신·기한 저장 + 칸 이동·Escape·grab 커서·고스트 body 포털·놓일 칸 강조·원본 흐림·드래그 저장·놓은 뒤 무팝업·기간 안내·기간 시작일+기한 동반 이동·폰 바텀시트 44px·다크). **프로브 함정(네 번째 기록)**: `addInitScript`는 리로드에도 재실행돼 저장소를 다시 덮으므로 리로드로 테마를 심을 수 없다 — 새 페이지로 열어야 한다. **후속(요청: "상세 팝업은 디자인 html에 있어 — 그대로 가져와줘")**: 디자인 원본의 `evOpen` 블록을 찾아(900×640 두 열) 그대로 이식했다 — 좁게 만들었던 첫 판을 걷어냈다. 왼쪽 열=제목 textarea(19px/800, blur에 한 번 저장 — 확정 규칙은 코어 신설 `cardTextValue`/`patchCardText`로 뽑아 **에디터의 카드 상세와 한 코드**)·상태 알약·시작일/기한·기간 진행 바(`N일 중 M일째` + 남은 날 + 하루 한 칸 pip, 오늘 칸만 진하게)·담당 아바타(공유 참가자)·분류 알약 + `직접 입력`, 머리=상태 점·`칸반 카드` 배지·분류·D-배지·`삭제`·`✕`, 발치=안내 문구·`이 칸반 열기`·`완료`(강조색 그라디언트). **날짜는 native input을 버리고 디자인의 날짜 팝오버**(`DatePop.tsx` — 미니 월 격자 + `오늘`/`지우기`)로: 원본이 이 팝오버 하나를 상세·새 일정·반복 종료일에 함께 쓰고 발치 두 버튼이 native 위젯에는 없다(PR3의 새 일정도 이걸 쓴다). **오른쪽 댓글 열은 우리 댓글 표를 그대로** — `CommentThreads`의 프롭 타입을 `EditorController`에서 **`CommentHost`(10 멤버)로 좁혀**(에디터 호출부는 구조적 타이핑으로 무변경) 홈이 댓글 포트를 그 모양으로 감싼 `useCalendarComments`를 넘긴다. 스레드·답글·멘션·좋아요·실시간이 두 화면에서 한 코드다. 색은 `homeEditorTheme(key, palette)` 신설로 홈 테마를 에디터 `Theme` 꼴로 옮겨 넘긴다(다크 홈에 밝은 열이 홀로 뜨지 않게. `palette`만은 칸반 고정 — #513). **원본과 갈라 둔 셋**: 상태는 알약 모양 그대로 **라디오 묶음**(←/→ 이동·`aria-checked` — #24의 규칙), 기한 `지우기`는 두지 않는다(지우면 방금 보던 항목이 달력에서 사라진다), 카드 배경색·긴급·메모는 칸반 카드 상세의 몫으로 남긴다. 폰은 바텀 시트 + 44px 타깃(원본 36/40은 데스크톱 값)
- ✅ **일정 캘린더 PR3(Geurio 일정 — 0033 · 새 일정 · 날짜별 시간표)** 일정 화면이 이제 **원천 둘**을 그린다: 칸반 마감과 **Geurio 일정**(회의·휴가·개인 약속 — 칸반에 없는 일정). ① **왜 별도 표인가**(`calendar_events`, 0033): 캘린더는 문서가 아니라 per-user 뷰라 본문(jsonb)에 넣으면 "누구의 일정인가"에 답이 없고, 문서를 지워도 내 일정은 남아야 하며, 본문은 자동저장마다 통째로 오가는 값이라 일정이 늘수록 저장·전송이 무거워지고 CRDT 병합 대상이 되어 끊긴 채 양쪽이 적으면 한쪽이 사라진다(0020 댓글과 같은 판단). **날짜는 로컬 날짜 문자열**(타임존을 끌어들이면 같은 종일 일정이 기기마다 다른 날에 놓인다), **시각은 `HH:MM` + 쌍 제약**(하나만 있으면 그릴 수 없다), `end_date >= start_date`. RLS 넷 다 `owner = auth.uid()`이고 **UPDATE를 직접 연다** — 이 표에는 남의 행이 없어 어떤 컬럼을 바꿔도 피해자가 자기 자신뿐이다(0022와 같은 이유. 컬럼을 좁히려 RPC를 두던 0019·0021의 근거가 여기엔 없다). `source`/`google_id`는 미리 둔다(구글은 **선택적 거울** — 우리 표가 정본). 반복·알림·참석자는 v1 제외(사용자 결정). ② **같은 `CalendarEntry` 모양으로 합친다**(`eventEntries`) — 격자·통계·목록·시간표가 종류를 가리지 않고 그린다. 다만 **고칠 것이 달라 팝업이 갈린다**: 칸반=상태·시작일·기한 / 일정=종일 토글·시각·위치·메모(`EventDetail` — 원본 `evIsSimple` 분기, 값이 있는 입력은 blur·Enter에 한 번 커밋). ③ **새 일정**(`NewEventModal` — 원본 `newEvOpen`): 제목·종일 토글·날짜(+종료 날짜)·시각 쌍·빠른 길이 칩(30/60/90)·위치·메모. 저장할 곳이 하나뿐이라 원본의 `[Geurio | Google]` 고르기는 **머리의 배지 하나**로 대신한다(고를 것이 하나뿐인 라디오는 UI가 아니라 장식이다). 시각 고르기도 native가 아니라 원본의 **15분 목록 팝오버**(`TimePop` — 고른 시각으로 스크롤이 맞춰진다). 진입 셋: 헤더 `새 일정` · 날짜별 보기 머리의 `＋` · 빈 시간표의 `일정 추가`(뒤 둘은 **고른 날짜가 곧 기본값**). ④ **날짜별 시간표**(원본 `agendaHours`/`agendaTimed`): 24행(행 36px)에 시각 일정을 절대 위치로 놓고 **겹치면 열을 나눠 나란히**(순수 `dayTimeline` — 묶음이 끝나면 열 수도 되돌아간다), 오늘이면 현재 시각 선, 첫 일정이 보이도록 스크롤(`focusTop` — 자정부터 훑게 두지 않는다), 종일 항목은 시간표 위의 납작한 행으로 남는다. 시각 일정이 없으면 시계 글리프 + 안내 + `일정 추가`. ⑤ **실브라우저가 잡은 것 둘(둘 다 실사용에서 곧 만날 자리)**: 시작 **시각**을 뒤로 옮기면 종료가 그 자리에 남아 "종료가 시작보다 앞선" 상태가 되고 저장이 막혔다(정규화가 시각 쌍을 버려 **종일로 되돌려** 시각이 통째로 사라지기까지 한다), 시작 **날짜**를 앞으로 당기면 클램프만 있어 하루짜리 일정이 그 사이만큼 긴 **기간 일정**으로 바뀌었다(8/28 하루가 8/5~8/28로 — 프로브 스크린샷). 둘 다 **길이를 지킨 채 따라오게** 고쳤다(달력 앱의 관례, 신설 순수 `addDays`는 `daysBetween`의 짝). 곁들여 `PillButton`에 `aria-pressed`(켜짐을 보조기술이 알 수 없었다). 코어 무변경 / 웹 +51 테스트(순수 14 — `dayTimeline` 겹침·`eventEntries`·시각·격자 구간·`addDays` / 로컬 어댑터 13 / Supabase 어댑터 11 / 통합 13 — 새 가드는 구현을 되돌려 실패 확인, 전체 1783), 실브라우저 35종(새 일정 종일·시각·빠른 칩·시각 팝오버 스크롤 / 상세 폭·칸반 상세와 갈림·blur 커밋·종일 토글·삭제·기간 표기 / 시간표 블록 3개·겹침 나란히·높이·현재 시각 선·스크롤·블록 클릭 / 빈 상태·고른 날짜 기본값 / 폰 바텀시트 44px·가로 넘침 0 / 다크 면 실측). 운영 문서 `backend.md` §18. **미검증**: 실제 RLS·제약 통과는 라이브 Supabase가 막혀 있어 유닛/통합으로만 확인했다(로컬 데모 어댑터가 같은 계약을 지켜 전 흐름을 실브라우저로 돌렸다)
- ✅ **일정 캘린더 제보 11건(프리뷰 확인)** 디자인 원본에 있는데 PR1에서 빠뜨린 블록들을 마저 이식하고, 새 일정 팝업을 다듬었다. ① **`새 일정`을 헤더 오른쪽으로** — 디자인 원본의 자리다(왼쪽 묶음은 "지금 어디를 보고 있는가"=제목·월 이동, 오른쪽은 "무엇을 할 수 있는가"=만들기·보기 토글). ② **날짜 버튼이 16px로 납작하던 문제** — `flex: 1`이 원인이었다: 이 버튼의 부모가 **세로 flex**(라벨 위·버튼 아래)라 `flex-basis: 0`이 **높이**에 걸려 `height: 40`이 통째로 무시됐다(실측 16px). 시각 버튼은 부모가 가로라 같은 값이 살아 있어 둘이 갈렸던 것 → `width: '100%'` + 38px로 시각 버튼과 같은 크기. ③ 빠른 길이 칩 30분·1시간·**2시간·3시간**(원본은 30/60/90). ④ **발치의 "Geurio 캘린더에만 저장돼요" 제거** — 그 자리를 **상황 문구**(저장 실패·`종료 시각이 시작보다 앞서요`·저장 중)에 내줬다. 늘 같은 안내를 걸어 두면 정작 알려야 할 때 눈에 띌 자리가 없다(디자인 원본의 발치도 빈 spacer + 취소/등록이다). ⑤ 머리의 `종일`·시작 시각 배지 제거. ⑥ **배경 dim** — `Modal`의 막은 호출부가 색을 주는데 이 팝업만 `background`를 빠뜨려 뒤 화면이 그대로 보였다(원본과 같은 `rgba(46,42,38,.32)` + blur 3px). ⑦ **영역 재분할** — 달력 영역에 캔버스 점 격자(17px)를 깔고 좌우 여백을 24px로, 요일 머리를 원본 꼴(10px/800/자간 .08em)로, **사이드를 자기 면을 가진 판**으로(카드가 떠 있는 열 → `--mf-card` 면 + 왼쪽 경계선, 미니 달력은 붙박이고 아래만 스크롤). 사이드 머리에 **✕**가 생겨 접을 수 있고(`calSide`에 `null` 추가 — 위 토글을 다시 눌러도 접힌다) 접으면 달력이 전폭을 쓴다(실측 808 → 1108px). ⑧ `오늘로` → **`오늘`**. ⑨ **주말 칸 색** — 일요일·공휴일은 파스텔 분홍, 토요일은 파스텔 하늘색(원본 #FEF8F5 / #F9FBFD). 값을 표에 적지 않고 **그 칸의 숫자 색에서 파생**했다(`--mf-cal-sun` = mix(card, danger, .05) / `-sat` = mix(card, info, .05)) — 숫자와 배경이 언제나 같은 색조를 쓰고 여섯 테마 × 다크에 값을 새로 정할 필요가 없다(실측 코랄 #fdf4f2 / #f5f8f9, 다크에서도 갈린다). 이웃 달 칸은 중립 그대로. `MonthCell.holiday` 자리를 열어 뒀다(공휴일 원천은 구글 연동 단계). ⑩ **통계 칩을 필터에서 목록으로** — 예전에는 누르면 달력이 그 항목만 남기고 통째로 비었다(제보: 캘린더가 변한다). 원본대로 **누르면 팝오버에 목록**이 뜨고 골라서 상세로 간다(최대 5줄 + `+N개 더`, 빈 통계는 `해당하는 일정이 없어요`). 배지는 며칠 지났는지까지 말한다(`statBadge` — `-5일`. 격자·사이드의 `dueBadge`가 `지남` 한 마디인 것과 다른 이유는 이 목록이 놓친 일을 훑어 고르는 자리라서다). `calendarStats`가 개수만이 아니라 **그 항목들**을 싣게 바꾸고(개수 = 목록 길이, 둘이 갈리면 그게 버그다) 쓰이지 않게 된 `filterByStat`·`calFilter` 상태를 제거. ⑪ **연/월 피커**(`MonthPicker` — 원본 `ymOpen`): 헤더의 `2026년 8월`을 누르면 월 격자(4열×3행), 가운데 연도를 누르면 연도 격자(3열×5행=15년)로 바뀌고 고르면 월로 돌아온다. 화살표는 보이는 격자를 따라 1년/15년씩, 발치에 `이번 달`. 달을 여러 번 넘겨 먼 달로 가는 대신 두 번 눌러 닿는다. 웹 +5 테스트(헤더 자리·연월 피커 왕복·`오늘` 문구·주말 색·통계 팝오버 2) + 기존 필터 테스트를 목록 계약으로 재작성(전체 1790), 실브라우저 27종(헤더 x=1230·피커 12/15칸·주말 색 실측·팝오버가 달력을 안 바꿈·사이드 300px와 접기·모달 dim/38px/칩 4종·다크·390px 모바일)
- ✅ **일정 캘린더 제보 13건(프리뷰 확인)** 배포된 화면을 실제로 써 보고 나온 열세 가지. ① **`새 일정`이 hover에서 색을 잃었다** — 원인은 `.mf-home .mf-ctl:hover`가 면을 `--mf-panel2`로 **`!important`** 갈아 끼우는 것: 그라디언트 버튼은 그 순간 회색 알약이 된다 → 그라디언트 버튼은 `mf-ctl-primary`(밝기만)로. ② **`저장할 캘린더` 구획을 되살렸다**(직전 판이 머리 배지로 줄였던 것). ③④ **팝업·팝오버 안 버튼에는 hover가 아예 없었다** — 모달·팝오버는 Radix **포털로 `body` 밑에** 그려져 `.mf-home` 안이 아니다(우클릭 메뉴가 `.mf-home-ctx`로 같은 문제를 푼 것과 같은 처방: `[data-modal-overlay]`·`[data-radix-popper-content-wrapper]`로 규칙을 다시 건다). 그리고 **켜진 것**(`aria-pressed`)은 면을 갈아 끼우면 손을 얹은 순간 꺼진 것처럼 보인다(제보: 고른 날짜가 주황이 아니다) → 면을 칠하는 규칙 셋 전부에 `:not([aria-pressed='true'])`를 붙이고 켜진 것은 **밝기만** 움직인다(다크는 `--mf-hover-bright`가 방향을 뒤집는다). ⑤⑩ **일정 팝업 셋에 dim + 등장 애니메이션** — `Modal`의 막은 **호출부가 색을 주는 구조**라 빠뜨리면 뒤 화면이 그대로 비친다: `MODAL_DIM` + `mf-dim-in`(막) + `mf-fade`(카드 — 설정 팝업과 같은 값). ⑥ **댓글 열 배경**을 카드와 가라앉은 면 사이로(`--mf-cal-cmt` = mix(card, panel2, .35) — 원본 #FDFBF8과 같은 관계). ⑦ **마감 목록을 날짜별 보기와 갈랐다**(원본 `dlOpen`) — 예전에는 사이드 하나를 **갈아 끼워** 둘 중 하나만 볼 수 있었다. 이제 마감 목록은 달력 위에 **겹치는 판**(`DeadlinePanel`, 272px)이고 날짜별 보기가 열려 있으면 그 폭만큼 왼쪽으로 비켜선다(`calSide: 'day'|null` + `calDeadline: boolean` — 한 칸에 담으면 "둘 다 켜짐"을 표현할 수 없다). ⑧ 주말 틴트를 0.05 → **0.03**. ⑨ 이웃 달 칸 구분선을 `--mf-border-soft` → **`--mf-border`**(가라앉은 면 위에서 옅은 선이 묻혔다). ⑪ **통계 칩 색쌍** — 원본은 순위마다 `{fg, dot}` 두 값을 손으로 골랐고 **점은 밝고 숫자는 짙다**(#D9694A / #C0563A). 우리 토큰을 그대로 쓰니 그 관계가 사라졌고, 특히 `--mf-star`(#e0a53c)를 숫자에 쓰면 흰 면에서 읽히지 않았다 → **한 색에서 두 값을 파생**(점은 면 쪽, 숫자는 글자 쪽 — 여섯 테마 × 다크에 값을 새로 정하지 않는다). ⑫ 미니 달력 숫자를 본문 글자색·600으로. ⑬ **오늘 칸의 배경을 없앴다** — 숫자가 이미 채운 원으로 말하는데 배경까지 바꾸면 "고른 칸"과 헷갈린다(고른 칸도 링을 걷고 면만 — 원본 `selRing: 'none'`). 웹 테스트 갱신·추가(마감 목록이 겹치는 판·오늘 칸 무배경·포털 hover CSS 계약), 전체 1791 통과. 실브라우저 18종 검증(마감 목록 272px가 날짜별 보기 왼쪽에 나란히·오늘 칸 요일 색 그대로·이웃 달 구분선·주말 톤·통계 색쌍 실측·새 일정 그라디언트 hover·팝업 dim/애니메이션·날짜 버튼 38px·켜진 알약과 고른 날짜가 hover에서 주황 유지·댓글 열 면 + 라이트/다크 스크린샷)
- ✅ **일정 캘린더 제보 6건(프리뷰 확인)** ① **댓글 로딩 스켈레톤** — "불러오는 중…" 한 줄이던 자리를 **실제 스레드와 같은 뼈대**(얼굴 원 + 이름 줄 + 본문 두 줄 × 2)로 바꿔, 글이 도착해도 자리가 크게 바뀌지 않는다. ② **버튼 hover 넷** — 원인이 셋이었다: `상태` 알약은 `RadioCards`에 클래스를 줄 자리가 없어 **hover 규칙이 닿지 않았고**(항목에 `className`을 열어 `mf-ctl`을 준다), `완료`는 그라디언트인데 `mf-ctl`이라 손을 얹으면 회색 알약이 됐으며(`mf-ctl-primary`로 — 새 일정과 같은 수리), `남기기`는 **클래스가 아예 없었다**. ③ 제목 텍스트 박스의 크기 손잡이 제거(`resize: none` — 고정 높이 두 열이라 늘리면 아래 필드가 밀린다). ④⑤ **글자가 작아 읽기 힘들다**는 제보 — 달력 칩·기간 바(10 → 11.5px, 높이 17 → 21·16 → 20)와 RNB 날짜별 항목(제목 11.5 → 13px, 행 26 → 33px)을 키우고 칸 최소 높이도 78 → 86으로. ⑥ **LNB 활성 표시가 둘이었다** — 스페이스 행이 `!activeDash`만 보고 `activeCal`을 몰라서, 일정을 열어도 이전 스페이스에 포커스가 남았다. 화면이 셋(스페이스·대시보드·일정)인데 각자 손으로 열거하다 **일정을 더할 때 한쪽만 빠진 것** → `isSpaceView(state)` 한 곳으로 모아 네 번째 화면이 생겨도 여기만 고치면 되게 했다(마퀴 가드도 같은 함수로). **곁들여 잡은 구조 문제**: 댓글 부품(`CommentPanel`)은 에디터와 홈이 함께 쓰는데 그 CSS가 `editor.css`에 있어 **홈에서는 hover·애니메이션이 통째로 없었다** → 규칙을 컴포넌트 곁(`editor/components/comments.css`)에 두고 컴포넌트가 직접 import한다(칸반 카드 `kanbanCard.css`와 같은 처방). hover 가드도 `aria-pressed`에 더해 **`aria-checked`**(라디오 묶음)까지 넓혔다. 웹 +4 테스트(LNB 포커스·제목/버튼 계약·스켈레톤·글자 크기 — LNB는 수리 전 실패 확인), 전체 1795 통과. 실브라우저 10종(스페이스 행 꺼짐·일정만 켜짐·칩 11.5px/21px·바 20px·RNB 13px/33px·`resize: none`·상태 꺼짐/켜짐·이 칸반 열기·완료·남기기 hover 실측) + 스켈레톤 CSS가 번들에 실렸는지 계산값 확인. **프로브 함정**: LNB 행 배경에 0.14s 전이가 걸려 있어 화면 전환 **직후**에 재면 중간값(반투명)이 나온다 — 전이가 끝난 뒤 재야 한다
- ✅ **달력 날짜 크기 + 격자선·이웃 달 색(요청, 두 차례)** 날짜 숫자를 11 → 13px로 키웠다가 **너무 크다는 후속으로 12px**(폰은 11 유지 — 칸이 좁다), 오늘 표시의 채운 원도 그 크기를 따라 20px. 격자선과 이웃 달 칸을 **전용 토큰**(`--mf-cal-grid`/`--mf-cal-out`)으로 분리했다 — 값은 **디자인 원본 그대로**(`#EDE1D3` / `#F5EFE7`, 원본의 `bd`·`blank().bg`)이고 나머지 여섯 테마는 같은 자리의 자기 색(경계선·가라앉은 면)을 쓴다. 코랄에서는 그 둘이 원본 값과 사실상 같아 관계가 성립하고, 값 하나를 모든 테마에 박으면 오션·포레스트의 차가운 면 위에 따뜻한 회색이 홀로 남는다(캔버스 그라데이션 #457과 같은 방식: 기본은 정확히, 나머지는 파생). 바깥 테두리·요일 머리 아래 선도 같은 토큰. **이웃 달 칸의 구분선은 원본과 달리 그린다** — 원본은 `bd: transparent`지만 "이전·다음달 날짜 구분선을 또렷하게"가 앞선 제보였다. 웹 +1 계약 테스트(칸 테두리·이웃 달 면이 전용 토큰·숫자 12px) + 테마 드리프트 가드 갱신, 실브라우저 실측(격자 `rgb(237,225,211)` · 이웃 달 `rgb(245,239,231)` · 숫자 12px / 다크는 자기 경계선 `#3a3128`)
- ✅ **캘린더 PR4(대시보드 캘린더 위젯)** 대시보드에 **일정**을 올린다. ① **위젯 모델 일반화** — 위젯은 여태 "(어느 문서를, 어떤 크기로)"였는데 일정은 **가리킬 문서가 없다**: `DashboardItemData.docId`를 선택으로 낮추고 `kind?: 'cal'`을 더했다(**없으면 문서** — 옛 블롭은 그때 전부 문서였으므로 그대로 읽힌다. `coerceDashboards`가 둘 중 어느 쪽도 아닌 항목만 버린다). ② **크기가 보기를 정한다**(디자인 원본의 그 규칙, 순수 `calWidgetMode`): 4×3부터 월간(작은 달력 + 옆 목록), 2×2부터 주간(요일 일곱 줄), 그보다 작으면 다가오는 마감 목록. 고를 것을 따로 두지 않은 이유는 크기가 이미 "얼마나 보여 줄까"를 말하기 때문이다 — 크기 순환·모서리 드래그·메뉴가 그대로 보기 전환이 된다. ③ **항목의 원천은 일정 화면과 같다** — `useCalendarEntries`를 화면에서 훅으로 떼어 둘이 함께 쓴다(같은 것을 두 벌로 두지 않는다). 다만 대시보드는 **위젯마다** 이 훅을 지나므로 두 훅(`useCalendarEntries`/`useCalendarEvents`)에 `enabled`를 뚫어 문서 위젯에서는 조회도 계산도 돌지 않는다(안 그러면 위젯 수만큼 일정 조회가 나간다). ④ **위젯은 보기 전용**(#518의 규칙) — 항목·칸을 누르면 그 날짜의 **일정 화면**으로 가고, hover 알약도 `일정 열기`다. 달 이동(‹ ›·오늘)은 **보는 사람의 상태**라 위젯 안에 두고 문서·블롭에 남기지 않는다. ⑤ **프리페치 게이트 확장** — 일정 화면은 전 스페이스의 본문을 받는데, 캘린더 위젯이 올라간 대시보드도 마찬가지다(활성 스페이스만 받으면 다른 스페이스의 마감이 조용히 빠져 **위젯이 거짓말을 한다**). 값을 치르는 계기는 "사용자가 그 위젯을 올렸다"는 사실이다(검색과 같은 판단). ⑥ **피커 첫 칸이 일정** — 문서가 아니라 화면이라 카탈로그(스페이스·검색) 밖에 붙박이로 두되, 검색어에 '일정'이 걸릴 때만 남는다(목록을 걸러 놓고 이것만 계속 뜨면 검색이 거짓말이 된다). **대시보드당 하나**이고 이미 올라가 있으면 "올림" 배지 + 다시 누르면 내려간다(문서 카드와 같은 규칙). 웹 +6 테스트(모델 2 + 통합 4), 전체 1802 통과. 실브라우저 7종(목록 보기가 **두 스페이스**의 마감을 함께 모음·주간 일곱 줄·월간 42칸+옆 목록·달 이동이 블롭에 안 남음·열기 알약→일정 화면·항목 클릭→그 날짜·문서 위젯 무회귀)
- ⏭️ **다음 후보**: 캘린더 PR5(구글 캘린더 읽기 겹치기 — GIS 증분 승인·설정 연동 구획·공휴일) / 멘션 메일(0021 mentions + Resend 인프라 재사용 — Edge Function 한 장) / (낮음) 홈 카드 목록 전면 가상화 — content-visibility로 부족해질 때(스페이스당 수백 맵 규모), 메인 그리드+검색 결과 한 세트 / 실기기 빌드·서명·스토어 제출은 로컬 툴체인 필요 / 확인 대기: 안드로이드 "안전하지 않은 앱 차단됨"(Play Protect — `chrome://webapks`)

> **로드맵 1·2단계 완료**: 웹 프로덕션화(코어 분리·React·인증/DB) + PWA/모바일 웹 + 실시간 다중편집 + 앱스토어 래핑(Capacitor). 라이브 백엔드/실기기 배포는 키·툴체인 설정 후 로컬에서.

> **M3 완결 + M4**: 로그인 → 홈 → 에디터 전부 React로 동작, `mindmap-core` 소비. 인증·문서 저장은 포트 추상화(Local/Supabase). 실 Supabase는 키 설정 시 활성(미설정 시 localStorage 데모).

> `mindmap-core`는 순수 TS(DOM/React/canvas 금지, lint 강제). 노드 크기는 `sizeOf` 주입.

## 규칙 (에이전트·사람 공통)
- `support.js`와 `*.dc.html`(디자인 원본)은 **변경하지 않는다.** 이식은 새 `packages/`에서 진행.
- 새 코드는 **TypeScript** 우선. 코어는 프레임워크·DOM 의존 없이 순수 로직으로.
- 비자명한 변경은 **테스트로 동작을 검증**한 뒤 커밋. 로컬은 정적 서버(`python3 -m http.server`)로 확인.
- 브랜치 접두사 `claude/`. main 직접 푸시 금지, PR로.
- 커밋/PR에 모델 식별자·비밀정보를 넣지 않는다.

## 로컬 실행
```bash
python3 -m http.server 8000   # http://localhost:8000/
```
`file://` 직접 열기는 동작하지 않음(형제 파일 fetch 필요).
