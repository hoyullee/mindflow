# MindFlow — 프로젝트 컨텍스트 (팀 공용)

이 문서는 사람과 Claude 에이전트가 **공유하는 단일 컨텍스트**입니다. 새 세션·새 에이전트는
작업 전 이 파일을 먼저 읽습니다.

**이 파일은 세션마다 통째로 실립니다 — 그래서 짧게 유지합니다.** 라운드별 기록은
`docs/changelog.md`, 프로브 함정은 `docs/probe-pitfalls.md`, 백엔드 운영은
`server/supabase/docs/backend.md`에 있고 **셋 다 자동으로 읽히지 않습니다**(필요할 때
`grep`으로 찾아 읽습니다). 여기에 남기는 것은 **다음 세션이 모르면 안 되는 것**뿐입니다 —
규칙 · 아키텍처 · 현재 상태 · 배포 절차.

## 제품 개요
MindFlow는 중심 주제에서 가지를 뻗어 생각을 정리하는 **마인드맵 웹 앱**입니다.
목표: **웹 서비스 → 모바일 웹(PWA) → Android/iOS 앱** 순으로 확장.

## 현재 코드베이스 (디자인 원본)
`claude/mindflow-design-impl-iyxiol` 브랜치는 Claude Design에서 가져온 **디자인 프로토타입**입니다.
프로덕션 기반이 아니라 **픽셀·인터랙션 레퍼런스**로 취급합니다.

| 파일 | 역할 |
| --- | --- |
| `MindFlow.dc.html` | 마인드맵 편집기 (약 3,200줄) — 대상 |
| `Home.dc.html` / `Login.dc.html` | 대시보드 · 로그인(데모) |
| `support.js` | dc-runtime. **수정 금지** (Anthropic 생성물, 프로토타입 엔진) |
| `vendor/` | React 18.3.1 UMD |
| `index.html` | 진입점 |

### dc 포맷 구조
각 `*.dc.html` = `<x-dc>` 템플릿(HTML + `{{ }}`·`<sc-if>`·`<sc-for>`) + `<script type="text/x-dc">`
안의 `class Component extends DCLogic` 컨트롤러. 런타임(`support.js`)이 템플릿을 React로 렌더링.
빌드 단계 없음. 상태는 `localStorage`(`mindflow_doc_<id>`), 페이지 간 `window.location` 이동.

### 마인드맵 엔진의 위치 (핵심 자산)
`MindFlow.dc.html`의 컨트롤러 안에 렌더링과 뒤섞여 있음:
- 데이터 모델(`nodes`, `floats`, `lines`, `zones`)
- 레이아웃 알고리즘 `_layout(nodes, layoutMode)`
- 직렬화 `serializeDoc()` / `loadDoc()` / `cloneNodes()`
- undo/redo, export(PNG·Markdown·JSON)

## 목표 아키텍처
```
packages/
  mindmap-core/   # 순수 TS. DOM/React 없음. 모델·레이아웃·직렬화·undo·export
  web/            # React + Vite + TS. core 사용, SVG/Canvas 렌더 (디자인 재현). PWA
  mobile/         # (2단계) Capacitor 또는 React Native, core 재사용
  desktop/        # 설치형 PC 앱(Electron 셸) — 원격 출처를 띄우고 설치 파일만 만든다
server/           # 인증(OAuth/이메일) + DB(Postgres) + 문서 동기화 API (+ 협업시 Yjs/CRDT)
```

## 로드맵 / 단계
1. **웹 프로덕션화**: dc → React+TS+Vite 이식, `mindmap-core` 분리, 실제 인증·DB·동기화. PWA화.
2. **앱 스토어**: 빠르게=Capacitor로 PWA 래핑 / 네이티브감=React Native(UI 재작성 + core 재사용).

## 진행 현황 (ADR-0001 기준)
**상세 기록은 `docs/changelog.md`에 있습니다** — 라운드별 원인·결정·검증이 그대로
들어 있습니다. 세션마다 자동으로 읽히는 것은 **이 파일 하나**이고 그쪽은 읽히지
않습니다(한 파일이던 시절 956KB가 매번 실렸습니다 — 그래서 갈랐습니다).

필요할 때 찾아 읽습니다 — `grep -n "키워드" docs/changelog.md`. **같은 자리를 두 번
고치기 전에 한 번 찾아보세요**: "왜 이렇게 돼 있나"의 답이 대개 거기 있습니다.

### 지나온 단계
- **ADR-0001 · M0** 모노레포(pnpm+Turbo·strict TS·CI·코어 순수성 lint) + **골든 안전망**(dc 원본을 헤드리스로 캡처)
- **M1a~c 코어**: 모델·`serializeDoc`/`parseDoc`·`HistoryStack` / `layout`(radial·right·down) / 라인 큐빅 기하 — 전부 dc 원본과 좌표 parity
- **M3** React 이식: 로그인·홈·에디터(a 렌더 → f 부분 리치텍스트)
- **M4** 백엔드 포트(`AuthProvider`/`DocStore`/`SpaceStore` + Local/Supabase 어댑터·RLS·낙관적 잠금)
- **M5** 실시간 협업(Yjs/CRDT + awareness) · **M6** PWA·모바일 반응형 · **M7** Capacitor 앱셸

### 지금 있는 것
- **문서 셋**: 마인드맵 / 화이트보드(자유 배치·그리기·프레임·반응) / 칸반(보드·리스트·타임라인) / **공책**(페이지 여러 장의 글 — 블록 16종·인라인 서식·표·콜아웃·토글·이미지·문서 링크·**일정 블록**·`/` 커맨드) — 종류마다 템플릿 갤러리
- **에디터 공통**: 리치텍스트·리스트(단계별 마커)·링크·멘션(공책은 `@` 허브로 사람·날짜·페이지를 한 메뉴에서 — 날짜·페이지는 인라인 칩)·이미지(Storage — 공책은 크기 조절·원본 보기)·댓글 핀(스레드)·**본문 인라인 댓글**(공책 — 고른 글에 형광 + 우측 열의 「본문 댓글 → 페이지 댓글」)·보드 임베드(공책 본문에 칸반·맵·화이트보드의 지금 상태 — 편집은 칸반 열 이동만)·공책의 우측 열(**일정** — 미니 달력·시간표 / 댓글)·**인라인 칩은 한 덩어리**(캐럿이 안으로 못 들어가고 Backspace 한 번에 통째로 — 날짜·사람·페이지 셋 다. 날짜 칩은 호버·방향키로 그날 일정을 띄우고 거기서 일정 상세·새 일정을 **공책 안에서** 연다)·버전 기록·내보내기(PNG·SVG·PDF·MD·JSON)
- **홈**: 최상위 화면 둘 — 스페이스(폴더·**전역 검색** — 제목과 내용을 훑고 `어디서 걸렸는지`를 종류 칩으로 보여 준다, 공책은 그 페이지로 바로 연다 · 휴지통) · 일정(달력) · 알림 센터 · 설정(프로필·계정·알림·버전 확인·테마 7벌)
- **협업·공유**: 실시간 공동 편집, 이메일 초대 / 링크 공유(보기 전용), 댓글·멘션 알림 — 멘션은 **메일**(30분 다이제스트)과 **웹 푸시**로도 나간다(읽었으면 안 온다)
- **구글 캘린더 연동**: 읽기·쓰기·참석자·회의실·근무 위치 — 민감 스코프 **검수 통과**, refresh token은 Edge Function이 서버에 보관
- **일정 알림**: 앱이 떠 있을 때(웹 스케줄러) · 모바일은 OS 예약(Capacitor) · 데스크톱은 트레이 상주
- **멘션이 앱 밖으로 나가는 길 셋**: 웹 푸시(1분) · 설치형 앱 배너(창을 닫아 둬도) · 메일(30분 다이제스트) — 셋 다 **읽었으면 오지 않는다**
- **배포 채널**: 웹(PWA) · 설치형 데스크톱(Electron — Win/mac 무서명 + MSIX 자가서명) · 모바일(Capacitor 스캐폴딩)

### 최근 라운드(제목만 — 자세한 것은 changelog)
- 모바일 웹의 공책 — 목록을 서랍으로·툴바를 한 줄로(제보: 화면이 틀어짐)
- Backspace로 윗줄에 붙을 때 키패드가 다시 올라오던 것 — 초점이 빈 한 프레임(제보)
- 삽입한 문서의 메뉴·보기 · 서식이 줄을 넘어 이어지는 자리(제보·요청 8건: 한글 첫 글자·줄 끝 공백)
- 켜 둔 서식은 못박힌다(토글이 아니라) · 빈 줄에서도 단추에 보인다 · 판 머리 「보기 전용」 제거(제보·요청 3건)
- 지웠다 다시 켜면 서식이 풀리던 것 — 빈 줄의 자리 채우개 `<br>`이 껍데기 때문에 한 글자로 세어졌다(제보 재보고)
- 공책 본문의 인라인 칩 — 날짜·페이지 링크와 `@` 멘션 허브(스펙 1판: 멘션·날짜칩)
- 공책 본문의 멘션에도 알림 — 트리거가 `pages[].blocks[]`를 훑지 않고 있었다(1판의 남은 절반)
- 날짜 칩 색을 스펙 값으로(제보) · 달 끝에서만 깨지던 근무 위치 구간 테스트(CI)
- 공책 본문의 인라인 댓글 — 고른 글에 달고 형광으로 되찾는다(스펙 2판: 본문댓글)
- 공책의 일정 — 본문의 일정 블록(`/일정`)과 우측 「일정」 탭(스펙 3판)
- 날짜 칩을 올리면 그날 일정 · 허브의 「일정 N」(스펙 4판)
- 멘션 칩을 올리면 프로필 카드 — 「댓글로 부르기」까지(스펙 5판: 완료 기준 전부)
- 대시보드를 걷어냈다 — 최상위 화면을 둘로(질문·결정, 순 -5,237줄. 배치 데이터는 남겨 둔다)
- 공책 10건 — 칩을 한 덩어리로·팝오버(종일·기간·스켈레톤·요일색·방향키)·일정 팝업을 공책 안에서·표 칸 여백 더블클릭(제보·요청)
- 공책 5건 — 요일에만 색·팝오버가 칸반 마감까지·스켈레톤 먼저·칩 넘어 Shift+Home·빈 줄의 타이핑 스타일(제보)
- 공책 6건 — 칸반 마감을 여는 길(읽기 전용 카드)·공휴일 요일색·팝오버 칩을 일정 페이지와 같게·IME 중 툴바·브라우저가 남긴 서식(제보·질문)
- 공책 6건 — 툴바가 색·형광을 읽는다·조합 껍데기가 색까지 그린다(첫 글자 계열 넷)·쓰는 중에는 서버 판으로 안 갈아 끼운다(제보)
- 공책 2건 — 껍데기를 서식 껍질 **밖**에 세운다(줄 끝 인라인 코드를 방향키로 벗어난다·색 바꾼 뒤 첫 글자의 구조적 틈)

- 📌 **라이브 배포 절차(브랜치 지형 — 머지만으로는 안 나간다)** 이 저장소는 브랜치가 셋이다: 작업 브랜치(`claude/…`) → **기본 브랜치 `claude/mindflow-design-impl-iyxiol`**(PR이 머지되는 곳) → **`main`**(라이브 `geurio.com`이 배포되는 곳). PR을 머지하면 기본 브랜치까지만 올라가고 **`main`은 그대로 멈춰 있다** — 자동으로 따라가는 워크플로가 없다(`.github/workflows/ci.yml`이 전부고 main으로 푸시하는 잡은 없다). 그래서 "머지·CI·프리뷰 전부 초록인데 라이브는 옛 화면"이 된다(실제로 제보를 받고서야 알았다: `main`이 한 커밋 뒤인 채 이전 세션이 만든 판을 서빙하고 있었다). **라이브 반영 = `git push origin claude/mindflow-design-impl-iyxiol:main`**(fast-forward — 새 커밋 없이 이미 CI 통과한 같은 커밋을 가리키게만 하므로 내용 변화가 없고, 문제가 생기면 이전 sha로 되돌린다). **이 푸시는 사용자 확인 없이 한다**(2026-09-11 상시 승인 — "앞으로 작업은 내가 별도의 요청이 있기 전까지 라이브까지 배포해줘"): 지정 브랜치 밖으로 나가는 동작이지만 사용자가 그 범위를 미리 열어 두었다. 그래서 **작업의 완료 지점은 머지가 아니라 라이브**다 — PR → PR CI(`verify`) → 스쿼시 머지 → 기본 브랜치 CI → `main` fast-forward → **Production 승격 확인**까지가 한 덩어리이고, 중간에서 멈추면 "머지는 됐는데 라이브는 옛 화면"이 된다(그 사고가 이 항목이 생긴 이유다). 되돌리는 것은 사용자의 말 한 마디이므로, 승인을 거둬 달라는 요청이 오면 이 문장을 원래대로 고친다. 확인은 **설정 › 버전 확인**으로 — 한 줄에 `버전 2026.09.23.2234`(CalVer, 빌드가 굳힌 값)와 그 아래 커밋 7자가 있다(콘솔 스탬프는 걷었다). 그 sha가 방금 올린 커밋이면 반영된 것이고, 그래도 옛 화면이면 서비스 워커 캐시다(그 사이트 탭을 모두 닫았다 열거나 SW Unregister). 참고: 이 원격 세션에서는 `geurio.com`도 게이트웨이 403이라 **라이브를 직접 열어 확인할 수 없다** — 사용자에게 확인을 부탁하고, 침묵을 결론으로 쓰지 않는다(아래 항목과 같은 규칙). **같은 브랜치에서 PR을 이어 낼 때는 머지하지 말고 새 base 위에 다시 세운다** — 우리 흐름은 **스쿼시** 머지라, 이미 랜딩한 내 커밋이 base에는 **다른 sha의 한 덩어리**로 들어가 있다. 그 상태에서 base를 `git merge`하면 git이 같은 내용을 두 갈래로 보고 **없던 충돌을 만든다**(실제로 CLAUDE.md가 그렇게 충돌했다). `git checkout -B <branch> origin/<base>` 뒤 **아직 랜딩하지 않은 커밋만 cherry-pick**하면 깨끗하고, 옮긴 뒤에는 `git diff <이전판> -- . ':!CLAUDE.md'`로 **패치가 동일한지** 대조해 손실이 없음을 확인한다. 그리고 **PR이 `mergeable: false`(dirty)면 CI가 아예 돌지 않을 수 있다** — GitHub이 머지 커밋을 만들지 못해서다: "CI가 30분째 안 보인다"면 워크플로를 의심하기 전에 PR의 `mergeable_state`를 먼저 본다. **덧붙여**: 머지 직전에 기본 브랜치가 **같은 기능을 다른 세션이 따로 구현한 판**을 이미 담고 있을 수 있다(이번엔 #480이 그랬다 — 파일 이름부터 `CommentDraftLayer` vs `CommentDraftBubble`이라 충돌 53곳). 그때는 **어느 판이 더 나아간 것인지**로 가르고(우리 브랜치가 그 9건 + 이후 시안·수정까지 담고 있었다), 상대 판에만 있는 것은 하나씩 골라 이식한다(핀 키보드 열기). 자동 병합이 **두 판을 동시에 import**해 놓는 경우가 있어 충돌 표시가 없는 파일도 눈으로 확인해야 한다(`Viewport.tsx`가 그랬다). **그리고 main 푸시가 프로덕션 배포를 만들었는지 확인한다** — CI 초록·머지·main fast-forward가 전부 정상인데도 라이브가 옛 판인 경우가 있다(제보로 알았다): Vercel이 **같은 커밋 SHA를 이미 Preview로 배포해 두면** main 푸시를 중복으로 보고 Production으로 승격하지 않을 수 있다(실측: `4427308`에 Preview 배포 둘 — base 머지 05:21Z와 main 푸시 05:26Z — 인데 Production은 그 앞 커밋 `e9bdd4c`에 멈춰 있었다. 지난 배포들은 같은 SHA였는데도 Production이 찍혔으니 조건은 확정하지 못했다 — CI를 기다려 5분 뒤에 푸시한 차이뿐이다). 확인은 GitHub 배포 API 한 줄이다 — `curl -sS -H "Authorization: Bearer $GITHUB_TOKEN" "https://api.github.com/repos/hoyullee/mindflow/deployments?environment=Production&per_page=1"`의 sha가 방금 올린 커밋이어야 한다(아니면 라이브는 아직 옛 판이고, 그때는 **서비스 워커 캐시가 아니므로 하드 리로드로 풀리지 않는다**). 푸는 길 둘: Vercel 대시보드에서 그 배포를 **Promote to Production**(클릭 한 번·새 커밋 없음), 또는 **새 커밋을 한 바퀴 돌려**(작업 브랜치 → PR → base → main) SHA를 바꿔 중복 판정을 비켜 간다. **그리고 그 push 명령의 소스 ref를 조심한다** — `git push origin claude/mindflow-design-impl-iyxiol:main`의 왼쪽은 **로컬 브랜치**가 있으면 그쪽으로 풀린다. 이 작업 트리에는 같은 이름의 로컬 브랜치가 남아 있을 수 있고 그건 대개 **한참 뒤처진 커밋**이라(실측 507500e vs 원격 645d493) `non-fast-forward`로 거절된다. 거절되고 멈추므로 위험하진 않지만 원인이 한눈에 보이지 않는다 — `git rev-parse claude/… `와 `origin/claude/…`를 견줘 보고, 다르면 **원격 커밋 sha를 직접 지정**해 넘긴다(`git push origin <sha>:refs/heads/main`).
- 📌 **어디를 고쳤으면 무엇을 배포하나 — 경로가 셋이다(질문)** 한 줄 규칙: **`apps/desktop/`을 고쳤으면 새 설치본, 그 밖은 웹 배포.** 껍데기(Electron 셸)는 원격 출처(`https://geurio.com/home`)를 그대로 띄우는 창이라 **화면에 보이는 것은 100% 웹 배포**다(Slack·Notion과 같은 구조) — 기능·버그·디자인은 물론 **OS 알림의 내용·발화 규칙**(스케줄러가 `apps/web`에 있다)과 타이틀 바의 **생김새**(그리는 것은 웹이다)까지. 새 설치본이 필요한 것은 메인 프로세스가 하는 일, 즉 **웹이 할 수 없는 일**뿐이다: 창(타이틀 바 **높이** — 창을 만들 때 정해진다·최소 크기·신호등 위치) / 상주(트레이·닫기=숨기기·로그인 시 실행) / 알림을 **띄우는 쪽**(`geurio:notify`·배너 유지·작업 표시줄 깜빡임·배지) / `geurio://` 딥링크 등록·단일 인스턴스 / 보안(외부 주소·랜딩 이동·브라우저 단축키 차단) / 포장(아이콘·AUMID·서명·프로토콜 선언).
  **경계선은 preload 창구 하나다** — 웹이 `window.geurio.*`에서 쓸 수 있는 것이 곧 그 셸이 할 수 있는 전부이므로, **새 창구를 뚫으면 반드시 새 설치본**이고 **웹 쪽은 그것이 없을 때를 견뎌야 한다**(이미 깔려 있는 옛 설치본이 그 상태다). 그래서 `DesktopBridge`의 나중에 들어온 멤버는 전부 **선택 필드**(`?`)다 — `titleBarHeight`가 교과서적인 예: 값을 안 내주는 옛 셸에서 폴백을 40으로 뒀다면 **네이티브 프레임 아래에 우리 바가 한 겹 더** 그려진다(그래서 폴백이 0이어야 한다). 판단 순서: ① `git diff --stat`에 `apps/desktop/`이 없으면 웹 배포만 ② 있으면서 `src/preload.ts`·`src/main.ts`·`src/shell.ts`·`electron-builder.yml`을 건드렸으면 새 설치본 ③ `apps/desktop/package.json`의 `version`만 올린 것도 새 설치본을 내는 일이다(그 값이 곧 `desktop-version.json`이다 — 절차는 `apps/desktop/README.md`).
  **세 번째 경로는 `supabase/`다**: 마이그레이션은 `main` 머지에 **자동 적용**되지만 **Edge Function은 손으로** `supabase functions deploy` 해야 한다(자동이 아니다). 이걸 빠뜨려 "앱은 새 판인데 서버가 옛 판"이 된 사고가 이미 있다(구글 캘린더 연동의 `redirect_uri_mismatch` — 앱은 코드를 `/auth/gcal`로 받는데 서버가 `postmessage`로 교환했다).
- 📌 **배포 확인 절차(에이전트 함정 기록)** 원격 세션의 프록시는 `vercel.com`·`*.vercel.app`을 막는다(응답 코드 000) — **배포 결과를 직접 볼 수단이 없다.** 그래서 GitHub에 남는 흔적(Deployments API·커밋 상태 `Vercel`·체크런 `Vercel Preview Comments`)으로 판단하게 되는데, 그 셋은 전부 **Vercel이 GitHub로 되보고하는 값**이고 **지연이 아주 클 수 있다**: 실측으로 어제는 푸시 후 **35초**였는데 다음 날 같은 브랜치에서는 **35분**이었다(빌드는 그전에 끝나 대시보드에는 완료로 보였다. 같은 시간대에 `Supabase Preview` 체크런도 함께 늦었다). 나는 30분간 0건인 것을 보고 **"배포되지 않았다"고 단정**해 불필요한 빈 커밋까지 푸시했고(되돌렸다), 그 전에는 `GET /commits/{sha}/status`의 `combined: pending`을 "빌드 중"으로 오독했다 — 그 값은 **상태가 0건일 때의 기본값**이다. **규칙**: 관측 가능한 것(GitHub Actions `verify`)과 관측 불가한 것(프리뷰 반영)을 갈라 보고하고, 간접 신호의 침묵을 결론으로 쓰지 않는다 — "확인할 수 없다"와 "되지 않았다"는 다른 말이다(RLS가 "행이 없다"와 "안 보인다"를 구분하지 못해 생긴 42501 사고와 같은 계열의 실수). 기다릴 때는 상태가 **non-pending으로 바뀔 때까지 폴링**해 두면 늦게 도착한 값도 놓치지 않는다 **CI 실행을 확인하는 쿼리도 두 번 틀렸다**(이번 라운드 실측): ① `/actions/runs?head_sha=<sha>`는 **갓 만들어진 커밋에 매칭되지 않는다** — 스쿼시 머지가 만든 base 커밋의 push 실행이 `branch=` 필터로는 즉시 보이는데 같은 시점의 `head_sha=` 필터는 60번 폴링(20분) 내내 빈 목록을 돌려줬다 ② 그 목록의 `name`은 **워크플로 이름**(`CI`)이고 `verify`는 그 안의 **잡 이름**이라, `name`으로 `verify`를 찾으면 영원히 0건이다(폴링이 정상 통과한 CI를 못 보고 18분을 돌았다). **확인은 두 곳으로**: PR은 `/commits/<sha>/check-runs`(잡 이름이 여기 있다), 브랜치 push는 `/actions/runs?branch=<branch>`에서 `head_sha`를 대조한다.



- 📌 **CI가 도는 지점(과금 분 = 이 목록)** 워크플로 트리거가 `push: ['**']` + `pull_request`라 **같은 커밋이 두 번** 돌고, fast-forward라 이미 통과한 `main` 커밋을 **다시** 돌고 있었다. 실측(30일): 998회 × 5분(회당 4.1분, 잡별 분 올림) ≈ **4,990분** — 그중 중복 265건(1,325분) + main 197건(985분)이 낭비다. 공개 저장소는 Actions가 무제한이라 청구서로 드러나지 않았을 뿐, 매번 4분씩 두 번 기다린 것은 우리다. 지금은 **두 곳만** 돈다: ① `pull_request`(머지 후보 — 우리 흐름은 언제나 PR을 지나므로 실질 게이트) ② **기본 브랜치 push**(squash 머지가 만든 새 커밋 — PR 각각은 초록인데 합쳐 놓으면 깨지는 경우를 잡는 유일한 자리). 뺀 것 둘: **작업 브랜치 push**(PR이 ①로 같은 커밋을 이미 검증한다 — PR 없이 돌려 보려면 `workflow_dispatch`), **main**(라이브 배포가 fast-forward이므로 main이 가리키는 커밋은 언제나 ①·②가 검증한 것이다). 더해서 `concurrency` + `cancel-in-progress`로 연속 푸시에 밀려난 실행을 취소한다(PR은 번호로, push는 ref로 묶는다 — 둘을 한 그룹에 넣으면 서로를 취소해 어느 쪽이 남을지 알 수 없다). **잡은 하나로 둔다**: 과금이 잡별 분 올림이라 4.1분짜리를 셋으로 쪼개면 벽시계는 빨라져도 분은 늘고 install이 되풀이된다. `paths-ignore`로 문서 커밋을 빼지 않은 이유는 `verify`가 필수 체크면 문서만 고친 PR이 **영영 초록이 되지 않아** 머지가 막히기 때문이다. **주의**: 작업 브랜치에 푸시했는데 CI가 안 보이면 고장이 아니다 — PR을 열면 ①이 돈다. **곁들여 잡은 시계 의존 테스트**: 알림 센터의 묶음 머리 테스트가 `오늘` 항목을 `Date.now() - 2h`로 심어서 **자정~새벽 2시에 돌리면 어제가 되어** `오늘` 머리가 사라졌다(이 정리의 첫 CI가 01:05 UTC에 돌아 실제로 깨졌고, 같은 시각에 로컬에서 재현·수리 후 통과 확인). 오프셋이 아니라 **오늘 09:00을 앵커**로 쓴다(아직 오지 않았으면 `지금`으로 물러선다). 남은 시계 의존 테스트들은 상대 시각 문구(`2시간 전`)만 단정해 날짜와 무관함을 확인했다 — **"같은 날인가"를 단정하는 테스트만 앵커가 필요하다**. **한 건 더 잡았다(2026-09-18)**: 구글 캘린더 근무 위치의 "구간 상한" 테스트가 시작일을 `inMonth(2)`(오늘 ±2일)로 잡고 **다음 달 20일**을 골라 "31일을 넘는다"를 단정했는데, 달의 20일 무렵에 돌리면 그 구간이 딱 30일이라 넘지 않는다 — **달의 어디쯤에서 돌리느냐로 갈리는 테스트**였다(달력을 두 달 넘겨 최소 48일이 되게 고쳤다). 시계 의존은 "오늘이 며칠인가"뿐 아니라 **"이번 달이 며칠짜리인가"와 "달의 몇 번째 날인가"**로도 온다. **세 번째(2026-09-26)**: 근무 위치 **구간** 테스트가 사흘을 `inMonth(3)·(4)·(5)`로 낱개로 잡았는데 `inMonth(n)`은 달을 넘으면 **반대로 접어**(오늘−n) 돌려준다 — 9/26에 `inMonth(5)`가 `09-21`이 되어 「끝은 배타적 다음 날」이 깨졌다. **구간은 낱개를 여러 번 부르지 않는다** — `spanInMonth(len)`으로 이어진 며칠을 한 번에 잡고 배타적 끝은 `nextDay()`로 만든다

- 🧰 **운영 백로그(무료 플랜·저장소 공개 검토에서 나온 것 — 순서까지 정해 둔다)** 기능이 아니라 **되돌릴 수 없는 위험과 약관**에 관한 일들이라 따로 적어 둔다. 순서가 중요하다: 백업 파일은 **모든 사용자의 이메일과 문서 본문**이 담긴 개인정보 덩어리인데, 공개 저장소의 Actions 아티팩트는 읽기 권한이 곧 전체 공개라 **거기에 두면 백업이 아니라 유출**이다 → 그래서 비공개 전환이 백업보다 먼저다.
  1. **저장소 비공개 전환**(사용자가 GitHub Settings에서 직접). 확인한 것: 비공개 저장소 자체는 Free 플랜에서 **무료**이고 Vercel·Supabase의 GitHub 연동도 그대로 동작한다(권한 재승인 한 번 뜰 수 있다). 바뀌는 것 둘 — **Actions 무료 분이 무제한 → 월 2,000분**(그래서 #421의 CI 정리가 선행 조건이었다. 초과해도 Free는 spending limit이 $0이라 자동 청구가 아니라 **Actions가 멈춘다**), **secret scanning·push protection을 잃는다**(공개 저장소 전용 무료 기능). 현재 히스토리·트리에 실제 비밀값은 없음을 스캔으로 확인했다(`.env` 추적 0건, `service_role`/`GOCSPX-`/Resend 키 패턴은 전부 "넣지 말라"는 문서 경고문).
  2. **gitleaks** — ①로 잃는 그물의 대체(CI 스텝 또는 pre-commit). 공개 히스토리에 키가 한 번 들어가면 영구히 남으므로 그물은 있어야 한다.
  3. **pg_dump 주기 백업 + Storage** — 무료 플랜에는 **일일 백업도 PITR도 없다**: 지금 이 앱에는 "되돌리기"가 아예 없다. 막으려는 사고는 ⓐ **마이그레이션 실수**(main 머지 시 프로덕션에 자동 적용된다 — 실패는 트랜잭션이 막아 주지만 **성공하면서 엉뚱한 일을 하는** 것은 그대로 적용된다) ⓑ SQL Editor에서 손으로 돌린 쿼리(운영 절차에 그 단계가 있다) ⓒ `delete_account()` cascade 같은 앱 버그 ⓓ 스페이스·폴더·댓글·일정은 휴지통이 없다. **못 막는 것도 적어 둔다**: Storage(`map-images`·`avatars`)는 DB가 아니라 별도라 함께 받지 않으면 본문의 `mfimg:` 참조만 살아 있는 반쪽 복원이 되고, `auth.users`는 권한 때문에 덜 떠질 수 있어(문서는 있는데 소유자 계정이 없다) 설계 시 확인해야 한다. **정해야 할 것**: 주기(하루 1회면 최대 24시간 손실 — 지금 규모엔 충분), Storage 포함 여부(용량 vs 반쪽 복원), 보관 위치(비공개·암호화). 시작하면 **개인정보처리방침 §3(보관 장소·위탁)에 한 줄이 필요하다** — 방침이 실제 구현과 어긋나면 그 자체가 구글 검수 반려 사유였다.
  4. **LICENSE 결정** — 지금 라이선스 파일이 없어 법적 기본값은 "모든 권리 유보"(보호는 되지만 사람들은 public을 오픈소스로 읽는다). 비공개로 가면 급하지 않고, 공개를 유지할 이유가 생기면 반드시 붙인다.
  5. **Vercel Hobby 비상업 조항** — 우리 배포는 서버리스 함수·ISR·이미지 최적화가 없는 **순수 정적 SPA**라 기술적으로 가장 싼 모양이지만, 유료 플랜이 붙거나 사업체 명의가 되는 순간 약관상 Pro가 필요하다(요금이 아니라 약관 문제).
  6. **한도 감시** — Supabase 대시보드 Usage에서 볼 두 줄은 **Realtime 메시지**(실측 근거: 2계정 하루 종일 공동 편집 = 86K건 = 무료의 4%. #231 이후 혼자 열어 둔 탭은 0건이라 **사용자 수가 아니라 동시 공동 편집자 수**에 비례한다)와 **egress**(썸네일 스트립·서명 URL 12시간·WebP로 두 번 조였다). 앱 안 신호도 하나 있다 — 이미지 업로드가 본문 인라인으로 폴백해 독칩 배너가 뜨기 시작하면 용량을 확인할 때다(`backend.md` §10).
- 📦 **Microsoft Store 제출 백로그(코드는 준비 끝 — 남은 것은 계정·등록 정보)** MSIX 채널은 자가서명으로 **로컬 설치까지 확인**됐고(#111) 실기기에서 로그인·내보내기 저장 대화상자까지 통과했다. 남은 것은 전부 Partner Center 쪽 일이라 백로그로 둔다. **확인한 것 둘**: 등록비가 **무료**다(개인은 2025년, 회사는 2026-05부터 — 신용카드도 필요 없다. 그래서 "돈을 낸 뒤 반려되면 손해"라는 걱정이 사라졌고, **일단 제출해 보는 것이 가장 싼 확인 방법**이 됐다) / 웹을 감싼 앱을 Microsoft는 **막지 않는다**(PWA를 Store에 올리는 것을 권장하고 PWABuilder라는 자체 도구까지 만든다. 검색에 보이는 "고유한 핵심 로직" 조건은 Microsoft가 웹을 훑어 **자동으로** 올려 주는 프로그램의 기준이고 직접 제출과 무관하다). **확인 못 한 것**: 정책 전문(`Microsoft Store Policies`)은 `learn.microsoft.com`이 이 환경에서 **fetch 차단**이라 조항 번호를 인용할 수 없다(WebSearch는 통해서 요약까지만 — 심사가 실제로 재는 핵심은 `10.1.1` "앱이 온전히 동작해야 한다"로 보인다). **정책보다 현실적인 위험은 심사자가 로그인을 통과하지 못하는 것**이다 — 우리 앱은 열면 로그인 화면이고 그 로그인이 브라우저를 거쳐 앱으로 돌아오는 특이한 절차라, 모르는 사람은 "기능이 동작하지 않는다"로 판정한다. Microsoft는 그래서 제출 화면에 **「심사자용 메모(Notes for certification)」** 칸을 두고 로그인이 필요한 앱은 테스트 계정을 적으라고 한다. 순서: ① 개발자 계정 등록 → 앱 이름 예약 ② 배정된 세 값(`Package/Identity/Name`·`Publisher`·`PublisherDisplayName`)으로 `apps/desktop/electron-builder.yml`의 `appx` 블록 교체 ③ **`CSC_LINK` 없이** `pnpm pack:appx`(Store 패키지는 Microsoft가 서명한다 — 자가서명은 로컬 테스트 전용) ④ 등록 정보(스크린샷·연령 등급·개인정보처리방침 `https://geurio.com/privacy`) + **심사자용 메모**(이메일 가입 계정 하나 + 맵·보드를 미리 만들어 둔 상태 + 절차 세 줄 — 빈 화면이면 "기능이 없다"로 읽힌다) ⑤ (선택) 정체성이 배정된 뒤 appx CI 잡. **버전 형식은 손볼 것이 없다** — `appInfo.getVersionInWeirdWindowsForm()`이 `package.json`의 `0.1.0`을 `0.1.0.0`으로 만들어 Store의 "revision은 0" 규칙을 자동으로 맞춘다(제출마다 그 `version`만 올리면 된다). 반려되면 어떤 시험·정책인지 메일로 오고 **재제출은 무료·무제한**이라, 잃는 것은 시간뿐이고 `.exe`/`.dmg` 직접 배포는 그대로 동작한다.
- 📌 **공책에 남겨 둔 것(의도적)**: **실시간 공동 편집을 붙이지 않았다**(사용자 결정) — 그래서 코어가 페이지·블록 순서를 **배열**로 든다(칸반의 `pos` 분수 인덱스는 끊긴 두 사람의 병합을 위한 것이다). 붙이려면 **순서 모델부터 다시 정해야** 한다. 그 밖: 토글 안에 여러 블록 중첩(지금은 머리 + 본문 한 줄), 페이지 단위 댓글(지금은 공책 한 권 단위).
- ⏭️ **다음 후보**: (낮음) 홈 카드 목록 전면 가상화 — content-visibility로 부족해질 때(스페이스당 수백 맵 규모), 메인 그리드+검색 결과 한 세트 / 실기기 빌드·서명·스토어 제출은 로컬 툴체인 필요 / 확인 대기: 모바일 로컬 알림 실기기 5단계(`apps/mobile/README.md`) · 데스크톱 트레이 상주 실기기(Windows·macOS — `apps/desktop/README.md`) · 안드로이드 "안전하지 않은 앱 차단됨"(Play Protect — `chrome://webapks`)

> **로드맵 1·2단계 완료**: 웹 프로덕션화(코어 분리·React·인증/DB) + PWA/모바일 웹 + 실시간 다중편집 + 앱스토어 래핑(Capacitor). 라이브 백엔드/실기기 배포는 키·툴체인 설정 후 로컬에서.

> **M3 완결 + M4**: 로그인 → 홈 → 에디터 전부 React로 동작, `mindmap-core` 소비. 인증·문서 저장은 포트 추상화(Local/Supabase). 실 Supabase는 키 설정 시 활성(미설정 시 localStorage 데모).

> `mindmap-core`는 순수 TS(DOM/React/canvas 금지, lint 강제). 노드 크기는 `sizeOf` 주입.

## 규칙 (에이전트·사람 공통)
- `support.js`와 `*.dc.html`(디자인 원본)은 **변경하지 않는다.** 이식은 새 `packages/`에서 진행.
- 새 코드는 **TypeScript** 우선. 코어는 프레임워크·DOM 의존 없이 순수 로직으로.
- 비자명한 변경은 **테스트로 동작을 검증**한 뒤 커밋. 로컬은 정적 서버(`python3 -m http.server`)로 확인.
- **실브라우저·하네스로 검증하기 전에 `docs/probe-pitfalls.md`를 훑는다** — 검증이 실패하면
  앱을 의심하기 전에 프로브를 의심한다(그 목록의 절반은 앱이 결백했던 사건이고, 셋은
  재발했다). 새 함정을 만나면 그 문서에 한 줄 더한다.
- **로컬 검증은 손댄 영역만**(요청) — 전체 스위트는 2,100건이 넘어 한 번 돌리는 데 2분이 넘는다.
  손댄 파일과 그 소비처의 테스트 파일만 지정해 돌리고(`npx vitest run <파일|디렉터리>`), 여기에
  루트 `pnpm lint`·`pnpm typecheck`(둘은 전체를 보지만 빠르다)와 프로브용 `vite build`를 더한다.
  **전 영역 회귀는 CI(`verify`)가 본다** — PR을 열면 그것이 전체를 돌리는 자리이므로, 로컬에서
  같은 일을 되풀이하지 않는다. 다만 코어(`packages/mindmap-core`)나 여러 화면이 함께 쓰는 부품
  (`components/`·`theme.ts`·`chrome.ts` 등)을 건드렸으면 그 파급 범위까지 지정해 돌린다.
- **작업을 마치면 `docs/changelog.md` 끝에 한 항목을 더한다**(원인·결정·검증·트레이드오프).
  `CLAUDE.md`는 **규칙·아키텍처·「지금 있는 것」이 바뀔 때만** 고친다 — 라운드 기록을 여기에
  쌓으면 그만큼 매 세션의 컨텍스트를 먹는다(956KB까지 자랐다가 가른 이력이 있다).
  「최근 라운드」 목록은 제목만 20줄 안쪽으로 유지한다.
- 브랜치 접두사 `claude/`. main 직접 푸시 금지, PR로.
- 커밋/PR에 모델 식별자·비밀정보를 넣지 않는다.

## 로컬 실행
```bash
python3 -m http.server 8000   # http://localhost:8000/
```
`file://` 직접 열기는 동작하지 않음(형제 파일 fetch 필요).
