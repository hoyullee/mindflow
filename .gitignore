# Dev-only dependencies (React UMD is vendored under ./vendor and committed)
node_modules/
npm-debug.log*
.DS_Store

# Monorepo build artifacts
dist/
.turbo/
*.tsbuildinfo
apps/web/dist/
# vite-plugin-pwa devOptions가 dev 서버 실행 시 생성하는 SW 산출물
apps/web/dev-dist/

# Secrets — never commit real Supabase URLs/keys or other env values.
# `.env.example` (checked in) documents the required variable NAMES only.
.env
.env.*
!.env.example
!*/.env.example
